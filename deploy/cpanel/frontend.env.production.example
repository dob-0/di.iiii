# Same-origin default for cPanel Node.js App deployments
VITE_API_BASE_URL=/serverXR

# Current compatibility mode:
# if production writes still depend on a browser token, the deploy workflow
# reuses the server API token here until auth hardening is finished.
VITE_API_TOKEN=change-me
