# Same-origin default for cPanel Node.js App deployments
VITE_API_BASE_URL=/serverXR

# Optional legacy compatibility only.
# Leave unset for normal builds so the public bundle does not contain
# the server write token. Editors establish a server session when needed.
# VITE_API_TOKEN=change-me
