# Known Fixes

**Check here before investigating any bug.** If a symptom matches a row, use the recorded fix instead of re-investigating.
When you solve something that took >5 min to find, add a row here and update `CURRENT.md`'s Last commit line.

## Recurring bug class: silent hardcoded fallback

A per-entity value (spaceId, auth scope, asset id) gets read from a source that can be
absent/malformed, and the code substitutes a hardcoded default or trusts the caller instead of
failing loudly or resolving the real value. Four confirmed instances so far — when touching
routing, auth, or scope-resolution code, check whether the change you're making could produce a
fifth:

1. Studio direct-project-link `spaceId` silently hardcoded to `'main'` instead of falling through
   to the project's real space (`getStudioLocationState`, row further down this table).
2. OAuth sessions hardcoded `spaces: null` (meaning *unrestricted* access) instead of a real scope.
3. `spaceId === 'wcc'` string-literal special case bypassing the real `isPublic` auth check.
4. Asset IDs trusted from the client instead of being verified against the actual content hash.

Guardrails: `scripts/check-fallback-patterns.mjs` (CI-gated) greps for the literal pattern;
`serverXR/src/fallbackContracts.test.js` (planned/see `docs/ai/audit-*.md`) encodes it as HTTP-level
contract assertions.

## A bare `*` route stops serverXR booting, it does not fail a request

serverXR is on Express 5, whose router uses path-to-regexp v8. That version
rejects an unnamed wildcard:

```js
router.get('/serverXR/og/*', h)        // PathError: Missing parameter name
router.get('/serverXR/og/*splat', h)   // ok — req.params.splat is an ARRAY
```

The trap is *when* it throws. Registration happens at module load, so a bad
pattern does not 404 or 500 — the process dies before it listens, and every
route in the app goes with it. Unit tests on the handler pass happily, because
the handler is never the thing that breaks.

Caught only by mounting the router on a real express app and issuing a request.
If you add a wildcard route, add that check with it.

| Symptom | Root cause | Fix | File |
|---------|-----------|-----|------|
| **`npm run dev:browser` silently served two-day-old code** — the main checkout sat parked on a merged feature branch, 115 commits behind origin/dev; the stack started clean, everything rendered, nothing anywhere said the tree was stale | A session ended without parking the main checkout back at origin/dev, and no tool reported the viewing surface's position: dev-stack printed no tree position at all, and `npm run state` deliberately skipped exactly the two shapes this takes (detached HEAD behind origin/dev, and a branch whose upstream is gone because it merged and was deleted) | `dev-stack.mjs` prints branch/sha/behind-count at startup and a loud STALE/DRIFTED warning with the fix command (`git fetch && git checkout --detach origin/dev`) when HEAD is off the origin/dev tip or the upstream is gone — read-only, counts against the local origin/dev ref (no fetch; offline is a real case), degrades to silence if git is unavailable. `repo-state` warns on both shapes. The Parking Rule added to `parallel-agents.md`: the main checkout is the user's viewing surface, leave it detached at origin/dev; branch work lives in `.claude/worktrees/`. Guard: new `collectStateWarnings` cases in `scripts/repo-state.test.js` | `scripts/dev-stack.mjs`, `scripts/repo-state.mjs`, `scripts/repo-state-lib.mjs`, `docs/ai/parallel-agents.md` |
| **`di` install dies on Windows with `tar (child): Cannot connect to C: resolve failed`** — the download arrives, the unpack fails, and the message names neither tar nor the drive letter. Found by CI on windows-latest, never reproducible on Linux or macOS | Windows usually has **two** tars. Windows ships bsdtar at `System32\tar.exe`, which understands `C:\...`; Git for Windows ships GNU tar, which is often first on PATH and reads a leading `C:` as a **remote host** spec (`host:path`) | Call bsdtar by full path (`$env:SystemRoot\System32\tar.exe`), falling back to whatever `tar` is with `--force-local`, which tells GNU tar a colon is just a colon. Applied in both places that unpack: `install.ps1` and `stageVersion()` (which is what `di update` runs). Guard: the `linux`+`windows`+`update` jobs in `install-matrix.yml` all unpack a real artifact | `install.ps1`, `scripts/di/install.mjs` |
| **A local di.iiii answers its API but 404s the whole app** — `/serverXR/api/health` is 200, `/assets/*.js` are 200, and every SPA route is 404. Reads exactly like a broken route table | `res.sendFile(absolutePath)` with no `root` makes `send` apply its `dotfiles: 'ignore'` rule to **every segment of the absolute path** — and a local install lives in `~/.di`. One hidden directory anywhere in the path 404s the entire app | `res.sendFile('index.html', { root: CLIENT_DIR })` — relative to a root there is no dot segment. Guard: the fixture in `serverXR/src/spaHostingContracts.test.js` deliberately lives under a hidden directory; three tests fail without the fix. **A test path without a dot in it proves nothing here** | `serverXR/src/index.js`, `serverXR/src/spaHostingContracts.test.js` |
| **`di: command not found` right after an install that reported success** — on macOS, with the shim written and a `~/.local/bin` that exists | Two compounding mistakes. (1) The installer decided whether `~/.local/bin` was usable by reading its **own** `process.env.PATH` — but it runs from a curl pipe, ssh or CI, whose PATH says nothing about the artist's terminal; on that Mac the directory existed and was *not* on the login PATH. (2) The rc fallback picked the first existing file from a fixed list, which on macOS is `.zshrc` — and a **login zsh never reads `.zshrc`** | Ask the login shell for the real PATH (`$SHELL -lc 'printf %s "$PATH"'`), and choose the rc file from `$SHELL` rather than from which files exist: `.zshenv` for zsh, `.bash_profile` before `.bashrc` on macOS. Guard: every `install-matrix.yml` job invokes `di` from a **fresh login shell** — running the shim by absolute path proves nothing | `scripts/di/bootstrap.mjs` |
| **`fs.rm(current, { recursive: true })` can delete the installed version on Windows** — not observed in the wild; found while writing the update path | On Windows `current` is a **junction**, and removing a junction recursively removes **what it points at**, not the link | All link removal goes through `unlinkLink()`: lstat first, unlink if it is a link, `rmdir` only if it is an empty directory, refuse anything else out loud. Guard: the `update` job runs on windows-latest and asserts `current` is still a link whose target is whole, after an update **and** a rollback | `scripts/di/install.mjs`, `scripts/di/bootstrap.mjs` |
| **The blob collector listed four stills of the br_id_ge rite for deletion** — images that existed nowhere else, while every automated check agreed they were unreferenced | An image dropped into a rich-text field is stored as `<img src="/serverXR/api/projects/<pid>/assets/<sha>">` and appears in **no asset list**. `collectReferencedHashes` only walked `projects/<pid>/assets/*.json`, the manifests an upload writes — and when the project named in that URL has since been deleted, its manifests went with it. The blob then belongs to nobody by that reckoning while a live document still displays it | Also scan each `projects/<pid>/document.json` (and `projects/_removed/*.json` where those are kept) for `/assets/<sha>` refs, and treat every hit as referenced. **Residual, stated plainly:** production deletes a project's document outright, so `_removed` exists only in the `di-spaces` snapshot — this prevents the class going forward but does not retroactively protect blobs whose owning document is already gone from the server. Those four are now held in the snapshot instead | `scripts/gc-space-blobs.mjs` |
| **Dormant projects pinned their dead assets forever while busy ones let them go in days** — 145 MB of blobs on production were uncollectable because ops from June still mentioned them; `platform-recordar-dii-project` alone held seven, including a 34 MB video, behind four ops | Op retention was bounded by **count only** (`maxHistory = 500` per project and space). How long history survived — and therefore how long every asset it mentions survives a garbage collection — depended on how *busy* the work was. A project doing 500 ops a week dropped old history at once; one sitting at 4/500 needed 496 more edits before anything trimmed, i.e. never | `appendProjectOps`/`appendOpsHistory` also take `maxAgeMs` (0 = off; wired to `MAX_OP_AGE_MS` = 30 days in `serverXR/src/index.js`) and delete ops past the bound in the same transaction. Safe because a client left outside the window is already handled: `hasOpGap()` sees a window that does not start at its next version and calls `resyncDocument()`. The bound must stay far longer than any retry — the full window doubles as the idempotency guard for resent op batches in `POST .../ops` | `serverXR/src/projectStore.js`, `serverXR/src/spaceStore.js`, `serverXR/src/index.js` |
| **The secrets backup wrote nothing and looked like it had only warned** — `deploy/secrets-backup.sh` ended with `tar: .: file changed as we read it` and left `~/di-backups/secrets/` empty, so the one gap the nightly backup does not cover stayed uncovered while appearing solved | The bundle was built *inside* the directory being archived (`tar -czf "$work/bundle.tar.gz" -C "$work" … .`). tar saw the directory grow underneath it and exited 1; under `set -euo pipefail` that aborted the run before the encryption step. `--exclude` does not help — it keeps the file out of the archive, not out of the tree tar is reading | Build the archive outside `$work` (`mktemp /dev/shm/secrets-bundle.XXXXXX.tar.gz`), extend the cleanup trap to remove it, and point the age/gpg branches at it. Verified end to end: bundle encrypts, decrypts and lists all six captured files | `deploy/secrets-backup.sh` |
| **The blob garbage collector would have deleted 145 MB that a catching-up client still needs** — `gc-space-blobs.mjs --apply` reported 33 collectable blobs (240 MB) on production; 21 of them were still referenced by the op-log | It decided "referenced" from the filesystem alone (a project holding `assets/<sha>.json`) and never opened `di.db`. `project_ops` is what a client replays via `GET .../ops?since=`, so a blob deleted underneath a retained op hands that client a 404 for an asset the current document no longer carries anyway | Reads `di.db` (default `<data root>/di.db`, beside `--spaces-dir`), collects every 64-hex run in `project_ops`/`space_ops` in one pass, and holds any blob it finds — reported as `held by op-log`, not silently skipped. **Fails closed**: if the database cannot be read it refuses to delete rather than falling back to the filesystem-only answer; `--ignore-db` restores the old behaviour explicitly. Verified on the production volume: 33 candidates → 12 collectable, 21 held | `scripts/gc-space-blobs.mjs` |
| **A destructive "Delete" button, and separately the global "Sign in" icon, land on top of the Chat window's message input on a phone** — found by opening the Chat window (not by the overlap script alone, which only checks the topbar) after the window-size clamp fix below started actually shrinking windows to fit small viewports rather than letting them overflow: a window that now properly fills the available height reliably lands flush against the true bottom edge, which is where `.raw-delete-fab` (fixed, z-index 1300, shown whenever a node is selected) and app-wide `AccountButton`'s "Sign in" icon (fixed `bottom:86px`, 30px tall, **z-index 9999 — above literally everything**, mounted on every route, not just Raw) both live. `AccountButton` is above the delete FAB too, so it wasn't just visually covering the chat input — it was also eating the clicks meant for it | `clampWindowFrame` reserved viewport-edge padding for the right/bottom edges uniformly (12px) with no awareness that the bottom-right corner is permanently occupied by fixed, high-z-index chrome outside the window system entirely | Added `RAW_WINDOW_BOTTOM_RESERVE` (120px — clears `AccountButton`'s top edge at 116px, which also covers the delete FAB's shallower footprint) applied only to the bottom-edge clamp, unconditionally on every window rather than only when a node happens to be selected (avoids a window resizing itself the moment selection changes). Guard: new `windowLayout.test.js` case reproducing Chat's exact default frame on an iPhone SE viewport, asserting its bottom edge clears the reserve | `src/raw/utils/windowLayout.js`, `src/raw/utils/windowLayout.test.js` |
| **The Help dialog's manual-path/"Jump to…" footer reads as cramped 2-3 line wraps on a phone** — not overflowing or overlapping (measured: both stay inside the 320px viewport with a clean 16px gap between them), but a monospace file path wrapped 3 lines in ~133px next to a button label wrapped 2 lines in ~80px is not legible, just technically not broken | `.raw-help-footer` is `display:flex; justify-content:space-between` with no narrow-width handling — fine down to tablet width, but two side-by-side columns both under 140px is too tight to read regardless of whether anything actually overflows | `flex-direction: column` below 480px, button set to full width. Same "not every fix is an overflow fix" lesson as the empty-hint fix above — found by opening the Help dialog in a real mobile viewport and looking, not by re-running the overflow checker (which correctly reported nothing wrong) | `src/raw/styles/raw.css` |
| **The topbar's empty-state hint reads as just "DOUBLE" on a real phone** (iPhone SE, 320px) — found by actually running `npm run verify:surfaces --mobile-only` against `/open/raw` and opening the screenshot, not by guessing; the tool's own "smallTaps" summary said nothing since this is a text-legibility problem, not a tap-target one. Also said "Double-click" on a touch device, which doesn't match the interaction | `.raw-topbar-location` ellipsis-truncates (correctly, per the earlier overlap fix), but at 320px the pill's own usable content width is ~40px once Help/Chat/⋯ claim their share — not enough for any hint text, full or shortened, to survive as more than a few letters. A first attempt swapped in a shorter string below 400px via CSS, which didn't help: both variants start with "Double-", so the truncated result looked identical either way | Dropped the topbar copy entirely below 400px (`display:none` on `.raw-topbar-location`) rather than fighting for character budget — same "shed before truncating unreadably" call already used for `raw-topbar-name`/`scale-control`. The canvas-center empty-state message (`RawGraphSurface`'s `emptyHint`, full viewport to work with) already reads fine at that width and was left as the sole onboarding hint. Separately: both hint strings now pick "Double-tap"/"Double-click" from `matchMedia('(pointer: coarse)')`, computed once on mount. Guard: re-ran `verify:surfaces --mobile-only` after the fix and confirmed the topbar cleanly omits the pill at 320px with no dangling empty box, and the canvas message reads "Double-tap…" under Playwright's touch-emulated device profiles | `src/raw/components/RawEditor.jsx`, `src/raw/styles/raw.css` |
| **A node's window swallows the entire mobile screen** — `universe.world`'s default window (680×480, tuned for desktop) rendered at that same fixed size on a 390px phone: wider than the whole viewport (right edge overflowing off-screen) and tall enough to leave no visible margin above/below, so a single opened node hid the rest of the graph with nothing on screen suggesting more content existed (user-reported, 2026-08-06, "too many open spaces… in mobile it close whole vision") | `clampWindowFrame` (`windowLayout.js`) only ever clamped a window's **position** (x/y) to stay inside the viewport; it never clamped **size**, so a desktop-tuned default just rendered as-is regardless of viewport | Added `maxWidth`/`maxHeight` derived from `viewportWidth`/`viewportHeight` (floor of 260×180, matching the existing minimum), and `width`/`height` now `clamp()` against them before the x/y math runs — so oversized-position clamping was already correct once size is bounded first. Guard: 3 new `windowLayout.test.js` cases (shrinks a 680×480 default to fit a phone viewport with no overflow on any edge; never shrinks below the 260×180 floor even on a very small viewport; leaves an already-fitting window untouched) | `src/raw/utils/windowLayout.js`, `src/raw/utils/windowLayout.test.js` |
| **The topbar showed a permanent, pointless tagline** (`GRAPH CONNECTS EVERYTHING` / `WORLD BUILDS THE SCENE` / `VIEW BUILDS THE INTERFACE`) once any node existed, and the World-scope toggle button rendered visibly off-center — a lone "World" floating far right of true center (user-reported screenshot, 2026-08-06) | `topbarLocationText` returned `workflow.title` (a static per-surface marketing string) for the entire life of the workspace, not just its empty state. That pill and the World toggle (`.raw-topbar-windows`, `flex:1; justify-content:center`) are siblings in `.raw-topbar-center` — with the pill present as a sibling consuming the left portion of that flex row, the toggle centered *itself* in whatever space was left over instead of the row as a whole, landing well right of true center | `topbarLocationText` now returns `''` (pill unrendered) once any node exists — the pill only ever shows its original empty-state hint. With the pill gone, `.raw-topbar-windows` naturally centers in the full row | `src/raw/components/RawEditor.jsx` |
| **Raw's topbar hint pill painted through the `Help` button** at ~890px viewport width (user-reported screenshot, 2026-08-06) — `DOUBLE-CLICK TO PLACE YOUR FIRST NODE` overlapped `Help`/`Size`/`Chat` instead of yielding space | `.raw-topbar-location` had `white-space: nowrap` and no `max-width`/`overflow`/`flex-shrink`, so it couldn't shrink; `.raw-topbar-right` is `flex-shrink: 0` and never yields either; `.raw-topbar.is-seeded` sets `overflow: visible`, so the collision painted instead of clipping. `verify:surfaces` never caught it — that script checks elements escaping the *viewport*, not siblings colliding with each other while both stay inside it. The breadcrumb trail that swaps into the same center slot (deep node nesting) had the identical gap: `.raw-topbar-crumb` had no `max-width`/ellipsis and clipped mid-glyph instead of truncating cleanly | `.raw-topbar-location`/`.raw-topbar-crumb` given `min-width:0; overflow:hidden; text-overflow:ellipsis; flex-shrink:1`; `.raw-topbar-center`/`-breadcrumb`/`-crumb-group` given `overflow:hidden`/`min-width:0` so children can actually shrink. New `900px`/`700px` breakpoints hide the `Size:` label then the whole scale control, following the existing `1200px` (`raw-topbar-name`) pattern of shedding least-essential controls before anything truncates unreadably. Guard: `scripts/check-toolbar-overlap.mjs` (new, `npm run check:toolbar-overlap`) — pairwise bounding-box intersection of `.raw-topbar-left/-center/-right` at 1440/900/889/700/390px, both the hint-pill and breadcrumb center states; wired into `src/raw/AGENTS.md`'s validation list and the `human-verifier`/`ux` project agents. See the new `ui-overlap-stress-test` skill for the general procedure (this bug class is not Raw-specific) | `src/raw/styles/raw.css`, `scripts/check-toolbar-overlap.mjs`, `.github/skills/ui-overlap-stress-test/SKILL.md`, `.claude/agents/human-verifier.md`, `.claude/agents/ux.md` |
| **Raw's node palette let you draw wires that carried nothing** — a value.color node into `universe.world.bgColor`, `value.string` into its `title`, or any wire touching `geom.*`'s `Geometry` output, `universe.space`/`universe.desk.3d`'s title/state/signal/preview outputs, `world.background.texture`, or any `view.*` node's position/width/height had zero effect, even though the port existed and accepted the edge | `computeNodeOutput` (`nodeGraphRuntime.js`) only had cases for `value.*`/`math.*`/`time`; several renderers (`buildWindowStateFromNode`, `getRawWorldBackgroundColor`) read `node.values.X` directly instead of through `evaluateNodeInput`; and a chunk of declared ports (`gridSize`, `slug`, `description`, `active`, `entry`, `state`, `signal`, `preview`, `texture`, per-node `position`/`width`/`height`) were never read by anything outside `nodeRegistry.js` — decorative from day one. Also, the "Streaming Prototype" overflow-menu preset in `RawEditor.jsx` bypassed the palette's implemented-type gate entirely, creating 9 unimplemented nodes that silently rendered as generic text boxes | `universe.world.title`/`.bgColor` now wire-evaluated for real (new tests cover both). `geom.cube.bounds` (vec3) now computed from resolved `size` and carries across a wire; the never-consumable `Geometry`-typed outputs on `geom.cube/sphere/plane` were removed rather than faked. Every other confirmed-dead port removed from the schema. The Streaming Prototype preset deleted outright. Guard: `nodeGraphRuntime.test.js` (bounds wiring), `viewportWorldState.test.js` (bgColor wiring), `RawEditor.test.jsx` (title wiring + preset removal via full suite pass) | `src/project/nodeRegistry.js`, `src/project/graph/nodeGraphRuntime.js`, `src/raw/components/RawEditor.jsx`, `src/raw/utils/viewportWorldState.js` |
| **A publish still reports success when no asset actually uploaded** — second half of the silent-publish bug, on the path that does not throw | `uploadAssetToServer` returns `null` *without throwing* on its guard clause (no `spaceId`, no file), and `syncAssetsForPublish` discarded the return value entirely, so the loop completed and publish reported "Scene synced to server." How it stayed hidden: `useServerPublishing.test.jsx`'s stub resolved `{ assetId }` while the real function resolves an asset entry keyed `id` — a mock drifted from its contract is invisible precisely when the result is thrown away | Check the result: `if (!uploaded?.id) throw`. Stub corrected to the real entry shape. Guard: `useServerPublishing.test.jsx` — "refuses to publish when an upload resolves without an asset entry" (without the fix it resolves `true` instead of rejecting) | `src/hooks/useServerPublishing.js` |
| **A lint rule switched off tree-wide to make a widened gate pass silences every future violation too** | Widening `npm run lint` to serverXR/scripts/shared surfaced 49 errors; two rules (`no-extra-boolean-cast`, `no-control-regex`) were set to `'off'` in the shared Node config to get to green. Only 4 real sites existed | Fix the sites instead: 3 redundant `Boolean(x) ? 1 : 0` casts dropped (the file already used the bare form one line below), and the one deliberate NUL match given an `eslint-disable-next-line` **on the line**. Both rules back ON. Guard: `scripts/lint-scope.test.js` asserts the gate names all four trees AND that neither rule is `'off'` | `eslint.config.js` `serverXR/src/spaceStore.js` `scripts/space-sync.mjs` |
| A code-mode published page (`<iframe srcDoc>`, no canvas ever mounted) still downloads and evaluates the whole 1.6MB `three-vendor` chunk — measured on prod `/br_id_ge`: FCP 2196ms, `document.querySelectorAll('canvas').length` stayed 0 across an 8.6s trace | `PublicProjectViewer.jsx` statically imported `LiveProjectScene` and `StudioViewport` at module scope, so both 3D renderers (and three/fiber/drei/xr behind them) were evaluated on every published page regardless of `entryView`. `SpaceSurfaceApp.jsx` already lazy-loads PublicProjectViewer itself, so these two imports were the direct pull | Both converted to module-scope `React.lazy(() => import(...))` and their render branches wrapped in `<Suspense>`; the existing "Loading live experience..." card was hoisted into a module-level `loadingOverlay` element and reused as the fallback so the visual is byte-identical to the pre-existing loading state. Guard: source contract + code-mode render test in `PublicProjectViewer.test.jsx`, watched failing with the static imports restored. **Not yet the whole win** — `useXrAr` still statically imports `@react-three/xr`, which keeps `three-vendor` on the code-mode path; deferring it needs the scene branch extracted into its own lazy module | `src/project/components/PublicProjectViewer.jsx` `src/project/components/PublicProjectViewer.test.jsx` |
| algoVrithm leaks GPU memory across an unattended exhibition day (~2.5MB per loop): the edit list runs on a ~53s cycle and remounts every sequence each pass | `DispersionSphere.jsx` built 2 geometries and 13 materials in `useMemo` and never disposed them on unmount, so each loop stranded a fresh set of GPU buffers. Four sibling sequences (ReelGlobe, ScanRoom, ScanCloudRoom, ScanShardRoom) already carried the disposal effect; this one was missed. The other sequences named in the original report are parked dead code, not in the shipped edit list | Copied the siblings' unmount-only `useEffect(() => () => { ...dispose() }, [deps])` pattern (see `ReelGlobe.jsx:621`), disposing sphereGeometry, shellGeometry, the fluid material, the 3 shell materials, floor/stone materials and the per-column materials. No automated guard yet — it needs a new `DispersionSphere.test.jsx` spying on the THREE prototypes' `dispose` | `src/algoVrithm/sequences/DispersionSphere.jsx` |
| **Publish reports "Scene synced to server." after every asset upload failed** — the scene op lands, the assets never do, and the next viewer gets a document referencing assets that are not on the server | `useAssetPipeline`'s `uploadAssetToServer` ended in `} catch { return null }` (and `if (!response?.assetId) return null`). `useServerPublishing.syncAssetsForPublish` awaits that call and **discards the return value** — it increments `completed` and returns `true` regardless — so `handlePublishToServer` reached `markServerSync('Published to server')` + the success alert with nothing uploaded. Sixth instance of the silent-fallback class: the failure had a return channel, nobody read it | `uploadAssetToServer` now throws (`Asset upload failed for "<name>": <cause>`) instead of returning null; the not-applicable early guard still returns null. `ingestAssetFile` wraps the throw so the local-upload path keeps its own "Shared asset upload failed…" wording, and `handlePublishToServer`'s existing `alert(error?.message)` surfaces the real cause. Guard: `src/hooks/useAssetPipeline.test.js` — including a case that wires the real pipeline hook into the real `syncAssetsForPublish` and asserts it rejects (seen failing with "promise resolved 'true' instead of rejecting") | `src/hooks/useAssetPipeline.js` `src/hooks/useAssetPipeline.test.js` |
| **`shared/sceneSchema.cjs` could drift from `src/shared/sceneSchema.js` unnoticed** — the server loads the CJS mirror at boot (`serverXR/src/index.js`, `loadSharedModule('sceneSchema.cjs')`) and applies scene ops with it, while every client uses the ESM copy | `schemaSync.test.js` guarded only the `projectSchema` pair; the scene mirror had constants-only coverage nowhere, so a hand-mirror edit that touched one file and not the other passed the pre-push gate while server and client normalized the same scene differently | New `sceneSchema ESM/CJS mirror equivalence` block in the same file: constants/defaults equality, `normalizeObjects`/`normalizePresentation` over three fixtures, and `applySceneOps` over an add/update(id-hijack)/delete/setSceneSettings batch. Verified to fail against a deliberately drifted copy of the mirror | `serverXR/src/schemaSync.test.js` |
| An uploaded `.svg` (or any file whose *extension* is on the allowlist but whose declared mime is `text/html`) executes script on the app's own origin when its asset URL is opened, against whatever session opens it | `isAllowedUpload` (`serverXR/src/index.js`) accepts on filename extension alone — `if (ext && ALLOWED_EXTENSIONS.has(ext)) return true` — independent of the declared mime, and both asset readers (`spaceStore.serveFile`, the project asset GET) echoed the stored, client-supplied `meta.mimeType` back as `Content-Type` with no `nosniff` and no `Content-Disposition`. No CSP anywhere | Shared `applyAssetSafetyHeaders` (`spaceStore.js`, used by both readers): always `X-Content-Type-Options: nosniff`; `text/html`/`xhtml`/`xml` forced to `application/octet-stream` + `attachment`; `image/svg+xml` keeps its type but gets `Content-Disposition: attachment`. Deliberate: SVG is a legitimate texture format (`src/utils/mediaAssetTypes.js`), and subresource loads (`<img>`, TextureLoader) ignore Content-Disposition while top-level navigation honours it — so scenes still render and the document context is gone. SVG bytes are NOT sanitized. Guard: `httpContracts.test.js` “serves script-capable assets as downloads” | `serverXR/src/spaceStore.js`, `serverXR/src/routes/projectRoutes.js`, `serverXR/src/httpContracts.test.js` |
| A signed-in account gets 403 “Only the space owner or an admin can manage this space” on its OWN sandbox — rename, delete, sync-key mint, invite links, and deleting a project inside it | Sandboxes are created with `ownerUserId: null` on purpose (`ensureOwnSandbox`, so they never count toward the owned-space quota), but `isSpaceOwnerOrAdminState` required a non-null `ownerUserId` matching the subject — so no one but an admin could ever own a sandbox | `isSpaceOwnerOrAdminState` also accepts a session whose subject derives the space id: `meta.kind === 'sandbox' && meta.id === getOwnSandboxSpaceId(state.subject)`, with `guest:` subjects excluded so guests still never qualify. `ownerUserId` stays null, quota untouched. Guard: `httpContracts.test.js` “lets a signed-in account manage its own sandbox space” | `serverXR/src/index.js`, `serverXR/src/httpContracts.test.js` |
| Any visitor can kick the br_id_ge keeper off the live mesh and publish under its identity | `meshHub.js` accepted a duplicate `node=` claim and closed the incumbent (`existing.close(4000,'Replaced')`) — but `node=` is caller-supplied and the relay is unauthenticated by default (`MESH_ROOM_SECRET` is not even settable in the Compose deployment) | A live holder (readyState OPEN/CONNECTING) is never evicted on an open relay; the new claimant is closed with 4409 “Node id in use”. Stale sockets are still replaced (the reconnect case), and a secret-gated room keeps newest-claim-wins since every claimant proved the same secret. Residual: with no secret, a keeper that drops off-network waits for TCP to reap its half-open socket — the hub has no heartbeat. Guard: `meshHub.test.js` “rejects a second claim on a live nodeId” | `serverXR/src/meshHub.js`, `serverXR/src/meshHub.test.js` |
| CI lint was green while serverXR had 49 lint errors | `"lint": "eslint src --ext .js,.jsx"` never reached serverXR/, scripts/ or shared/, and the single serverXR eslint block declared `sourceType: commonjs` for its ESM vitest specs (35 parse errors) | Widened the lint script to `eslint src serverXR scripts shared vite.config.js eslint.config.js`; split serverXR runtime (commonjs) from its `*.test.js` (module + vitest globals); added a Node block for scripts/ + shared/*.cjs; shared `nodeRules` allow empty catch blocks and the `Boolean(x) ? 1 : 0` SQLite idiom | package.json, eslint.config.js |
| three-vendor TDZ guard reported "passed" while proving nothing | `check-three-vendor.mjs` silently `continue`d past any dependency missing from node_modules, and passed with an empty allowlist if the vite.config.js manualChunks scan matched nothing — and no workflow ran it at all | Uninstalled/unparseable deps now fail the check with a `npm ci` hint; an empty allowlist fails too; wired `npm run check:three-vendor` into ci.yml | scripts/check-three-vendor.mjs, .github/workflows/ci.yml |
| A push to `dev` could deploy space code to PRODUCTION | deploy-space-code.yml passed no target, so `space-code-push.mjs` fell back to `DEFAULT_LIVE_URL = https://di-studio.xyz/serverXR`, and it looped over every `spaces/*/code` dir rather than the changed one | Workflow resolves the staging target explicitly, asserts the host is staging.di-studio.xyz before pushing, fails fast on a missing token, and derives the space list from `git diff --name-only <before> <sha> -- 'spaces/*/code/**'` | .github/workflows/deploy-space-code.yml |
| **Staging answered the public internet in plain HTTP** | `docker-compose.staging.yml` never overrode the base file's `${PORT:-80}:8080`, so the staging client published on 0.0.0.0:8081 — the whole SPA plus an unauthenticated `/serverXR/api/health` host fingerprint (node version, kernel, cpu, uptime), confirmed answering live at `http://167.233.216.253:8081/`. Production was already covered by `docker-compose.caddy-hardened.yml`; staging had no equivalent | `ports: !override` binding to `${STAGING_BIND_ADDR:-172.17.0.1}` (the docker0 gateway that `host.docker.internal` resolves to), so production's Caddy still reaches it and the public interface does not. `!reset` does NOT work here — it empties the field and cannot carry a replacement value; `docker compose config` rendered the client with no ports at all | `docker-compose.staging.yml` |
| **A nightly backup that stops running is invisible** | `deploy/vps-backup.sh` wrote its only failure signal to `/root/backups/backup.log`, which nothing reads. The detector existed, the notification hop did not | `fail()` writes a `/root/backups/BACKUP-FAILED` marker, POSTs to `$BACKUP_ALERT_WEBHOOK_URL` when configured, and exits non-zero; `set -Eeuo` + `trap ... ERR` catches the unguarded steps too. The channel itself is still an open choice | `deploy/vps-backup.sh`, `docs/deploy/OFFBOX_BACKUP.md` |
| **A cleanup labelled "staging" could delete on production** | `data-cleanup.mjs` mapped `env: staging` to `LIVE_API_URL`, the same key `space-code-push.mjs`/`space-sync.mjs` treat as PRODUCTION — and the confirmation gate keyed off `plan.env === 'prod'`, so the mislabelled path deleted with no prompt | Staging resolves from `STAGING_API_URL` first (`LIVE_*` legacy alias only), and the gate now keys off the resolved URL: any host that is not localhost and not `staging.*` demands the `delete on prod` phrase, with a loud warning when label and host disagree | `scripts/data-cleanup.mjs` |
| **A space-code push with no target published to the live site** | `space-code-push.mjs` carried `DEFAULT_LIVE_URL = 'https://di-studio.xyz/serverXR'` — the same silent-hardcoded-fallback already removed from `space-sync.mjs` on 2026-08-05, left behind in this copy. The CI path was safe (it passes `--to` and refuses non-staging targets); manual/`npm run space:code-push` runs were not | No default target: `--to` / `STAGING_API_URL` / `LIVE_API_URL`, otherwise a loud error naming both environments | `scripts/space-code-push.mjs` |
| Client and server disagreed forever about which edit won after a version conflict | On a 409 the client replayed the remote ops on top of its optimistic local ops, while the resubmit made the server apply the local ops on top of the remote ones — opposite orders, no reconciliation | After conflict recovery that actually applied remote ops, silently take the server's snapshot (`resyncDocument`: `replace-document` + re-apply the still-queued ops), and filter the 409 body's `pendingOps` (the whole retained window) to `version > baseVersion` so already-applied ops are not replayed | `src/project/hooks/useProjectDocumentSync.js` |
| Client silently missing ops after a long disconnect, while reporting itself up to date | The reconnect catch-up set `versionRef` to `latestVersion` even when the server's bounded op window (maxOpHistory 500) did not reach back to the client's version | `hasOpGap()` — if the returned window does not start at `fromVersion + 1` (or is empty while the server is ahead), resync the full document instead of applying a partial window | `src/project/hooks/useProjectDocumentSync.js` |
| Editor stuck on "Loading project…" over a perfectly good document, onboarding suppressed | `reloadDocument`'s stale-snapshot guard returned early after `load-start` with no terminal dispatch when the realtime catch-up overtook the in-flight initial GET | Dispatch `load-success` with the document already in state and the newer version — reject the stale snapshot, but end the load | `src/project/hooks/useProjectDocumentSync.js` |
| Project stream read "connected" while the document sat silently behind the server | A rejected `onReady` (post-connect catch-up) was dropped by an empty `.catch(() => {})` in the sync service | Forward the rejection to a new optional `onReadyError`; the hook degrades the stream state and pushes a visible activity error | `src/project/services/projectSyncService.js` |
| **Every asset in production ships uncompressed** — `curl -sI` on a hashed chunk returns `content-length: 1614468` and no `content-encoding`, while `nginx.conf` plainly has `gzip on` and the right `gzip_types`. Nothing is misconfigured on inspection and no error appears anywhere | nginx's `gzip_proxied` defaults to **`off`**, and Caddy fronts the client container, adding a `Via` request header — which is exactly the signal nginx uses to classify a request as proxied. So every request through the real deployment path was excluded from compression while the gzip block looked correct. Invisible to status-code and header-presence checks alike, because the *absence* of `content-encoding` is not an error | `gzip_proxied any;` plus `gzip_vary on;` next to `gzip on;`. three-vendor.js: 1,614,468 B → 459,321 B. Verify after deploy with `curl -sI -H 'Accept-Encoding: gzip' https://di-studio.xyz/assets/<chunk>.js \| grep -i content-encoding`. Guard: `deploy-compose.test.js` | `nginx.conf` |
| **A restore from a corrupt backup destroys production and restores nothing** | `vps-restore.sh` ran `rm -rf uploads spaces snapshots di.db … && tar xzf <archive>` inside one `sh -c`, after `docker stop`. `rm` runs first and unconditionally, so a truncated or non-di.iiii archive left the volume empty with the site down — the failure mode a restore exists to prevent | Verify before touching anything (`gzip -t` + assert a `.backup-snapshot.db` member, both *before* the confirmation prompt and the `docker stop`), extract into `/data/.restore-stage-<stamp>` first, then swap by **moving** the live data to `/data/.pre-restore-<stamp>` rather than deleting it. A failed restore now leaves the volume untouched; a completed one is reversible. Guard: `deploy-compose.test.js` | `deploy/vps-restore.sh` |
| **A reserved word can be claimed as a space id, and the resulting space is permanently unreachable** — creating a space with id `studio`/`beta`/`raw`/`admin` succeeds, then every visit to it renders the app's own lane instead, and the word is burned for everyone | `isReservedSpaceSlug` was applied on `PATCH` (added with the slug-hijack fix) but never on `POST /api/spaces`. Space **id** and **slug** resolve on the same URL segment, so guarding only the renameable field left the creation path open | Apply the same guard to the normalized id in `POST /api/spaces`, returning 400. Guard: `fallbackContracts.test.js` — "rejects a reserved word as a new space id" | `serverXR/src/routes/spaceRoutes.js` |
| **Any signed-in account can bind its own space to a stranger's project**, after which the server writes a repo's contents into that project using its own GitHub App credentials | `POST /api/spaces/:spaceId/github-link` called `requireSpaceOwnerOrAdmin`, which proves the caller owns the space **in the URL** — then took `projectId` from the **body** with no check that the project belongs to that space. `syncLinkedSpace` runs immediately on the resulting link. Ownership of one identifier was mistaken for ownership of the request | Look the project up scoped to the space before creating the link — `loadProjectMeta(SPACES_DIR, ctx.spaceId, projectId)` selects on `(id AND space_id)`, so a foreign project is simply not found → 404. Guard: `fallbackContracts.test.js` — "github-link will not bind a space to a project from a different space" | `serverXR/src/index.js` |
| **A file in this repo has no diff and no grep hits — `git show` says `Bin 11633 -> 12856 bytes` and `grep` finds nothing that is plainly in the file** | A **literal NUL byte** in the source. `space-sync.mjs` used one as the `**` placeholder in its glob translator: valid JS, works perfectly, and git classifies the whole file as binary. Every review of it for months was a review of nothing — which is how four copies of the same engine drifted apart twice without anyone seeing a line of it | Write the placeholder as a backslash-u escape in the source, never as a typed control character. `node -e 'console.log(require("fs").readFileSync(F).includes(0))'` finds them; `file <path>` saying "binary data" for a text file is the tell. Guarded by `scripts/space-sync.test.js` | `scripts/space-sync.mjs` |
| **The repo is supposed to be master for a linked space's names, and a rename never arrives** — content syncs byte-perfect, the manifest says one title, all tiers say the old one, and nothing errors | `space-sync.mjs` sent `title` (and originally `slug`) only in the POST that *creates* the project. Every later run pushed files and nothing else, so any field set at creation was frozen for the life of the project | Both are now PATCHed on every run, and report `403`/`409` without failing the content sync. `di-space*.json`'s `label` is the title; `slug` is the vanity URL segment | `scripts/space-sync.mjs` |
| **A sync you meant for staging lands on the live site** | `space-sync.mjs` carried `DEFAULT_LIVE_URL = 'https://di-studio.xyz/serverXR'`, so omitting `--to` published to production rather than failing. The linked repos' vendored copies had dropped it; the upstream copy the docs point at had not | No default target — a sync with no `--to` and no `LIVE_API_URL` now errors. Run `npm run space:sync:vendor` to prove the four copies are equal; it did not exist before 2026-08-05, which is why both drifts went unnoticed | `scripts/space-sync.mjs`, `scripts/space-sync-vendor.mjs` |
| **A space synced to staging reads perfectly but every write to it 500s** — the CI space-sync authenticates, reports "space exists / project exists", then fails on `PUT …/document` and `PATCH …/projects/:id`; server log shows `EACCES: permission denied, open '/data/spaces/<id>/projects/<p>/document.json.<n>.<uuid>.tmp'` | `sync-space-to-staging.sh` imports with `docker exec -u root` (it must, to write `/app/scripts`), so everything the import creates under `/data/spaces/<id>` is **root-owned**, while the server process runs as `app` (uid 100). Reads don't care — the dirs are world-readable — so the space looks perfectly synced until something tries to write. Compare `ls -ld /data/spaces/*`: freshly synced spaces are `root root`, untouched ones `app app` | The script now reads the server's own uid:gid out of the container (`id -u`/`id -g`, never hardcoded) and `chown -R`s the imported space back to it, per space, right after import. To repair an already-broken space: `docker compose … exec -T -u root server chown -R 100:101 /data/spaces/<id>` | `scripts/sync-space-to-staging.sh` |
| **A manual `docker compose up -d` on the VPS silently downgrades the host by weeks** — the site stays up and healthy, only `/serverXR/api/health`'s `release` gives it away (stale `gitCommit`, or all-`null` on an image predating `releaseInfo.js`). Hit **production** on 2026-08-04 during a restart to pick up new env vars | Deploy pins `IMAGE_TAG=prod-<sha>`/`staging-<sha>` **inside its SSH session only** and never writes it to the host `.env`, so a later manual compose op falls back to `${IMAGE_TAG:-latest}` — resolved against the host's **local image cache**. Each host only ever pulls its namespaced tag, so its copy of `latest` is whatever was pulled the last time that tag was used: two weeks old on both hosts. The registry's `latest` was current the whole time, which is why this is invisible from GitHub. Staging's `${IMAGE_TAG:-staging}` default (audit batch 2) did not save it either — its `.env` carried an explicit `IMAGE_TAG=latest`, and an explicit value beats the default. Same silent-fallback class as that fix, one layer lower: the default was right, the resolution wasn't | Both deploy workflows now write the tag they actually ran into the host `.env` (after `up -d`, not before), so any later manual compose op reproduces the deployed image. Guard: `src/deploy-compose.test.js` asserts both workflows persist it and that it happens after `up -d`. **Diagnose with `docker inspect <container> --format '{{.Config.Image}}'` and the `release` block of `/api/health` — a healthy 200 proves nothing about which build answered it** | `.github/workflows/deploy-vps.yml` `.github/workflows/deploy-vps-staging.yml` |
| Any 2D canvas on a HiDPI display paints into the top-left fraction of itself and leaves the rest black, with a hard rectangular edge through the picture — 66% of the frame at a 1.5x display, 50% at 2x. Looks correct on any machine or headless browser running at ratio 1 | The caller scales the context by `devicePixelRatio` (`ctx.setTransform(ratio, 0, 0, ratio, 0, 0)`) after sizing the backing store, and the draw routine then opened with `ctx.setTransform(1, 0, 0, 1, 0, 0)`, throwing that scale away. Every sketch drew in CSS pixels into a buffer `ratio` times larger. **Playwright's default `deviceScaleFactor` is 1**, so no headless check catches it — pass `deviceScaleFactor: 1.5` or read `ctx.getTransform().a` and assert it is not 1 | The draw routine never touches the transform; the caller owns it. Regression guard: `beatSketches.test.js` asserts `paintFrame` makes no `setTransform` call at all | `src/algoVrithm/landing/beatSketches.js`, `src/algoVrithm/landing/beatSketches.test.js` |
| A Studio space card for a space with **no published project** shows the editor instead of the scene — "Enter VR", "Enter AR" and a "Mode: Navigate" chip, top-right, inside a thumbnail | `?preview=1` is the card iframe's contract, but only `PublicProjectViewer` ever read it (`PublicProjectViewer.jsx:53`). A space without a `publishedProjectId` falls through to the generic `<App />` (`SpaceSurfaceApp.jsx:124`), which ignored the flag. Worse, the cluster is rendered **because** the editor UI is hidden (`EditorOverlays.jsx:20`, `!isUiVisible`) — so a thumbnail, which has no UI by definition, was the one place it always showed. The iframe also shares localStorage with the Studio owner viewing it, so a stored `ui-visible:<space>=true` from their own session could open the full toolbar in the card | `isPreviewRequest()` (`src/utils/previewMode.js`); `EditorOverlays` drops `enter-vr`/`enter-ar`/`interaction-mode` under it and **keeps `exit-xr`** (no way out of a session is worse than a button nobody sees); `useUiState` treats preview as UI-hidden, ahead of the stored value. Guard: `EditorOverlaysPreview.test.jsx` — watched fail (one test per half) with each condition reverted. Verified in a browser at DPR 2 against the same backend with and without the flag: identical scene, chrome only in the control | `src/utils/previewMode.js`, `src/components/EditorOverlays.jsx`, `src/components/EditorOverlaysPreview.test.jsx`, `src/hooks/useUiState.js` |
| **Keyboard scrolling is dead on every long page** — Space, PageDown/PageUp, the arrows, Home and End do nothing on `/`, `/wiki`, `/wcc` and `/algovrithm`. A wheel works, so it only reads as broken to someone using keys | `html/body/#root` are `position: fixed` (`src/styles/base.css`), so the document never scrolls and each page root owns the scroll — and the browser only drives a scroller that is FOCUSED. A plain `<main>`/`<div>` cannot take focus, so focus sits on `BODY`, the unscrollable fixed element. Focusing the root on mount fixes it only until the first click, which hands focus straight back to `BODY` — anything keyed on where focus happens to be keeps finding a new way to be wrong. Fixed on `/algovrithm` alone first, and stayed broken everywhere else for weeks because the fix was written into that page rather than shared | `useKeyboardPageScroll(ref)` handles the keys outright on the ref'd scroller (typing, `Space` on a button and browser shortcuts are left alone), applied to all four roots. Guard: `useKeyboardPageScroll.test.jsx` — watched fail (10 of 13) with the hook neutered. Verified in a real browser at DPR 2: all four scroll on PageDown and End **with focus forced back onto `BODY`**, and `/wcc`'s pinned ScrollTrigger track follows a keyed scroll | `src/hooks/useKeyboardPageScroll.js`, `src/hooks/useKeyboardPageScroll.test.jsx`, `src/landing/LandingPage.jsx`, `src/wiki/WikiPage.jsx`, `src/wcc/landing/LandingPage.jsx`, `src/algoVrithm/landing/AlgoVrithmLanding.jsx` |
| The `/algovrithm` poster canvas shows two beats fighting at every handover — grey mush where the frame should be black-on-white, bars and rings overlaid with neither readable, and what looks like a torn frame with a hard horizontal seam | `paintFrame` drew every live beat at its cross-fade weight, faithfully reproducing the piece's 1.2s overlaps. But each sketch OPENS by filling the frame with its own world colour, so at a seam the second fill washes the first at half strength and both beats land on the average of their two worlds — two white-world beats crossing turn the frame mid-grey and the black forms in it vanish. A true dissolve needs each sketch composited opaquely offscreen and then blended | `paintFrame` draws only the strongest live beat, at full alpha (ties → the later one, matching `leadBeatAtSec`). Found by sampling per-row mean luminance across the whole 53s loop and looking for the largest adjacent-row jump — it sat at 201 on one fixed row for seconds; a screenshot of that moment showed the mud. Regression guard: `beatSketches.test.js` asserts one `save` per frame at a seam where `beatsAtSec` returns 2 | `src/algoVrithm/landing/beatSketches.js`, `src/algoVrithm/landing/beatSketches.test.js` |
| **The full suite fails intermittently — a different file each time** (`installBundleContracts`, `SpaceHub`, `PublicProjectViewer`), all passing in isolation and on an idle machine, so "green" stops meaning anything | Three defaults sized for an idle machine, all measuring the *scheduler* rather than the behavior under test. **Reproduce it deliberately by running two full suites concurrently** — 5 of 6 runs failed that way, vs 8/8 clean sequentially. (1) Vitest's 5s per-test budget applies to contract tests that spawn a real serverXR process; two failed at 5074ms and 5095ms, and `waitForHealth` alone allows 15s for the boot, so the per-test budget could never cover it. (2) Testing Library's `findBy*`/`waitFor` default to 1000ms; PublicProjectViewer's walk-mode button resolved at 1405ms. (3) `SpaceHub` queried the preview iframe **synchronously** — it is two settles behind the card (IntersectionObserver sets `visible`, the effect that runs after *that* render asks `requestPreviewBoot` and sets `booted`), so it was a race, not an assertion | (1) `vi.setConfig({ testTimeout: 25_000, hookTimeout: 40_000 })` in all five server-spawning contract files. (2) `configure({ asyncUtilTimeout: 5000 })` in `src/setupTests.js` — still far under the per-test budget, so a genuinely stuck query fails with the same message, just later. (3) The two SpaceHub preview reads go through `waitFor`. Verified by re-running the same two-suites-at-once reproduction: 10 summaries, 1561/1561 every time. **Note the racy `getFreePort` (`listen(0)` → close → hand the port to a child) duplicated in all five contract files — not the cause here, but a real TOCTOU if these ever run more parallel** | `src/setupTests.js` `src/studio/components/SpaceHub.test.jsx` `serverXR/src/*Contracts.test.js` |
| **One-click GitHub→space sync says "not configured" and push webhooks never arrive**, on both prod and staging, with nothing in the logs — while `br_id_ge` keeps syncing fine (it uses its own `sync-space.yml` CI path, not the App) | The App's three secrets lived in cPanel's `~/.config/dii/<env>.deploy.env`, baked into the server env by `write-server-env.mjs`. The 2026-07-15 VPS move replaced that whole mechanism with compose, and `docker-compose.yml` was written with only the OAuth `GITHUB_CLIENT_*` vars — `GITHUB_APP_ID`, `GITHUB_APP_PRIVATE_KEY_B64` and `GITHUB_APP_WEBHOOK_SECRET` were never carried over, so `githubApp.isConfigured()` was false for three weeks. Silent by design: an unconfigured App answers `{configured: false}` and 401s the webhook rather than erroring, so no alert ever fired. Same silent-fallback class as staging's `:latest` image tag, applied to a feature's secrets — **a var absent from compose never reaches the process, whatever the host `.env` says** | The three vars are passed to the `server` service in `docker-compose.yml` (and `STAGING_`-prefixed in `docker-compose.staging.yml`), documented in `.env.example`, and the rotation runbook was rewritten for the VPS. `_PATH` is deliberately not passed — `getPrivateKey()` prefers it, so an empty one would shadow `_B64`. Guard: `src/deploy-compose.test.js` derives the required names from the `process.env.GITHUB_APP_*` reads in `githubApp.js`, so a new var can't be added there and left out of compose the same way | `docker-compose.yml` `docker-compose.staging.yml` `.env.example` `docs/ops/ROTATE_GITHUB_APP_SECRETS.md` |
| **Every `<Canvas>` dies with `R3F: Hooks can only be used within the Canvas component!`** on a page that did not change - thrown from `<XR>`'s `useThree`, landing page included, and reproducible in a brand-new browser with an empty cache | Worktrees symlink `node_modules` back to the main checkout, so vite's default `cacheDir` (`node_modules/.vite`) was ONE shared dep-optimizer cache for every dev server on the machine. A second server re-optimizing rewrites the chunk files under the first: its `@react-three_fiber.js` still pointed at the pre-swap chunk while `@react-three_xr.js` resolved the post-swap one, so R3F was loaded twice - two module instances, two React contexts, and the store lookup crossed them. Both chunks answer 200, nothing is missing, and no application code is involved, so it reads as an app bug | `cacheDir: path.resolve(ROOT_DIR, '.vite-cache')` in `vite.config.js` - ROOT_DIR is the config file's own directory, so each worktree caches in itself and servers stop stomping each other. Diagnose lookalikes by listing loaded `react-three-fiber.esm-*` chunks: more than one distinct hash IS the bug. Guard: `vite-cache-dir.test.js` | `vite.config.js` `.gitignore` |
| **A published code page never sees its own URL query.** `di-studio.xyz/<space>/<page>?just=bkyi` loads, the top URL keeps the param, but `new URLSearchParams(location.search)` inside the page is empty — so a hand-over between two published pages (br_id_ge rite → field: "go to the core I just made") silently lands on a generic page | Published code pages render through `PublicProjectViewer` as `srcDoc`, and a srcdoc document has no URL of its own: its `location` is `about:srcdoc`, so `search`/`hostname` are empty. Nothing was broken in the navigation (`target="_top"` from the sandboxed iframe works — `allow-top-navigation-by-user-activation` is in `PAGE_SANDBOX`); the parameter is dropped at the iframe boundary. Cannot be recovered inside the page: `history.replaceState` throws on an opaque URL, and `parent.location` is cross-origin unless the space sets `deviceAccess` (which adds `allow-same-origin`) | The bootstrap injected into every preview document now declares `window.diiPageQuery` (+ a parsed `window.diiPageParams`), passed down from the shell's `window.location.search`. Pages read `new URLSearchParams(window.diiPageQuery \|\| location.search)` — the fallback keeps local mirrors and `file://` working. The query is address-bar controlled, so it is serialized with every `<` rewritten to `\u003c` (a bare `JSON.stringify` would let `</script>` close the bootstrap tag). Guards: `presentationPreviewDocument.test.js` (injection, escaping, empty default) | `src/utils/presentationPreviewDocument.js` `src/project/components/PublicProjectViewer.jsx` |
| **Any anonymous visitor can read the GitHub App's full installed-repo list** (including collaborators' private repos) plus the App name and install URL, by hitting `/api/github/repos` after a plain page load | `requireSignedInUser` returned true whenever `state.authenticated && state.type === 'session'`. Guests carry the SAME signed session cookie as accounts and `readAuthSession` resolves them to `type: 'session'` on every request after issuance — the literal `type: 'guest'` exists only in the issuance branch. So the type test identified nobody. The route's comment scopes it to "every signed-in user by design"; anonymous guests are not that audience. The 60s `_repoCache` made it cheap to poll. Sixth instance of the same guest-identity confusion audit batch 1 fixed for commons publishing | Identify by subject: `state.type === 'guest' \|\| isGuestSubject(state.subject)`, matching every other guest check in the file. Guard: `fallbackContracts.test.js` asserts a guest cookie gets 403 from both GitHub routes | `serverXR/src/index.js` |
| **Collaborators silently hold divergent documents** — the same entity under different ids on different clients, until a full reload | `normalizeIncomingOps` filled in a missing `opId` but left `payload` untouched. `applyProjectOps`' create/upsert cases mint a random id when the payload lacks one, so the server persisted ITS id and then broadcast the ORIGINAL id-less op — every client re-applied it through the same shared code and minted a different id. The sender's optimistic id never matched the server's either. `invertSingleOp` already refused to invert id-less creates, acknowledging the hazard; apply still accepted them | New pure `serverXR/src/opValidation.js`: `findIdlessCreateOp` rejects createEntity/createNode/createEdge/upsertAsset without a usable string id, wired into `POST /api/projects/:id/ops` as a 400 `op_missing_id`. Guards: `opValidation.test.js` + a `fallbackContracts.test.js` HTTP case | `serverXR/src/opValidation.js` `serverXR/src/routes/projectRoutes.js` |
| **Collaborators' live edits arrive late or in bursts on the deployed stack**, while realtime works perfectly under the Vite dev proxy | The SSE endpoints (`/api/projects/:id/events`, `/api/spaces/:id/events`) fall into nginx's generic `location /serverXR/` block, which keeps `proxy_buffering on` — so nginx is free to hold small SSE writes. Same class as the known `/serverXR/mesh` miss, where the websocket upgrade was dropped by the same generic block. **Never probe this with curl over HTTP/2 — it returns false 404s** | `res.setHeader('X-Accel-Buffering', 'no')` on both SSE responses (nginx honors it per-response, so no nginx.conf change is needed). Guard in `src/deploy-compose.test.js` | `serverXR/src/routes/projectRoutes.js` `serverXR/src/routes/spaceRoutes.js` |
| **`/serverXR/api/health` reports the wrong `deployEnv`** for the host you are actually on | Both deploy workflows pushed `dii-*:${{ github.sha }}`, but the images differ — `DEPLOY_ENV` is baked into `release.json` at build time. On the normal fast-forward dev→main promote the same sha is rebuilt and the tag overwritten, so a re-pull could hand a host an image claiming the other environment | Namespaced the per-commit tags: `:staging-<sha>` and `:prod-<sha>`. **`IMAGE_TAG` is no longer a git ref** — the remote `git checkout` of the compose files now uses a separate `GIT_SHA`. Guard in `src/deploy-compose.test.js`; documented in `docs/deploy/VPS_DOCKER_DEPLOY.md` | `.github/workflows/deploy-vps.yml` `.github/workflows/deploy-vps-staging.yml` |
| `StrictHostKeyChecking=yes` in the deploy workflows was decorative | `known_hosts` was populated by an unauthenticated `ssh-keyscan` of `VPS_HOST` seconds before connecting — trust-on-first-use, repeated on every single deploy. Anything that answered got the deploy session and the key it carries, and could fake a successful run while prod never updated | Pin the real key via a new `VPS_HOST_KEY` repository variable (`ssh-keyscan` once, from a trusted machine). Absent, the workflow warns loudly and falls back to the old behavior rather than failing the deploy. Guard in `src/deploy-compose.test.js` | `.github/workflows/deploy-vps.yml` `.github/workflows/deploy-vps-staging.yml` |
| **/admin → Manage shows a "Public link" that opens the wrong surface** — a project titled "Studio"/"Beta"/"Raw" links to that lane's hub instead of the project | The field passed `project.slug \|\| project.id` into the **vanity** slot, but project ids are minted from the title with no reserved-word check (only *slugs* are validated against reserved words, at PATCH time). So a legal id like `studio` produced `/{space}/studio`, which RootApp dispatches to the Studio hub before `/api/resolve` is ever consulted. ProjectSwitcher already handled this correctly | Use `buildVanityProjectPath` only when a real `slug` exists, else `buildPublicProjectPath` (`/{space}/p/{id}` — "the guaranteed-stable fallback: use it whenever only raw ids are in hand"). Guards in `AdminManageSection.test.jsx` | `src/components/preferences/AdminManageSection.jsx` |
| **`A` (select all) and `Alt+A` (deselect all) do nothing** in Studio, though the shipped help dialog advertises both; with a selection, `A` starts an all-axis transform instead | StudioShell's pre-modal axis branch included `'a'` and consumed it with `preventDefault` + `stopImmediatePropagation`. StudioShell is StudioEditor's child, so its window listener registers FIRST and the stopImmediatePropagation killed StudioEditor's select-all handler on the same target. Alt+A fell in too — the branch excluded shift/ctrl/meta but not alt | Dropped `'a'` from the arming list (all-axes stays inside ModalTransform's own capture handler, where the modal documents it). Guard: `src/studio/utils/studioKeyboardContract.test.js` — also asserts the help dialog still promises both, so code and docs can't drift apart again | `src/studio/components/StudioShell.jsx` |
| **`Esc` during a modal transform (G/R/S) commits the move instead of cancelling it**, persisting and broadcasting the transform the user was aborting | `handleKeyDown` routed Escape into the same `finish()` → `commitIfMoved()` branch as Enter and Space, and nothing ever restored the captured `e.base` transforms. The help dialog documents 'Click · Enter · Space: Confirm' and 'Esc: Cancel' as distinct, matching the Blender convention this operator copies | Escape now runs `revertToBase()` (restores pos/rot/scale from `e.base`, pushes it through `onPreview`, clears `moved`) before `finish()`. Guard: `ModalTransform.test.jsx` covers Esc-reverts and Enter-still-commits | `src/studio/components/ModalTransform.jsx` |
| **Ctrl/Cmd+F doesn't open find-in-page** in Studio when an entity is selected — the viewport camera frames the selection instead | The frame-selected branch checked only the key, unlike every sibling binding that shares a key with a browser shortcut (Delete, A) which guard on `!meta`. Ctrl/Cmd+F is page-preventable in Chrome and Firefox, so `preventDefault` really did suppress the find bar | Added the `!meta` guard. Covered by `studioKeyboardContract.test.js` | `src/studio/components/StudioEditor.jsx` |
| **Backspace in the node graph deletes the scope you are standing inside**, cascading over its whole subtree and dumping you back to the parent with everything gone (activity just says "Deleted node.") | The Delete/Backspace effect fired `onDeleteNode(selectedNodeId)` whenever anything was selected, with only an input-focus guard — never checking that the node is among the surface's rendered cards. Entering a card keeps it selected (pointerdown selects, then dblclick enters), so the selection is the scope itself. Present in BOTH lane forks | `if (!nodeById.has(selectedNodeId)) return` inside the handler, in `RawGraphSurface.jsx` and `BetaGraphSurface.jsx`. Guards in both lanes' surface tests | `src/raw/components/RawGraphSurface.jsx` `src/beta/components/BetaGraphSurface.jsx` |
| **Clicking a node in a Raw/Beta world panel opens the inspector but shows no in-scene selection pill** (entities selected the same way do show theirs), and clicking empty space never clears a node selection | Both editors and both WorldPanelWindows pass `selectedNodeId`, but the outer `RawViewport`/`BetaViewport` components never destructured it and never forwarded it to `SceneContent` — so `selected={node.id === selectedNodeId}` always compared against `undefined`. `onPointerMissed` also only called `onSelectEntity(null)` | Accept and forward the prop in both viewports; `onPointerMissed` clears the node selection via `onClearSelection` when one exists (an op, so only paid when needed) and otherwise keeps the cheap local entity clear. Guards in both viewport tests | `src/raw/components/RawViewport.jsx` `src/beta/components/BetaViewport.jsx` |
| **"Fullscreen world" / overlay on a second World panel opens a DIFFERENT world** than the one clicked | `universe.world` is not a singleton, but the fullscreen/overlay renders always display `worldNode` — the scope's live-marked (Raw) or first-created (Beta) world — while the panel buttons only flipped a boolean | Raw: `markWorldLive(node)` before engaging, reusing the existing `liveWorldNodeIdByScope` marker (also now the implementation of `onSetLive`). Beta has no marker, so it records `requestedWorldNodeId` in local state, renders `displayedWorldNode`, and clears the request when both views close | `src/raw/components/RawEditor.jsx` `src/beta/components/BetaEditor.jsx` |
| **A published space silently diverges from what collaborators see** until a full reload — the viewer is missing edits that everyone else has, with nothing in the console | `onReady`'s `await listProjectOps(...)` had no error handling, and `projectSyncService.handleReady` wraps `onReady` in `.catch(() => {})`. After an SSE reconnect the catch-up could fail silently; the stream then delivered op N+5, which applied straight onto the version-N document and advanced the version — ops N+1..N+4 never fetched, never applied. Both the walk-mode scene and the public viewer had it | `try/catch` around the catch-up in both, falling back to `reloadDocument()` | `src/components/LiveProjectScene.jsx` `src/project/components/PublicProjectViewer.jsx` |
| **Ambient audio in a published space stutters back to the start** whenever anything unrelated changes (a collaborator nudges volume, a loop toggle, an editor slider tick) | The HTML-audio fallback effect decided whether to swap sources with `audioEl.src !== sourceUrl`. `HTMLMediaElement.src` always reads back ABSOLUTE, while `sourceUrl` is a server-relative `/serverXR/api/…` path in every real document — so the comparison was **always** unequal and every effect re-run paused, reassigned `src` and re-buffered the file | Compare against `resolveAudioSrc(sourceUrl)` (exported from the same module), which absolutizes the url the same way the element does. Guard: `AudioObject.test.jsx` counts `src` assignments across a volume-only rerender | `src/objectComponents/AudioObject.jsx` |
| **A Beta/Raw local workspace silently stops saving** — the editor keeps working, then a reload reverts to a much older document | `writeLocalWorkspaceDocument` returns `false` when `localStorage.setItem` throws (realistic: the whole node document is stringified on every change), but both autosave effects discarded the return value, so saving stayed dead for the rest of the session with no warning | Both effects check the result and raise a one-time `console.error` + alert, the same shape as the asset-store quota path in `useAssetRestore` | `src/beta/components/BetaEditor.jsx` `src/raw/components/RawEditor.jsx` |
| **A .studio.zip export or scene archive contains HTML where an image/model should be** (re-import then uploads the corrupt bytes), or a restored asset renders as a broken texture with no error anywhere | Four more instances of the *blank prod images* class: `fetchAssetBlob` (studioProjectBundle), `resolveAssetEntries` (sceneArchive), `hydrateAsset` (useAssetRestore) and the PDF branch of `handleCreateFromAsset` all accepted any `response.ok` with no content-type check. A stored verbatim `/api/…` candidate hits nginx's SPA fallback on prod and answers **200 text/html**, so the HTML shell is consumed as the asset's bytes — and in sceneArchive it also set `resolved = true`, bypassing the `onMissingAsset` report meant to surface bad assets | All four now reject HTML via the shared `isHtmlLikeMimeType` (`src/utils/assetContentType.js`) and fall through to the next candidate; the PDF branch additionally resolves through `mountRelativeApiUrl(asset.url) \|\| buildProjectAssetUrl(...)`. Guards: `sceneArchive.test.js`, `studioProjectBundle.test.js`, `useAssetRestore.test.jsx`, plus `src/services/assetUrlFetch.test.js` — a source-level invariant that fails on any `fetch(asset.url)` in the watched editor files, so instance #10 can't ship | `src/project/transfer/studioProjectBundle.js` `src/services/sceneArchive.js` `src/hooks/useAssetRestore.js` `src/studio/components/StudioEditor.jsx` |
| **"Couldn't load this space → Retry" never recovers** — clicking Retry forever changes nothing, only a full page reload fixes it | `ensureGuestSession` memoized `getApiSession().catch(() => null)` at module scope, so a single transient failure on first paint (backend restarting) cached a resolved-null promise for the page's lifetime. Every later `reloadDocument` — including the Retry button and the 3s auto-retry — awaited that same promise and proceeded with no session cookie, 401ing forever on an auth-required deployment | The cache is cleared inside the `catch`, so only a *successful* attempt stays memoized. Extracted to `src/services/guestSession.js` to make it testable; guard: `guestSession.test.js` | `src/services/guestSession.js` `src/components/LiveProjectScene.jsx` |
| **Mouse-look and touch-look are dead in a published space** (WASD still works), reproducible just by naming an entity `gate`/`threshold`/`Entrance` | Second instance of the playerRef-identity bug: the gate-entity effect did `playerRef.current = { ... }` while Walker's input listeners, wired once on mount, close over the *original* object. Look mutated the discarded object; the camera's useFrame read the new one. (The spawn effect right above it had already been fixed for exactly this and carries the explanatory comment.) The literal also dropped `altY` | `Object.assign(playerRef.current, { ... })` — mutate in place, never replace. Guard: `src/components/livePlayerRef.test.js` fails on any `playerRef.current =` reassignment in the file, so a third instance can't ship | `src/components/LiveProjectScene.jsx` |
| **Beta tab freezes with many nested worlds**, and one node's floating windows appear over a different scope's graph | `visibleViewNodes` filtered on `isPanelNode` + `frame.visible` only — no scope, no fullscreen check — so every `universe.world` in the whole document kept a live `<Canvas>`/WebGL context mounted in every scope. Browsers cap ~16 concurrent contexts, then kill the oldest; the context guard remounts, killing another, and the tab locks. Raw had already fixed this via `selectMountedPanelNodes`; the fix was never ported to the Beta fork | Ported `selectMountedPanelNodes` into `src/beta/utils/windowLayout.js` and used it in BetaEditor (scope-filtered, `[]` behind a fullscreen world). Guard: `src/beta/utils/windowLayout.test.js`, mirroring Raw's | `src/beta/components/BetaEditor.jsx` `src/beta/utils/windowLayout.js` |
| **The whole Studio editor keyboard and the drag gizmo go dead** — delete/copy/paste/duplicate/frame/escape all no-ops, gizmo never appears — until a reload. Happens after starting a modal transform (G/R/S) in a collaborative session | `StudioSceneContent` renders `ModalTransform` only while `transformOp && selectedEntities.length > 0`, and the projectStore prunes selection when the entity leaves the document (a collaborator's delete) or clears it on `replace-document`. ModalTransform's cleanup committed and re-enabled controls but never called `onCancel`, so `transformOp` upstream stayed truthy forever and every handler early-returns on it | An **unmount-only** effect (`[]` deps — not the main effect's cleanup, which also runs on an `op.seq` change and would cancel the *new* op) calls `onCancel` when `sessionRef.current` is still open. Guard: `ModalTransform.test.jsx` covers unmount-cancels, seq-change-doesn't, and no double-cancel after Enter | `src/studio/components/ModalTransform.jsx` |
| **staging.di-studio.xyz silently runs production code** after any manual `docker compose` in the staging checkout (reboot recovery, restart after an OOM) — `/serverXR/api/health` even self-reports the wrong environment | `docker-compose.staging.yml` never overrode `image:`, so it inherited `docker-compose.prod.yml`'s `${IMAGE_TAG:-latest}`. `:latest` is pushed only by the **production** workflow; the staging workflow pins `IMAGE_TAG=<sha>` inside its SSH session and never writes it to the staging checkout's `.env`. The `:staging` tag the staging workflow pushes was referenced by nothing. Silent-hardcoded-fallback class, applied to deploy config | Both staging services now pin `ghcr.io/…/dii-{server,client}:${IMAGE_TAG:-staging}`. Guard: `src/deploy-compose.test.js` asserts staging never defaults to `latest`, prod still does, and the `:staging` tag is still pushed | `docker-compose.staging.yml` |
| **A space's GitHub sync silently empties the project**: after a webhook/manual sync the project keeps only its code files — assets list, `projectMeta`, and owner opt-ins like `presentationState.deviceAccess` (the camera fix) are gone, and the sync run reports success | `syncLinkedSpace` merged onto `httpRequest(docUrl).then(r => r.json()).catch(() => ({}))`: a network error, a non-2xx, or a JSON parse failure all collapsed to `{}`, and the following PUT is a **full no-baseVersion document replace**. So any transient failure of the internal GET published an empty base over the live document | The merge moved into the pure `spaceSyncPlan.buildSyncedDocumentBody({ current, codeFiles })`, which throws when `current.document` is missing/not an object; the caller now also checks `curRes.ok` and rejects instead of swallowing. The trailing `publishedProjectId` PATCH logs instead of `.catch(() => {})`. Guard: `spaceSyncPlan.test.js` | `serverXR/src/index.js` `serverXR/src/spaceSyncPlan.js` |
| **A live-space (V1 scene) edit looks saved, then vanishes on reload** — no error, no red dot, only a `[sync] Op flush failed:` line in the console | `useLiveSync`'s `flushLiveOps` handled 409 conflicts but sent every other failure (network blip, 500, expired auth) to `console.warn` + `break`. There was no retry timer and no error state, so the flush only ran again on the next local edit — the Studio/Beta document path had been fixed for exactly this (`useProjectDocumentSync` retry contract) and the scene-sync path was never updated | Non-409 failures now set `sceneFlushError` and schedule a re-flush after `LIVE_FLUSH_RETRY_MS` (4s), matching `SYNC_RETRY_DELAY_MS`; a successful submit clears the error. Guard: `useLiveSync.test.jsx` asserts the same ops are re-sent and the error clears | `src/hooks/useLiveSync.js` |
| **An edit disappears right after a version conflict**, with an unhandled promise rejection in the console and no "sync failed" indicator | In `flushQueue`, the batch is `splice`d off the queue *before* submit. On a 409 with no `error.data.pendingOps` the handler awaited `listProjectOps(...)`; if that call threw (backend flapping — the same conditions that cause sustained 409s), the exception unwound past the `pendingQueueRef.unshift(...batch)` on the line below, so ops already applied optimistically were dropped. Directly violated the file's own "never drop an edit" contract | Re-queue **before** the catch-up await, and wrap the catch-up in its own try/catch that surfaces `pending-sync-error` and schedules the standard delayed retry. Guard: `useProjectDocumentSync.test.jsx` ("never drops the batch when the 409 catch-up request itself fails") | `src/project/hooks/useProjectDocumentSync.js` |
| **Switching projects hangs on "Loading project…" forever** (or, rarer: an edit made in project A lands in project B) | The editors render `useProjectDocumentSync` without a `key`, so a project switch reuses the hook instance — but `versionRef`, `pendingQueueRef` and the seen-op sets were never reset. `reloadDocument`'s stale-version guard (`nextVersion < versionRef.current`) then compared the *new* project's version against the *old* project's and returned with no terminal dispatch, leaving `loading: true` with nothing to re-trigger it. Ops still queued for A flushed with B's `projectId` | An effect keyed on `projectId` resets `versionRef`, the pending queue, the seen-op set/order and both timers before the load runs. Guard: `useProjectDocumentSync.test.jsx` ("loads a second project whose version is lower") | `src/project/hooks/useProjectDocumentSync.js` |
| **A node becomes permanently undeletable and every collaborator's editor crashes on delete** — server 500s and clients throw `RangeError: Maximum call stack size exceeded` inside the projectStore reducer | `applyProjectOps`' `deleteNode` collected descendants with no visited-set guard, while `normalizeProjectNode` accepts any `parentId` including a self-reference or a mutual cycle. One `createNode {id:'a', parentId:'a'}` poisons the document forever. (Audit batch 1 fixed the identical `deleteEntity` half; this is the `deleteNode` twin that was missed) | `if (toDelete.has(id)) return` as the first line of `collect`, in **both** `src/shared/projectSchema.js` and `shared/projectSchema.cjs`. Guard: `projectSchema.test.js` covers a self-parent and a mutual cycle | `src/shared/projectSchema.js` `shared/projectSchema.cjs` |
| **Prod `/main` (public viewer) shows no images — only primitives + dim dark placeholder tiles; every asset request is 200, zero console errors.** Network tab shows image fetches going to `/api/projects/…/assets/…` returning `content-type: text/html` (the SPA shell via nginx fallback), while document/events correctly go to `/serverXR/api/…` | Documents carry `assets[].url` recorded as server-relative `/api/…` paths (written when the API answered at the site root). `LiveProjectScene`'s assetMap used a stored `asset.url` **verbatim** — the `buildProjectAssetUrl`/`apiBaseUrl` remount only ran when `url` was empty, and `registerAssetSources` (which does remount, incl. `buildApiMountedUrl`) only runs inside the Studio editor, never the public viewer. TextureLoader fails silently on the HTML payload → placeholder tile. The 200s make it invisible to status-code checks — a fifth instance of the *silent fallback* bug class (nginx SPA fallback masquerading as the asset) | `mountRelativeApiUrl()` exported from `assetSources.js` (remounts relative `/api/…` onto `apiBaseUrl`, leaves absolute URLs alone). **The load-bearing fix is in the shared `buildAssetMap`** — `/main` actually renders via PublicProjectViewer's orbit view → `StudioViewport` (the LiveProjectScene walk-mode assetMap patch alone changed nothing; found by stack-tracing the image loads). Beta/Raw/PortalObject share the same helper. Guards: `buildAssetMap.test.js`, `assetSources.test.js`. Diagnose lookalikes by checking `content-type` of asset responses, not status | `src/project/viewport/buildAssetMap.js` `src/components/LiveProjectScene.jsx` `src/services/assetSources.js` |
| Raw: entering a world node via the window's "Enter ›" button changes scope (breadcrumb shows the world) but never engages fullscreen, and inside the world scope there's no `← World`/fullscreen affordance at all — while the panel's "Fullscreen world" button from the *parent* scope works | `handleEnterNode` set `setIsWorldFullscreen(true)` then changed scope, but `worldNode` was resolved only among *children* of the current scope. Once the scope became the world itself, `hasWorldNode` went false and the no-world effect immediately reset fullscreen — a set-then-cancel race between the enter handler and the cleanup effect | Inside a world scope, the world *is* the scope node: `resolveScopeWorldNode()` (extracted to `viewportWorldState.js`) returns the scope node when it's a `universe.world` before falling back to live-marked/first world child. Guard: `viewportWorldState.test.js` regression case | `src/raw/components/RawEditor.jsx` `src/raw/utils/viewportWorldState.js` |
| Palette says "authoring only — doesn't compute or render anything yet" on a node type that demonstrably computes (found on `time` right after it was implemented) | Implementing a node type means removing it from `UNIMPLEMENTED_NODE_TYPES` AND deleting its `authoringOnly` flag — the implementing commit did only the first; nothing tied the flag to reality | Dropped the stale flag. Guard: `nodeRegistry.test.js` parses `nodeGraphRuntime.js` for `case '<typeId>':` and asserts no evaluated type carries `authoringOnly` | `src/project/nodeRegistry.js` `src/project/nodeRegistry.test.js` |
| Camera dead in published pages online (esp. mobile — br_id_ge rite's getUserMedia/MediaPipe lamp), while the same HTML works served standalone/locally | TWO stacked causes. (1) Published/preview iframes had `sandbox` but no `allow` attribute → permissions policy denies camera/mic/sensors. (2) Even with `allow`, a `srcdoc` iframe whose sandbox lacks `allow-same-origin` has an **opaque origin**, and Chrome rejects `getUserMedia` there outright (`SecurityError: Invalid security origin`) — no attribute combination short of a real origin can fix it. Local serving has no iframe, hence "works locally, dead online". Found by the Playwright fake-camera probe, not code reading — the `allow` fix alone looked complete | (1) `allow="camera; microphone; fullscreen; xr-spatial-tracking; accelerometer; gyroscope; magnetometer"` on all six published/preview iframes. (2) Owner opt-in `presentationState.deviceAccess` (normalized in BOTH `src/shared/projectSchema.js` and `shared/projectSchema.cjs`) — when true, PublicProjectViewer/StudioPresentationSurface render `sandbox` with `allow-same-origin` so the page gets a real origin; default stays fully isolated. Guards: PublicProjectViewer.test.jsx asserts allow contains camera, sandbox has allow-same-origin only with the opt-in | `src/project/components/PublicProjectViewer.jsx` `src/studio/components/StudioPresentationSurface.jsx` `src/components/PresentationCanvas.jsx` `src/shared/projectSchema.js` `shared/projectSchema.cjs` |
| **CI red on a run where every test passes**: `Test Files 170 passed / Tests 908 passed` followed by `Errors 1 error` → `ReferenceError: window is not defined` at `getCurrentEventPriority` ← `refreshStatus` (`hooks/useDriveImport.js:49`), attributed to `AssetPanel.test.jsx`. Blocked the prod deploy 2026-07-28. | `useDriveImport` kicks off `getDriveStatus()` from a mount effect and calls `setStatus` when it resolves, with no unmount guard. When the surface closes (or a test file tears down) before the fetch settles, the setState runs against a dead tree — in jsdom the environment is already gone, so React's internals touch an undefined `window`. Vitest reports it as a suite-level *error* rather than a test failure, so the run exits 1 while every assertion is green. Timing-dependent: it does not reproduce reliably. | An `alive` ref set in a mount effect and cleared on unmount; both `setStatus` calls in `refreshStatus` are gated on it. General rule for this class: any `await` in a hook that setStates on resolve needs an unmount guard — look for the "all tests passed but exit 1" shape and read the `Errors` line, not the failure list. | `src/hooks/useDriveImport.js` |
| **br_id_ge co-presence dead: `wss://di-studio.xyz/serverXR/mesh` returns HTTP 404 on prod AND staging while `/serverXR/api/health` returns 200 (found 2026-07-28, mid-festival).** Every `meshHub.test.js` case was green the whole time. | The hub is only reachable through the client container's nginx, and `nginx.conf` had a WebSocket-upgrade `location` for `/serverXR/socket.io/` but none for `/serverXR/mesh`. The upgrade request fell into the generic `location /serverXR/` block, which uses the default `proxy_http_version 1.0` and so drops `Upgrade`/`Connection` (hop-by-hop headers nginx strips). The backend saw a plain `GET /serverXR/mesh`, no `upgrade` event fired, Express had no such route → 404. The VPS migration (2026-07-15) rebuilt this config and carried over only the socket.io rule; on cPanel the upgrade passed generically through Apache config that lived outside the repo. | Added a `location /serverXR/mesh` block mirroring the socket.io one (`proxy_http_version 1.1`, `Upgrade`, `Connection "upgrade"`, `proxy_read_timeout 86400s`). `proxy_pass` must keep the full `/serverXR/mesh` prefix — `meshHub.js` compares the upgrade URL with a strict `!==`, so any rewrite to `/mesh` silently drops the connection. Deploy rebuilds the **client** container, not `server`. Guard: `meshHub.test.js` now parses `nginx.conf` and asserts the mesh block exists, forwards the upgrade, and preserves the prefix — verified to fail against the pre-fix config. | `nginx.conf` `serverXR/src/meshHub.test.js` |
| algovrithm's two white worlds (test pattern, metaball field) render a mid-grey sky while their own content's fog wash is true white — visibly two different "whites" in one room. Tunnel whites unaffected, so it reads as a scene bug, not a renderer setting | R3F's default ACES filmic tone mapping maps 1.0 → ~0.8, and the only thing still subject to it was `scene.background` — every material in the piece opts out with `toneMapped={false}`, and fog is applied inside those materials before tone mapping | `flat` on the piece's `<Canvas>` (NoToneMapping). The piece is authored in absolute values; the background now honours them too. Comment at the Canvas is the guard — any lit-material clipping this exposes is tuned at the material, not by re-enabling the curve | `src/algoVrithm/AlgoVrithmExperience.jsx` |
| `npm run dev:xr` dies instantly on Windows with `spawnSync openssl ENOENT`, so no HTTPS cert is made, the headset can only reach a plain-http LAN address, WebXR stays off and there is no Enter VR button — but the identical command works from Git Bash. Reads as "VR is broken" | `dev-xr-cert.mjs` spawned the bare name `openssl`. Git for Windows ships one at `usr/bin` and deliberately keeps that directory OFF the system PATH, so Git Bash finds it and PowerShell/cmd do not | `resolveOpenssl()` probes in order: `OPENSSL_BIN` env override, then PATH (`openssl version`), then known Windows locations with Git's `usr/bin` first; throws with the `setx OPENSSL_BIN …` command in the message instead of a bare ENOENT. Resolved once in `main()` and passed into `generate()`. Guard: five `resolveOpenssl` cases in `dev-xr-cert.test.js` | `scripts/dev-xr-cert.mjs` `scripts/dev-xr-cert.test.js` |
| Guest work silently lost at sign-in: OAuth/token login replaces the guest cookie, orphaning the guest sandbox until the 7-day sweep eats it (the Share window promised "sign in to keep this space" but nothing carried it) | Session upgrade minted the new identity without looking at the old one — the guest cookie is still on the upgrade request, but nothing read it | `promoteGuestSandbox` runs at both upgrade moments (OAuth callback via `onSessionUpgrade`, token login in POST /api/auth/session): if the prior guest built anything, `spaceStore.moveSpace` re-homes the sandbox (row, ops, projects, directory) onto the account's sandbox id; never clobbers non-empty account sandboxes; redirect gains `&kept=1` → AuthReturnNotice says "your sandbox came with you". Guards: spaceStore moveSpace tests + httpContracts keep-the-room case | `serverXR/src/spaceStore.js` `serverXR/src/index.js` `serverXR/src/routes/authRoutes.js` `src/components/AuthReturnNotice.jsx` |
| Guests land in an empty private void, every new browser mints another `sandbox-*`, and no space is shared — the space model reads as noise (sandbox flood, unclear "where am I / what's mine") | Guest scope was sandbox-only (or one global space *instead*, never both); sandboxes derived per session cookie with nothing reusable per identity; nothing communal existed by default | Three-place model: communal `open` space ensured at boot (kind `global`, public, `canAccessSpace` grants it to every authenticated session via `setCommunalSpaceId` — no cookie re-mint needed), one deterministic sandbox per identity (`getOwnSandboxSpaceId(subject)`, accounts get it `permanent`, implicit access without cookie scope), admin list gets `sandboxSummary` + `POST /api/admin/sandboxes/purge`, daily open-space scene snapshot + `POST /api/spaces/:id/restore-snapshot`. Guards: `authAccess.test.js` + three httpContracts cases | `serverXR/src/authAccess.js` `serverXR/src/index.js` `serverXR/src/routes/spaceRoutes.js` `serverXR/src/spaceStore.js` |
| Admin directory (`/admin` → Manage) flooded with dozens of identical "Guest Sandbox" spaces (26 spaces, most of them sandboxes) | Every anonymous page view called `/api/auth/session`, which eagerly provisioned a sandbox space (FS dir + DB row) at issuance — pure viewers of LIVE spaces each minted one, and they lived the full 30-day `SPACE_TTL_MS`; `GET /api/spaces` also listed every sandbox to anyone with access (admins see all) | Sandboxes now provision lazily on first real space access (`ensureGuestSandbox` in the `/api/spaces/:spaceId` middleware; the guest's own list entry is synthesized until then), are hidden from everyone else's `GET /api/spaces` (admin included, direct GET by id still works), and idle ones reap after 7 days (`SANDBOX_TTL_MS`, second prune query). Guards: httpContracts guest-sandbox case + spaceStore sandbox-TTL prune test | `serverXR/src/index.js` `serverXR/src/routes/spaceRoutes.js` `serverXR/src/spaceStore.js` `serverXR/src/config.js` |
| `open-pr` check shows a red FAIL on every task-branch push in `dob-0/di.iiii` (exit 4, `GH_TOKEN:` empty in the log) | `auto-pr.yml` is the fork-side template shipped in-repo so forks inherit it; upstream intentionally has no `UPSTREAM_PR_TOKEN` secret, so `gh pr create` dies | Job-level `if: github.repository != 'dob-0/di.iiii'` skips it upstream (guard also in the template); fork behavior unchanged | `.github/workflows/auto-pr.yml` `docs/templates/fork-auto-pr.yml` |
| Clicking an in-page `href="#id"` link inside a code-space (public viewer/preview) blanks the iframe with CORS errors from origin `null` for the shell's own bundles | srcdoc iframes inherit the parent shell's base URL, so a fragment click navigates the sandboxed iframe to the shell URL — which can't boot with an opaque origin (no `allow-same-origin`) | Preview bootstrap intercepts `a[href^="#"]` clicks (capture phase), prevents default, and `scrollIntoView`s the target element | `src/utils/presentationPreviewDocument.js` `presentationPreviewDocument.test.js` |
| Clicking Export project appears to do nothing on asset-heavy projects such as WCC | Nineteen large assets (~200 MB) downloaded sequentially with no UI feedback, then JSZip tried to recompress already-compressed media before triggering the download | Show `Exporting n/total` and `Packing %`, fetch three assets concurrently, disable duplicate clicks, keep errors visible, and use ZIP STORE mode | `studioProjectBundle.js` `StudioEditor.jsx` `StudioShell.jsx` `StudioShellPanels.jsx` |
| Studio project export contains asset metadata/URLs but not the actual files | Export serialized only the normalized document JSON | Export one `.studio.zip` with `project.json` plus every project asset under `assets/`; ZIP import re-uploads binaries using stable asset IDs, while legacy JSON import remains supported | `src/project/transfer/studioProjectBundle.js` `StudioEditor.jsx` `StudioShellPanels.jsx` |
| Studio selection lacks V1 parity: no primary/secondary bounding highlights, stale IDs survive document changes, F frames only primary, and Structure replaces multi-selection | Studio implemented selection independently with only a primary label and primary gizmo | Add orange primary/green secondary BoxHelpers, deduped/pruned selection state, visible/unlocked select-all, multi-frame, modifier-aware Structure rows, and multi-selection Inspector context | `projectStore.js` `StudioEditor.jsx` `StudioViewport.jsx` `StudioShell.jsx` `StudioShellPanels.jsx` |
| VR: left-stick strafe and right-stick turn both moved backwards on a real headset (push left → strafe right; push left → view turns right) | A prior session's "hardware verified" fix (2026-07-05) for the strafe sign was never actually re-tested and was wrong; the right-stick turn direction came from `useXRControllerLocomotion`'s own built-in default, which also ran backwards on this device's axis convention | `moveFromStick` strafe now uses standard Gamepad-API convention (`strafe: xAxis`, no negation, confirmed fixed 2026-07-06) | `src/components/xrFlyControl.js` `src/components/xrFlyControl.test.js` `src/components/LiveProjectScene.jsx` |
| VR: right-stick turn/fly reports became internally impossible (2026-07-06) — user reported "still backwards" for both possible turn signs, and separately reported fly backwards (right after a debug-only, non-functional commit) despite fly being independently confirmed correct 3 times before that | A single test round produced contradictory results that cannot both be true for consistent hardware (both signs of a 1-bit flip can't both be "wrong"; a no-op commit can't change behavior) — almost certainly rushed/confused testing across many rapid redeploys, not real hardware behavior changing | Added a live raw-axis debug readout to get an objective number instead of a direction description, then removed it per user request before it was used. Reverted BOTH signs to their best-evidenced state: `flyVertFromStick` back to `-yAxis` (3x independently confirmed), `xrTurnSpeed` back to negating the base speed (geometrically verified, matches the original VR bug report). **User confirmed correct on real headset 2026-07-07** — do not re-flip either sign without a fresh deliberate re-test | `src/components/xrFlyControl.js` `src/components/xrFlyControl.test.js` `src/components/LiveProjectScene.jsx` |
| AR/passthrough (headset in mixed-reality mode with controllers connected) had no stick-based fly — right-stick fly only applied when `isVr` was true | Vertical fly in `XrLocomotion` only added `stickVert` in the VR branch; the AR/`flyMode` branch only summed `touchVert` (handheld phone AR's on-screen buttons), so a controller connected during an AR session was ignored | Added `stickVert` to the `flyMode` branch too, alongside `touchVert` — same right-stick-Y fly as VR now works in AR/passthrough sessions that have a controller, with handheld phone AR unaffected (no controller ⇒ `stickVert` is 0) | `src/components/LiveProjectScene.jsx` |
| `A` selects all Studio entities but dragging the gizmo moves only the primary entity | Studio attached TransformControls directly to the primary entity and had no shared selection pivot | Add a centroid pivot for 2+ selected entities, preview matrix deltas across the selection, and batch-commit all transforms on release | `src/studio/components/StudioViewport.jsx` `src/studio/utils/multiTransform.js` |
| Studio gizmo hotkeys have no `X/Y/Z` axis constraint, and bare `X` deletes the selection | Studio had no axis state wired to Three.js `TransformControls`; Editor also reserved X for delete | Add shared gizmo-axis state, drive `showX/showY/showZ`, toggle repeated axis back to all, reset on G/R/S, and reserve Delete/Backspace for deletion | `StudioShell.jsx` `StudioViewportLayout.jsx` `StudioPresentationSurface.jsx` `StudioViewport.jsx` `StudioEditor.jsx` |
| Studio toolbar `G/R/S` selects a gizmo, but the matching keyboard key launches a different modal operator | The key handler conditionally called `onStartTransform` while the toolbar directly set `gizmoMode` | Make keyboard `G/R/S` use the same gizmo-mode state transition as the toolbar buttons | `src/studio/components/StudioShell.jsx` `StudioShell.test.jsx` |
| Studio floating-panel close and collapse buttons do nothing in Firefox | The button `pointerdown` bubbled into the draggable header, which prevented the default and captured the pointer before the click completed | Ignore interactive descendants in `usePanelDrag`'s drag-start handler; regression test verifies controls are not captured | `src/hooks/usePanelDrag.js` `src/hooks/usePanelDrag.test.js` |
| Socket.IO polling works locally but Firefox cannot upgrade `/serverXR/socket.io` to WebSocket through Vite | The `/serverXR` dev proxy forwarded HTTP only because WebSocket proxying was not enabled | Add `ws: true` to the Vite `/serverXR` proxy | `vite.config.js` |
| Studio lazy module fails on `localhost:5174` while HMR WebSocket is refused on `5173` | Two `npm run dev` stacks were running; `strictPort: false` let the second Vite server drift to 5174 while its direct HMR fallback still targeted configured port 5173 | Set Vite `strictPort: true`; a duplicate dev client now fails fast instead of opening a split HTTP/HMR session | `vite.config.js` |
| White screen / TDZ crash in prod | `manualChunks` missing drei peer deps → circular chunk init order | All drei peer deps in `three-vendor`: `detect-gpu`, `maath`, `camera-controls`, `@monogrid/gainmap-js`, `@react-spring/three` | `vite.config.js` |
| Infinite loading / auth never resolves | No timeout on session fetch | `AbortController` 8 000 ms timeout | `src/hooks/useAuthSession.js` |
| 100+ cascade errors when backend is 503 | `requireAuth` stays false → `AuthGate` skips error screen | Error check moved before `!requireAuth` | `src/components/AuthGate.jsx` |
| Browser can escalate role when both cookie + bearer token are sent | Auth resolution prioritized token over session | Auth now prefers valid session cookie, then falls back to token | `serverXR/src/index.js` |
| Browser back/forward can be inconsistent after route changes | Mixed manual history mutations and router navigation caused divergent history ownership | Route through `appNavigate`; use router `navigate` when mounted and full page navigation fallback when not mounted | `src/utils/appNavigate.js` `src/hooks/useAppRoute.js` |
| Graph nodes stop at left edge while dragging | Drag clamped `x >= 0` | Allow overflow left, clamp top/right only | `BetaGraphSurface.jsx` `DesktopWindow.jsx` `windowLayout.js` |
| Staging serves old build after push | Actions workflow still running | Wait 2–3 min: `gh run list --workflow publish-cpanel-prebuilt-v2.yml` | `.github/workflows/` |
| `assetId is required` on upload | Dead `|| crypto.randomUUID()` fallback removed | SHA-256 must be computed before calling `buildProjectAssetMeta` | `serverXR/src/projectStore.js` |
| Asset served under a sha256 URL doesn't match its bytes / stale cached asset after re-upload | Upload routes trusted a client-supplied `assetId` without hashing the file, so different bytes could replace an immutable-cached content address | Server verifies sha256-shaped ids against the actual content hash (400 on mismatch); contract test guards it. Legacy uuid-style ids still overwrite as before | `serverXR/src/assetHash.js` `routes/projectRoutes.js` `routes/spaceRoutes.js` `projectContracts.test.js` |
| 503 after deploy (server crashes on start) | `shared/projectSchema.cjs` out of sync with `src/shared/projectSchema.js` | Always update both files together; CJS is what serverXR actually loads | `shared/projectSchema.cjs` |
| V1 `/main` objects not draggable | `interaction-mode` button excluded from desktop controls; UI hidden by default gives no affordance to switch to Edit mode | Removed `'interaction-mode'` from exclusion filter in `EditorLayoutContainer`; added it to `hiddenUiButtons` + `EditorOverlays` filter so "Mode: Edit/Navigate" button shows even when UI is hidden | `src/components/EditorLayoutContainer.jsx`, `src/hooks/useControlButtons.js`, `src/components/EditorOverlays.jsx` |
| 503 after deploy — SQLite OOM or build failure | CloudLinux LVE blocks WASM (`node-sqlite3-wasm` OOM), no C++ toolchain for `better-sqlite3` prebuilt | Use Node.js built-in `node:sqlite` (`DatabaseSync`) — zero deps, no WASM, works on Node 22.5+ and 24+ | `serverXR/src/db.js` |
| Beta canvas requires two double-clicks | `preventDefault` on `pointerdown` suppresses `dblclick` (Pointer Events spec); pan start fires on first click of a double-click | Added `event.detail >= 2` guard in `handleSurfacePointerDown`; `user-select: none` on surface CSS replaces the prevented default | `BetaGraphSurface.jsx` `beta.css` |
| Cursor shows wrong type across Beta editor | Multiple issues: I-beam on topbar spans, `grabbing` flash on single click, `crosshair` persists during node/window drag, input ports wrong cursor, window header buttons blocked by `preventDefault` | Added `isPanMoving` state (cursor only `grabbing` on actual drag movement); `cursor: default; user-select: none` on topbar; `draggingNodeId` in surface cursor; port `--in`/`--out` classes; `dragMode` state in DesktopWindow with button-target guard in `startDrag` | `BetaGraphSurface.jsx` `DesktopWindow.jsx` `beta.css` |
| Node 0 UI messy — duplicate "Node 0" shown in topbar AND scope bar below it | Scope bar (`← Exit \| Node 0`) was rendered inside the shell AND breadcrumb in topbar both showed Node 0, causing visual redundancy | Removed scope bar entirely; topbar breadcrumb (`◈ › Node 0`) is the single navigation source; `graphTopInset` simplified to `workspaceTop`; canvas hint updates to "Double-click to place your first node." when inside Node 0 | `BetaEditor.jsx` `BetaGraphSurface.jsx` `beta.css` |
| Vite 8 build fails: `Cannot find package 'esbuild'` in custom JSX-transform plugin | Vite 8 dropped its bundled esbuild (switched to rolldown/oxc by default); `transformWithEsbuild` now needs esbuild as an explicit dependency | Added `esbuild@0.28.1` (patched, outside vulnerable `0.17.0-0.28.0` range) as a direct devDependency | `package.json` `vite.config.js` |
| staging.di-studio.xyz/serverXR/api/health returns 503, no `lsnode` worker for `serverXR-staging` ever spawns | Staging's `.htaccess` had a stray manually-added `RewriteEngine On` / `RewriteRule ... 127.0.0.1:4001` block stacked below the CloudLinux Passenger markers (production's `.htaccess` has only the Passenger block); mod_rewrite intercepts the request before Passenger spawns the app, and nothing listens on raw TCP 4001 (CloudLinux Passenger-for-Node uses its own internal socket) | Removed the extra rewrite block so staging's `.htaccess` matches production's structure, then `touch serverXR-staging/tmp/restart.txt` | `~/staging.di-studio.xyz/serverXR/.htaccess` on the cPanel host (not in this repo) — deploy script only rewrites between the Passenger BEGIN/END markers, so any manual addition below them survives every redeploy |
| CI `npm ci` ERESOLVE after bumping vite to 8 (local `npm install` looked fine) | `vite-plugin-restart`'s peerDependency caps at `vite@^7`; `npm ci` enforces strict peer resolution with no `--legacy-peer-deps` fallback like local installs can use | Inlined its ~20-line restart-on-`public/`-change logic directly as a plugin in `vite.config.js` and dropped the dependency — removes the peer conflict entirely instead of masking it | `vite.config.js` `package.json` |
| 3D viewport collapses to a tiny sliver (canvas defaults to 300×150) on any route that renders `<StudioViewport>` outside the main Studio app shell — reproduced on the V1 public viewer (`/main`) | `studio.css` (which defines `.studio-viewport-shell { height: 100% }`) was only ever imported by `StudioApp.jsx`; consumers like `PublicProjectViewer.jsx` rendered the component without that stylesheet ever loading, so the wrapper had no height and the canvas fell back to its intrinsic default size | Moved the `studio.css` import into `StudioViewport.jsx` itself so the component is self-contained for any consumer | `StudioViewport.jsx` |
| Dragging (graph canvas, nodes, window headers/resizers, 3D viewport) selects page text; mobile pinch-zoom/native gestures fight with app-level drag/pan | No global `user-select` baseline; several drag surfaces (`.beta-window-header`, `.beta-window-resizer`, `.beta-graph-surface`, `.beta-graph-node-card`, `.studio-viewport-shell`) were missing `touch-action: none` | Added global `user-select: none` on `html/body/#root` with explicit `user-select: text` opt-in for `input`/`textarea`/`contenteditable`; added `touch-action: none` to the drag surfaces listed above | `base.css` `beta.css` `studio.css` |
| CI `npm test` randomly fails `BetaHub.test.jsx` with `ReferenceError: window is not defined` from inside react-dom — passes 100% of the time locally | `SpaceSyncPanel`'s `checkStatus()` fires a `fetch` on mount with no `AbortController`; if the component unmounts (test teardown) before it resolves, the `.catch` callback calls `setStatus` after jsdom's `window` is already torn down. Pure timing race — surfaces under CI's different scheduling, not local idle-machine timing | Gave `checkStatus` its own `AbortController` (same pattern already used by `run()`), aborted on unmount, ignore `AbortError` in the catch | `src/components/SpaceSyncPanel.jsx` |
| CI `npm test` randomly fails with `ReferenceError: window is not defined` from react-dom, originating in `StudioShell.test.jsx` — passes locally (same class as the `SpaceSyncPanel` row above) | `useAuthSession`'s mount-time `refresh()` fetch had an 8s abort timeout but nothing tied to unmount: if the component unmounts (test teardown) before `getApiSession` settles, the `finally`'s `setLoading(false)` runs after jsdom's `window` is torn down | Abort the in-flight fetch on unmount (`abortRef`) and guard every state update with `mountedRef` (reset to `true` on effect re-run for StrictMode). Regression guard: `useAuthSession.test.jsx` "aborts the in-flight session fetch on unmount" | `src/hooks/useAuthSession.js` `src/hooks/useAuthSession.test.jsx` |
| Adding a new SQLite column for an existing deployed DB | `CREATE TABLE IF NOT EXISTS` only covers fresh databases — SQLite has no `ADD COLUMN IF NOT EXISTS` | Added `ensureColumn(db, table, column, definition)` helper that checks `PRAGMA table_info` before `ALTER TABLE`, called after `db.exec(SCHEMA)` in `initDb` | `serverXR/src/db.js` |
| Public/unauthenticated space access was a single hardcoded `spaceId === 'wcc'` string check in `RootApp.jsx` | One-off special case instead of a real per-space flag — doesn't generalize (at the time, reads weren't auth-checked server-side at all, see next row) | Added real `isPublic` column on space metadata (`serverXR/src/spaceStore.js`, `db.js`), `useSpacePublicFlag` hook fails closed while loading, `RootApp.jsx` wraps with `AuthGate` unless the space is confirmed public | `serverXR/src/spaceStore.js` `src/hooks/useSpacePublicFlag.js` `src/RootApp.jsx` |
| The login wall never actually appeared for ANY space, public or not, once `REQUIRE_AUTH=true` was set | `GET /api/auth/session` auto-issues a "guest" session (role: editor, spaces: `GUEST_SPACES=['main']`) on every page load before a login form can render; `AuthGate` only checked `authenticated`, never which spaces the session was scoped to; and `GET` routes (`/api/spaces/:id`, `/scene`, `/api/projects/:id`, `/document`) had zero scope checks server-side at all — only writes were scoped (`requireWriteRole`). Net effect: anyone could browse (read-only) any space's full Studio editor regardless of `isPublic` | Added `requireReadRole` middleware mirroring `requireWriteRole` (same `isAuthScopeAllowedForSpace` check, GET/HEAD only, with an `isPublic` bypass via `loadSpaceMeta`); `AuthGate` gained a `requiredSpaceId` prop and shows an "Access restricted" panel instead of silently rendering when an authenticated session is out of scope | `serverXR/src/index.js` (`requireReadRole`) `src/components/AuthGate.jsx` `src/RootApp.jsx` |
| Every GitHub/Google sign-in got unrestricted editor access to every space, with no scoping step at all | `issueSessionAndRedirect` (`serverXR/src/routes/authRoutes.js`) hardcoded `spaces: null` on every OAuth session — `null` means "no restriction" per `isAuthScopeAllowedForSpace`. The `users` table had no `spaces` column to even restrict it | Added `users.spaces` column (default `'[]'` for new accounts via `ensureColumn`, real pre-existing SQL-NULL rows also parse to `[]` deny-all); `upsertUser` defaults new users to no access and never overwrites existing scope on repeat login; new admin-only `GET /api/users` / `PATCH /api/users/:id {spaces: [...] \| null}` lets an admin grant a specific account access to specific spaces, or `null` for unrestricted (own admin accounts); `issueSessionAndRedirect` now reads `user.spaces` instead of hardcoding `null`. **Action taken live:** the two real pre-existing accounts in the dev DB (dob-0/GitHub, Gevorg Grigoryan/Google) were granted `spaces: null` (unrestricted) via the new endpoint so normal OAuth sign-in keeps working as before — do the same on staging/prod DBs after deploying this | `serverXR/src/db.js` `serverXR/src/userStore.js` `serverXR/src/routes/authRoutes.js` `serverXR/src/routes/userRoutes.js` |
| Landing page sections below the hero (`What is di.iiii?`, `How to use di.iiii`, `Made for everyone`, etc.) render with only their eyebrow label visible — title/body/cards invisible, persistent 3D wireframe background floats over empty space | `GridFloorBackground` renders a `position: fixed; z-index: 0` Three.js canvas spanning the *entire* scrollable page, not just the hero. CSS paints non-positioned static elements before positioned z-index:0 elements, so every section's static content was painted first, then visually covered by the fixed canvas painted on top of it. The hero text avoided this by already using `position: relative; z-index: 2` on `.lp-hero-inner` | Added `position: relative; z-index: 1; background: var(--di-black)` to `.lp-section`, lifting every section above the fixed background and giving it its own solid panel, matching the existing hero pattern | `src/landing/landing.css` |
| Test for a sandboxed-iframe `postMessage` listener silently never fired the handler, even though the real code path worked | `waitFor` only waits for the DOM mutation (iframe appears) to be observable — it resolves *before* React's passive `useEffect`s (which attach the `message` listener) actually run, since those are scheduled after paint. Dispatching the event immediately after `waitFor` raced ahead of the listener being attached | Added an empty `await act(async () => {})` after the `waitFor` to flush pending effects before dispatching the test event; also had to stub `iframe.contentWindow` and `event.source` via `Object.defineProperty` (jsdom's real cross-document `contentWindow` identity isn't reliably comparable) — same pattern as `PresentationCanvas.test.jsx` | `src/project/components/PublicProjectViewer.test.jsx` |
| Sign-in popover on staging/production showed "No sign-in providers configured" — nobody could log in via OAuth | `GET /api/auth/providers` (`serverXR/src/routes/authRoutes.js`) just echoes `Boolean(process.env.GITHUB_CLIENT_ID)` / `GOOGLE_CLIENT_ID` (`serverXR/src/config.js`); the cPanel deploy workflow only pushes code, never sets host env vars — OAuth secrets had simply never been configured on either host | Not a code fix — registered/rotated OAuth app credentials per environment (GitHub: `staging di`, `di-studio`; Google: separate client per env) and set `GITHUB_CLIENT_ID/SECRET`, `GOOGLE_CLIENT_ID/SECRET`, `OAUTH_CALLBACK_BASE_URL`, `OAUTH_FRONTEND_URL` directly in each cPanel Node app's environment, then restarted | cPanel host env vars only — not in this repo. Code referenced: `serverXR/src/config.js`, `serverXR/src/routes/authRoutes.js` |
| `GET /api/spaces` (the full space list) revealed every space's metadata — id, label, `isPublic`, `publishedProjectId`, `sceneVersion`, timestamps — to anyone, even unauthenticated, because `requireReadRole` only checks scope when `req.requiredSpaceId` is set, and the bare list route never sets it | The route had no per-item filtering at all; an unauthenticated caller's `state.spaces` resolves to `null` ("unrestricted"), so a naive `isAuthScopeAllowedForSpace` check without first checking `state.authenticated` would have let unauthenticated callers see *everything* | Filter the list to `space.isPublic \|\| (state.authenticated && isAuthScopeAllowedForSpace(state.spaces, space.id))`; skipped entirely when `config.requireAuth` is false | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/index.js` |
| Production: Studio showed "Server error" thumbnails for several spaces (`beyond-form`, `azd`, `br-id-ge`); `GET /serverXR/api/projects/:id/document` returned 500 with `EACCES: permission denied` writing `document.json.*.tmp` | `/var/lib/docker/volumes/dii_data/_data/spaces` (top-level dir) was correctly owned by the container's `app` user (uid 100/gid 101), but every space/project underneath it had been reset to `root:root` at some point — `dii-server-1` runs as non-root `app` and can't write into root-owned directories. Only projects that happened to trigger a schema-coercion rewrite in `readProjectDocument` surfaced the error; others silently worked until their next write. Origin unconfirmed — `deploy/vps-restore.sh` extracts as root inside a bare `alpine` container with no ownership fixup, so any manual/future restore would reproduce this exact bug even though the script had never been run against prod before this incident | Live fix: `chown -R 100:101 /var/lib/docker/volumes/dii_data/_data/spaces` on the VPS. Regression guard: added `chown -R 100:101 /data` as the last step of `vps-restore.sh`'s extraction, so ownership is forced correct regardless of what the archive or extracting container's defaults produce | `deploy/vps-restore.sh` (VPS filesystem itself is not in this repo) |
| Staging deploys silently stopped applying (cPanel Git Version Control showed `SQLITE_IOERR (disk I/O error)` on its own internal `vc_deploy.sqlite`); a push to `dev` never reached the live server even though GitHub Actions succeeded | `~/deploy-backups` on the cPanel host grew to 23GB/25GB (the account's full quota) because the existing prune step (`cpanel-apply-prebuilt-release.sh`, keep-newest-N) only runs as a side effect of a *successful* deploy — once the disk filled, deploys couldn't run, so nothing could ever prune it: a self-sustaining deadlock. 114 backup dirs had accumulated since 2026-06-17 | Manually pruned to newest 5 per environment via `find ... -name "*-staging\|production" \| sort \| head -n -N \| xargs rm -rf` (freed ~20GB), then added a standalone `scripts/cpanel-prune-deploy-backups.sh` on a daily 3am cron job (`~/scripts/` on the host, independent of any deploy succeeding) so this can't deadlock again. Lowered the default keep-count from 5 to 3 in both the cron script and the per-deploy prune step to slow future growth | `scripts/cpanel-prune-deploy-backups.sh` `scripts/cpanel-apply-prebuilt-release.sh` — cron entry lives only on the host (`~/scripts/cpanel-prune-deploy-backups.sh`, not git-tracked) |
| Editing the project linked to the `main` space in Studio Hub had zero visible effect on the landing page / Studio Hub background | `GridFloorBackground.jsx` rendered a hardcoded `LANDING_PROJECT_ID = 'test-file-project'` regardless of whatever project was actually linked/published to the `main` space via Studio Hub's "Change project" — two disconnected notions of "the main project" | `GridFloorBackground` now fetches `main`'s real `publishedProjectId` via `getServerSpace('main')` at mount and uses that, falling back to the old constant only if the lookup fails or nothing is linked | `src/components/GridFloorBackground.jsx` |
| After the above fix, the landing page still showed stale/wrong project content even though the network tab showed the correct project's document being fetched | `useLiveProjectDocument` (`LiveProjectScene.jsx`) had no guard against a stale `projectId`'s document fetch resolving *after* a newer `projectId` had taken over — this was latent and harmless while `projectId` was always a constant, but became a real race the moment a consumer (`GridFloorBackground`) started swapping it at runtime. The older project's slower response would land last and silently overwrite the correct doc | Added an `activeProjectIdRef` set in an effect; `reloadDocument` captures its own `requestedProjectId` and drops the response if `activeProjectIdRef.current` no longer matches by the time it resolves | `src/components/LiveProjectScene.jsx` (`useLiveProjectDocument`) |
| Image/video/model entities in legacy-imported projects (`source: "legacy-import-studio"`) never render anywhere outside Studio — zero asset network requests fire at all | Asset metadata's `url` field is empty for these (`registerAssetSources`, the registry that normally fills it in, only ever runs inside Studio's own `useAssetRestore` editor hook — never in the public/live viewer path). `ImageObject`/`VideoObject`/`ModelObject` fall back to a `data` prop (`entity`'s resolved `asset.url`) when the registry-backed lookup is empty, but that fallback was also empty | `LiveProjectScene`'s `assetMap` now fills in `buildProjectAssetUrl(projectId, assetId)` (already-existing helper, same endpoint Studio itself uses) whenever `asset.url` is empty, so the `data` fallback prop actually has something to use | `src/components/LiveProjectScene.jsx` |
| Walk/fly navigation only ever worked on the landing page background and WCC exhibition — visiting any other space's real public URL gave orbit-only camera controls, with no way to walk or fly around it, for spaces created before or after the feature shipped | Three independent renderers exist for what looks like "the" 3D viewer: `LiveProjectScene.jsx` (landing/WCC, has Walker/fly), `PublicProjectViewer.jsx` -> `StudioViewport` (the actual generic `/:spaceId` public viewer, orbit-only, no Walker at all), and Studio's own editor viewport. A feature added to one was never propagated to the others | Added a "Walk / Fly" toggle to `PublicProjectViewer.jsx` (scene entry-view only — fixed-camera/code presentation modes are a deliberate per-project choice and stay untouched) that swaps in `LiveProjectScene` and back to orbit via its `onExit` callback. Available on every space, past and future, without changing default behavior for existing visitors | `src/project/components/PublicProjectViewer.jsx` |
| WCC's exhibition ring/hub (`/wcc`, `WccExhibition.jsx`) didn't get any of the strafe/pitch-range/drone-fly fixes made to `LiveProjectScene.jsx` in the same session | `WccExhibition.jsx` has its own complete hand-duplicated copy of the Walker/joystick/fly-button code, predating `LiveProjectScene`'s generalization — it never actually imports or reuses the shared component, despite a misleading nearby comment suggesting it does. Per-artist "Enter space" views (via `WccExperience.jsx`) *do* use the real shared component and were unaffected | Ported the same specific fixes (A/D strafe, `WALK_PITCH_LIMIT`/`FLY_PITCH_LIMIT` split, drone-decoupled fly movement) directly into `WccExhibition.jsx`'s own copy, mirroring `LiveProjectScene.jsx` line for line. Full de-duplication (rewriting the hub to reuse the shared component) was scoped out as a bigger, riskier refactor — WccExhibition has extra hub-only features (portals, zones, artist staging) layered on top | `src/wcc/scene/WccExhibition.jsx` |
| Mobile fly mode had no way to ascend or descend at all | Altitude in fly mode is controlled by Space/Q (up) and C/E (down) -- keyboard-only, no touch equivalent existed anywhere (joystick only ever drove horizontal movement, confirmed double so after the drone-decoupling fix made forward/strafe horizontal-only in fly mode too) | Added a `vertTouchRef` fed by two new press-and-hold on-screen buttons (▲/▼), shown only on mobile while `flyMode` is active, stacked above the existing Fly toggle. Ported identically into both `LiveProjectScene.jsx` and `WccExhibition.jsx`'s duplicated copy | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css`, `src/wcc/scene/WccExhibition.jsx` |
| VR/AR sessions had no movement at all — entering a headset session always started at world (0,0,0), completely disconnected from wherever you were walking/flying right before entering | No `<XROrigin>` was ever rendered inside `<XR>` in either `LiveProjectScene.jsx` or `WccExhibition.jsx`, and no locomotion was wired up beyond `@react-three/xr`'s default teleport-pointer (point-and-click teleport only, no continuous movement) | Added `useXRControllerLocomotion` (the library's standard smooth-locomotion hook: left stick moves, right stick turns) driving an `<XROrigin>` that's synced with the same `playerRef` the desktop/touch Walker uses — position carries over correctly entering and leaving a session. **Not verified on real hardware** — this environment has no headset and WebXR emulation is explicitly disabled (`emulate: false`); confirmed only that it builds/lints/mounts without crashing. Needs a real-device check | `src/components/LiveProjectScene.jsx`, `src/wcc/scene/WccExhibition.jsx` |
| GitHub-sync calls failed on cPanel with `fetch failed` / `WebAssembly.Instance(): Out of memory` while `curl` to the same URL returned 200 | Node's global `fetch` (undici) instantiates a WASM HTTP/1.1 parser; under CloudLinux LVE virtual-memory limits that WASM instantiation OOMs. Network itself is fine — only undici's WASM is blocked (same root cause as the SQLite-WASM row above) | Replaced `fetch` with a tiny `node:https`/`node:http` client (`httpRequest`) for the whole GitHub-sync path — the built-in HTTP parser is native C, no WASM | `serverXR/src/httpClient.js` `serverXR/src/githubApp.js` `serverXR/src/index.js` |
| GitHub-sync "first sync failed: connect ECONNREFUSED 127.0.0.1:<port>" — link saved + App auth OK, but the document write/publish step couldn't reach the server's own API | `syncLinkedSpace` writes through the server's OWN tested HTTP document/publish routes via a loopback to `http://127.0.0.1:${config.port}`. Under Passenger (cPanel) the app is fronted by a Unix socket and nothing actually binds that TCP port, so the loopback is refused | Made the base URL env-overridable: `SELF_API_URL` (set to the public origin, e.g. `https://di-studio.xyz/serverXR`) is used when present, falling back to the loopback for normal TCP-listen deployments. Set `SELF_API_URL` in each cPanel app's `.env` | `serverXR/src/index.js` (`internalApiBase`) — plus per-host `.env` `SELF_API_URL` |
| The three rows above reference `src/wcc/scene/WccExhibition.jsx` — searching for it turns up nothing | That file no longer exists; it was consolidated away after those fixes were recorded | `WccExperience.jsx` now renders the WCC hub through the shared `LiveProjectScene.jsx` directly — there's only one Walker/XrLocomotion implementation to maintain now. Treat those three rows' file paths as historical only | `src/wcc/WccExperience.jsx`, `src/components/LiveProjectScene.jsx` |
| VR fly (right-thumbstick-Y, see row above) has no way for a first-time headset user to discover it — no keybind text is visible once inside a VR session (unlike AR, VR gets no dom-overlay) | Only a desktop hint bar (`live-scene-hint`) existed; nothing told a VR user the right stick both turns (X) and flies (Y) | Added a pre-entry DOM hint next to the "Enter VR" button (`.live-scene-hint--vr`), plus a floating in-world `Text` hint inside `XrLocomotion` that tracks the headset's own look direction every frame (not the rig's yaw) and auto-dismisses after 14s or on the first real stick input | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css` |
| Exhibition walker: scrolling tilts the camera into the floor on real mice; mouse look silently dead ("can move but can't look") on Wayland or after Esc | Wheel handler guessed "trackpad vs mouse" from delta size (≤60px pixel-mode = trackpad) — hi-res mouse wheels send small pixel deltas and pass the guard, pitching the view; a denied `requestPointerLock` (Wayland browsers, Chrome's post-Esc cooldown) was caught-and-ignored with no fallback | Wheel never touches pitch: deltaY dollies forward/back through the bounds clamp (scroll = zoom, all deltaModes normalised), deltaX still turns; drag-on-canvas is the look fallback when lock is denied. Regression guard: `npm run check:input` (13 contracts, runs in CI + gates deploys). Never guess input device class — see golden rule | `src/components/LiveProjectScene.jsx` (Walker), `scripts/input-check.mjs` |
| VR fly inverted on real hardware: pushing the right stick up descends, down ascends | A session "fixed" the yAxis sign with a comment claiming Quest verification that never happened — the WebXR xr-standard-thumbstick yAxis is NEGATIVE when pushed up (standard Gamepad convention), so the original negation was correct | Sign convention extracted to `flyVertFromStick()` (`src/components/xrFlyControl.js`), user-verified on a headset 2026-07-05; unit test encodes the convention — do not flip the sign without re-testing on physical hardware | `src/components/xrFlyControl.js` `src/components/LiveProjectScene.jsx` |
| VR left-stick strafe mirrored on real hardware: push left slides right (forward/back fine) | `useXRControllerLocomotion`'s translation rotates the raw stick vector by the camera's full world quaternion — its strafe axis comes out mirrored on-device | Library translation disabled (`translationOptions: false`, right-stick turn kept); left-stick movement is ours: `moveFromStick()` axes drive motion along the camera's horizontal forward and its cross-product right (`right = forward × up`), pure geometry from the verified-correct forward. Unit-tested | `src/components/xrFlyControl.js` `src/components/LiveProjectScene.jsx` |
| Desktop "I can move but can't look" reported in a live space — reproduced under Playwright with the real backend down (502s), NOT reproducible against the live deployed site or a properly-running local stack; existing mouse-look code (pointer lock + drag-look fallback) passes all 13 `check:input` contracts when the backend is up | `reloadDocument()`'s catch was silent: a failed project-document fetch left `doc` null forever, no error, no retry. The loading overlay (`.live-scene-loading`, opaque, `pointer-events: all` while `!doc`) then blocks all mouse/wheel input indefinitely and invisibly, while WASD (independent of `doc`) keeps responding — exactly the reported symptom, likely from a real but transient backend hiccup on the user's original attempt | `useLiveProjectDocument` now tracks `loadError`, auto-retries once after 3s, and the overlay shows a visible "Couldn't load this space — Retry" instead of spinning forever. Regression guard: 2 new `check:input` contracts (forced 502 → visible error → auto-retry recovers) | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css`, `scripts/input-check.mjs` |
| A full-codebase audit found the `wcc` space bypassing the login gate every other space goes through | `RootApp.jsx` special-cased `spaceId === 'wcc'` and routed straight to `WccExperience`, skipping `useSpacePublicFlag`/`AuthGate` entirely — a regression of the `isPublic`-flag fix recorded further up this table (the "hardcoded `spaceId === 'wcc'` check" row); if WCC were ever marked private, unauthenticated visitors would still get the full client UI | Added `WccSurfaceRoute`, which runs the same `useSpacePublicFlag('wcc')` check as every other space and only skips `AuthGate` while `isPublic` is true. Regression guard: 2 new `RootApp.test.jsx` cases (gated when not public, open when public) | `src/RootApp.jsx`, `src/RootApp.test.jsx` |
| Same audit: `GET /serverXR/api/events` returned recent request URLs and error text to anyone, with no auth check, in any configuration | The route has no `requiredSpaceId`, so the global `requireReadRole`/`requireWriteRole` middleware (which only activates once `requiredSpaceId` is set) never applied to it | `/api/events` now checks `req.authState` and returns `{ events: [] }` to non-admins when `REQUIRE_AUTH` is on (unchanged, full detail, when auth is disabled — local dev). `/api/health` deliberately stays fully public; it's polled unauthenticated by the deploy pipeline, smoke checks, and CI. Regression guard: 3 new `httpContracts.test.js` cases | `serverXR/src/routes/statusRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Same audit: a failed project-document write (network blip, 5xx, expired auth) was silently dropped — the UI kept showing an edit as saved, and it vanished on the next reload with no error, no retry | `flushQueue`'s catch branch only handled 409 (version conflict); any other error just logged a low-visibility activity message and `break`, after the batch had already been spliced out of the pending queue | Non-409 failures now put the batch back at the front of the queue, set a `pendingSyncError` on the store (surfaced as a red `scc-sync-dot` in Studio's control cluster), and retry automatically after 4s. Regression guard: new `useProjectDocumentSync.test.jsx` case forcing a 500 then a successful retry | `src/project/hooks/useProjectDocumentSync.js`, `src/project/state/projectStore.js`, `src/studio/components/StudioControlCluster.jsx`, `src/studio/components/StudioEditor.jsx`, `src/project/hooks/useProjectDocumentSync.test.jsx` |
| "Open Studio" / "Open in Studio" direct links (admin Manage panel, and any bookmarked/typed `/studio/projects/:id` URL with no space segment) loaded the wrong project's assets, metadata, and publish state — the project document itself loaded fine, but everything else silently ran against space `main` | `buildStudioProjectPath` omits the space segment from the URL whenever its `spaceId` arg is falsy; `getStudioLocationState`'s matching route branch then hardcoded `spaceId: defaultSpaceId` ('main') instead of leaving it unset, which pre-empted `StudioEditor`'s own three-tier fallback (`spaceId \|\| document.projectMeta?.spaceId \|\| DEFAULT_PROJECT_SPACE_ID`) from ever reaching the project's real space. Triggered in practice by `AdminManageSection.jsx`'s "Open in Studio"/"Open" buttons building the link from `space?.id`/`space.id` — a `useMemo` lookup against the async-loaded `spaces` list that can resolve to `null`/stale independently of the already-loaded `project` object | `getStudioLocationState`'s space-less project branch now returns `spaceId: null` so the existing `StudioEditor` fallback chain works as designed. Both admin buttons now build the link from `project.spaceId` (the DB-sourced field that travels with the project row itself, `serverXR/src/projectStore.js`'s `rowToMeta`) instead of the separately-loaded `space` object. Regression guards: `studioRouting.test.js` asserts `spaceId` is `null` for a space-less URL; `AdminManageSection.test.jsx` asserts "Open in Studio" navigates using the project's own spaceId | `src/studio/utils/studioRouting.js`, `src/components/preferences/AdminManageSection.jsx`, `src/studio/utils/studioRouting.test.js`, `src/components/preferences/AdminManageSection.test.jsx` |
| Same audit: undo/redo in both Studio and Beta silently diverged from the server — an undo was never persisted or broadcast, and the next real edit after it could reference an entity the server never actually restored | Both editors' Ctrl+Z/Ctrl+Y dispatched `replace-document` straight to the local reducer, bypassing `useProjectDocumentSync` entirely (the only path that calls `updateProjectDocument`/reconciles `versionRef`) | Undo/redo was first routed through `replaceDocument` (network-backed PUT); since 2026-07-08 it replays **inverse ops** through `applyLocalOps` → `POST /ops` (`useOpHistory` + `invertProjectOps`) — same rule, better mechanism: still network-backed, now granular and collaborator-safe instead of a whole-document swap. The invariant to preserve is *undo must go through a network write path*; the component tests assert inverse ops reach the ops submit path and `replaceDocument` is never called for undo | `src/project/hooks/useOpHistory.js`, `src/shared/projectSchema.js`, `src/studio/components/StudioEditor.jsx`, `src/beta/components/BetaEditor.jsx`, `src/studio/components/StudioEditor.test.jsx`, `src/beta/components/BetaEditor.test.jsx` |
| User on Linux/Wayland reported mouse-look "doesn't work" on `/wcc/scene` on staging — not reproducible via headless OR headed real-Wayland Playwright (pointer lock engaged fine, camera rotated correctly in both) | `DRAG_LOOK_SENSITIVITY` (the fallback used when pointer lock is denied — exactly the population most likely to hit it) was deliberately tuned 3x gentler than `POINTER_LOCK_SENSITIVITY` on the untested theory that drag distances "run larger." For a normal-sized human drag gesture this just reads as unresponsive, not broken — user self-diagnosed as "maybe it's the speed" | First unified `DRAG_LOOK_SENSITIVITY` to equal `POINTER_LOCK_SENSITIVITY` (too sensitive per live user testing), then walked down through 0.75x and 0.5x (both still too sensitive) to **0.35x** of `POINTER_LOCK_SENSITIVITY`, user-confirmed. Verified via `npm run check:input` (15/15) at each step | `src/components/LiveProjectScene.jsx` |
| Studio viewport: mouse wheel doesn't zoom, it rotates the camera instead | `StudioOrbit` set `cc.mouseButtons.wheel = e.ctrlKey ? DOLLY : ROTATE` to give trackpad two-finger swipe a "look around" feel — but a plain mouse wheel scroll *also* has `ctrlKey: false`, identical to a trackpad swipe on this signal. Every non-pinch wheel event (i.e. every ordinary mouse wheel) got routed to ROTATE instead of zoom. Same "guess the device from the event" anti-pattern the golden rules already called out once for the WCC Walker (`docs/ai/golden_rules.md` "Input handling never guesses the device") | Removed the override entirely — wheel now always dollies (the `CameraControls` `mouseButtons.wheel: DOLLY` default already declared in JSX), matching the golden rule's "wheel deltaY = dolly, never rotate" semantic. Trackpad pinch (ctrlKey) still dollies too, redundantly but harmlessly | `src/studio/components/StudioViewport.jsx` |
| Walk-mode's mouse/movement tuning constants (look sensitivity per input method, walk/fly speed, pitch limits, joystick/bounds) were inline `const`s at the top of `LiveProjectScene.jsx`, mixed in with unrelated rendering constants — each tuning pass (like the drag-look sensitivity walk above) had to be found and edited in place, with no single obvious location | User asked to "sync mouse and global things... in walk mode" — one canonical place to tune walk mode instead of scattered inline consts | Extracted all walk-mode tuning into `src/components/walkModeConfig.js`, grouped by category (movement, look sensitivity, wheel/dolly, pitch limits, joystick/bounds) with `DRAG_LOOK_SENSITIVITY` defined as a ratio of `POINTER_LOCK_SENSITIVITY` rather than an independent magic number. `LiveProjectScene.jsx` now imports from it. No behavior change — verified via `check:input` (15/15) and the full test suite | `src/components/walkModeConfig.js`, `src/components/LiveProjectScene.jsx` |
| Studio's Scene panel: Transform inspector keeps sliding further down the more objects are in the scene, forcing a scroll past the whole entity list every time | The entity list (`.spa-list`, shared by the Structure list, Drive picker, Commons picker, and Files library) had no height cap — it grows with content, and shares one scroll region (`.sfp-content`) with the `StudioInspector`/Transform section rendered directly below it in the same panel | Capped `.spa-list` to `220px` with its own `overflow-y: auto`, so the entity list scrolls independently and Transform's position is no longer a function of scene size. Applies to all four `.spa-list` usages, all of which had the same latent unbounded-growth issue | `src/studio/styles/studio.css` |
| Inspector's Move/Rotate/Scale gizmo buttons showed garbled characters (`âœ¢` `âŸ³` `â‡²`) instead of icons, for every user | The three icon strings in `GizmoModeButtons.jsx` were double-encoded UTF-8 bytes, not the intended single-codepoint glyphs — flagged by the 2026-07-07 full audit, byte-for-byte confirmed | Replaced with the real glyphs `✢` `⟳` `⇲`. Regression guard: new `GizmoModeButtons.test.jsx` asserts the exact glyphs render and no `â` mojibake marker appears | `src/components/inspector/GizmoModeButtons.jsx`, `src/components/inspector/GizmoModeButtons.test.jsx` |
| Shift+D duplicated the selection twice per keypress (two overlapping clones); Delete/Backspace fired its handler twice (masked because delete is idempotent, but burned an extra undo snapshot) | Two independent window keydown listeners — one in `StudioEditor`, one in `StudioShell` — both matched Shift+D and Delete/Backspace and both called the same non-idempotent handler (`StudioShell`'s `onDuplicateSelected`/`onDeleteSelected` props ARE `StudioEditor`'s handlers) | Removed the bindings from `StudioShell`; `StudioEditor`'s handler is canonical (it also guards input focus, modal transforms, selection, and covers Ctrl/Cmd+D). Regression guard: 3 new `StudioShell.test.jsx` cases asserting the shell never fires duplicate/delete on those keys | `src/studio/components/StudioShell.jsx`, `src/studio/components/StudioShell.test.jsx` |
| Five Studio features silently did nothing: Save current view persisted stale data, Frame Selected (F/".") was dead from the editor, double-click insert always used the ring layout, exiting XR never restored the camera, and a project's saved view was ignored on load | `StudioEditor`'s `controlsRef` was a plain `useRef` that no component ever attached — the real `CameraControls` instance lives two levels down in `StudioViewportLayout`'s `ViewPane`, which only registered a `fitToBox` closure | Panes now register their live controls ref into `shared.paneControlsRef`; the editor's `controlsRef` is a `useMemo` proxy whose `.current` getter resolves the active pane's instance at call time. The perspective pane opens on `worldState.savedView` when present. StudioShell's duplicate F binding removed (editor owns F/"."). Regression guard: `StudioViewportLayout.test.jsx` (ref registration + saved-view-on-load) | `src/studio/components/StudioEditor.jsx`, `StudioShell.jsx`, `StudioViewportLayout.jsx`, `StudioViewportLayout.test.jsx` |
| Collaborator presence/cursors die permanently after a backend restart or network blip — only a full remount (switching space) brings them back | The socket is created with `reconnection: false` and a retry was only scheduled from `connect_error`; a plain mid-session `disconnect` just flipped `isConnected` to false | `disconnect` now takes the same `scheduleRetry` path as `connect_error` (excluding the hook's own cleanup, reason `io client disconnect`). Regression guard: `useSpaceSocket.test.js` reconnection cases | `src/hooks/useSpaceSocket.js`, `src/hooks/useSpaceSocket.test.js` |
| `fetch failed` / `WebAssembly.Instance(): Out of memory` on cPanel from the local↔live sync endpoints (`/api/sync/spaces/:id/{status,pull,push}`) — the same OOM the GitHub-sync path was already cured of | `syncRoutes.js` still used global `fetch` (undici's WASM HTTP parser, blocked under CloudLinux LVE); the bug class was reintroduced in a sibling path after the first fix | Migrated `liveFetch` to the shared `httpClient.js` (`node:https`, native parser). New contract test scans all serverXR source and fails on any global `fetch` call site, so the class can't ship a third time | `serverXR/src/routes/syncRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Deleting a space asset that only a legacy V1 `scene.json` references succeeded silently and broke that scene's rendering — the opposite of what the delete guard promises | The used-by check scanned project documents' `media.assetId` only; V1 scenes reference assets directly on objects (`assetRef`, `asset`, `mediaVariants`…) and were never scanned, though `spaceStore.collectSceneAssetRefs` already enumerates exactly those | Delete guard now also runs `collectSceneAssetRefs` over the space scene and reports `Space scene (V1)` in the 409 `usedBy`; `?force=1` still overrides. Regression guard: new httpContracts case | `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/index.js`, `serverXR/src/httpContracts.test.js` |
| Unauthenticated clients could hammer guest-session issuance (real FS/DB writes per call), login, OAuth, sync-key minting, and uploads at unlimited rate | No request-time throttle existed anywhere in serverXR — abuse was bounded only by the guest-session TTL sweep | Zero-dep fixed-window limiter (`rateLimit.js`, keyed on first-hop X-Forwarded-For since the app runs behind cPanel's proxy without `trust proxy`) applied to those five surfaces; 429 + Retry-After. Guards: `rateLimit.test.js` + login-throttle contract test | `serverXR/src/rateLimit.js`, `serverXR/src/index.js`, `serverXR/src/rateLimit.test.js`, `serverXR/src/httpContracts.test.js` |
| Scenes with lights or groups silently corrupt when the server normalizes a document (op application / replace): every `pointLight`/`spotLight`/`directionalLight`/`ambientLight`/`group` entity becomes a plain `box`, and `parentId` (grouping) is stripped from entities and nodes | `shared/projectSchema.cjs` (the hand-maintained CJS mirror serverXR loads) had drifted from `src/shared/projectSchema.js`: 5 entity types missing from `ENTITY_TYPES` (unknown types coerce to `'box'`), no light/group default components, no `parentId` in either normalizer, and a different assets-window default. The schema-sync test never caught it because it only checked the CJS against hardcoded invariants — its "cannot import the ESM" comment was stale | Synced the CJS mirror, and made `schemaSync.test.js` import BOTH modules and diff-check constants + `normalizeProjectDocument` + `applyProjectOps` over representative fixtures (clock fields zeroed). Hand-mirror misses now fail `test:schema-sync` (pre-push gate + CI) with a real diff instead of passing silently | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| Deleting a group in Studio removed its children on every client but the server kept them — the orphans resurrected on the next reload (found while building op-log undo, 2026-07-08) | One more CJS-mirror drift the fixture-based equivalence test happened not to cover: ESM `applyProjectOps.deleteEntity` cascades through `parentId` descendants, the CJS mirror deleted only the one id | Cascade ported to `shared/projectSchema.cjs`; regression guard: a delete-with-children equivalence case in `schemaSync.test.js`. Lesson repeated from the row above: when touching any `applyProjectOps` branch, add an equivalence fixture that actually exercises it | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| SpaceHub "Sign in to create" button did nothing (console error) — signed-out visitors could never start sign-in from the Spaces page | `onClick={login}` passed the click event object as the token to `useAuthSession.login(token)` → `POST /api/auth/session` with a garbage token; the page has no token field at all, OAuth is the real flow | Button now reveals the OAuth provider links (`getApiAuthProviders` + `getOAuthUrl`, same flow as AccountButton). Regression guard: `SpaceHub.test.jsx` "sign-in button reveals working OAuth provider links" | `src/studio/components/SpaceHub.jsx`, `src/studio/components/SpaceHub.test.jsx` |
| Returning guests counted as real accounts: they could create named spaces (`POST /api/spaces` 201) and the hub showed them Create/quota UI, despite the "guests cannot create spaces" contract | Guest identity lives in a normal session cookie; on later requests `authState.type` is `'session'`, so every `type === 'session'` check treated `guest:<uuid>` subjects as owning accounts. Only issuance-time responses said `'guest'` | `isGuestSubject()` in `authAccess.js` (the `guest:` subject prefix is the shared convention); space creation, session `type`, and quota/`canCreateSpace` now exclude guest subjects. Regression guard: httpContracts "gives each guest a private sandbox" case | `serverXR/src/authAccess.js`, `serverXR/src/index.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Dragging in the editor sometimes shows a translucent "screenshot" ghost of the element following the cursor while nothing actually moves (reported on prod Studio) | Links and images are natively draggable in every browser; the app's own drag interactions all use pointer events but nothing suppressed native `dragstart`, so a drag starting on an `<a>`/`<img>` (panel links, thumbnails) launched an HTML5 drag with its snapshot ghost instead | Global capture-phase `dragstart` preventDefault installed at startup (`suppressNativeDrag()`, opt-out via `draggable="true"`) plus `img, a { -webkit-user-drag: none }` in the base stylesheet. Regression guard: `suppressNativeDrag.test.js` | `src/utils/suppressNativeDrag.js`, `src/index.jsx`, `src/styles/base.css` |
| Duplicate (Shift+D/Ctrl+D) and copy/paste on a group produced an empty group shell — every child silently dropped; copy also ignored multi-selections | `handleDuplicateSelected`/`handleCopySelected` cloned only the selected entities themselves — no `parentId` traversal, so hierarchies (the whole point of Group) never made it into the clone; clipboard additionally read only the primary `selectedEntity` | Subtree-aware helpers in `src/studio/utils/entityClipboard.js`: `collectSubtree` (parent-first), `topLevelTargets` (a group + its child selected together clones once), `cloneSubtree` (fresh ids, remapped parentIds, root-only +0.4 offset). Duplicate, copy, paste, cut all use them. Regression guard: `entityClipboard.test.js` deep-copy cases | `src/studio/utils/entityClipboard.js`, `src/studio/components/StudioEditor.jsx`, `src/studio/utils/entityClipboard.test.js` |
| Published scenes silently diverged from the editor: audio entities never played (no renderer case), authored lights didn't illuminate, grouped children rendered at local coordinates (flat entity map), hidden entities stayed visible, new appearance/media props (materials, video sound, text align) reached Studio but not live pages — and portal-embedded groups drew a phantom box | `LiveProjectScene` keeps a hand-copied entity renderer (for billboard text/gate glow/walker) that had drifted from `EntityContent` — same drift class as the CJS schema mirror; `EntityContent` itself also lacked a `group` case so groups fell to the box default | Viewer gained audio/light/group cases, a parent-first hierarchy walk (roots only at top level), and the `runtime.visible` gate; `EntityContent` got `case 'group': null`. Tripwire: `rendererParity.test.js` source-compares case labels, schema ENTITY_TYPES coverage, and consumed appearance/media keys between the two renderers — any one-sided feature fails CI | `src/components/LiveProjectScene.jsx`, `src/project/viewport/EntityContent.jsx`, `src/project/viewport/rendererParity.test.js` |
| Model pipeline was internally inconsistent: OBJ/STL loaded fine in the renderer but Studio refused to place them ("+ Add" disabled, viewport drop rejected); FBX uploaded per the server whitelist but rendered nothing; Draco/Meshopt-compressed GLBs (the default export of Blender/asset stores) failed silently to an invisible model; and embedded animation clips never played — `gltf.animations` was discarded and the deep clone broke SkinnedMesh bindings anyway | Each layer kept its own format list (server `ALLOWED_EXTENSIONS`, `modelFormats.js` loaders, Studio `MODEL_EXTENSIONS` whitelist) and they drifted; GLTFLoader had no decoders registered; `ModelObject` extracted only `gltf.scene` and cloned with `.clone(true)` | Layers reconciled (glb/gltf/obj/stl/fbx placeable end-to-end), Draco (wasm in `public/draco/`) + Meshopt decoders registered, FBXLoader added, `SkeletonUtils.clone` + `AnimationMixer` play embedded clips with `media.playAnimations`/`animationSpeed` controls in both schema mirrors and both renderers. Guards: `assetFormats.test.js` (every loadable format is placeable), rendererParity media keys, animated-GLB E2E (pixel-verified spin + freeze) | `src/objectComponents/ModelObject.jsx`, `src/utils/modelFormats.js`, `src/studio/utils/assetFormats.js`, `public/draco/`, `src/studio/utils/assetFormats.test.js` |
| WCC exhibition on Wayland: walking and fly work but the mouse doesn't move the view at all — third report in this family, and again not reproducible via Playwright (headless OR headed, Chromium/Brave, X11 or native Wayland ozone, localhost or prod: all fine) | Playwright injects input through the browser's own pipeline and never exercises the OS path — but a real mouse's locked-look deltas come from the compositor's relative-motion protocol, and some Wayland setups GRANT `requestPointerLock` and then deliver `movementX/movementY = 0` on every event. The walker only had a fallback for *denied* lock; a granted-but-dead lock froze the view while keyboard (walk/fly) kept working | Broken-lock watchdog in the Walker: 30 consecutive all-zero locked mousemoves (real motion always produces nonzero deltas, and mousemove only fires on motion) ⇒ abandon the lock, latch `lockBroken`, never re-request it — clicks then flow to the existing drag-look fallback. Threshold in `walkModeConfig.js` (`BROKEN_LOCK_ZERO_MOVES`). Regression guard: 2 new `check:input` contracts (now 17) dispatching synthetic zero-delta locked moves — lock abandoned, drag-look takes over | `src/components/LiveProjectScene.jsx` (Walker), `src/components/walkModeConfig.js`, `scripts/input-check.mjs` |
| Wanted a public `wss://` for the br_id_ge live co-presence mesh; a raw `ws` server on its own port isn't reachable (LiteSpeed/Passenger only proxy the one app), and it was unclear whether the front even upgrades native WebSockets | LiteSpeed **does** proxy native WS upgrades (101), but **only under the `/serverXR/*` backend context** — a probe of `wss://di-studio.xyz/socket.io` (root) returns HTTP 200 (SPA), while `wss://di-studio.xyz/serverXR/socket.io` returns 101. Socket.IO masks this because it silently falls back to HTTP long-polling, so "Socket.IO works" does NOT prove native WS works. An arbitrary `/serverXR/<x>` upgrade returns a TCP *socket hang up* (forwarded to Node, then destroyed by engine.io) — proof the whole `/serverXR/` context is WS-proxied, not just the socket.io path | Attach a raw `ws` `WebSocketServer({noServer:true})` to the SAME `httpServer` as Socket.IO, path-routed to `${basePath}/mesh` via one `httpServer.on('upgrade')` handler (ignore non-matching paths). Coexists with engine.io's own upgrade listener because engine.io only destroys a foreign-path upgrade after `destroyUpgradeTimeout` (1s) *and only if `socket.bytesWritten <= 0`* — completing the ws handshake synchronously writes the 101 first, so it's never destroyed. No new port, no env change (public by default; optional `MESH_ROOM_SECRET`). Regression guard: `serverXR/src/meshHub.test.js` (socket.io keeps `websocket` transport + mesh fan-out on same server) | `serverXR/src/meshHub.js`, `serverXR/src/index.js` (calls `initializeMesh(httpServer, config)`), `serverXR/src/meshHub.test.js` |
| "Set as live project" reported *"Published this project"* while the space stayed private — visitors hit a login wall and the creator had no idea; the only cure was knowing the Public toggle lives on a different page (UX audit 2026-07-10, worst creator-facing finding) | Publish intent is split across two flags on two surfaces: `publishedProjectId` (editor Share window) and `isPublic` (SpaceHub card), with a success message that claimed more than the action did. Visibility staying an explicit choice is by design (see SpaceHub `handleLinkProject` comment) — but the state was invisible at the moment of intent | Share window's Live-space-route card now always discloses the space's visibility ("Space is public…" / "⚠ Space is private — visitors will see a login wall") with a one-click "Make space public" button; the set-live activity message tells the truth for both cases; SpaceHub's ⚠ warning gained the same one-click "Make public" for owners/admins. No silent flips — visibility remains explicit. Regression guard: `PublishPanel.test.jsx` (warning + button when private, confirmation when public, nothing before meta loads) | `src/studio/components/StudioEditor.jsx`, `StudioShell.jsx`, `StudioShellPanels.jsx`, `SpaceHub.jsx`, `src/studio/styles/studio-space-hub.css`, `src/studio/components/PublishPanel.test.jsx` |
| OAuth sign-in returned with zero feedback: success redirected to the landing page with no marker (users couldn't tell it worked — the avatar just quietly changed), and the server's `?auth=error` failure marker was silently ignored by the client | The OAuth flow is a full-page redirect, so the moment of return is the only chance to confirm the outcome — and `issueSessionAndRedirect` redirected to plain `frontendUrl` while no client component ever read `?auth=*` | Server now appends `?auth=ok` on success; new `AuthReturnNotice` (mounted once in RootApp, so it works on every surface incl. the landing redirect target) reads `?auth=ok\|error` once, shows a di-styled toast ("Signed in as X." / "Sign-in failed — please try again."), and strips the marker via replaceState preserving other params. Regression guard: `AuthReturnNotice.test.jsx` (ok, error, param-preservation cases) | `serverXR/src/routes/authRoutes.js`, `src/components/AuthReturnNotice.jsx`, `src/components/authReturnNotice.css`, `src/RootApp.jsx`, `src/components/AuthReturnNotice.test.jsx` |
| Creators clicking StudioHub's "Settings" (or BetaHub's "admin" footer link) hit the admin-only console gate and got bounced back — a mislabeled dead end, since no user-settings surface exists | The link was rendered for every role while `/admin` (`PreferencesPage`) gates non-admins out; the audit called it the "Settings dead-end" | The link now renders only for `role === 'admin'` and is labeled "Admin" (StudioHub) — non-admins simply don't see a door that was always locked. Account-level actions (name, role, sign out) remain in the AccountButton popover | `src/studio/components/StudioHub.jsx`, `src/beta/components/BetaHub.jsx` |
| Studio (the main shipped editor) was unusable on phones: it computed `isMobile` (MUI `down('sm')`) and threaded it into StudioShell, which destructured it and never used it — phones got the desktop draggable-floating-panel UI. The one purpose-built mobile shell (`MobileEditorShell`) serves only the deprecated legacy `App.jsx` lane | The prop was wired but the shell had no mobile branch; the good mobile pattern lived on the wrong lane and is too coupled to the legacy panel-registry to port wholesale | StudioShell now branches on `isMobile`: panel content extracted into a shared `panelBodies` map (desktop floating panels render it unchanged), and phones get a top context bar (back · project title · Edit toggle) + five-window bottom nav (Create/Scene/World/Share/Code), one window at a time as a bottom sheet (`studio-mobile.css`, safe-area aware). Regression guard: 3 StudioShell.test.jsx mobile-chrome cases | `src/studio/components/StudioShell.jsx`, `src/studio/styles/studio-mobile.css`, `src/studio/components/StudioShell.test.jsx` |
| Studio `/studio` Spaces list (SpaceHub): page cannot scroll — spaces below the first card are unreachable, most visible on phones (guest hub with open call live) | `html/body/#root` are `position: fixed; height: 100%` (`src/styles/base.css`), so the document NEVER scrolls anywhere in the app — every full-page surface must own its own scroll. `StudioHub` does (`.studio-hub-root` sets `overflow-y: auto`) but SpaceHub's `.ssh-root` never got the rule; its `min-height: 100vh` also overshot the real mobile viewport (address-bar chrome) | `.ssh-root` now mirrors the `.studio-hub-root` pattern: `overflow-y: auto; overflow-x: hidden; -webkit-overflow-scrolling: touch`, and the `min-height: 100vh` was dropped (`.studio-shell-root` already supplies `height: 100%`). Regression guard: SpaceHub.test.jsx asserts the `.ssh-root` block declares `overflow-y: auto` and has no `min-height: 100vh` | `src/studio/styles/studio-space-hub.css`, `src/studio/components/SpaceHub.test.jsx` |
| `/algovrithm` landing: the page cannot be scrolled — the head and the poster canvas are the whole visible page, and the transport, the seven beat cards and the "why this space has no editor" note below them are unreachable | Third instance of the same class as the SpaceHub row above: `html/body/#root` are `position: fixed; height: 100%` (`src/styles/base.css`), so the document never scrolls and every full-page surface must own its scroll. `.avl-root` set only `min-height: 100vh`, so its 2264px of content overflowed a 900px fixed viewport with `overflow-y: visible` — 1364px of page silently discarded, with no error and nothing to see | `.avl-root` now mirrors `.wcc-landing`: `height: 100vh; overflow-y: auto; overflow-x: hidden; overscroll-behavior: contain`. Regression guard: `AlgoVrithmLanding.test.jsx` asserts the `.avl-root` block declares both, and was confirmed failing against the unfixed file | `src/algoVrithm/landing/algoVrithmLanding.css`, `src/algoVrithm/landing/AlgoVrithmLanding.test.jsx`. **Obsolete by removal 2026-08-04** — the timeline and its clips were deleted with the rest of the page's editorial furniture; the row stays because the CLASS (a full-page surface that only sets min-height) is still live everywhere else |
| Links opened from inside any rendered code project (published viewer, Studio preview, Beta, V1 presentation) white-screen in Firefox/WebKit: the new tab loads the app HTML but every module is blocked with "Module source URI is not allowed" / CORS errors against the app's own origin | Code projects render in `sandbox`ed iframes without `allow-popups-to-escape-sandbox`, so a tab opened via `window.open`/`target=_blank` INHERITS the sandbox and gets an opaque origin — its module loads become cross-origin and die. Chromium is more forgiving, which is why direct-URL testing never reproduced it (found via br_id_ge portal-map node clicks) | Added `allow-popups-to-escape-sandbox` to the shared sandbox attribute of all four content-preview iframes (PublicProjectViewer, StudioPresentationSurface, PresentationCanvas, BetaEditor). `allow-same-origin` stays off by design (would defeat the sandbox). Regression guards: sandbox-attr assertions in `StudioPresentationSurface.test.jsx` + `PresentationCanvas.test.jsx` | `src/project/components/PublicProjectViewer.jsx`, `src/studio/components/StudioPresentationSurface.jsx`, `src/components/PresentationCanvas.jsx`, `src/beta/components/BetaEditor.jsx` |
| Beta graph wires were invisible — edges existed in the document, `svg path` elements were in the DOM (the "renders visible wires" test passed), but no wire ever painted on screen | The wire `<svg>` was sized `width/height: 100%` of `.beta-graph-stage`, which collapses to ZERO height (every child is absolutely positioned) — and Chromium paints nothing inside a zero-area svg, `overflow: visible` or not. JSDOM cannot observe paint, so the test named "renders visible wires" never caught it | The svg now has its own 1×1 box with `overflow: visible` (paths render outside a non-zero viewport fine). Regression guard: the wires test additionally asserts the svg's height is not a percentage and overflow is visible — pinning the cause, since paint still can't be asserted in JSDOM | `src/beta/components/BetaGraphSurface.jsx`, `src/beta/components/BetaGraphSurface.test.jsx` |
| WCC exhibition on Wayland, FOURTH report: walk/fly work, mouse look dead — even with the zero-delta watchdog (row above) deployed | Live-diagnosed with `?inputdebug=1` (built for exactly this): lock GRANTED, mousemove FIRING, but every event carries `movementX=-1, movementY=0` — a degenerate constant crawl. Vertical look fully dead, horizontal imperceptible (~1px/event), and the all-zero watchdog never trips because the deltas are nonzero (`zero-streak: 0/30` on screen). Same HUD also showed `buttons: 0` mid-drag, so the drag-look fallback's `e.buttons === 1` gate was one broken-compositor report away from failing too | Watchdog generalised from all-zero to dead deltas: 30 consecutive locked moves with `|movementX| ≤ 4 && |movementY| ≤ 4` (`BROKEN_LOCK_DEAD_DELTA_MAX`; a live headed-Firefox event capture on the user's KDE Wayland box showed locked sweeps arriving as ±1..±4 noise in BOTH axes while the same sweep unlocked yields 50-120px deltas — an earlier `movementY === 0` condition kept resetting on the ±1 Y-jitter) ⇒ abandon lock, latch `lockBroken`, drag-look takes over. Engage-time garbage — one wild spike ~18ms after engage (-19,-116 captured, railed the pitch) plus, in some Chromium builds, a synthetic position-sized event — is handled with a settle window (`BROKEN_LOCK_SETTLE_MS`, 200ms): locked deltas inside it count toward the dead-streak but are never applied to the view. Drag-look now trusts our own pointerdown/up pair (left button only, `setPointerCapture` + pointercancel/blur cleanup) instead of mousemove's `e.buttons`, and derives its deltas from `clientX/clientY` (the visible cursor cannot lie) instead of `movement*` — the same compositor poisons `movementX/movementY` on UNLOCKED moves too, which had made the fallback itself blind (user re-test on staging, Firefox: lock denied for focus, drag-look dead). Const renamed `BROKEN_LOCK_DEAD_MOVES`. Regression guard: 3 new `check:input` contracts (now 20) — constant (-1,0) locked moves on a fresh page, plus a poisoned-movement drag; drag scenarios also probed green in real Firefox (denied lock + real drag, poisoned drag, no-button move). Diagnose future reports with `?inputdebug=1` FIRST — it turned three unreproducible reports into a one-screenshot root cause | `src/components/LiveProjectScene.jsx` (Walker), `src/components/walkModeConfig.js`, `scripts/input-check.mjs` |
| Full-codebase deep audit (2026-07-14, 7-agent parallel find + adversarial verify across studio/project/beta/shared/serverXR/scripts) surfaced 16 confirmed real bugs, headlined by two critical ones: any authenticated editor could push/pull ANY space's scene via `/api/sync/spaces/:id/*` regardless of grants, and no window in the entire Beta lane could be dragged or resized | **Sync auth bypass:** `syncRoutes.js` has no `/api/sync/spaces/:spaceId` middleware setting `req.requiredSpaceId`, so the global per-space scope check silently no-ops (`canAccessSpace(state, null)` always passes). **Beta drag/resize dead:** `DesktopWindow`'s pointermove/pointerup-attaching effect gated on a mutable ref (`interactionRef.current`) instead of the `dragMode` *state* and excluded `dragMode` from its dependency array — starting a drag never re-ran the effect, so listeners were never attached, for every window in the entire lane. Other 14, by area: rate limiter trusted attacker-suppliable first-hop X-Forwarded-For (defeats all abuse throttling); `data-cleanup.mjs`'s prod-delete confirmation read the wrong field and always showed "0 spaces"; `jsonStore.readJson` silently overwrote corrupted files with a truncated recovery, no backup; Studio's "Save current view" used a nonexistent camera-controls API (`.object`/`.target`) and silently no-opped; a document-load race could revert the project on slow connections; client/server typeId validation diverged on `createNode`, causing permanent per-client document drift; presentation fixed-camera lacked a far>near guard (degenerate frustum); anonymous inscriptions had a lost-update race (no CAS, unlike `/ops`); Drive OAuth tokens fell back to a hardcoded, publicly-visible encryption key; project delete could leave a space silently unpublished on partial failure (2 components); shared assets went stale in the commons after a space move/delete; Studio's numeric fields couldn't accept a typed leading `-`; a dead no-op cleanup stub masked what FK cascade actually does; and a Beta density feature was half-wired (stub function, orphaned CSS, unused field) | Sync routes: added the missing `req.requiredSpaceId` middleware (mirrors `/api/spaces/:spaceId`). Beta drag: effect now gates on `dragMode` state (in its deps), `draftRef` replaces the closed-over `draft` read in the pointerup handler. Rate limiter: key on the LAST XFF hop (nginx's own append), not the first. Full fix list is 16 files across `serverXR/src/{routes/syncRoutes.js,rateLimit.js,jsonStore.js,driveTokenStore.js,projectStore.js,spaceStore.js,commonsStore.js,routes/inscriptionRoutes.js,index.js}`, `src/{shared/projectSchema.js,studio/components/StudioEditor.jsx,studio/components/StudioInspector.jsx,studio/components/StudioHub.jsx,studio/components/StudioProjectsPanel.jsx,project/hooks/useProjectDocumentSync.js,beta/components/DesktopWindow.jsx,beta/utils/deviceDetection.js}`, `shared/projectSchema.cjs`, `scripts/data-cleanup.mjs`. Regression guards: `httpContracts.test.js` (sync-route scope denial), `DesktopWindow.test.jsx` (new file, drag→onPatch), `jsonStore.test.js` (new file, corruption backup), `rateLimit.test.js` + `projectSchema.test.js` (updated to assert the corrected behavior) | see file list above |
| Viewport goes permanently black mid-session: console shows "WebGL context was lost." / "THREE.WebGLRenderer: Context Lost." and nothing recovers until a manual page reload (seen on staging open-jam, Firefox/Linux) | GPU driver resets, memory pressure, or too many concurrent GL contexts make the browser kill the WebGL context. three calls `preventDefault()` on `webglcontextlost` (so restoration is possible), but no app code listened for lost/restored — and on Linux/Mesa the browser often never fires `webglcontextrestored`, so the dead canvas persisted forever | Shared `useWebglContextGuard` + `WebglContextLostOverlay` wired into all four R3F surfaces (StudioViewport, SceneCanvas, LiveProjectScene, BetaViewport): on loss show an overlay and auto-remount the Canvas once (key bump after 2s — a fresh element gets a fresh context); browser-restored contexts just clear the overlay; repeated losses (GPU genuinely under pressure) wait for the overlay's "Restore 3D view" button instead of remount-looping. Regression guard: `WebglContextGuard.test.jsx` (3 cases) | `src/components/WebglContextGuard.jsx`, `webglContextGuard.css`, `src/studio/components/StudioViewport.jsx`, `src/components/SceneCanvas.jsx`, `src/components/LiveProjectScene.jsx`, `src/beta/components/BetaViewport.jsx` |
| 6-dimension full audit (2026-07-16, 6-agent parallel) found login CSRF: GitHub/Google `passport.authenticate` never requested a `state` param, so the OAuth authorize→callback round-trip had zero anti-CSRF binding — an attacker could start their own login flow and lure a victim into completing it, silently signing the victim into the attacker's account | Both strategies are constructed with no `state`/`store` option, so passport-oauth2 falls back to `NullStore` (`node_modules/passport-oauth2/lib/state/null.js` — `verify` always calls back `true` regardless of the value), and this app's `session: false` login flow can't use passport's built-in session-backed state store anyway | Hand-rolled signed/timing-safe/TTL'd `state` param (same HMAC pattern as the existing Drive-connect flow in `integrationRoutes.js`, just not bound to a user id since no one's signed in yet): `signLoginState()` is passed as the literal `state:` string to `passport.authenticate(...)` on the `/api/auth/{github,google}` routes (passport sends it straight through, bypassing NullStore on the way out); `requireValidLoginState` middleware verifies it by hand on the `/callback` routes before `passport.authenticate` runs, since NullStore would otherwise accept anything. A parallel finding — project routes ostensibly missing the space-membership auth checks `spaceRoutes.js` has — turned out to be a false positive on investigation: `index.js` already sets `req.requiredSpaceId` upstream for every `/api/projects/:projectId*` route via `resolveProjectContext`, and the shared `requireReadRole`/`requireWriteRole` gates enforce `canAccessSpace` on it before the route file ever runs. The one real gap found there was narrower: `/api/projects/:projectId/assets` POST had no rate limiter, unlike the equivalent space-asset upload route — fixed with the same `uploadLimiter` | `serverXR/src/routes/authRoutes.js`, `serverXR/src/index.js` |
| Same audit: staging's 0.4/0.2-vCPU ceiling on `docker-compose.staging.yml` and production's `docker-compose.yml` CPU limits were never actually enforced — staging could in theory starve production despite the whole point of the small-footprint design being that it couldn't | `deploy.resources.limits` is a Swarm-mode-only Compose key; plain `docker compose up` (no `--compatibility`, no Swarm, which is how both `deploy-vps.yml`/`deploy-vps-staging.yml` invoke it) parses and then silently ignores it entirely — the numbers were documentation, not a real ceiling | Switched every service in `docker-compose.yml` and `docker-compose.staging.yml` from `deploy.resources.limits.{cpus,memory}` to the top-level `cpus`/`mem_limit` keys, which the Docker Engine applies directly regardless of Swarm/`--compatibility`. Kept the exact same numbers (they already summed to a correct 1.8 vCPU / 2 vCPU host budget from an earlier rebalance) — only the enforcement mechanism changed | `docker-compose.yml`, `docker-compose.staging.yml` |
| Same audit: Space Hub cards loaded the full-resolution original for every space's preview image (no thumbnails anywhere), and `GET /api/spaces` had no pagination — both fine at today's scale, both would bite as the space/sandbox population grows | No image-resize path existed at all; `listSpaces()` (`SELECT * FROM spaces ORDER BY updated_at DESC`) had no LIMIT, and its `GET /api/spaces` handler filters (isPublic/canAccessSpace/sandbox synthesis) the *entire* result in JS before responding, so pagination has to happen after filtering, not in SQL | Added `sharp` (ships prebuilt Alpine/musl binaries — safe for the `node:22-alpine` Docker build target, unlike the cPanel-era native/WASM issues logged above, which were about a restricted shared-hosting environment this app no longer deploys to). `serveAsset`'s `?w=<width>` query param resizes+caches a `.thumb-<width>.webp` variant per (asset, width) next to the original (clamped to 640px, images only, falls back to the original on any failure); asset delete now also sweeps cached variants (`removeAssetThumbnails`). `SpaceHub.jsx`'s preview `<img>` requests `?w=480`. `GET /api/spaces` pagination is opt-in via `?limit=`/`?offset=` — omitting them returns the full list exactly as before (no existing caller passes them yet), but `total`/`offset`/`limit`/`hasMore` are now available for a future "load more" UI | `serverXR/src/spaceStore.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/index.js`, `src/services/serverSpaces.js`, `src/studio/components/SpaceHub.jsx` |
| Same audit: `client`/`caddy` had no `healthcheck` (only `server` did — `caddy`'s `depends_on: client` was `service_started`, so it could start proxying to a container that was up but never actually serving), and no service anywhere capped its log driver, so 24/7 containers on a small VPS had no ceiling on `json-file` growth | Never added when the compose stack was first written — `docker inspect`/`docker compose logs` on the real host confirmed no `max-size` anywhere and the default json-file driver is genuinely unbounded | Added `logging: {driver: json-file, options: {max-size: "10m", max-file: "3"}}` to every service in `docker-compose.yml` (the staging override inherits it — `logging` is a mapping field, and staging's file never sets that key, so plain Compose merge picks up the base's). Added a `wget`-based `healthcheck` to `client` (hits `/`) and `caddy` (hits its own loopback-only admin API at `:2019/config/` — independent of TLS/domain state, so it can't false-negative on the inert `STAGING_DOMAIN` block with no DNS yet); `caddy`'s and `tunnel`'s `depends_on: client` tightened from `service_started` to `service_healthy` now that the healthcheck exists | `docker-compose.yml` |
| **PRODUCTION OUTAGE (2026-07-16):** the `client`/`caddy` healthcheck fix directly above took production down for ~5 minutes on the very next real deploy — `dii-client-1` was recreated, never passed its new `wget -qO- http://localhost/` healthcheck, timed out to unhealthy, and `dii-caddy-1` — now gated on `depends_on: client: condition: service_healthy` — never started at all, taking down the only container publishing ports 80/443 | Not fully confirmed, but the leading theory is `nginx:alpine`'s official image doesn't ship `wget`/busybox the way `node:22-alpine` does (whose own healthcheck, using the identical command, has always worked) — the check itself may simply have been unrunnable inside that container. This was never verified against a real container before shipping | Reverted immediately: dropped the `client` and `caddy` healthchecks and both `depends_on: client` conditions back to `service_started` (their pre-existing state), keeping the unrelated/harmless `logging:` caps. Site confirmed back at 200 within ~5 min. **Do not re-add a `client`/`caddy` healthcheck without first confirming the exact check command actually runs inside that specific image** — e.g. `docker run --rm <image> sh -c 'which wget \|\| which curl'` — before it ever reaches a real deploy | `docker-compose.yml` |
| **OAuth sign-in on di-studio.xyz intermittently failing with "Sign-in failed — please try again." (2026-07-16)**, seen the same day as the login-CSRF `state` audit fix above and several production redeploys/container restarts (healthcheck incident + revert) | The fallback-secret theory below (first fix attempt) turned out to be wrong — `AUTH_SESSION_SECRET` was already correctly configured on prod, confirmed by SSHing into the VPS. **The real bug, found the same way (curl the live endpoint, look at the actual response) once the earlier fix "worked, then broke again ~24 minutes later":** `router.get('/api/auth/github', passport.authenticate('github', { state: signLoginState(stateSecret) }))` calls `signLoginState()` — and therefore `passport.authenticate(...)` — exactly **once**, at route-registration time (server startup), not per request. That one state value gets baked into the closure and reused for every `/api/auth/github` hit for the rest of the process's life. Live-verified: two `curl` requests to prod seconds apart returned the byte-identical `state` in the redirect. `verifyLoginState` rejects any state older than `STATE_TTL_MS` (10 min), so sign-in only worked within 10 minutes of container start and failed for everyone — both GitHub and Google — after that, until the next restart. This is why the first fix appeared to work immediately after its own redeploy (fresh state, timestamp ≈ startup) and then broke again once 10 minutes passed | **Actual fix:** wrapped `passport.authenticate(...)` in a per-request handler — `(req, res, next) => passport.authenticate('github', { state: signLoginState(stateSecret) })(req, res, next)` — so the state is signed fresh on every hit. Regression test calls the authorize handler twice and asserts the two `state` values differ. Verified live on prod post-deploy: two direct `curl`s to `/api/auth/github` and `/api/auth/google` each now return different `state` values. The fallback-secret fix from the first attempt (unchanged, still shipped) remains a legitimate hardening for any deployment that legitimately runs with no `AUTH_SESSION_SECRET` — it just wasn't the cause of *this* incident. **`AUTH_SESSION_SECRET` was confirmed already set on both prod and staging** (`/opt/di.iiii/.env`, `/opt/di.iiii-staging/.env` via `STAGING_AUTH_SESSION_SECRET`) — nothing to configure there. Separately, **staging's OAuth was found fully unconfigured** (`GITHUB_CLIENT_ID`/`SECRET`/`GOOGLE_CLIENT_ID`/`SECRET`/`OAUTH_CALLBACK_BASE_URL` all empty in `/opt/di.iiii-staging/.env`, confirmed via `GET /api/auth/providers` returning `{github:false,google:false}`) — since resolved: user created a dedicated "staging di" OAuth App on GitHub and used a pre-existing dedicated "staging di" Google OAuth client, both configured with staging's callback URLs. **Gotcha that cost real time**: `docker-compose.staging.yml` maps the container's `GITHUB_CLIENT_ID`/`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`OAUTH_CALLBACK_BASE_URL` env vars from `STAGING_`-*prefixed* host variables (`${STAGING_GITHUB_CLIENT_ID:-}` etc.), not the bare names — editing the bare `GITHUB_CLIENT_ID=` line in `/opt/di.iiii-staging/.env` (which also exists in the file, left over from the shared base `.env.example` template) has zero effect on the running container; only the `STAGING_`-prefixed lines matter there. `GET /api/auth/providers` now returns `{github:true,google:true}` on staging and both flows were verified live end-to-end (real GitHub + Google sign-in) | `serverXR/src/routes/authRoutes.js`, `serverXR/src/routes/authRoutes.test.js`, `serverXR/src/routes/integrationRoutes.js` |
| Visiting `staging.di-studio.xyz/wcc` (or `/br_id_ge`) showed "Access restricted — your session isn't scoped to 'wcc'. Allowed: open, sandbox-guest\<id\>" — looked like a broken auth/scoping bug | Not a bug — staging's database is genuinely empty of that content. Staging was set up as a brand-new, isolated environment (2026-07-15/16) with its own DB from scratch, specifically so deploys could be tested without touching prod's real data; nothing had ever synced prod's actual spaces (`wcc`, `br-id-ge`, `beyond-form` — real content built over months in Studio) into it. Staging only auto-creates the default `open` space at boot. Note the *id* uses hyphens not underscores (`br-id-ge`, `beyond-form`) — `br_id_ge`/`beyond_form` (with underscores) are the display/repo names, not the space id; `scripts/space-bundle.mjs`'s slug regex (`^[a-z0-9-]{3,48}$`) will reject the underscore form outright, which is a useful early signal if you get the id wrong | Used `scripts/space-bundle.mjs export <id>`/`import <bundle>` — an existing tool built for exactly this ("move a space to another install," strips secrets/sync-keys) — to copy `wcc`, `br-id-ge`, `beyond-form` from prod's DB into staging's. Ran it via `docker cp` into each running container (the script isn't baked into the production image — dev/ops tool only) and `docker exec`'d it there, since that's where Node + the actual `/data` volume both already are. Hit one wrinkle: `import`'s `require('../serverXR/src/db.js')` is a literal relative path from the script's own file location, but the container's image flattens `serverXR/src/` to `/app/src/` (no `serverXR/` prefix) — fixed by placing the script at `/app/scripts/space-bundle.mjs` and symlinking `/app/serverXR → /app` so the relative require resolves. Verified prod's `updatedAt` timestamps were unchanged after export (read-only), and all three spaces return `isPublic: true` from staging's `/api/spaces/:id` afterward. Cleaned up the temp script/bundle files from both containers and the VPS host when done | `scripts/space-bundle.mjs` (pre-existing, unmodified — this incident was ops/data, not a code change) |
| **Full 6-phase repo audit (2026-07-16, per-lane parallel agents: serverXR, schema/op-log, node system, 3D/viewport, Studio/Beta frontend, infra/deploy) found ~28 issues; the top 5 were fixed the same session** — see below for what shipped and what didn't | (1) `syncRoutes.js`'s `/api/sync/spaces/:spaceId/{status,pull,push}` never sanitized `req.params.spaceId` before using it in a filesystem path, and the auth-scope middleware (`index.js`) treats a normalization failure as `requiredSpaceId = null` → "unscoped, allow" (`authAccess.js`'s `isAuthScopeAllowedForSpace`) — a scoped user could reach a traversal payload (e.g. `..%2f..%2fetc` decoded by Express into a literal `..` id) that bypasses scope and hits the filesystem raw. (2)+(3)+(4) `POST /api/spaces/:spaceId/ops`, `POST /api/projects/:projectId/ops`, `PUT /api/projects/:projectId/document`, and `replaceSceneAndBroadcast` all check a version number across multiple `await`s (read version → read document → compute → write document → append ops → bump meta) with no atomicity around the whole sequence — two concurrent requests at the same `baseVersion` both pass the check and both write, one silently clobbering the other while both callers got `200`; `project_ops`/`space_ops` also had a non-unique index on `(id, version)`, so duplicate-version rows landed with no rejection and no read-order tiebreaker. (5) audit claimed no automated backup existed — **false positive**, the audit only reviewed the git repo; a working nightly backup cron (`/root/vps-backup.sh`, `17 3 * * *`) was already live on the VPS, just never committed to the repo, so nothing here documented it | (1) `syncRoutes.js` now normalizes+rejects the space id with 400 before touching the filesystem, same pattern every other route already used — regression test hits all three routes with traversal payloads. (2)(3)(4) new `serverXR/src/asyncLock.js` (`createKeyedLock`) serializes the whole check-then-write per space/project id, re-fetching the current version *inside* the lock rather than trusting a pre-lock snapshot; applied to all four call sites. `db.js` gained a `dedupeAndUniqueOps` migration — deletes any pre-existing duplicate-version rows (keeps highest `seq`) then makes `idx_space_ops`/`idx_project_ops` genuinely `UNIQUE`, so a future reintroduction of this bug class fails loudly instead of silently corrupting data; `opsSelect` queries also gained a `seq ASC` tiebreaker. Regression tests fire two **truly concurrent** (`Promise.all`, not sequential) HTTP requests at a real spawned server and assert exactly one 200 + one 409, plus DB-level tests proving the UNIQUE index rejects a duplicate insert and the migration dedupes pre-existing ones. Golden rule added for the general pattern. (5) committed the backup script into the repo for the first time (`deploy/vps-backup.sh`, mirrors the live `/root/vps-backup.sh` exactly) plus a new companion `deploy/vps-restore.sh` that didn't exist before — validated by dry-running the extraction against a disposable scratch Docker volume (never touching production) and confirming the restored DB opens and contains real data. Documented both in `docs/deploy/VPS_DOCKER_DEPLOY.md`, including the still-open gap (backups are VPS-local only, no off-box copy yet) | `serverXR/src/routes/syncRoutes.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/routes/projectRoutes.js`, `serverXR/src/asyncLock.js`, `serverXR/src/db.js`, `serverXR/src/spaceStore.js`, `serverXR/src/projectStore.js`, `deploy/vps-backup.sh`, `deploy/vps-restore.sh`, `docs/deploy/VPS_DOCKER_DEPLOY.md`, `docs/ai/golden_rules.md` |
| Audit finding #10: GHCR image pull auth on the VPS was undocumented — could be either a silent public-exposure or a working setup nobody wrote down | Checked directly: no `docker login` existed on the VPS (`~/.docker/config.json` empty), yet every deploy's `docker compose pull` had been succeeding — proved via an anonymous, credential-free `curl` against the GHCR API that both `dii-server` and `dii-client` were **publicly pullable** (200 on an anonymous pull token + tags/list). Never a deliberate decision — just GHCR's default visibility, never explicitly set, undermining the point of the repo being private (compiled backend/frontend source was pullable by anyone with the URL, though not live secrets — those come from runtime env vars, not the image) | User flipped both packages to private via GitHub's package settings UI (re-verified after: anonymous pull now 403). Fine-grained PATs turned out not to support GHCR package permissions at all (no "Packages" option in the account-permissions picker) — had to fall back to a classic PAT (`read:packages` scope only) instead. `docker login ghcr.io` as root on the VPS, once — persists in `/root/.docker/config.json` and covers both prod and staging (same Docker daemon, same box). Verified both images still pull successfully post-login. Documented in `docs/deploy/VPS_DOCKER_DEPLOY.md` including rotation steps; the token itself lives only on the VPS, never in this repo or CI | `docs/deploy/VPS_DOCKER_DEPLOY.md` (no code — this was an infra/access finding) |
| Audit finding #17 claimed `updateNode`'s raw `patch.values` merge was unnormalized (unlike `updateEntity`/`updateComponent`, which route through `normalizeEntity`) — read as an exploitable "unbounded garbage in node.values" gap | **Correction on closer inspection: not actually exploitable.** `applyProjectOps` (`src/shared/projectSchema.js`/`shared/projectSchema.cjs`) already calls `normalizeProjectDocument` on the *entire* document right before returning, regardless of which op ran — that call normalizes every node via `normalizeNodesList` → `normalizeProjectNode` anyway, so `updateNode`'s merged result was already being normalized, just via an outer pass rather than inline in its own case. Verified directly: a crafted non-string `assetRef` patch produced `null` in both the pre-fix and post-fix code, proving no behavior difference exists in the current pipeline | Routed the merge through `normalizeProjectNode` inline anyway — not because it changes behavior today, but for architectural consistency with the sibling `update*` cases (each op's own code should be locally correct, not depend on an external final-pass contract) and as defense-in-depth if a future refactor ever applies a single op without that outer wrapper. Applied identically to both the ESM and CJS mirror; `schemaSync.test.js` confirms parity | `src/shared/projectSchema.js`, `shared/projectSchema.cjs` |
| Audit finding #24: `shared/projectSchema.cjs` (the hand-maintained CJS mirror) was missing three exports the ESM original has — `defaultPresentationFixedCamera`, `defaultWorkspaceState`, `normalizeWorkspaceState` — a silent drift of exactly the kind the schema-sync equivalence tests exist to catch, just on the export surface rather than normalization behavior | The CJS mirror is hand-written (not generated), so any new ESM export has to be remembered and copied manually — these three were added to `src/shared/projectSchema.js` at some point without the matching `module.exports` update, and nothing checked export-surface parity, only behavioral equivalence on a fixed fixture set | Added the three missing exports to `shared/projectSchema.cjs`. `serverXR/src/schemaSync.test.js` gained a regression test asserting all three exported members match their ESM counterparts, so a future one-sided export addition fails the schema-sync gate instead of silently drifting | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| Audit finding #28: `AudioObject.jsx`'s HTML-`<audio>` fallback (used for formats `PositionalAudio` can't decode, e.g. FLAC) never released the underlying `Audio` element on real component unmount — its per-render effect's cleanup only called `.pause()`, unlike the sibling early-return branch a few lines above it (triggered when `canUsePositional` flips true) which correctly cleared `.src` and nulled the ref. A removed audio node left a live `HTMLAudioElement` (and its decoder/network resources) referenced only by a stale ref, never garbage collected | The per-render cleanup can't simply clear `.src` either, because it re-runs on every dependency change (volume/loop/paused/sourceUrl), and clearing `.src` there would interrupt a legitimate in-flight source swap, not just a real unmount | Added a second, `[]`-dependency effect whose only job is real-unmount teardown (`.pause()` + `.src = ''` + null the ref) — same pattern already used for GLTF disposal in `ModelObject.jsx` earlier in this audit pass. Regression test spies on the global `Audio` constructor, mounts with a forced-fallback (`.flac`) source, unmounts, and asserts `.pause()` was called and `.src` was cleared; confirmed failing against the pre-fix code via `git stash` before shipping | `src/objectComponents/AudioObject.jsx`, `src/objectComponents/AudioObject.test.jsx` |
| Audit finding #25: "No general schema-migration mechanism" for the project document shape (`shared/projectSchema.cjs`/`src/shared/projectSchema.js`) — confirmed real, not a false positive | `PROJECT_DOCUMENT_VERSION` is stamped onto every document but never read/compared/branched on anywhere — it's a one-way stamp, not a version gate. The only thing standing in for migration is defaulting-tolerant normalization (each `normalize*` helper defaults a missing field), which only covers *additive* schema changes; a breaking change (rename, enum change, restructure) has no detection or transform path today | **Deliberately documented as accepted debt rather than code-fixed this pass** — a real migration mechanism (versioned transform registry dispatched by `source.version`) needs a design pass across ~8 call sites that construct/read project documents, and no breaking document-shape change has actually happened yet to force the issue; building one speculatively risked exactly the kind of premature abstraction this project's own operating rules warn against. Documented in full in `docs/architecture/PROJECT_SURFACES.md` ("Document Schema Versioning" section), including the concrete rule for next time: any *breaking* `projectSchema` change must ship its own explicit read-time transform + fixture regression test, not rely on normalization's defaulting | `docs/architecture/PROJECT_SURFACES.md` (no code — documentation-only, matches audit finding #5's backup-script precedent of "verify/document rather than assume") |
| Audit finding #23: `nodeGraphRuntime.js`'s single-pass graph evaluation (`evaluateNodeOutput`/`evaluateNodeInput`) had no memoization — a node feeding two or more downstream consumers (a diamond dependency, e.g. one `sin`/color/value node driving both the viewport light and grid, or multiple `BetaViewport` nodes sharing an upstream math chain) got fully recomputed from scratch once per consumer per render, instead of once per pass. Perf-only (no wrong output, just wasted CPU that scales with fan-out and graph depth) | The existing `stack` parameter is a `Set` used purely for cycle detection — it's rebuilt fresh on every top-level `evaluateNodeOutput`/`evaluateNodeInputs` call and never shared across sibling call chains within the same render, so it never doubled as a cache | Added `outputCache: new Map()` to the context object `createNodeGraphContext` already builds once per pass (memoized via `useMemo(..., [document])` at every call site — `BetaViewport.jsx`, `BetaEditor.jsx`, `viewportWorldState.js` — so one cache naturally spans every node evaluated in that pass). `evaluateNodeOutput` checks the cache by `${node.id}:out:${portId}` before computing (factored the switch into a new `computeNodeOutput` helper) and writes the result after. Caching is safe through the cycle-detection short-circuit for every node that ISN'T itself part of a cycle: the `stack.has(key) → return undefined` truncation happens before the cache write, so only nodes actually sitting on the cycle stack ever get a truncated result cached — an acyclic node reached via two different paths always computes and caches its one true value before either caller sees it. (A node genuinely inside a cycle has no well-defined value to begin with — memoization just makes whichever consumer evaluates first fix that frame's arbitrary truncated result for the rest of the pass, which is fine since cyclic output was never meaningful.) Regression test spies on `Math.sin` (the only thing `math.sin` calls) with a shared upstream `sin` node feeding two `math.add` consumers and asserts it's called exactly once across both; confirmed calling it twice pre-fix via `git stash` | `src/project/graph/nodeGraphRuntime.js`, `src/project/graph/nodeGraphRuntime.test.js` |
| Audit finding #26: `Dockerfile`, `serverXR/Dockerfile`, and `docker-compose.yml` (`caddy`/`tunnel`) all pinned base images to floating tags (`node:22-alpine`, `nginx:alpine`, `caddy:2-alpine`, `cloudflare/cloudflared:latest`), not digests — a rebuild/pull tomorrow could silently use a different actual image than today's, with no diff or review | Never deliberately decided — just never pinned. Confirmed the actual image behind each tag via `skopeo inspect` rather than guessing | Re-pinned all four to `tag@sha256:<digest>` (the digest each tag currently resolves to, so this change is a no-op for what's actually running — zero deploy risk from image content changing). Documented the trade-off (no automatic security patches — deliberate, re-pin manually) and the re-pin procedure in `docs/deploy/VPS_DOCKER_DEPLOY.md` ("Base Image Pinning"), including the caution that `docker-compose.yml` is the same file that caused the 2026-07-16 healthcheck outage, so future edits here get the same care | `Dockerfile`, `serverXR/Dockerfile`, `docker-compose.yml`, `docs/deploy/VPS_DOCKER_DEPLOY.md` |
| Audit finding #27: the `client` container (nginx serving the built SPA) ran as root, unlike `server` which already had a dedicated non-root `app` user — an inconsistent hardening posture, and a real risk if nginx or a served asset were ever exploited (root inside the container, though the base image already avoided some worse defaults) | Never addressed when the client Dockerfile was first written; server got the non-root treatment, client didn't | Swapped `nginx:alpine` for `nginxinc/nginx-unprivileged:alpine` (official pre-hardened variant, runs as `nginx` user, listens on 8080) rather than hand-patching root-owned nginx directories with no Docker daemon available locally to test the result first — see the user's explicit choice between options when asked. Rippled the resulting 80→8080 container-internal port change through `nginx.conf`, `docker-compose.yml` (client ports/healthcheck/tunnel target), and `Caddyfile` — all host-facing ports are unchanged, only the internal one moved. Full writeup incl. the one residual unverified risk (whether this image ships `wget` the same way plain `nginx:alpine` was confirmed to) in `docs/deploy/VPS_DOCKER_DEPLOY.md` ("Client Container Non-Root Hardening") — watch the first staging deploy closely, same as the 2026-07-16 healthcheck incident | `Dockerfile`, `nginx.conf`, `docker-compose.yml`, `Caddyfile`, `docker-compose.caddy-hardened.yml`, `docs/deploy/VPS_DOCKER_DEPLOY.md` |
| Beta deep audit (2026-07-16/17): Node 0 is a normal, deletable node whose card is deliberately hidden from the graph canvas ("topbar is node0's presence"), but the *entire* Beta topbar (back-to-hub, breadcrumb, Chat, Outliner, Help, node-size control) is gated on it existing (`hasNodeZero`) — deleting it silently removed that whole UI with no warning and no way back except double-clicking the canvas to place a fresh one | Two separate, both-reachable deletion paths existed with no guard: `handleDeleteSelected` (the "Delete" FAB / Delete key off the graph surface) and the `onDeleteNode` callback passed to `BetaGraphSurface` (the graph canvas's own internal Delete/Backspace listener). The second one is non-obvious — it fires off `workspaceState.selectedNodeId` globally, and Node 0 *can* be selected via the Outliner (which lists all graph-surface nodes, Node 0 included, since `matchesNodeTypeSurface` always returns true for the 'graph' surface) even though it has no visible graph-canvas card | Added `isRootGraphNode(node, rootTypeId)` — pure, testable — to `src/project/graph/nodeGraphAuthoring.js` (shared engine, so Studio's graph view gets the same guard for free once it gains write access; it's read-only today). Both `BetaEditor.jsx` call sites now `window.confirm(...)` before deleting a node this returns true for, matching the existing destructive-action pattern already used for project/space deletion elsewhere. Regression tests cover both call sites, a confirm-and-cancel-survives-intact case, and a normal-node-never-prompts case; verified live — selected Node 0 via the Outliner, clicked Delete, confirmed the real browser `confirm()` fires with the right message and Node 0 + the topbar both survive a Cancel | `src/project/graph/nodeGraphAuthoring.js`, `src/beta/components/BetaEditor.jsx` |
| Mouse-look (pointer-lock/drag camera rotation) silently stopped working on `/wcc/scene` and any other public project with `worldState.spawn` set — WASD movement worked, `?inputdebug=1` showed healthy lock state and changing `yaw`/`pitch` numbers, but the rendered camera never turned. Pre-existing bug (not caused by the 2026-07-17 perf audit), found by `git log -L` blaming the exact effect rather than guessing/bisecting build changes | Commit `a79c689c` (2026-06-29, "data-driven spawn") reassigned `playerRef.current = {...}` (a whole new object) once a project's `worldState.spawn` loaded. `Walker`'s mouse/touch-look listeners are wired up in a mount-only `useEffect` (deps include the stable `playerRef` container, not `.current`) that closed over the *old* object at mount time — mouse-look kept mutating that orphaned object forever, while the camera's `useFrame` and the debug HUD both read `playerRef.current` fresh, so WASD (also read fresh per-frame) and the yaw/pitch numbers looked fine but the camera never moved | Changed the spawn effect to `Object.assign(playerRef.current, {...})` — mutates the existing object in place instead of replacing it, so the mount-time closure and the per-frame reader stay pointed at the same object | `src/components/LiveProjectScene.jsx` |
| User onboarding to Beta felt wrong for the intended TouchDesigner-style workflow: a brand-new project forced a manual "double-click to place Node 0" step before anything else was possible, then auto-entered its interior and hid its topbar/whole-UI behind that first click — user explicitly said this didn't match "enter any node like a folder, Node 0 is just the root dir" | Node 0 (`universe.node0`) was special-cased everywhere: `useNodeGraphScope({ rootTypeId: NODE_ZERO_TYPE_ID })` auto-created-and-entered it on load, the whole topbar was gated on `hasNodeZero`, its graph card was hidden ("topbar is node0's presence"), and an audit pass the same day had just added it to `SINGLETON_TYPE_IDS`/`nodeRegistry.js`'s `singleton:true` as a consistency bug fix — all of this assumed Node 0 must be a forced, unique, auto-entered root, which is exactly the model the user rejected | **Product decision 2026-07-17, confirmed explicitly by the user: Node 0 is now an ordinary node type — placeable, deletable, enterable exactly like any other node, no auto-creation, no auto-entry, no singleton enforcement.** Reverted the same-day singleton fix (`SINGLETON_TYPE_IDS` in both schema files, `nodeRegistry.js`'s `singleton:true`) — this is an intentional reversal, not a missed regression, flagged in comments at every reverted site so a future audit pass doesn't silently re-add it. `BetaEditor.jsx`: dropped `rootTypeId` from `useNodeGraphScope` (the hook's generic auto-enter-root effect now never fires — true document root, `currentScopeId === null`, is just a plain always-available scope), removed `handleStartFromNodeZero`/`hasNodeZero`/`NODE_ZERO_TYPE_ID`, topbar chrome now always renders (no more collapsed pre-seed state), double-click always opens the node palette directly, deleting any node (Node 0 included) no longer shows a special confirm dialog. Regression tests rewritten to match (`BetaEditor.test.jsx`, `nodeRegistry.test.js`, `projectSchema.test.js`) | `src/beta/components/BetaEditor.jsx`, `src/beta/components/BetaEditor.test.jsx`, `src/project/nodeRegistry.js`, `src/project/nodeRegistry.test.js`, `src/shared/projectSchema.js`, `shared/projectSchema.cjs`, `src/shared/projectSchema.test.js`, `docs/architecture/RECURSIVE_NODE_CORE.md` |
| User report: "after walk/fly, to go back to previous view mode you need to exit and enter space again" on a real published space (not the landing page's decorative background — that was a wrong first guess; also added a real, separate view-toggle there, but it wasn't the actual complaint). The mechanism actually already worked (confirmed live: click "Walk / Fly" → walk around → click the header button → back to the orbit viewer, `Walk / Fly` button reappears) — the real defect was the label | `PublicProjectViewer.jsx` renders `LiveProjectScene` in walk mode with `showChrome`, whose header exit button was hardcoded to the text `← Exit` — identical wording to the landing page's own "Exit space" button, which really does leave the page. In this context the button only swaps `navMode` back to `'orbit'` *within the same page*, so the label read as "this will kick me out" when it wouldn't | Added an `exitLabel` prop to `LiveProjectScene` (default unchanged, `← Exit`, still correct for `WccExperience.jsx`'s real full-exit case). `PublicProjectViewer.jsx` now passes `exitLabel="← View mode"` for its walk-mode round trip. Regression test mocks `LiveProjectScene` to assert the walk-mode button reads "← View mode" (not "← Exit") and that clicking it returns to the `Walk / Fly` button | `src/components/LiveProjectScene.jsx`, `src/project/components/PublicProjectViewer.jsx`, `src/project/components/PublicProjectViewer.test.jsx` |
| User wanted to freely place multiple Worlds/Lights/Backgrounds/Grids (and generally build "like lego") instead of hitting the one-per-scope restriction — a session earlier the same day had shipped a warning UI for exactly that block, then the user asked for the opposite. Broader than a single bug: the request evolved (via a deep research pass across TouchDesigner, Houdini, Blender, Nuke, Unreal Blueprints, vvvv, Resolume, Cables.gl, Max/MSP, VCV Rack, QLab, Ableton) into "no node type should be a singleton at all" | The remaining singleton restrictions (`time`, `source.ar`, `universe.world`, `world.light`, `world.background`, `world.grid`) were the same class of forced-uniqueness the Node 0 reversal above already rejected for one type — just not yet generalized. `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` were still enforcing it in both schema files | **Product decision 2026-07-19: no node type is a singleton, anywhere, full stop.** Deleted `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` outright from both `src/shared/projectSchema.js` and `shared/projectSchema.cjs` (not just unused — gone, so a future pass can't accidentally re-enforce them; **do not re-add without checking with the user first**, same rule as the Node 0 row). Stripped the now-dead `singleton:true` from the 6 affected `nodeRegistry.js` definitions. Removed the same-day blocked-create warning from Beta (`getPaletteBlockReason`, `NodePalette.jsx`'s `getBlockReason` prop, the CSS) since there's nothing left to block. For the few types where exactly one "active" result is still wanted (World/Light/Background/Grid), added an explicit **active marker** instead of a restriction — `workspaceState.activeNodeIdByTypeScope` (new, generic, keyed `` `${typeId}::${scopeId}` ``) alongside the pre-existing `liveWorldNodeIdByScope`, following the "hierarchy is the connection" pattern confirmed via Kantan Mapper (a shipped TouchDesigner tool: adding a mapping shape is just adding a child, no wire). Landed as a **new lane, `src/raw/`**, forked wholesale from Beta rather than patched in place (first lane-fork in the project, no prior graduation policy existed — see `docs/architecture/PROJECT_SURFACES.md`'s "On forking a new lane from Beta" section) — Beta itself keeps its simpler `.find()`-first-match lookups (kept as the original sketch). Also added a universal, fully inert "view as code" inspector section (`values.__code`) to every node type, not just `node.null`. Full design writeup: `docs/architecture/RECURSIVE_NODE_CORE.md`'s "Nesting" and "The `raw` lane" sections | `src/shared/projectSchema.js`, `shared/projectSchema.cjs`, `src/shared/projectSchema.test.js`, `serverXR/src/schemaSync.test.js`, `src/project/nodeRegistry.js`, `src/project/nodeRegistry.test.js`, `src/project/graph/nodeInspectorSections.js`, `src/project/graph/nodeInspectorSections.test.js`, `src/beta/components/BetaEditor.jsx`, `src/beta/components/NodePalette.jsx`, `src/beta/styles/beta.css`, `src/raw/**` (new), `src/RootApp.jsx`, `docs/architecture/RECURSIVE_NODE_CORE.md`, `docs/architecture/PROJECT_SURFACES.md` |
| Live click-through of the free-nesting work above (previous row) found the headline feature didn't actually work: nesting anything *inside* a `universe.world` node — the whole point of "put geometry/light/background inside a World and see it render" — silently landed the new node as a **sibling** of World instead of its child, at any nesting depth. The 3D viewport correctly renders nothing, because nothing is really parented to it. First caught by trusting a wrong UI signal (clicking World's topbar toggle *looked* like scope navigation — a breadcrumb-ish highlight appeared) and reported as working; only re-verified as broken by fetching the raw document from `/api/projects/:id/document` and reading real `parentId` values directly, which is the only way this was actually caught | `universe.world` (and every other `render:'panel-2d'` node type — Text/Image/Browser/stream monitors) is deliberately excluded from `RawEditor.jsx`'s `graphCardNodes` list, because it gets its own floating `DesktopWindow` instead of a graph card. But `onEnterNode`/`handleEnterNode` (the only thing that calls `scopeEnterNode` and actually changes `currentScopeId`) was wired *only* to a graph card's double-click — so panel-2d nodes had no reachable way to enter their own scope at all. World's topbar button and its `DesktopWindow`'s Pin/Minimize/Close only ever toggled window-visibility state (`values.frame`), never scope | Added an `Enter ›` button to `DesktopWindow.jsx`'s header (new `onEnter` prop, renders only when passed), wired in `RawEditor.jsx` to the same pre-existing `handleEnterNode(node.id)` graph cards already used — no scope-navigation logic needed changing, it already handled `universe.world` correctly (including flipping to fullscreen 3D), it just had no caller. Applies to every panel-2d type uniformly, consistent with "no node type is exempt from free-nesting". Verified live at both 1 level (root World) and 4 levels deep (`Light → Add → World`): new children now get the correct `parentId`, confirmed via the same raw-document API fetch that caught the bug, and render live in the 3D viewport (a cube/sphere actually appears on the grid) | `src/raw/components/DesktopWindow.jsx`, `src/raw/components/DesktopWindow.test.jsx`, `src/raw/components/RawEditor.jsx`, `src/raw/components/RawEditor.test.jsx` |
| Landing "Open Studio" (and nav "Studio") dumped guests into the stripped-down jam editor at `/open/studio/projects/open-jam` instead of the studio hub | The open-jam work made the open-space hub auto-forward into the communal jam project ("door, not a lobby", `StudioHub.jsx`); the landing CTAs already pointed at `/open/studio`, so every "Open Studio" click hit the door | Point `OPEN_STUDIO_HREF` at `/open/studio?browse=1` (the hub's existing forward opt-out); "Step inside" keeps the plain door URL on purpose. Regression tests lock both hrefs | `src/landing/LandingPage.jsx` |
| Browser Back is dead after "Step inside": a visitor who enters the open space from the landing page can never get back out, Back just re-enters the jam editor | The open-space auto-forward used a *pushing* navigate, so history read landing → `/open/studio` → `…/projects/open-jam`. Back popped to `/open/studio`, whose forward effect fired again and pushed the visitor straight back in — a self-rebuilding trap | Forward with `{ replace: true }` so the door consumes its own history entry and Back reaches whatever preceded it. `appNavigate` already supported the option. Regression test asserts the option, not just the path | `src/studio/components/StudioHub.jsx` |
| Raw lane: nesting worlds a few levels deep freezes the tab — console fills with "WebGL context was lost."/"THREE.WebGLRenderer: Context Lost.", the context-guard overlay flickers, and the page stops responding entirely (worse than the single-canvas loss two rows up, which recovers) | `RawEditor`'s panel-window list filtered the *whole document* (`nodes.filter(isPanelNode)`) instead of the current scope, so every `universe.world` node at every depth kept a live `<Canvas>` mounted in every scope. Browsers cap concurrent WebGL contexts (~16); past that they kill the oldest, `useWebglContextGuard` answers a loss by remounting the Canvas, that remount evicts another context, and the guard cascades into an unbreakable remount loop. Fullscreen world made it worse — the panel viewports stayed mounted underneath a view that covered them completely | Pure selector `selectMountedPanelNodes` (`src/raw/utils/windowLayout.js`): mount only panels whose `parentId` matches `currentScopeId` (panels are now scoped exactly like graph cards) and mount none at all while a world is fullscreen — the fullscreen `RawViewport` renders outside the panel list, so nothing blanks. Contexts stay bounded by scope width, not document size. Regression guard: 5 cases in `src/raw/utils/windowLayout.test.js` incl. a 24-deep nesting chain asserting <16 mounts | `src/raw/utils/windowLayout.js`, `src/raw/utils/windowLayout.test.js`, `src/raw/components/RawEditor.jsx` |
| `syncRoutes.test.js > pull proceeds to fetch the live scene when the space is writable` fails with "Test timed out in 5000ms" on some machines/networks and passes on others — nothing in the product changed between runs | The test never injected `httpRequest`, so `registerSyncRoutes` fell back to its real `node:https` default and the handler made an actual network call to `https://live.example` with an 8000ms client timeout — inside a 5000ms test timeout. Offline or behind a slow/hijacking resolver that's a hang; it only "passed" where DNS happened to reject fast. Every route test must inject `httpRequest` — the default is real network | Inject `vi.fn().mockRejectedValue(...)` as `httpRequest` in the read-only-enforcement `setup()` and assert the intent directly (`httpRequest` was called ⇒ the writable check let it through; `next` called with that exact error) instead of the vague "next got something". Suite went 5.7s → 0.7s. Checked the rest of the repo: `httpContracts.test.js` only fetches its own spawned `server.baseUrl`, no other test reaches the real network | `serverXR/src/routes/syncRoutes.test.js` |
| Uploaded photos leaked their EXIF — GPS coordinates, device model, capture timestamp — to anyone who could open the asset URL. Published spaces are public, so a phone photo dropped into a scene published the photographer's location | Originals were stored and served byte-for-byte (`spaceStore.js:508`); a repo-wide grep for `exif` returned zero hits, so nothing ever stripped metadata. sharp thumbnails do drop it, but the thumbnail is a derivative — the full-size original is what a visitor downloads. Found by the 2026-07-28 privacy audit (`docs/ai/privacy-data-inventory.md`), not by a report | Shared `scrubImageMetadata` (`serverXR/src/assetScrub.js`) re-encodes JPEG/PNG/WebP/TIFF/AVIF/GIF on ingest; sharp doesn't copy input metadata, so EXIF/IPTC/XMP are gone. Three traps handled: (1) `.rotate()` bakes EXIF orientation into pixels before the tag dies — skipping it turns portraits sideways; (2) it must run BEFORE the content hash, since the asset id is the sha256 of the stored bytes, so a client-precomputed id is dropped for scrubbed files and the recomputed id returned (bundle-import callers already remap); (3) `req.file.size` is stale after re-encode — re-stat or every scrubbed asset records a wrong size. SVG skipped (sharp would rasterize it), non-images pass through, and a scrub failure logs and continues rather than failing the upload. Regression guard: 7 cases in `serverXR/src/assetScrub.test.js` incl. a real GPS-EXIF JPEG and an orientation-6 portrait asserting 40x60 -> 60x40. **Not retroactive — assets uploaded before this keep their metadata** | `serverXR/src/assetScrub.js`, `serverXR/src/assetScrub.test.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/routes/projectRoutes.js`, `src/wiki/wikiContent.js` |
| Landing "Open Studio" opens a project list instead of all spaces — the label promises the top level and delivers one room inside it | `studioHref` resolved to `/open/studio?browse=1` for signed-out and guest sessions (`LandingPage.jsx:127`). That path is `STUDIO_PAGE_HUB` -> `StudioHub.jsx`, which despite the name is a **single space's project list** (the open space's), not the spaces hub. The `?browse=1` only suppressed StudioHub's auto-forward into the jam editor, so an earlier fix ("opens the browsable hub, not the jam door") stopped one level too deep, and the follow-up that added session-awareness only corrected the signed-in branch. Because these are static `href`s rather than click handlers, a signed-in owner also got the guest destination during the tick before `getApiSession` resolved | All five Studio controls now use `buildStudioSpacesPath()` -> `/studio` -> `STUDIO_PAGE_SPACES` -> `SpaceHub`, the actual list of all spaces, for every session. Verified on staging that `/studio` signed-out is fully usable (Spaces, GRID/MAP, Open Space, sandbox, "Sign in to create"), so no session loses out. Dropped the now-dead `OPEN_STUDIO_HREF`, `buildStudioHubPath` and `useAuthSession` imports. Regression guard: the two `LandingPage.test.jsx` cases that asserted `/open/studio?browse=1` now assert `/studio`, plus a case pinning the same destination across all three session states | `src/landing/LandingPage.jsx`, `src/landing/LandingPage.test.jsx` |
| User wanted to freely place multiple Worlds/Lights/Backgrounds/Grids (and generally build "like lego") instead of hitting the one-per-scope restriction — a session earlier the same day had shipped a warning UI for exactly that block, then the user asked for the opposite. Broader than a single bug: the request evolved (via a deep research pass across TouchDesigner, Houdini, Blender, Nuke, Unreal Blueprints, vvvv, Resolume, Cables.gl, Max/MSP, VCV Rack, QLab, Ableton) into "no node type should be a singleton at all" | The remaining singleton restrictions (`time`, `source.ar`, `universe.world`, `world.light`, `world.background`, `world.grid`) were the same class of forced-uniqueness the Node 0 reversal above already rejected for one type — just not yet generalized. `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` were still enforcing it in both schema files | **Product decision 2026-07-19: no node type is a singleton, anywhere, full stop.** Deleted `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` outright from both `src/shared/projectSchema.js` and `shared/projectSchema.cjs` (not just unused — gone, so a future pass can't accidentally re-enforce them; **do not re-add without checking with the user first**, same rule as the Node 0 row). Stripped the now-dead `singleton:true` from the 6 affected `nodeRegistry.js` definitions. Removed the same-day blocked-create warning from Beta (`getPaletteBlockReason`, `NodePalette.jsx`'s `getBlockReason` prop, the CSS) since there's nothing left to block. For the few types where exactly one "active" result is still wanted (World/Light/Background/Grid), added an explicit **active marker** instead of a restriction — `workspaceState.activeNodeIdByTypeScope` (new, generic, keyed `` `${typeId}::${scopeId}` ``) alongside the pre-existing `liveWorldNodeIdByScope`, following the "hierarchy is the connection" pattern confirmed via Kantan Mapper (a shipped TouchDesigner tool: adding a mapping shape is just adding a child, no wire). Landed as a **new lane, `src/seed/`**, forked wholesale from Beta rather than patched in place (first lane-fork in the project, no prior graduation policy existed — see `docs/architecture/PROJECT_SURFACES.md`'s "On forking a new lane from Beta" section) — Beta itself keeps its simpler `.find()`-first-match lookups (kept as the original sketch). Also added a universal, fully inert "view as code" inspector section (`values.__code`) to every node type, not just `node.null`. Full design writeup: `docs/architecture/RECURSIVE_NODE_CORE.md`'s "Nesting" and "The `seed` lane" sections | `src/shared/projectSchema.js`, `shared/projectSchema.cjs`, `src/shared/projectSchema.test.js`, `serverXR/src/schemaSync.test.js`, `src/project/nodeRegistry.js`, `src/project/nodeRegistry.test.js`, `src/project/graph/nodeInspectorSections.js`, `src/project/graph/nodeInspectorSections.test.js`, `src/beta/components/BetaEditor.jsx`, `src/beta/components/NodePalette.jsx`, `src/beta/styles/beta.css`, `src/seed/**` (new), `src/RootApp.jsx`, `docs/architecture/RECURSIVE_NODE_CORE.md`, `docs/architecture/PROJECT_SURFACES.md` |
| Live click-through of the free-nesting work above (previous row) found the headline feature didn't actually work: nesting anything *inside* a `universe.world` node — the whole point of "put geometry/light/background inside a World and see it render" — silently landed the new node as a **sibling** of World instead of its child, at any nesting depth. The 3D viewport correctly renders nothing, because nothing is really parented to it. First caught by trusting a wrong UI signal (clicking World's topbar toggle *looked* like scope navigation — a breadcrumb-ish highlight appeared) and reported as working; only re-verified as broken by fetching the raw document from `/api/projects/:id/document` and reading real `parentId` values directly, which is the only way this was actually caught | `universe.world` (and every other `render:'panel-2d'` node type — Text/Image/Browser/stream monitors) is deliberately excluded from `SeedEditor.jsx`'s `graphCardNodes` list, because it gets its own floating `DesktopWindow` instead of a graph card. But `onEnterNode`/`handleEnterNode` (the only thing that calls `scopeEnterNode` and actually changes `currentScopeId`) was wired *only* to a graph card's double-click — so panel-2d nodes had no reachable way to enter their own scope at all. World's topbar button and its `DesktopWindow`'s Pin/Minimize/Close only ever toggled window-visibility state (`values.frame`), never scope | Added an `Enter ›` button to `DesktopWindow.jsx`'s header (new `onEnter` prop, renders only when passed), wired in `SeedEditor.jsx` to the same pre-existing `handleEnterNode(node.id)` graph cards already used — no scope-navigation logic needed changing, it already handled `universe.world` correctly (including flipping to fullscreen 3D), it just had no caller. Applies to every panel-2d type uniformly, consistent with "no node type is exempt from free-nesting". Verified live at both 1 level (root World) and 4 levels deep (`Light → Add → World`): new children now get the correct `parentId`, confirmed via the same raw-document API fetch that caught the bug, and render live in the 3D viewport (a cube/sphere actually appears on the grid) | `src/seed/components/DesktopWindow.jsx`, `src/seed/components/DesktopWindow.test.jsx`, `src/seed/components/SeedEditor.jsx`, `src/seed/components/SeedEditor.test.jsx` |
| algovrithm froze in a headset: the piece plays and loops on the monitor, but putting the glasses on gives a still frame — whichever frame it had reached at the moment the session started. Reads as a broken installation | `useRitualClock` drove the playhead from its own `window.requestAnimationFrame` loop. R3F keeps ONE window-rAF loop and explicitly skips any root whose `gl.xr.isPresenting` is true, handing that root to `gl.xr.setAnimationLoop` on `sessionstart` — so a presenting scene renders from the headset frame callback while window rAF is under no obligation to fire at all. The scene kept rendering at 72Hz; only the clock stopped. Tethered desktop headsets hide it, because the mirrored 2D window still gets rAF | Clock no longer ticks itself: `useRitualClock` exposes a stable `advance(deltaSec)` and `RitualClockDriver` (inside the Canvas, deliberately ABOVE the Suspense boundary so a suspending asset cannot stop the piece) calls it from `useFrame` — one clock, fed by whichever loop is presenting. Same change added `loop` (wrap carrying the overshoot; the seam needs no transition because `bookendAmount` already holds the veil at full opacity across both t=0 and t=durationSec). Guards: `ritualClock.test.js`, 13 cases incl. wrap-carries-remainder, hold-when-loop-off, delta clamp, and does-not-move-on-its-own | `src/algoVrithm/ritualClock.js` `src/algoVrithm/RitualClockDriver.jsx` `src/algoVrithm/AlgoVrithmExperience.jsx` `src/algoVrithm/ritualClock.test.js` |
| algovrithm on a phone/tablet: Enter AR works, Enter VR is simply absent, and the author-only diagnostic that exists to explain a missing Enter VR button says nothing at all | `xrAvailability` returned XR_READY when `supportedXrModes.vr || supportedXrModes.ar`, but the Enter VR button keys off `vr` alone. An AR-capable device therefore reported ready (silencing the chrome) while still offering no VR — reproducing the exact silent failure the module was written to prevent, now disguised by a working Enter AR button sitting next to it | Readiness is judged on VR alone, plus a new `XR_AR_ONLY` state naming the cause (ARCore/ARKit handheld AR, no headset attached) and pointing at a different device rather than a retry — Recheck cannot conjure a headset onto a phone, so the button reads `No VR on this device` instead of `No VR · Recheck` in that state. Guards: xrAvailability.test.js `is NOT ready when only AR is supported`, `tells an AR-only device it is the wrong device`, `does not confuse AR-only with a missing headset` | `src/algoVrithm/xrAvailability.js` `src/algoVrithm/xrAvailability.test.js` `src/algoVrithm/AlgoVrithmExperience.jsx` |
| algovrithm's glitch transitions appear in the headset but NOT on the flat page — the desktop preview cuts between scenes with no noise wall at all, while VR shows them correctly. Reported as "in my pc simulation of vr theres no noisy glitch after each scene why in vr it like that". Nothing throws, nothing warns, and the piece is otherwise identical in both, so it reads as a rendering quirk rather than a bug | `TransitionVeil` parents its inverted sphere to the camera (`camera.add(mesh)`) so the strips lock to the VIEW rather than the world — that part is the design and is correct. What it depends on is the camera being reachable from the scene root: `renderer.render(scene, camera)` draws by walking **scene**'s descendants, and the camera is only the point of view, so a camera with `parent === null` is not in that walk and neither is anything hanging off it. In XR the session camera lives inside the rig `XROrigin` mounts, which IS in the scene, so the veil drew in the headset from day one. On the flat page R3F's default camera is standalone, so the veil left the rendered graph on frame one and was never drawn again. Note the audio listeners parented to the same camera (`ReelGlobe.jsx`, `SpatialScore.jsx`) are FINE and needed no change — three.js calls `camera.updateMatrixWorld()` in `render()` and that cascades to children, so matrices update off-graph; only *rendering* requires scene reachability. Same class as the 2026-07-31 PositionalAudio bug: an Object3D that is never made reachable is inert, silently | Extracted `attachVeil(mesh, camera, scene)` and added `if (!camera.parent) scene.add(camera)`. Deliberately conditional — an unconditional `scene.add(camera)` would reparent the XR camera out of its rig and break head tracking, a far worse bug than the one being fixed, so only a camera that belongs to nothing gets adopted. Guards: TransitionVeil.test.js `adopts a parentless camera so the veil is drawn on the flat page` (walks the parent chain to the scene root — the same question the renderer asks), `never reparents a camera that already belongs to a rig`, `is idempotent across frames` | `src/algoVrithm/TransitionVeil.jsx` `src/algoVrithm/TransitionVeil.test.js` |
| algovrithm's front door drew a **blank rectangle** — no picture at all, no exception, no failed request, nothing in the console. Reproducible only in a dev build, which is what makes it worth a row: the production build was fine, so a check that only ever looked at `npm run build` would have shipped it as working | `createHeroField`'s `dispose()` called `WEBGL_lose_context`. React 18 StrictMode mounts every effect twice in development — create, dispose, create — and the second `createHeroField` got a **dead** context, whose programs will not link, so it returned `null`. The 2D poster could not take over either: `getContext('2d')` on a canvas that has already been handed a WebGL context returns `null`, so the fallback silently failed for the same reason the primary did. Both renderers unavailable, both failing by returning null rather than throwing. The only visible symptom was `canvas.width === 300` — the default — which no test asserted | `dispose()` releases the programs and the buffer and leaves the context alone; it is not ours to destroy, and it dies with the canvas anyway. General rule: **never call `loseContext()` in a React cleanup** — StrictMode makes cleanup a normal part of mounting, not only of unmounting. Guards in `heroField.test.js`: `does not destroy the context it was handed`, `can be created again on the same canvas after a dispose`, and `sizes its drawing buffer to the frame it was given` (the 300×150 tell) | `src/algoVrithm/landing/heroField.js` `src/algoVrithm/landing/heroField.test.js` |
| algovrithm's front door was **black for the whole four seconds** of the halo beat — a quarter of the loop showing nothing, on a page whose only picture is the loop | The halo's rings were placed with the piece's own geometry: ripples on a floor sheet 1.54m below the eye. A ring of world radius R on a sheet h below lands at screen tan R/h, and the piece's rings are born at 1.2m — so a NEWBORN ring is already at tan 0.78 while this frame's half-height is tan 36° = 0.727. Every ring in the beat was born outside the frame and only ever moved further out. The port was faithful and the result was empty: in the headset the geometry is right because the visitor looks down and the ring is around their feet, which a fixed frame cannot do. Not caught by the first look either — a cold single-frame render showed one ring, because the halo integrates from the frame delta and a first draw has `delta = 0`, so it drew the newborn and none of its history | The door stands 20m off the sheet (`HALO_EYE_HEIGHT`, = RING_DEAD), so a ring's life is the picture's life: born near the centre at tan 0.06, crossing the frame edge as it dissolves. Every other number in the beat is still the piece's. Two lessons beyond the fix: **a port can be faithful and still be empty**, because the piece's geometry assumes a viewer who can look around; and **any beat that integrates motion must be warmed up before it is judged** — park the playhead by playing the loop in at a token resolution, not by drawing one cold frame. Guard: `stands far enough off the halo sheet for its rings to be in frame` | `src/algoVrithm/landing/heroField.js` `src/algoVrithm/landing/heroField.test.js` |
| algovrithm's front door: the PAUSE control sat **on top of the artist's statement** on every phone and tablet — "PAUSE" printed straight over "I don't experience this as a future", both words unreadable. Desktop was fine, which is why it survived a look: at 1440px the reading column starts 288px in and the control sits in the left margin | `.avl-hold` is `position: fixed` at the bottom-left with `background: none`, so the statement scrolls underneath it. The gutter that saved desktop does not exist below ~900px, where the statement runs to `clamp(1.1rem, 5vw, 4rem)` — the control's own left offset. Measured: phone 390×844 put the control at x 20–64 / y 798–826 inside a statement box of x 20–371, y 738–1874. The first repair — a soft bottom fade — was **not enough and looked like it was**: a line still read through the word PAUSE, the same collision in a politer register | The page dims toward its bottom edge and the control sits in the dim: `.avl-hold::before`, a fixed full-width strip, `var(--avl-void) 0 45%` then fading out. Opaque up to 45% because that clears the control's cap height at every width in the sweep; only the top half fades. Same scrim vocabulary the reading state already uses, so no new bar or surface. `.avl-statement` gains a matching `padding-bottom` so the last sentence can be scrolled clear rather than resting in the dim. jsdom does no layout, so the guards hold the thing whose absence caused it — a backing layer exists, its first stop is the solid token at ≥40%, and the statement reserves room — all three confirmed failing against the old CSS. Re-checked by looking, at 390/820/1440 | `src/algoVrithm/landing/algoVrithmLanding.css` `src/algoVrithm/landing/heroField.test.js` |
| algovrithm's front door showed the reel globe as **empty frames** — "no videos just frames without video". The beat is the footage beat, and it was the one beat carrying nothing of the work in it | The WebGL port invented a flat `plate` grey per cell, then a per-plate gradient, because the door deliberately loads no video: the reels are 197 MB in `src/algoVrithm/assets/` and one clip alone is 27 MB. The door exists so a visitor who followed a link does not pay for the piece before deciding to enter it, so shipping the footage there would be a larger version of the problem the door was built to prevent. But "no video" had been allowed to mean "no image", which is a different decision and nobody made it | One still out of every clip, packed into an 8×4 atlas of 31 greyscale frames at **86 KB** — `scripts/build-reel-atlas.mjs`, output committed so a fresh clone renders and the build needs no ffmpeg. Seeks 34% into each clip rather than frame 0 (phone captures routinely open on black or a hand). Three traps, all found by looking: the frames came out **mirrored**, because the visitor is inside the shell looking at its inner face — sample `1.0 - u`, and the giveaway was text in two reels reading backwards; the atlas is **NPOT** (864×768) so it needs CLAMP_TO_EDGE + LINEAR + no mipmaps or WebGL 1 samples pure black, silently; and cells need a half-texel inset or LINEAR bleeds the neighbouring reel across the seam. Greyscale on purpose — the palette holds the work to two hue bands and phone footage obeys no such rule. Loads fire-and-forget with the gradient plates as the fallback, so a slow or failed connection costs nothing. Guard: `the reel atlas and the shader agree` reads `reelAtlas.json` and checks the three grid literals baked into the shader | `scripts/build-reel-atlas.mjs` `src/algoVrithm/landing/heroField.js` `src/algoVrithm/landing/reelAtlas.webp` `src/algoVrithm/landing/reelAtlas.json` `src/algoVrithm/landing/heroField.test.js` |
| Studio's algovrithm card has a **Director** button, and it opened the front door instead — a page with no director on it and no way to reach one | `codeSpaces.js` set `directorPath` to `` `${ALGO_VRITHM_PATH}?director` ``. Since the front door was split off, `ALGO_VRITHM_PATH` is the landing page, not the piece; the director lives on `ALGO_VRITHM_SCENE_PATH`. It failed silently because the landing simply ignores a query param it does not read, so the button "worked" — it navigated, and to a real page | Point it at `` `${ALGO_VRITHM_SCENE_PATH}?director` ``. The suite did not catch it because `StudioHub.test.jsx` asserted the same hardcoded literal the code used, so both moved out of truth together; the assertion is now built from `ALGO_VRITHM_SCENE_PATH` plus a negative check that it is NOT the door, which is what makes a further route move fail loudly instead of staying green. General shape worth remembering: when a route is split in two, every stored path built from the old constant now means the other half of the split, and a query param that nothing reads makes the mistake invisible | `src/studio/utils/codeSpaces.js` |
| algovrithm on a phone: entering the piece put the chrome on top of itself. With the director on, the FULL SCREEN pill wrapped into a two-line circle sitting **on the word "algovrithm"**, and a three-line WebXR message lay across the subtitle. Without it, "· PRESS H FOR THE DIRECTOR" still ran under the pill. Desktop was clean at every width, which is why it survived: at 1440px the corners are 1100px apart | The title block and the button cluster were SIBLINGS, each `position: absolute` into its own top corner, so nothing ever told either one about the other — they only looked separate while the left one stayed short. Two things then made it long on a phone and only on a phone: the author-only `No VR · Recheck` diagnostic is a rendered *paragraph* (correctly so — a headset browser has no hover) sitting in a right-pinned flex row, so it grew leftwards until it squashed the pill; and the panel hint advertises a key that the device it is being shown on does not have — `usePanelToggle` is explicit that the absence of a keyboard IS the device check, so on a phone that line is both unfollowable and the longest string in the header. Reserving a fixed gutter for the corner is NOT a fix: the corner holds one pill or three depending on what the device supports, so any number is wrong somewhere | One bar, not two overlapping ones: `.algo-vrithm-chrome` becomes a flex row with `justify-content: space-between`, the words in a `.algo-vrithm-chrome-text` child with `min-width: 0` (without it a flex item will not shrink below its content, so the sentence shoves the buttons off-screen instead — the same defect wearing the other mask) and the actions `flex: 0 0 auto` so the text wraps and the tap targets never shrink. The diagnostics move to a new `XrDiagnostics` block in the bottom authoring stack — they were already gated on the panel being open, so they were never audience-facing and never belonged in the audience's header. The hint is hidden under `(pointer: coarse) and (hover: none)`. Guards: `chromeLayout.test.js` pins the structure (7 cases, confirmed failing against the old CSS), and `verify-algovrithm.mjs` measures real boxes at 375 and 390px. **That browser guard shipped with a hole first** — it pressed H before measuring, which opens the panel and therefore deletes the hint, so it passed on a build that visibly overlapped; it now measures with the panel closed AND open and fails on either. A box test is the right tool here, against this file's own warning about box tests, because every part of the top bar is pinned and opaque — the warning is about the *statement*, which scrolls under a fixed control by design | `src/algoVrithm/algoVrithm.css` `src/algoVrithm/AlgoVrithmExperience.jsx` `src/algoVrithm/chromeLayout.test.js` `scripts/verify-algovrithm.mjs` |
| algovrithm's card in Studio was **blank** while every other space showed a picture. Nothing was broken and nothing had been forgotten — the space simply could not reach a preview | The card falls back in a fixed order: a custom image, else a live miniature of the published project, else nothing. The middle branch needs `isPublic && publishedProjectId`, and a CODE space has no project document at all — its scene is React in `src/` — so it can never reach it. The row had also been created fresh on production and carried no `previewImageAssetId`, while staging's row had one, so the two tiers disagreed and only the deployed host could show which | Uploaded the captured reel-globe frame from the piece itself as the prod space's `previewImageAssetId` — the card's job is to show what the visitor will get, so a captured frame, never a logo or a placeholder. An audit of all 8 prod spaces found `open` in the same hole for a different reason (it forwards into a shared jam project and has no `publishedProjectId` of its own). The general rule is now written down: every space gets an automatic preview with no action from anyone, the owner can override it with their own image, and `handleUseLivePreview` is the way back to automatic — see "Every space shows a preview by default" in golden_rules.md, which carries a one-liner that audits a deployed host | `src/studio/components/SpaceHub.jsx` `docs/ai/golden_rules.md` |
| Logging out of di.iiii did not end the session — `DELETE /api/auth/session` only cleared the cookie, so a copy of it taken beforehand (another device, a saved header, anything that read the cookie jar) kept full access for the rest of the 12h TTL. There was no way to revoke a session at all, on any surface | Sessions are self-contained HMAC-signed payloads (`authSession.js`): the server holds no session table, so verification is signature + expiry and nothing else. Nothing about a logout could reach a token that had already left the browser. The privacy review had this as an open item ("no session revocation", CURRENT.md) — the piece it was missing is a per-account counter to sign INTO the payload, which is the standard fix for stateless sessions and costs one column, not a session store | `users.token_version` (`ensureColumn` in `db.js`, the same idempotent ADD COLUMN path `is_unrestricted` uses, so an existing prod DB with rows migrates on boot). It is stamped into the payload at every issue site that has a user row, checked on verify, and logout increments it — which invalidates every session for that account on every device at once. The trap the guards exist for: **most sessions have no user row**. Guests, sandboxes and API-token identities carry the same signed cookie with a subject that is not in `users`, so a naive `payloadVersion !== storedVersion` locks all of them out instantly (watched failing exactly that way). The lookup returns `null` for a missing row and the check only fires on a finite number; guest subjects skip the query entirely. The realtime entry point needed the same check or logout would only be half a logout: `socketHandlers.js` verifies the same cookie, so a revoked session could still open a Socket.IO connection and keep writing. Guards: four in `httpContracts.test.js` (before-logout session rejected, second device rejected, subject-with-no-user-row still verifies, and a revoked cookie refused at the socket handshake with the valid case asserted first so the test cannot pass by breaking sockets) and the migration's idempotency in `db.test.js` | `serverXR/src/db.js` `serverXR/src/userStore.js` `serverXR/src/authSession.js` `serverXR/src/index.js` `serverXR/src/routes/authRoutes.js` `serverXR/src/socketHandlers.js` `serverXR/src/db.test.js` `serverXR/src/httpContracts.test.js` |
| A CODE-mode published project page (`/br_id_ge`, `/wcc` — an `<iframe srcDoc>` that never mounts a canvas) still downloaded and evaluated **three-vendor, 1,614,468 bytes raw / ~452 KB gzipped**, before first paint | An earlier pass put both scene renderers behind `React.lazy()` and asserted exactly that in `PublicProjectViewer.jsx`'s own source — and the page kept shipping three-vendor, because `useXrAr` (`@react-three/xr`) and `cameraFraming` (`three`) were still **static** imports of the viewer. Rollup drops a chunk from the critical path only when *nothing* statically reachable from the entry needs it; one static import anywhere in that closure pulls the whole chunk back. The lazy renderers made the fix look done and the source-level guard agreed with it, so nobody walked the built graph | Everything that touches three — both renderers, the XR store, the camera-framing math and the camera state they drive — moved into `PublicProjectSceneSurface.jsx`, which sits *inside* the existing `lazy()` boundary; the viewer keeps only the code-mode iframe, the chrome and the sync. Overlay styles were split into `publicViewerStyles.js` so the surface can render the Enter AR/VR button without dragging the viewer's module in. Measured on the real build by walking `dist/assets` static `from"…"` edges: the code-mode closure went **1,846,155 → 221,886 bytes raw (541,190 → 74,677 gzipped), 21 → 19 chunks**, and three-vendor now appears only inside `__vite__mapDeps` (the dynamic-import preload list). Guard: `publicViewerCodeModeGraph.test.js` walks the static import graph from the viewer and fails with the offending chain printed — confirmed failing on the pre-fix imports. General shape: **a lazy boundary is only as lazy as the smallest static import that crosses it**, and only the built chunk graph can say whether it did | `src/project/components/PublicProjectViewer.jsx` `src/project/components/PublicProjectSceneSurface.jsx` `src/project/components/publicViewerStyles.js` `src/project/components/publicViewerCodeModeGraph.test.js` |
| The public co-presence relay `/serverXR/mesh` was ungated on both tiers, and could not be gated: any visitor could claim the rite's keeper node id and publish under its identity | `meshHub.js` read `MESH_ROOM_SECRET`, but **no compose file passed it**, so the deployment had no way to set it. Worse, the check it implemented was all-or-nothing (`roomSecret && query.get('secret') !== roomSecret` on every upgrade) — and visitors' browsers are mesh clients too (`br_id_ge/index.html`, `field.html` embed the relay URL), so switching it on would have either killed co-presence or forced the secret into public HTML, where it is not a secret and also lands in access logs and browser history | Made the gate **asymmetric**: the relay stays open to anonymous visitors, but a *reserved* node id (`keeper`/`keeper-*`, configurable via `MESH_PROTECTED_NODE_PREFIXES`) is refused with 401 at the upgrade unless it proves the secret. Eviction now keys on whether the claimant *proved* the secret rather than on whether one is merely configured — closing a second hole where, with a secret set, any anonymous visitor could evict any other. Added the vars to both compose files with distinct per-tier names. Guards: `meshHub.test.js` (6 cases: visitor joins unsecured, reserved id refused without/with wrong secret, admitted with it, authenticated keeper reclaims its own live id, unauthenticated duplicate still refused) and `deploy-compose.test.js` (the deployment actually passes the vars, staging's differs from production's) | `serverXR/src/meshHub.js`, `docker-compose.yml`, `docker-compose.staging.yml`, `serverXR/src/meshHub.test.js`, `src/deploy-compose.test.js`, `docs/deploy/VPS_DOCKER_DEPLOY.md` |
| Prod, staging and the dev box each called the SAME linked space something different — `br_id_ge`, `br_id_ge`, and `br_id_ge XR_ Notations:vi.ritual`. Every page inside them was byte-identical and every sync run had reported success. Noticed only because the user happened to open all three Studio views side by side | The space's own `label` was sent in the `POST /api/spaces` that CREATES a space and nowhere else — the third instance of this exact shape after the project slug (fixed in engine v3) and the project title (v4). It was also read from whichever *page* manifest ran first (`manifest.label` is that project's title), so provisioning a fresh tier from `di-space.landing.json` would have named the whole space "the landing — the door". Nothing could detect any of it: the manifests described four projects and nothing described the space, and no command compared one tier to another | Engine v5. `di-space.space.json` declares the space — `label`, `isPublic`, the tier map, and the project manifests that must exist — and step 1b PATCHes the declared fields on every run (`SPACE_FIELDS`/`TIER_FIELDS`, so reconcile/audit/docs cannot disagree). Per-tier intent is declared rather than remembered (staging `openInscriptions:false`), and `governed:false` marks the dev box as shown-but-never-enforced. `--all` syncs every declared page in one command; `--audit` reads all tiers, prints one table and exits 1 on undeclared difference — read-only, so it is safe against prod, and it runs in CI *after* the sync because a successful sync is not the same as tiers agreeing. Neither mode deletes: the audit reported 70 undeclared projects on the dev box and removed none. The first audit found four faults nobody had reported — null `rite`/`field` slugs on dev (404 at their own door), the stale `the field` title, and `br-id-ge-needs` missing entirely. Guards in `space-sync.test.js`, each confirmed failing against v4 | `scripts/space-sync.mjs` `scripts/space-sync.test.js` `br_id_ge/di-space.space.json` `br_id_ge/.github/workflows/sync-space.yml` |
| Every space on production was **ownerless** — `ownerUserId: null` on all 8 — so publish, invite, rename and delete each fell through to a platform admin. One person was the bottleneck for every space in the product, and nothing said why | `ownerUserId` was written in exactly one place: the `POST /api/spaces` that creates a space, read from the *session* that made the request (`spaceRoutes.js:219`). A space provisioned by an API token has no session, which is how every repo-linked space arrives — so they were all born ownerless. `ownerUserId` was also absent from the PATCH field list, so there was no route back: the store had supported the update since the quota work, and no caller could reach it. The **fourth** instance of the create-only-field class in one day, after the project slug (engine v3), the project title (v4) and the space label (v5) — this one a level up, on the platform's own object rather than a synced one | `PATCH /api/spaces/:id` accepts `ownerUserId`, admin-only (handing someone a space is a grant, not a preference — an owner cannot give their own space away). Explicit `null` releases it back to the platform; an id matching no account 404s and a `guest:` subject 400s, rather than silently storing a string nobody can sign in as. Ownership and scope were **separate grants**, so assigning one without the other left the new owner unable to open their own space — the route now adds the space to the owner's scope in the same call, best-effort so scope can never fail the ownership write. `serverSpaces.js` forwarded neither `ownerUserId` nor `slug` (**"Edit public link" in Preferences → Manage had been a no-op**: the button existed, the server supported it, the client dropped the field); both are now forwarded explicitly because both are meaningfully `null`. Preferences → Manage gained an "Owner & access" section that names the missing owner in words. Guards: the HTTP contract covers gate/unknown-id/guest/clear/no-side-effect (seen failing against the old route), the store covers reassign-and-clear, and the component covers the one-click handover | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/httpContracts.test.js` `serverXR/src/spaceStore.ownership.test.js` `src/services/serverSpaces.js` `src/components/preferences/AdminManageSection.jsx` `src/components/preferences/AdminManageSection.test.jsx` |
| algovrithm's **Director opened on di-studio.xyz and could not save from it**. The timeline was reachable from Studio, a collaborator could retime the whole piece on the live site, and the work vanished on reload. Nothing failed loudly — the button was there and it was pressed | "Save to source" POSTs `/__algovrithm/edit-list`, which is a **Vite dev-server middleware** (`vite.config.js`, `apply: 'serve'`). It is dev-only structurally and correctly so — it writes a source file — but it was the ONLY save the panel had, so the piece could be retimed only on the one machine running `npm run dev`. The deeper hole under it: a CODE space (its scene is React in `src/`, not a project document) had nowhere on the server to keep anything its author tunes. Every knob such a piece grows lands in the same trap | Two halves. **Platform:** `GET/PUT /api/spaces/:id/settings` — one small JSON blob beside the space's scene, deliberately opaque (what the keys mean belongs to the piece; a schema here would need editing every time a piece grew a knob), bounded at 64 KB, gated by the space's own read/write rules. **Piece:** the Director falls through to that blob when the dev endpoint is not there, storing a *timing overlay* — `{rowId: {startSec, endSec}}`, changed fields only — rather than a copy of the edit list. A row carries a Component, a backdrop and paragraphs of argument about why a beat is that length; none of that is data. So the file stays the source of truth, the overlay says how this space differs, and clearing it always lands back on the file. The overlay resolves BEFORE the clock is built (`useSavedTiming`, 1.5s deadline, late answers ignored) — dropping it in later would jump the playhead mid-beat in front of an audience, and a dead backend must cost frames, not the show. The button now names where it landed ("saved to this space ✓" vs "saved ✓") because the two places differ in whether the work survives a deploy. Verified on a **production build with no dev server**: retimed 5.6→8s, saved, cold reload came back at 8 | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/spaceStore.js` `src/services/spaceSettings.js` `src/algoVrithm/timingOverlay.js` `src/algoVrithm/useSavedTiming.js` `src/algoVrithm/AlgoVrithmExperience.jsx` `src/algoVrithm/DirectorPanel.jsx` |
| An invited collaborator had **no written path**. The only door the repo offered was `ONBOARDING.md` — install Node, install `gh`, fork, clone, two `npm install`s — which is the wrong door for someone whose entire job is to build inside a space in a browser tab. And when the invite failed, the card they landed on said what was denied and nothing about what to do | Nothing was broken; the surface simply had no words. The sign-in card said only "Sign in to continue" while an unspent invite was in hand, so nothing told the person that the grant would follow whichever account they picked. The restricted card — which is where an invitee actually ends up, because a guest session already exists and therefore `authenticated` is true and the sign-in card never renders — offered a scope list and a logout button. Also unwritten, and the first wall a collaborator hits: an invite makes you an **editor of one space**, and publishing / renaming / the public link / further invites are owner-gated (`PATCH /api/spaces/:id`), so "Set live" is not yours to press | A wiki article, `joining-a-space` in Getting started, is the browser-only path end to end — five steps from the link to the first saved edit, then plainly what an invite does and does not give you. `README.md` and `ONBOARDING.md` both now fork at the top into the two doors instead of assuming the developer one. In the app, both AuthGate cards link to it whenever an invite is being carried, and the sign-in card names the space and says the invite is spent on the account you choose. Guards in `AuthGate.test.jsx`, seen failing against the old copy. Note for anyone testing this: with guests enabled the sign-in card is the rare branch, not the common one — screenshot the restricted card | `src/wiki/wikiContent.js` `src/components/AuthGate.jsx` `src/components/AuthGate.test.jsx` `README.md` `ONBOARDING.md` |
| Of the 8 spaces on production, exactly **one** was declared anywhere. `main`, `open`, `wcc`, `azd`, `algovrithm`, `beyond-form` and `platform-recordar` had no file in any repo saying what they are supposed to be called or whether they are supposed to be public — the same undetectable condition that let three tiers of `br_id_ge` answer to three different names for months | The engine only understood a space that a repo also supplies pages for: `--all` on a space manifest with an empty `projects` list exited with "lists no projects". That excluded every space di.iiii actually owns — `main`, `open`, `wcc` and `azd` are authored in Studio, and `algovrithm`'s scene is React in `src/algoVrithm/`, so none of them has a page a manifest could ever push. `--audit` could already *read* such a declaration; nothing could apply one, and a declaration you cannot apply is a comment. The two linked repos were a second, quieter case: both had a v4 project manifest with `"live": "https://di-studio.xyz/serverXR"` pinned and no tier map, so a run that forgot its target wrote to the live site while reading like a rehearsal | Engine v6: an empty `projects` list is a **space-only declaration**, not an error. The space step (ensure + reconcile declared fields) was split out of `syncOne` as `reconcileSpace` and is what `--all` runs on its own. `spaces/<id>/di-space.space.json` now declares the five di.iiii-native spaces, `beyond_form` and `platform_recordar` got one each (and lost their prod pin, so a targetless run now errors instead of publishing), and `npm run spaces:audit` walks all of them — read-only, exit 1 on drift. The audit also had to learn what "extra" means here: `wcc` holds 11 Studio-authored projects and is supposed to, so calling them "not in the repo" read as 11 faults and pointed at deleting the show. All 8 spaces now audit clean, prod and staging agreeing on every declared field. **Nothing was renamed in the process** — `platform-recordar`'s label is still its own id, which is a live change and the space owner's call, not a sync's. Guard: a real subprocess run against a throwaway HTTP server asserts the PATCH goes out and no `/projects` request is made, confirmed failing against v5 | `scripts/space-sync.mjs` `scripts/spaces-audit.mjs` `scripts/space-sync.test.js` `spaces/README.md` `spaces/*/di-space.space.json` |
| The public repo carried the **grant calendar** — EMAP, Horizon Europe, ECHOES, Prix Ars, Venice College Immersive and FIVARS with amounts, deadlines and contact emails — plus a named warm-contacts tracker, the revenue model, the outreach email and four unsent announcements. Anyone who cloned `dob-0/di.iiii` had all of it | Nothing was misconfigured and no scan could have caught it: the repo has no tracked credentials, and `auto-push-readme.sh`'s secret scan looks for secrets. This material is not secret, it is simply **not the reader's business** — the wrong axis was being checked. `docs/promo/` had been written as ordinary documentation and then just kept sitting in a public repo, and the near-term calendar had additionally been copied into `PROGRESS.md`, so removing the folder alone would have left the deadlines behind | Moved `docs/promo/**` into a private `di.iiii-ops` (`promo/`), the `br_id_ge-ops` pattern, and scrubbed the two places that quoted it (`PROGRESS.md`'s 2026-07-19 entry, `privacy-data-inventory.md`). Cut **by kind, not by file**, and deliberately narrow: deploy docs, `ROTATE_GITHUB_APP_SECRETS.md` (a rotation procedure containing no secret), the VPS IP (a public DNS record), `/opt/di.iiii` and the `ssh dii-vps` alias all stayed — a self-hoster needs them, and moving `known-fixes.md`/`golden_rules.md` would break every agent instructed to read them. **`git rm` does not remove history**: every version is still readable in the public repo, so the stakeholder list and the calendar must be treated as already seen. Rule written down as "Working material is not documentation" in `golden_rules.md` | `docs/promo/` (removed) `PROGRESS.md` `docs/ai/privacy-data-inventory.md` `docs/ai/golden_rules.md` `di.iiii-ops/README.md` |
| Scrolling the Inspector panel with a mouse wheel silently mutated an object's Position/Rotation/Scale/Dimensions if the cursor merely passed over one of those narrow axis fields — no click, no focus, just a scroll gesture on the way past | `Vector3Control`'s `onWheel` adjusted the value on every wheel event delivered to the input, with no focus check. The newer `StudioInspector.jsx` number fields already guard this (`if (document.activeElement !== el) return`); this older, still-used control never got the same guard | Added the same focus guard as the first line of the handler — a wheel event only adjusts the value when that specific field is already focused. Guard: `Vector3Control.test.jsx`, watched failing without the guard. The 2026-08-06 trace concluded this control "cannot be eyeballed because its only render path is dead" — **that trace was wrong**: it checked only the `isLocalRootWorkspace` branch, but `SpaceSurfaceApp`'s *fall-through* still renders the legacy `App` for any space with no published project (e.g. `/open` — add `?ui=show`, the guest surface defaults to hidden UI). **Verified 2026-08-13 in a real browser** (staging, guest sandbox, `/open?ui=show`, sphere selected): wheel over an unfocused Position X field left the value untouched; after clicking into the field the same wheel stepped it. Guest edits there are sandboxed — a changed value does not persist across sessions | `src/components/inspector/Vector3Control.jsx` `src/components/inspector/Vector3Control.test.jsx` |
| An audio object's Inspector showed Autoplay/Loop as **"Off"** whenever those fields had never been explicitly set, even though the audio was actually playing back autoplaying and looping | `useObjectFactory.js` and the real renderer (`AudioObject.jsx`) both default an unset `audioAutoplay`/`audioLoop` to `true`; the Inspector's two toggle buttons read the raw field with `Boolean(x)`/`x ? 'On' : 'Off'`, which resolves `undefined` to "Off" — contradicting the default used everywhere else | Derived `audioAutoplayOn`/`audioLoopOn` once, using the same `typeof x === 'boolean' ? x : true` resolution as the renderer, and used them for both toggles and the preview element. Guard: `InspectorAudioControls.test.jsx`, watched failing without the fix. **Verified 2026-08-06 in a real browser** (staging, guest sandbox): imported a fresh audio file with nothing set, both Autoplay and Loop showed On | `src/components/inspector/InspectorAudioControls.jsx` `src/components/inspector/InspectorAudioControls.test.jsx` |
| A portal entity's "enter" click silently did nothing on its own public page, for every visitor, whenever the space/project id or slug merely **started with** the word "studio" (e.g. `studio-tour`, `studioworks`) | `PortalObject.jsx` detected "inside the Studio editor" (the one condition that disables the portal's click handler) with `window.location.pathname.includes('/studio')` — a substring match, not a path-segment match, so a public URL like `/expo/studio-tour` was misclassified as the editor | Replaced the substring check with `isStudioEditorPath()`, a regex matching only an actual `/studio` path segment (mirrors `src/studio/utils/studioRouting.js`'s reserved-segment logic). Guard: `PortalObject.isStudioEditorPath.test.js`, 3 cases, all passing; non-visual logic fix, no browser needed to confirm the regex | `src/project/viewport/PortalObject.jsx` `src/project/viewport/PortalObject.isStudioEditorPath.test.js` |
| Seven of the eight primitive-shape renderers (sphere, cone, cylinder, plane, ring, torus, capsule) could render invisible, inverted, or zero-size with no error, given a negative/zero/NaN authored dimension | `normalizeEntity` (`projectSchema.js`) only clamps `primitive.size` (a vector) — the scalar fields (radius/height/width/tube/inner/outer) pass through unvalidated, and legacy-import fallbacks (`\|\| 0.5`) don't catch negative values either. `BoxObject.jsx` already guards this exact case; the seven siblings never got the same treatment | Added `src/objectComponents/safeDimension.js` (abs + finite check + clamp to `[0.001, 100]`, mirroring `BoxObject`'s inline logic) and applied it to the relevant geometry args in all seven renderers. Guard: `safeDimension.test.js`, 5 cases. Changes what geometry actually gets built at malformed-input values — **checked 2026-08-06 in Studio** (real browser, sphere radius): the Inspector input itself rejects a negative value outright, and the sphere stayed valid at every step down to a very small clamped size, no crash or invisible/inverted geometry. The deeper "malformed authored JSON" path (import/legacy data bypassing the input's own validation) still relies on the passing unit tests, not a fresh eyeball — there's no raw scene-JSON editor in Studio's UI to reach it directly | `src/objectComponents/{Sphere,Cone,Cylinder,Plane,Ring,Torus,Capsule}Object.jsx` `src/objectComponents/safeDimension.js` `src/objectComponents/safeDimension.test.js` |
| A Socket.IO connection kept a revoked/downgraded user's live access working indefinitely — an admin removing a space from someone's scope, or downgrading editor→viewer, took effect on their HTTP requests within 60s but **never** on their already-open socket, which kept emitting/receiving scene and cursor events for the space they'd just been cut off from | `getSocketAuthState()` resolved role/scope from the signed cookie once at handshake and never re-checked the DB; every downstream handler (`ensureSpaceAccess`, `ensureProjectAvailable`, the join-space/cursor/selection/chat checks) read that frozen snapshot. `initializeSocket()` was never handed the `getFreshDbIdentity` resolver the HTTP path already uses — only `lookupTokenVersion` (logout revocation) was wired through | Passed `getFreshDbIdentity` into `initializeSocket`'s config; added `applyFreshDbIdentity()` (same override contract as the HTTP path, no-op for tokens/guests/no-DB-row subjects) and a `refreshSocketAuthState()` helper called at every access-control decision point, so an open socket now picks up a DB-side role/scope change within the same cache window HTTP requests get. Guard: 3 new cases in `socketHandlers.test.js`, watched failing without the fix; full `test:server-contracts` still 77/77 | `serverXR/src/index.js` `serverXR/src/socketHandlers.js` `serverXR/src/socketHandlers.test.js` |
| A client that raced ahead to a newer document version could have that version **regress backward** on a stale/out-of-order SSE broadcast, with no visible error — the next local edit then submitted a stale `baseVersion` and got a spurious 409, forcing an unnecessary conflict-recovery cycle | `applyRemoteOps()` unconditionally wrote `versionRef.current = version` (and dispatched it to the reducer) whenever `Number.isFinite(version)` was true, with no check that it was actually newer than the currently-tracked version. The reducer's `apply-ops`/`replace-document` cases mirrored the same gap | Computed `nextVersion` as `version > versionRef.current ? version : null` and used that instead of the raw value in both branches — unseen op content still applies, the version number just never moves backward. Guard: new case in `useProjectDocumentSync.test.jsx`, watched failing (`expected 2 to be 3`) without the fix; 12/12 in the file pass, 67 across the broader sync/schema suites | `src/project/hooks/useProjectDocumentSync.js` `src/project/hooks/useProjectDocumentSync.test.jsx` |
| Undo did not restore a node overwritten by a colliding `createNode` op (duplicate/retried op, concurrent-client race) — the overwrite was silently non-undoable, unlike the identical collision case on `createEntity`/`createEdge` | `invertSingleOp`'s `createNode` case treated any id collision as a no-op (`if (nodes.has(node.id)) break`) instead of restoring what the forward op had overwritten, the way its `createEntity`/`createEdge` siblings already do | Added the same prev-lookup-and-restore branch to `createNode`'s inverse, in both the ESM schema and its CJS mirror. Guard: new case in `projectSchema.test.js` (round-trips a collision back to the original node), watched failing without the fix; `schemaSync.test.js` confirms the ESM/CJS mirrors stayed equivalent | `src/shared/projectSchema.js` `shared/projectSchema.cjs` `src/shared/projectSchema.test.js` |
| The node palette tagged `universe.desk.3d` ("3D Desk") as "authoring only / doesn't compute or render anything yet", even though it has a real viewport body in both Beta and Raw | `authoringOnly` is documented to mean no `nodeGraphRuntime.js` case AND no viewport rendering. `universe.desk.3d` got a real viewport body (translucent boundary box + wireframe) when a prior audit finding was fixed, but the stale flag on its registry entry was never removed — the existing regression guard for this same bug class (see the `time` node row) only parses `nodeGraphRuntime.js` for `case` statements, so it's blind to viewport-only implementations | Removed the stale `authoringOnly: true` line from the `universe.desk.3d` definition. Guard: existing `BetaViewport.test.jsx` "renders a visible body for universe.desk.3d instead of null" already proves the rendering claim; metadata-only fix, no browser check needed | `src/project/nodeRegistry.js` |
| **Every video in the app was downloaded twice**, and `/wcc/main` transferred **131MB per visit** — the same 12.36MB clip fetched six times (74MB) and another 10.85MB clip twice, all `206`, all with correct `immutable` cache headers | `VideoObject.jsx` created a **second** `<video>` element whose only purpose was reading `videoWidth`/`videoHeight` for the plane's aspect ratio. Two elements per object, each issuing its own range requests. `/wcc/main`'s ten portals are all `mode: 'embed'`, so it renders ten artist projects at once; `meri-andreasyan` has three entities sharing one clip → 3 objects × 2 elements = 6 downloads. Nothing looked wrong: no error, no 4xx, and caching was already configured correctly — the requests were concurrent, so no cache entry existed yet for any of them to hit | Deleted the probe element; the texture's own video already fires `loadedmetadata` with the same dimensions. `useVideoTextureSource` now returns `size` alongside `texture`, with a `readyState >= 1` check because a cached video can reach HAVE_METADATA before the listener attaches. Then a refcounted module-level cache so objects wanting the same source share one element and texture. The cache key includes `muted`/`volume`/`loop`: an HTMLVideoElement has ONE volume, so sharing across objects that disagree would let whichever mounted last silently win for all of them — those keep separate elements. Measured against prod on identical data: **6× → 1×, 2× → 1×, 131MB → 83MB**. Guards: one element per source, sharing across three identical objects, no sharing when settings differ, and the element surviving until the last holder unmounts — all watched failing ("got 3", "got 2"). **Visible change, needs an artist's eye**: three planes of one clip are now frame-identical by construction where they could previously drift | `src/objectComponents/VideoObject.jsx` `src/objectComponents/VideoObject.test.jsx` |
| Beta's in-app Help still told a new user to "double-click to create node 0" — a workflow that was intentionally removed (double-click now always opens the node palette) | `betaGuide.js`'s copy was never updated when the node-0-creation flow was reversed (2026-07-17, see the row above it in this table); `BetaGraphSurface.jsx`'s `onDoubleClick` has wired straight to `openPalette()` with no node-0-specific branch since | Reworded the three stale "node 0" references in `GUIDE_SECTIONS` to describe what double-click actually does. Content-only fix, verifiable by reading the string values — **confirmed 2026-08-06**: checked the Help dialog's Start Here/World tabs and their All Controls panels, wording reads naturally, no "node 0" left anywhere | `src/beta/utils/betaGuide.js` (now deleted, see the Beta-retirement row below) |
| Deleting `src/beta/` (2026-08-06, Beta retired/absorbed into Raw) would have silently broken Studio's build — `docs/studio-beta-fork-map.md` had called the Studio↔Beta cross-link "navigation only — not real coupling", but `StudioGraphSurface.jsx`/`StudioWorldSurface.jsx` directly imported `BetaGraphSurface`/`BetaViewport` for Studio's read-only graph/world preview panels, unconditionally (not dev-gated) | The fork-map doc's "navigation only" claim covered `StudioHub.jsx`↔`BetaHub.jsx`'s one-line cross-links, but missed these two components importing Beta's actual canvas/viewport as their rendering engine — the kind of coupling only surfaced by trying the deletion, not by reading the doc | Repointed both to Raw's `RawGraphSurface.jsx`/`RawViewport.jsx` (drop-in compatible: same read-only prop shape, superset of optional props) and their `beta.css` import to `raw.css` (equally `.raw-*`-namespaced, zero collision with `studio.css`, same verification the original beta.css import used). Second, smaller latent bug found the same way: `SpaceSyncPanel.jsx` (shared, used by both `RawHub.jsx` and the old `BetaHub.jsx`) hardcoded `beta-hub-sync-*` class names regardless of caller — so Raw's copy was only ever styled by accident, if `beta.css` happened to already be loaded in the same SPA session via Studio. Renamed to lane-agnostic `space-sync-*` classes and moved the (previously dead, mis-prefixed `raw-hub-sync-*`) CSS rules in `raw.css` to match. Guard: full existing suite (1692 tests) plus `npm run build` catch either regression — a missing/renamed prop or class throws or renders visibly broken | `src/studio/components/StudioGraphSurface.jsx`, `src/studio/components/StudioWorldSurface.jsx`, `src/components/SpaceSyncPanel.jsx`, `src/raw/styles/raw.css` |
| br_id_ge's crew-only tech rider (project id `br-id-ge-needs`, title literally "needs dash" — equipment lists, open/settled task tracking) was public and copy-linkable in the in-app project switcher, right alongside the field/rite/landing — found by clicking the switcher on the live site, not by reading any doc (the di-bo bot's own docs already say this doc "has no place on the public surface," but nothing enforced that in the switcher itself) | `ProjectSwitcher.jsx` had no visibility concept at all — `sortProjectsForSpace` only reordered `listProjects(spaceId)`'s result, it never filtered it, so every project row the API returned (including internal/crew documents) rendered as a clickable, copy-linkable public entry | Added `SPACE_PROJECT_HIDDEN` (same per-space-keyed shape as the existing `SPACE_PROJECT_ORDER`) and filter by it before sorting — `br_id_ge: ['br-id-ge-needs']`. Hides from the switcher only; the project itself, and its direct URL, are untouched — crew still reaches it the same way. Guard: new `ProjectSwitcher.test.jsx` case, watched failing without the fix | `src/project/components/ProjectSwitcher.jsx`, `src/project/components/ProjectSwitcher.test.jsx` |
| The public `ProjectSwitcher` pill (`br_id_ge ▾` and its siblings on every other space) rendered at full contrast — solid dark bubble, opaque border — all the time, not just when in use. Flagged from a live phone screenshot as "the left corner button is ugly", and it's the open `CURRENT.md` item "`br_id_ge ▾` chip covers the letter-row": fixed top-left over a space's own content, at full contrast it visually competes with whatever renders underneath, on any space, not just br_id_ge | `pillStyle` was a single static style object — no hover/focus/open distinction, so the switcher looked identical whether idle or in use. This component sits over arbitrary per-space content it has no knowledge of, so full-time full-contrast chrome will clash with *some* space's design by construction | Split into `pillStyleIdle` (low opacity, thinner border, smaller) and `pillStyleActive` (the original full-contrast style), switched via existing `open` state plus a new `pillHover` state (hover/focus). Does not solve the underlying overlap with a space's own canvas-rendered content (that would need coordination with each space's own scene, out of this component's scope) — reduces the visual weight of the conflict instead. Guard: new `ProjectSwitcher.test.jsx` case asserting idle vs. hover/open background, watched failing without the fix | `src/project/components/ProjectSwitcher.jsx`, `src/project/components/ProjectSwitcher.test.jsx` |
| Two nodes could not be wired together on a phone at all — the Raw graph editor was read-only on touch, and nothing in the test suite noticed | The wire drag started on the output dot's `pointerdown` and the drop was an `onPointerUp` handler on the *input* dot. On touch the browser grants the pointerdown target **implicit pointer capture**, so every later event for that pointer — including `pointerup` — is retargeted to the output dot and never reaches the input dot under the finger. `handleInputPointerUp` could not fire, ever. Compounding it: the port dots were 8x8px against a 44px target, and `RawGraphSurface.test.jsx` stubbed `setPointerCapture` with `vi.fn()`, so the drag tests passed green over exactly the semantics that were broken | Drops no longer depend on the release landing on the target element. `resolveWireDrop` finds the nearest **compatible** input port within `PORT_DROP_RADIUS_PX` (36 screen px, so tolerance is constant across zoom) and the window-level `pointerup` creates the edge; the per-dot handler is gone, so there is one code path for mouse and finger. The output dot also releases implicit capture for non-mouse pointers. A `::after` pseudo-element widens the hit box to 44px horizontally without moving anything visible, capped at the 22px row pitch so adjacent ports do not overlap. Guards: a touch-drag test that releases 14px short of the port and does **not** stub `setPointerCapture`, plus a far-release test asserting no edge | `src/raw/components/RawGraphSurface.jsx` `src/raw/styles/raw.css` `src/raw/components/RawGraphSurface.test.jsx` |
| Zooming out on a phone opened the create-node palette over the graph | `.raw-graph-zoom-controls` sits *on* the graph surface, so tapping its buttons twice quickly bubbled to the surface's `onDoubleClick` and was read as a double-click on empty canvas. Tapping the button repeatedly is the only way to zoom on touch (there is no wheel), so the faster you zoomed the more reliably it fired. `shouldStartPan` already excluded these controls from panning — node creation never got the same exclusion | `handleSectionDoubleClick` returns early when `event.target.closest('.raw-graph-zoom-controls')` matches. Guards: one test that double-clicking Zoom out does not call `onDoubleClick`, and its inverse so the fix cannot be "passed" by disabling canvas creation entirely | `src/raw/components/RawGraphSurface.jsx` `src/raw/components/RawGraphSurface.test.jsx` |
| Every `env(safe-area-inset-*)` in the app resolved to 0, silently neutering Studio's existing notch handling | `src/index.html`'s viewport meta lacked `viewport-fit=cover`, which is what makes the insets resolve to real values. `src/studio/styles/studio-mobile.css` had been written against those insets and was quietly getting zero on every notched phone — the CSS was present, correct, and inert | Added `viewport-fit=cover` to the viewport meta, and safe-area padding to Raw's fixed chrome (topbar, delete FAB, zoom controls, selection scaffold). Note this makes Studio's pre-existing safe-area rules start applying for the first time | `src/index.html` `src/raw/styles/raw.css` |
| `verify:surfaces` reported ALL CLEAN for `/raw` while auditing the sign-in card instead of the editor | Two compounding blind spots. (1) `/raw` is the local node workspace and it loads **empty** — no nodes, no ports, no wires — so every graph-editor defect was outside the tool's reach even though the route was in the default page list. (2) Editor lanes sit behind `AuthGate`; with no session the script audited the gate's panel, which is small, clean and overflow-free, and reported success. A clean audit of a sign-in card is indistinguishable from a clean audit of the surface | The run now seeds the workspace's own localStorage with the all-nodes example via `addInitScript` before first paint, accepts `--token`/`VERIFY_API_TOKEN` to sign in through the context's shared cookie jar, and **detects the gate**: landing on a Sign in / Open the public view button is recorded as a problem and printed as `[AUTH-GATED]`, so a clean result can no longer be misread. Sub-32px tap findings on `/raw` went 2 -> 8 once it was actually looking at the editor | `scripts/verify-surfaces.mjs` `src/project/graph/examples/allNodesExample.js` |
| Scene edits stopped saving and **nothing said so** — the Studio status panel read "Scene stream connected" the whole time, because presence and the SSE read path were genuinely healthy. Only the write POSTs were failing. Recovered from the 2026-08-01 audit backlog (`wf_29dddb4e-571`), whose Fix phase never ran | `useLiveSync` sets `sceneFlushError` and retries every 4s, and it does reach app state through the `...liveSyncState` / `...session` spreads. It died at `useAppState`'s **explicit destructure** (it listed `sceneStreamState` and `sceneStreamError` and simply omitted `sceneFlushError`), so no context, no status item, no pixel. A grep for the identifier finds almost nothing precisely because every hop in between is a spread — the value is invisible in the source, not just on screen | Threaded `sceneFlushError` through `useAppState` → `useAppContextValues` → `EditorLayoutContainer` → `useStatusItems`, and gave it **its own row** rather than folding it into the stream row: a flush failure is not a stream failure, and the whole defect is that a connected stream masked it. Guards in `useStatusItems.test.js`: `surfaces a flush failure even while the scene stream reads as connected`, and `shows no flush row when writes are healthy` | `src/hooks/useAppState.js` `src/hooks/useAppContextValues.js` `src/components/EditorLayoutContainer.jsx` `src/hooks/useStatusItems.js` `src/hooks/useStatusItems.test.js` |
| A portal in **embed** mode rendered blank tiles where the same assets displayed fine in the host scene — only for older imported projects, which is why it read as "that project is broken" rather than as a portal bug | `EmbeddedScene` called `buildAssetMap(doc)` with no `fallbackProjectId`. That fallback is what rescues assets written without a `url` by the legacy import gap; an embedded document has no `projectMeta.id` to fall back on either, so `url` resolved to `null` and the tile drew empty. `LiveProjectScene` passes `projectId` at its own call site, so the same document looked correct everywhere except through a portal | Pass the `projectId` the component already has in scope: `buildAssetMap(doc, projectId)`, with `projectId` added to the memo deps | `src/project/viewport/PortalObject.jsx` |
| `di up` never returned on Windows — the server started, printed its URL, and the terminal then hung forever. Found only because the CI job burned its full six-hour timeout; every unit test and every Linux job was green | `spawn(..., { detached: !isWindows })` with `if (!isWindows) child.unref()`. On Linux the detach+unref pair lets the CLI exit while the server keeps running; on Windows neither happened, so the parent Node kept a handle on the child and its event loop never emptied. The artist sees a working app and a dead prompt — and `di down` is unreachable because the terminal that would run it is stuck | `detached: true` and `child.unref()` on every OS, plus `windowsHide: true` so the detached child opens no console window of its own. Guard: `timeout-minutes: 25` on the two Windows jobs, so a hang costs minutes and reads as a failure instead of a six-hour amber run | `scripts/di/runner-node.mjs` `.github/workflows/install-matrix.yml` |
| Every message the CLI printed said `di`, even on a machine where it had deliberately installed itself as `dii`. "stop it with: di down" pointed the artist at somebody else's binary | `decideCommandName()` falls back to `dii` when a foreign `di` already owns the name — that path works — but `ui.mjs` hardcoded the string `di` in the help text, the running banner, the update hint and the uninstall hint. The rename was real and invisible in every subsequent sentence | The shim exports its own basename (`DI_COMMAND=$(basename "$0")`, `%~n0` on Windows) and `ui.mjs` reads it into `CMD`, validated against `[A-Za-z0-9_.-]+` and defaulting to `di`. Verified by installing into a home where a foreign `di` exists and reading the output: banner, help and stop hint all say `dii` | `scripts/di/ui.mjs` `scripts/di/shim/di` `scripts/di/shim/di.cmd` |
| algovrithm's audio graph **grew silently all day**: nothing crackled, nothing threw, but a long-running installation accumulated WebAudio nodes every ~53s loop | ReelGlobe built its `THREE.AudioListener` (+ limiter) and 31 `PositionalAudio` panners per MOUNT, and the piece loops — a listener wires its master gain into `context.destination` at construction, and unmount only ever did `camera.remove()`, so every pass left one more listener, limiter and 31 dead panner chains permanently connected. The shared `MediaElementAudioSourceNode`s (which can never be rebuilt) also fanned out one extra connection per pass | The ears are now a page-lifetime singleton (`reelEars()` in ReelGlobe.jsx), same contract as `reelPlayers()`: built once, `camera.add/remove` per mount, volumes driven to 0 on unmount instead of tearing the chain down. A source that missed its `MediaElementAudioSourceNode` at build time (context not usable yet) is retried on each mount | `src/algoVrithm/sequences/ReelGlobe.jsx` |
| "Copy edit list" output, pasted over `sequences/index.js`, **silently dropped `veil: false`, `transform` and `travel`** — the grey dip returned on top of the portal reveal and moved rows snapped back to origin | `formatEditListSource` only emitted the fields it knew (`source`/`backdrop`/`lights`/`asset`); `formatTransformSource` existed for exactly this and was never called; `veil`/`travel` had no emitter at all. The in-place "Save to source" path was never affected (it only touches fields it can locate) | The formatter now emits `transform` (via `formatTransformSource`), `travel`, and `veil: false` — each only when the row actually carries it, per the no-invented-defaults rule. Regression tests in `editList.test.js` | `src/algoVrithm/editList.js` |
| The reel globe was the piece's phone/headset frame-killer: 31 clips, 189MB, **all decoding at once at 720×1280** while a cell is drawn ~240px wide | The compression the code pointed at for a week (`scripts/compress-reels.mjs`, written 2026-07-30) was never actually run over the folder | Ran `node scripts/compress-reels.mjs --replace` (2026-08-08): 360×640, `+faststart`, audio kept — 189MB → 81MB, pool decode 25.9MP → 7.1MP per frame. Verified 19/19 `verify:algovrithm` checks + looked at all four beats; the globe opens full with no visible loss. Run the script over anything new dropped into `assets/` | `src/algoVrithm/assets/*.mp4` |
| The Windows shim executed as gibberish — `'etlocal' is not recognized`, `'em' is not recognized`, then `di.iiii is not installed here ()` with an empty `%DI_HOME%`. Nothing in the message names the real problem, and the same file had worked in the previous CI run | `scripts/di/shim/di.cmd` had LF-only line endings. cmd.exe re-seeks the batch file by the byte length it believes each line had, so a missing CR costs it one byte per line, cumulatively — later lines start executing with their first characters eaten (`setlocal` → `etlocal`, `rem` → `em`). Adding two comment lines was enough to push a file that had worked past the threshold. A stray em dash in a comment made it worse: a batch file is read in the console code page, not UTF-8 | `.gitattributes` pins `*.cmd`/`*.bat` to `eol=crlf` so the checkout is correct on every platform, the file was converted, and the em dash replaced with ASCII. Guard: `scripts/di/shim.test.js` asserts every line of `di.cmd` ends in CRLF and the file is pure ASCII, plus the mirror rule for the sh shim (a CR in a shebang makes the kernel hunt for an interpreter with an invisible byte in its name) — watched failing with an LF-only file | `.gitattributes` `scripts/di/shim/di.cmd` `scripts/di/shim.test.js` |
| The approval gate's fail-loud net **never fired** — a gated route missing its `gateOrApply` call answered 2xx as if the net didn't exist, and no test noticed | Mounted as `router.use('/api', net)`: Express strips the mount prefix, so inside the middleware `req.path` was `/users/42` while every `GATED_ROUTES` pattern is anchored `^\/api\/…` — zero matches, `gateRequired` never set. A second bug hid behind the first: the net never evaluated `bodyTest`, so a path-only fix would have 500'd every ordinary space PATCH. And the block path called `res.end` from inside a `writeHead` that node invokes *during* `end` — recursive end, `ERR_INTERNAL_ASSERTION` | Net mounted bare (`router.use(net)`) so `req.path` is the router-relative `/api/…` under every mount target; the registry lives in `approvalGate.js` (exported, one source with `spaceRoutes.js`'s sensitive-fields list) and `find()` runs `bodyTest(req.body)`; the block intercepts `end`/`write` and only passes `writeHead` through for its own 500. Guard: `approvalGate.test.js` mounts the router exactly as production does — watched it fail against the prefixed mount | `serverXR/src/approvalGate.js` `serverXR/src/index.js` `serverXR/src/approvalGate.test.js` |
| Placing a webcam node with a live camera locked the whole Raw editor in a "Maximum update depth exceeded" loop (console floods, UI janks; mic node had the identical latent bug) | `renderViewNodeContent` passed the capture panels **inline-lambda** live-output callbacks (`onFrameChange={(id, tex) => handleLiveOutputChange(...)}`). The panels' effects list that callback in deps and their cleanup clears the port on parent state — so with an active capture each render made a fresh lambda → effect cleanup+refire → liveOutputs set→delete→set → re-render, forever. With no active texture the clear path is a no-op, which is why the loop only appeared once a camera actually granted | Hoisted stable `useCallback` wrappers (`handleFrameOutputChange`, `handleMicOutputChange`) beside `handleLiveOutputChange` and pass those. Guard: `RawEditor.test.jsx` "passes the same onFrameChange reference across re-renders" — mocks the panel, captures the prop across a liveOutputs-mutating re-render, watched failing against the inline-lambda version | `src/raw/components/RawEditor.jsx` `src/raw/components/RawEditor.test.jsx` |
| Two edits to one node within 800ms merged into one undo step and Ctrl+Z/redo destroyed one of them (move the Agent window, send the first message, undo, redo — the window position was permanently lost) | `useOpHistory`'s coalescing signature keyed `updateNode` on top-level patch keys, which for node values is ALWAYS just `values` — so a frame drag and a chatId write looked identical and coalesced; `undoOps` kept the first op's inverse while `redoOps` was replaced by the second | The signature now descends one level into `patch.values`, so only same-field bursts (drags, typing) coalesce | `src/project/hooks/useOpHistory.js` |
| A panel window dragged off-screen was lost forever — no floor on the overflow clamp, no window list, and the stranded frame persisted into the document; closing a panel was equally irreversible (nothing ever set `frame.visible` back to true) | `clampWindowFrame` swapped the two-sided clamp for a one-sided `Math.min` under `allowOverflow*`, and `onClose` wrote `visible:false` with no reopen affordance anywhere (the allNodesExample comment referenced a "Windows menu" that does not exist) | Overflow now keeps ≥72px of the window inside the viewport and never lets the header rise above y=0 (guards in `windowLayout.test.js`, old `-140` expectation replaced with reasoning); `DesktopWindow` re-clamps on resize/orientation (keyboard, rotation); entering a hidden panel node's graph card reopens its window instead of entering an empty scope | `src/raw/utils/windowLayout.js` `src/raw/utils/windowLayout.test.js` `src/raw/components/DesktopWindow.jsx` `src/raw/components/RawEditor.jsx` |
| On a phone, scrolling the node palette placed whatever node the finger first landed on; a chromeless (kiosk) scope was a dead end on touch (exit was Escape-only); breadcrumbs lost taps to the topbar scroller; iOS zoomed on every chat/palette input focus; the window resize handle sat on the Send button's corner | Palette committed on `pointerdown` (fires before scroll intent is knowable); the chromeless-scope safety argument assumed a keyboard; crumbs were ~20px inside an `overflow-x` strip; inputs were 13-14px (Safari's auto-zoom floor is 16px); `.raw-window-resizer` hit-tests above the body | Palette commits on `click` (browsers suppress click after a scroll drag); browser/hardware BACK now pops one scope level (history pushState arm + popstate); crumbs get finger-size padding ≤640px; inputs 16px ≤640px; input row right-padding clears the handle | `src/raw/components/NodePalette.jsx` `src/raw/components/RawEditor.jsx` `src/raw/styles/raw.css` |
| Every WebXR-capable browser (desktop Chrome included) was classified as a VR headset and shipped 25–43% oversized nodes to first-time users | `navigator.xr?.isSessionSupported?.('immersive-vr')` returns a **Promise** — always truthy — so the VR branch short-circuited before the mobile/tablet/desktop checks could run | UA-based headset detection (`OculusBrowser|Quest|Pico|WolvicVR`); the promise API cannot be awaited in a sync init path | `src/raw/utils/deviceDetection.js` |
| Adding or deleting any node yanked the graph canvas back to a full-graph re-fit — the exact behaviour the code's own comment says was designed out; clicking a window title bar inflated zIndex forever and pushed a real undo entry per click | `scopeKey` included `nodes.length` and `nodes[0].id`, so every create/delete missed the fit guard; the focus handler bumped zIndex unconditionally | Key is the scope id alone; the bump is skipped when already topmost | `src/raw/components/RawGraphSurface.jsx` `src/raw/components/RawEditor.jsx` |
| Chats over 200 messages silently fed the model the OLDEST 40 turns forever; a reply stream that died without a terminal event locked the composer until remount; closing the window kept burning the user's tokens to completion | `listMessages` was `ORDER BY rowid ASC LIMIT 200` so `slice(-40)` of the FIRST 200 froze the context window; the SSE client's only exits were reject or silent EOF; nothing wired the existing abort plumbing (client had no AbortController, server never watched `res` 'close') | last-N window (DESC + reverse); the parser synthesizes an error on EOF-without-terminal and the panel resets in `finally`; `res.on('close')` aborts the upstream request (both backends' `signal` paths were already written — now actually connected) | `serverXR/src/aiChatStore.js` `serverXR/src/routes/aiChatRoutes.js` `src/services/aiChatApi.js` `src/raw/components/AgentChatPanelWindow.jsx` |
| On the 5-family models the chat proxy's 4096 `max_tokens` ceiling silently truncated or fully blanked replies | Thinking is ON by default on claude-sonnet-5/opus-5 and `max_tokens` caps thinking + visible reply TOGETHER — thinking could consume the whole 4096 leaving an empty assistant turn that was happily persisted | 16k default / 64k ceiling (streaming, so no HTTP-timeout concern), `stopReason` forwarded to the panel which flags `max_tokens` truncation; plus a wall-clock deadline (socket-idle timeout alone never bounds a trickling stream) | `serverXR/src/anthropicClient.js` `serverXR/src/anthropicClient.test.js` |
| **Tagging a release published nothing.** `v0.2.1` and `v0.3.0` both ran `release.yml` to completion-with-failure and created no GitHub Release at all, so `gh release list` was empty and `releases/latest` — which the `di` installer resolves against — had nothing to return. It reads as "tagging is broken" | The runtime pack sat *after* `Stage cPanel release`, and that step was never passed `VITE_API_TOKEN`, so it threw `Missing VITE_API_TOKEN for cPanel release build` every time and skipped everything downstream. cPanel is a legacy fallback the repo moved off on 2026-07-15 (`docs/deploy/LIVE_DEPLOY.md`) — a path nobody uses was able to stop the only artifact anyone installs. Lint and tests had already passed, so the run looked like it got most of the way | Moved `Pack the di runtime` ahead of every cPanel step; made the three cPanel steps conditional on a `steps.cpanel.outputs.possible` probe for the secret (and actually pass the secret when it exists, which it never did before); set `fail_on_unmatched_files: false` so skipped legacy zips cannot fail the upload of the runtime. Guard: `scripts/di/releaseWorkflow.test.js` asserts the ordering, the tag-derived artifact name, the conditionals and the upload patterns — watched failing against the old workflow on all four counts | `.github/workflows/release.yml` `scripts/di/releaseWorkflow.test.js` |
| **Pulling a space from live erased every op the local space had.** `POST /api/sync/spaces/:id/pull` — a route an artist runs on their own machine — took the whole op-log with it, and a push did the same upstream. Nothing failed and nothing warned; the loss only surfaces later, when a catching-up client has no history to replay or the blob gc decides an asset is unreferenced | `replaceSceneAndBroadcast` called `writeOpsHistory`, which is delete-all-then-insert (`spaceStore.js:408-417`). Every whole-scene write went through it: `PUT /scene`, `restore-snapshot`, and both sync directions. The op-log is what `GET /ops?since=` replays and what `gc-space-blobs.mjs` scans to decide a blob is still referenced, so wiping it strands both at once | Append instead: `appendOpsHistory(spaceId, [resetOp], maxOpHistory, maxOpAgeMs)` — both limits were already destructured into the route module. Safe because `applySceneOps` already treats a mid-log `replaceScene` as a full reset, so replay from any earlier version still converges. `writeOpsHistory` is no longer injected into `registerSpaceRoutes` at all, so a future route that reaches for it fails loudly at the call site. Guard: `serverXR/src/syncContracts.test.js` seeds three ops, replaces the scene, and asserts all three are still replayable — watched failing on dev, where exactly one op survives | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/index.js` `serverXR/src/syncContracts.test.js` |
| **A stale whole-scene PUT silently overwrote newer work.** `POST /ops` has had an optimistic-concurrency check since it was written; `PUT /scene` — which replaces everything rather than editing part of it — had none, so the more destructive route was the less protected one | No `If-Match`/version handling existed on the route, and `replaceSceneAndBroadcast` took no expected version. The counter only ever incremented, so a caller could not express "I based this on version N" even if it wanted to | Optional `If-Match: "<n>"` / `?baseVersion=<n>`, answered with the same `409 { latestVersion, pendingOps }` shape `POST /ops` already returns — so `useLiveSync` and `useServerPublishing` needed no new client code. A malformed precondition is a **400, never a silent unconditional write**. `config.sceneReplace.requirePrecondition` (off online, ON for `di` installs via `~/.di/di.env`) makes it mandatory where no legacy callers exist, and unconditional replaces that shrink a scene log a warning naming the object-count delta. Guards: stale-precondition 409, malformed 400, and 428-when-required, all in `syncContracts.test.js` | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/config.js` `scripts/di/bootstrap.mjs` |
| **Assets present upstream disappeared from the manifest after a sync round trip.** Pull-then-push permanently deleted `scene.assets[]` entries that the local machine had simply never downloaded, and baked the pulling host's origin into the scene | `GET /scene` returns a rendering, not the stored scene: `filterAvailableSceneAssets` drops every manifest entry whose file is missing on THIS disk, and `hydrateSceneAssetManifest` rewrites every `asset.url` to the serving host. Sync read that filtered, rewritten response and wrote it to the other instance — so the reader's local gaps became the writer's permanent deletions | `GET /scene?verbatim=1` returns exactly what is stored, plus `missingAssetIds` listing what the normal read would have dropped and `assetsBaseUrl` reported beside the scene rather than inside it. Sync uses it in both directions, and **refuses entirely against a peer that does not return `missingAssetIds`** — an older server ignores the query and answers with its filtered rendering, which is indistinguishable except for that key. Guards: byte-identical verbatim round trip with a missing asset, and a refusal test for the old-peer case | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/routes/syncRoutes.js` `serverXR/src/routes/syncRoutes.test.js` |
| **The sync row said "in sync" for two entirely different scenes**, and a lost update reported itself as a network error | `SpaceSyncPanel` compared `local.objects` to `live.objects` — equal counts meant "in sync", so any two three-object scenes matched. Separately, `syncRoutes` push flattened a remote `409` into `502 "Live server returned 409"`, i.e. the one status that means "someone else's work would have been overwritten" was displayed as a bad connection | The panel shows both sides (`local v41 · 3 obj   live v13 · 9 obj`) and claims nothing — version counters are per-install and not comparable, so `/status` returns `relation: 'unknown'` deliberately. A 409 is passed through as 409 and rendered as "live changed while you looked — nothing was published". Found while looking at the result on a phone: the longer message truncated to `local v0 · 0 …`, hiding the live side entirely, so the row now wraps below 560px with each side unbreakable. Guards: `SpaceSyncPanel.test.jsx` (watched failing — the old code really did print "in sync · 3 objects" for v41 vs v13) and a 409-passthrough case in `syncRoutes.test.js` | `src/components/SpaceSyncPanel.jsx` `src/components/SpaceSyncPanel.test.jsx` `src/raw/styles/raw.css` `serverXR/src/routes/syncRoutes.js` |
| **Sync wrote artist data inside the directory `di update` deletes.** On a `di` install, every pull left a copy of the scene in `~/.di/versions/<version>/spaces/<id>/scene.json` | `syncRoutes` wrote a git-tracking copy to `path.resolve(config.directories.root, '..')/spaces/…`. `directories.root` is `serverXR/`, which in a local install resolves inside the immutable version directory that `di update` and `pruneVersions` remove — violating the one structural promise the CLI makes, that `data/` is the artist's and `versions/` is disposable | Removed. `scripts/space-pull.mjs` already writes the git-tracked copy for the dev workflow that wanted it, so nothing was lost | `serverXR/src/routes/syncRoutes.js` |
| A published page embedded in another page arrived as an opaque rectangle pasted across its host — on br_id_ge's ending it covered the shared body and the visitor's own drawn mark, and its bottom edge cut a hard line through the closing words | `PublicProjectViewer` paints `#05070a` on the `<main>` shell AND on both code-view iframes, unconditionally. The embedded page cannot undo that from inside: published pages are sandboxed WITHOUT `allow-same-origin`, so the host's `contentDocument` restyle always threw, and `field.html` was forced to fall back to painting opaque paper of its own (its CSS says so, and names the fix it was waiting for). Nothing was broken in the host — the viewer simply had no idea it was a window | `?embed=1` on the published viewer: transparent shell, transparent iframes, no Made-with badge, no Walk/Fly, and no black full-bleed LoadingScreen flash. `br_id_ge` had already been passing `&embed=1` for months against a viewer that ignored it. Guards: three cases in `PublicProjectViewer.test.jsx` (scene page, code page, and the un-embedded default keeping its dark shell + badge) — watched all three fail against the unconditional background | `src/project/components/PublicProjectViewer.jsx` `src/project/components/PublicProjectViewer.test.jsx` |
| A published page could not tell which tier it was running on, so every page linking to a sibling hardcoded `https://di-studio.xyz` — staging's rite embedded PRODUCTION's field and read production's crossings. The one surface meant to rehearse the ending was the one surface that could never rehearse it | Same root cause as the missing `diiPageQuery`: a published page is a **srcdoc** iframe with no URL of its own, so `location.origin` is an opaque `"null"` and `location.hostname` is empty. The existing `location.hostname.endsWith('di-studio.xyz')` check in `field.html` therefore always fell through to its literal fallback — it had never once taken the relative branch | `window.diiPageOrigin` is handed down by the same bootstrap that already hands down the query. `br_id_ge`'s `fieldHref()` and `field.html`'s API base read it, keeping their literals as the fallback for an older viewer or a file opened off disk. Guards: two cases in `presentationPreviewDocument.test.js` | `src/utils/presentationPreviewDocument.js` `src/project/components/PublicProjectViewer.jsx` `src/utils/presentationPreviewDocument.test.js` |
| A panel that fires a request and reports the answer through a parent callback could sit on "Asking…" for ever, having actually received a perfectly good reply. Found in the real editor while looking at the first keeper node; every unit test of the component passed | The unmount effect that aborts the in-flight request listed the parent's callback in its dependency list. `RawEditor` passes that callback as an inline arrow, so its identity changes on **every** parent render — each render therefore ran the effect's cleanup, aborted the live request, and the `if (signal.aborted) return` guard returned *before* setting any status. Nothing tore down, nothing errored, and the network call succeeded: the stub model box logged the request and answered it. Unit tests could not see it because they render the component alone, with a stable callback and no parent that re-renders | Callback held in a ref (`onReplyChangeRef`, refreshed in an effect), unmount cleanup keyed to `[node.id]` alone. The pattern generalises: any panel using the `liveOutputs` side channel is exposed to this, because `renderViewNodeContent` builds every one of those handlers inline. Guard: `still delivers the answer when the parent re-renders mid-request` re-renders the parent twice with a fresh callback identity while the request is pending — watched failing (5s timeout, reply never arrives) with the dependency restored | `src/raw/components/KeeperPanelWindow.jsx` `src/raw/components/KeeperPanelWindow.test.jsx` |
| The keeper panel's content overflowed its own window by exactly its padding — 398px of content in a 368px window — clipping the reply's right edge on a phone | `raw.css` sets `box-sizing` per rule rather than globally (three occurrences in ~3.6k lines), so a new `.raw-keeper-panel { width: 100%; padding: 12px }` inherited `content-box` and measured 100% **plus** 24px. Invisible on desktop, where the window is wide enough to absorb it; only a phone viewport makes it clip. The sibling capture panels avoid it by having no horizontal padding, not by being correct | `box-sizing: border-box` on the panel and its inputs. Verified by measuring in a real phone context (390×844, DPR 3): `scrollWidth` 398 → 366 against `clientWidth` 366, and the panel's right edge moved inside the window's | `src/raw/styles/raw.css` |
| `?embed=1` still rendered an embedded page as a **black box** when opened on its own — shipped that way to production. The rite's ending only looked right because br_id_ge injects the same override into the frame itself, the very workaround the mode was added to retire | The mode cleared `<main>` and both code-view iframes, but `html`, `body` and `#root` carry `background: var(--di-black)` from `base.css` and kept painting underneath. Measured on both tiers after shipping: `main` transparent, document black. A second, subtler trap sat on the fix — `PublicProjectViewer` declares `const document = state.document`, which shadows the global for the whole function scope, so a bare `document.documentElement` in an effect resolves to a project document and silently does nothing | `html.dii-embed, html.dii-embed body, html.dii-embed #root { background: transparent }` in `base.css`, toggled by the viewer via **`window.document`** (deliberately, with the shadowing named in a comment) and removed on unmount so routing away takes the ground back. Guard: `PublicProjectViewer.test.jsx` asserts the class is added in embed mode and gone after unmount — watched failing against the shipped version | `src/styles/base.css` `src/project/components/PublicProjectViewer.jsx` `src/project/components/PublicProjectViewer.test.jsx` |
| Every link preview on production broke — Telegram, Slack, WhatsApp and every other crawler got a **404 and no card at all** for every di.iiii URL. Strictly worse than the platform tile the change had been written to replace, and invisible to anyone browsing normally, because only crawler user-agents take that path | The og route was declared `router.get('/serverXR/og/*splat', …)` inside a router that `index.js` already mounts with `app.use(normalizedTarget, router)` where the target IS `/serverXR`. So the server really answered `/serverXR/serverXR/og/…` (confirmed: that path returns 200 on prod), while the nginx crawler rule proxies to `/serverXR/og$uri`. Every sibling route in that router is declared `/api/…` for exactly this reason — this one carried the prefix twice. The existing tests could not catch it: all five exercise `ogHtml()`, the pure string builder, and never mount the router at all, so the path was never once requested | Route declared `/og/*splat`, relative to the mount. Guards: three cases that build a real express app, `app.use('/serverXR', router)` exactly as `index.js` does, and hit it over a live socket — the path nginx proxies to returns 200 with the space's title, the double-prefixed path returns 404, and an unknown space still gets a card rather than a 404. Watched all three fail against the shipped declaration and pass against the fix | `serverXR/src/routes/ogRoutes.js` `serverXR/src/routes/ogRoutes.test.js` |
| `di up` on a machine with no network hung the terminal after the app was already running and printed — and did it on **every single start**, which is precisely the opposite of what the function's own doc-comment promises ("remembers when it last looked so an offline machine is not retrying on every start") | Two halves, both in the once-a-day update notice. `install.mjs`'s `latestRelease()` fetched `api.github.com` with **no timeout at all**, while every other network probe in the CLI is bounded by `probe.mjs`'s 3s `NET_TIMEOUT_MS` — so a captive portal, the normal state of venue wifi, held it for the OS TCP timeout. And `cli.mjs` wrote `lastUpdateCheck` **after** the await, so a check that failed never recorded, and the machine that cannot reach the network was exactly the machine that retried every time. The `catch` swallowed the error, so nothing ever surfaced | `writeState` moved **before** the request — "we looked" is the fact worth remembering; whether anyone answered is not — and `latestRelease({ timeoutMs })`, bounded only on the automatic path. `di update` keeps its unbounded call deliberately: that request is the one the artist just asked for. Guards: four cases in `updateCheck.test.js`, two of them reading the source, because the ordering is the fix and is not observable from outside the module. The wiki's "nothing phones home" was false and now says what actually happens | `scripts/di/cli.mjs` `scripts/di/install.mjs` `scripts/di/updateCheck.test.js` `src/wiki/wikiContent.js` |
| A local install (`di up`) kept telling its owner to sign in: the landing said "Sign in only to edit" and "3 free spaces — sign in and create", on a machine with auth off where every space is already theirs | `DI_LOCAL` existed only server-side (agent board, aiChat gates) — no payload the client receives carried it, so the browser had no way to know it was on a local install and rendered hosted-product copy unconditionally | One boolean surfaced, no new "mode": `local: DI_LOCAL === '1'` on `/api/auth/session` AND on `/api/config` (the landing already fetches config — it must never mint a guest session just to learn where it runs). Landing swaps the two hosted sentences for local-truthful ones when `local && !requireAuth`. Guards: `httpContracts.test.js` boots with/without `DI_LOCAL=1` and asserts both payloads; `LandingPage.test.jsx` asserts the copy switch both ways | `serverXR/src/index.js` `serverXR/src/routes/configRoutes.js` `src/hooks/useAuthSession.js` `src/landing/LandingPage.jsx` |
| The access-restricted card was a dead end wearing a stack trace: "your session isn't scoped to 'br_id_gr'. Allowed: open, sandbox-guestfa58…" — raw ids as prose, not one way onward. The user hit it himself by mistyping a space name | The card printed `spaces.join(', ')` and offered nothing but the account button. And it could not tell a private space from a nonexistent one, so a typo got scope language about a space that was never real — even though `GET /api/spaces/:id` already 404s for it | The card offers doors: "Open Space" and "Your private sandbox" buttons (session's `openSpaceId`/`sandboxSpaceId`, never raw ids) plus the existing `ProviderSignInButtons`. `useSpacePublicFlag` now reports `exists` (404 ⇒ false, other errors fail safe to true) and a mistyped address gets "Nothing lives at …" with the same doors. Guards: four cases in `AuthGate.test.jsx` (no raw ids, doors navigate, sign-in present, unknown-space variant) plus a `requireAuth:false` fixture asserting the card is unreachable on a local install — both fixtures used to hardcode `requireAuth: true`, so the local short-circuit was never asserted at all | `src/components/AuthGate.jsx` `src/hooks/useSpacePublicFlag.js` `src/components/AuthGate.test.jsx` |
| Typing `/raw` into the address bar walled every guest: the bare lane route defaults to the restricted `main` space, where a guest session has no scope — while the landing's own Raw links had been carefully pointed at `open` for exactly this reason | `getRawLocationState` resolves a bare `/raw` to `DEFAULT_RAW_SPACE_ID` (`main`) and RootApp fed that straight into AuthGate; the session's `openSpaceId` was sitting in the auth payload the whole time. In-app links dodge it; typed URLs took the wall | Routing marks a defaulted (not URL-named) space with `isDefaultSpace`, and `LaneDefaultSpace` bends only those toward a space the session can actually enter (`openSpaceId`) — a URL that names its space is a deliberate address and keeps its (now door-bearing) wall. Unrestricted sessions and local installs pass through unchanged. Guards: `rawRouting.test.js` (flag on defaulted routes only) + `LaneDefaultSpace.test.jsx` (scoped-elsewhere → open; scoped-in / unrestricted → default; waits for the session) | `src/raw/utils/rawRouting.js` `src/studio/utils/studioRouting.js` `src/components/LaneDefaultSpace.jsx` `src/RootApp.jsx` |
| A returning guest's cookie promised 30 days while their sandbox was swept after 7 idle — day 10 arrived with a valid session scoped to a room that had been emptied, and guest snapshots are never revived | `GUEST_SESSION_TTL_MS` was a literal 30 days; the sweep reads `config.sandboxTtlMs` (7 days). Two constants, one contract, no link between them | The guest session TTL *is* `config.sandboxTtlMs` now — one env (`SANDBOX_TTL_MS`) moves both. The wiki's "about 30 days" guest claim corrected to the week it really is. Guard: contract test asserts a minted guest session's `expiresAt` lands within an hour of the sandbox TTL | `serverXR/src/index.js` `src/wiki/wikiContent.js` `serverXR/src/httpContracts.test.js` |
| A laptop with Docker Desktop merely open installed di.iiii in docker mode — the one mode with no `DI_LOCAL`, a non-loopback `remoteAddress`, and no reachable `claude` binary, so the agent board and local Claude chat 404 while the wiki promises them | `decideMode` preferred docker whenever the daemon ran and images were pullable; node was the fallback. Docker running is a fact about the machine, not a choice about di.iiii | Node wins whenever viable; docker is chosen only by explicit `--docker`/`DI_MODE=docker` or when node isn't viable. Install-time only — the recorded mode of an existing install never flips (`readState(home).mode`). **Behavior change, flagged in the PR.** Guards: two new `decideMode` cases (node beats running docker; downloadable node beats running docker) | `scripts/di/detect.mjs` `scripts/di/detect.test.js` `docs/deploy/DI_CLI.md` |
| The CI job named "works with the network unplugged" could not fail for the thing it names: it asserted `curl /main \| grep id="root"`, and the SPA shell answers that for ANY state — including a misconfigured install showing an auth wall | The server serves the SPA for every route; asserting the shell asserts only that the process is up. The offline claim ("the artist owns the door") was never executed | The job now also asserts `/serverXR/api/auth/session` answers `"requireAuth":false` — the sentence that actually distinguishes a usable local install from a walled one. The shell check stays as the liveness half | `.github/workflows/install-matrix.yml` |
| `di up` in docker mode put the artist's work in an anonymous volume while `di where` pointed at the named `di-local_data` volume — a backup or migration following `di where` would copy the wrong (empty) place | `runner-docker` passed a single `-f docker-compose.di.yml`, but that file is only an override: `!reset`/`!override` tags and **no volume definitions**. Composed alone the named volume never exists. CI never caught it because CI has always (correctly) passed both files — and the tarball didn't even contain the base file | `composeFiles()` returns base-then-override exactly as CI runs it; `pack-runtime` stages `docker-compose.yml` into the tarball alongside the override (base's extra services are profile-gated and don't start). Guards: `runnerDocker.test.js` asserts the pairing against a real fixture install and that the packer names both files | `scripts/di/runner-docker.mjs` `scripts/di/paths.mjs` `scripts/pack-runtime.mjs` `scripts/di/runnerDocker.test.js` |
| A stopped local server (di down, closed laptop) got a permanent request drip from every open tab — EventSource retrying every ~3s forever on two streams, socket.io capping its backoff at 5s forever on a third | Scene and project SSE relied on the browser's uncapped auto-retry ("allow browser to retry automatically"); `useProjectPresence` used socket.io defaults. The 15s cooldown idiom existed (`apiClient`'s `SERVER_UNAVAILABLE_COOLDOWN_MS`, `useSpaceSocket`) and these three never adopted it | After 3 straight errors the EventSource services close and sit out a 15s cooldown, then reconnect with the same args; one success resets the count; explicit disconnect cancels the pending retry. Presence passes `reconnectionDelayMax: 15000`. Guards: cooldown/reset/cancel cases in both service tests + an `io()` options assertion | `src/services/sceneSyncService.js` `src/project/services/projectSyncService.js` `src/project/hooks/useProjectPresence.js` `src/services/sceneSyncService.test.js` `src/project/services/projectSyncService.test.js` `src/project/hooks/useProjectPresence.test.jsx` |
| On a local install with no network, a scene containing a portal **never mounted the portal at all** — not just its label: drei's `<Text>` suspends on a font preload that goes to a CDN, the fetch never settles into the suspend cache, and everything sharing that Suspense boundary stays unmounted forever. Same class for LiveProjectScene's billboard text and the VR hint. (Plain text entities were safe all along — the shared viewer draws those onto a 2D canvas.) XR sessions also fired controller-model fetches at a CDN that isn't there | drei `<Text>` without a `font` prop routes through troika's unicode-font-resolver, which fetches its index JSONs AND the actual .woff files from cdn.jsdelivr.net at render time; `@pmndrs/xr`'s `DefaultAssetBasePath` is jsdelivr too. Verified with EYES in a hard-offline browser (dead proxy, loopback only — route interception cannot see troika's worker fetches): pre-fix the portal's torus and label are simply absent and a jsdelivr request is attempted; post-fix both render and the only requests are loopback | A static-weight Inter woff (32 KB, instanced from the already-vendored variable woff2) ships in `public/fonts/` and every drei `<Text>` names it (`TROIKA_FONT_URL`); non-latin still falls back to the resolver when a network exists. XR controller/hand models switch off (`model: false`) only when the session says `local` — pointers and teleport keep rendering; hosted keeps its CDN models. Wiki claim now true and says so precisely | `public/fonts/inter-regular.woff` `src/project/viewport/troikaFont.js` `src/components/LiveProjectScene.jsx` `src/project/viewport/PortalObject.jsx` `src/hooks/useXrAr.js` `src/services/apiClient.js` |
| Everything a published page saved evaporated on every load — even after the owner opted into deviceAccess and the frame demonstrably had a real origin (`self.origin` = the tier). br_id_ge's rite wrote its crossing hand-off (`br_id_ge_last_crossing`) into the void for weeks: the field's seat was never claimable from storage (only via `?me=`), and the room's tunnel door could never see a proof. Invisible in every unit test and every code read: `getItem`/`setItem` succeed, `location.origin` reads `"null"` for ALL srcdoc pages (real origin or not), and the empty read looks like "the visitor never crossed" | The preview bootstrap (`presentationPreviewDocument.js`) called `installStorageShim` **unconditionally** — replacing `window.localStorage`/`sessionStorage` with an in-memory Map in every published page, deviceAccess or not. Written for opaque frames where touching storage throws; it kept running after `allow-same-origin` gave opted-in frames the real thing, and shadowed it. Diagnosed only by cross-writes in a live browser: parent's key invisible to frame, frame's key invisible to parent, and `Object.keys(frame.localStorage)` returning method names instead of stored keys | Feature-detect by WRITING (setItem+removeItem of a probe key — some policies expose a Storage that throws only on write) and shim only where native storage actually refuses; opaque frames keep the crash-guard unchanged. Guards: two cases in `presentationPreviewDocument.test.js` execute the real bootstrap against a working native storage (must remain untouched) and a throwing one (must get the shim) — the first watched failing against the unconditional call | `src/utils/presentationPreviewDocument.js` `src/utils/presentationPreviewDocument.test.js` || A brand-new user who signed in with GitHub/Google before ever playing as a guest got zero onboarding — dropped on an empty Spaces page with a 0/3 counter and no coach marks, while a guest got the full touch→add→share sequence | `shouldShowStudioCoach` gated on `authType !== 'guest'`, so the resolved `'session'` type was excluded along with the genuinely-unwanted unresolved (null) state; the test suite asserted the buggy contract as intended behavior | Gate is now `if (!authType) return false` — any resolved identity is coached once (the shared `STUDIO_COACH_DONE_KEY` still makes it once-per-browser); guards: `studioGuide.test.js` (shows-once-for-session case) and `StudioCoachMarks.test.jsx` (session renders step 1, unresolved renders nothing) | `src/studio/utils/studioGuide.js` `src/studio/utils/studioGuide.test.js` `src/studio/components/StudioCoachMarks.test.jsx` |
| Every CSS rule after `.raw-agent-run-panel-tail` in raw.css — the whole timeline-panel stylesheet, the director-window overrides, and the zen wordmark — silently applied only INSIDE an agent-run panel; the timeline panel shipped unstyled and nobody noticed | The rule at raw.css:3890 never closed its brace. CSS nesting is real syntax now, so an unclosed block doesn't error — every later top-level rule becomes a nested descendant rule of the broken one, valid and wrong | Closing brace restored; guard: `src/raw/styles/cssBraceBalance.test.js` asserts every stylesheet in src/raw/styles closes each block it opens, naming the file:line of the first imbalance | `src/raw/styles/raw.css` `src/raw/styles/cssBraceBalance.test.js` |
| On a phone, the seeded starter desk covered its own graph: the World and welcome windows stacked from the top edge and between them filled a 390×844 screen, so every card sat behind them — including the Studio card that the welcome text tells the visitor to tap. Found by looking at the real viewport at DPR 3; passed every unit test and every desktop screenshot | The narrow branch of `buildStarterWorkspaceDocument` stacked the second window directly below the first. The graph surface CENTRES the card cluster vertically in the visible area, so the middle band is exactly where cards land — and exactly what the two stacked windows occupied | Windows now sit against the two edges (World at the top, welcome pinned to the bottom, its height derived from the band rather than picked) leaving a clear middle band; phone copy shortened and `.raw-text-panel-content` drops to 17px under 640px so the last line — the one naming the way in — is not below the fold. Guard: `starterWorkspace.test.js` asserts no visible seeded window overlaps the middle band of a 390×844 viewport | `src/project/graph/examples/starterWorkspace.js` `src/project/graph/examples/starterWorkspace.test.js` `src/raw/styles/raw.css` |
| A floating window and a graph node card were the SAME rectangle — measured in the browser: identical `1px rgba(77,249,255,.3)` border, identical `border-radius: 0`, no shadow on either, fills 1.6% apart (`#000` vs `#0a0a0a`) — so nothing showed which lay on the canvas and which floated above it. On one screen 23 elements wore that border, 326 were square, **0 cast any shadow**; raw.css declared it in 67 rules | Cyan was the border of everything (windows, cards, buttons, palette, HUD, topbar), so it distinguished nothing. Adding a shadow does NOT fix it: black shadow on a black canvas is invisible — rendered and compared before ruling it out | One job per colour: **neutral** = furniture (window/palette/topbar/HUD/resting controls), **family hue** = what a node is (card edge + icon via `--card-family`, and the hosting window's top stripe via `--window-accent`), **cyan** = interaction only (selected/hover/focus), **port hue** = data type (untouched). Depth comes from a lifted fill + 1px top highlight, not shade. Guard: `src/raw/styles/colourRoles.test.js` asserts each sentence of that rule and was watched failing against the old CSS | `src/raw/styles/raw.css` `src/raw/styles/colourRoles.test.js` `src/raw/components/DesktopWindow.jsx` `src/raw/components/RawEditor.jsx` `src/raw/components/RawGraphSurface.jsx` |
| A window's title was 18.72px browser-default bold while the same node's card label was 12px — the window shouted, the card whispered, and the size was never a decision | `.raw-window-header h3` set exactly one property: `margin: 0`. Everything else fell through to the UA stylesheet's `h3` | Authored at 13px/700 to sit with the lane's other labels. Also: the header's four buttons were pulled OUT of the shared bordered-button rule (they repeated the frame's own edge, which is why a window read as a card containing four cards), given `font-family: inherit` (they were rendering in Arial — no font was declared), a 44px floor, and a red Close hover | `src/raw/styles/raw.css` `src/raw/styles/colourRoles.test.js` |
| The node-enter chevron — the ONLY way into a container node on a phone — was a 32px target, and window header buttons were 37px, both under the 44px one-finger floor stated in a comment in the same stylesheet | The coarse-pointer branch set `min-width/height: 32px`; the window buttons inherited a shared rule with no min size at all | 44px both, with the chevron's negative margins retuned so the card header stays exactly 44px tall. Guard: `colourRoles.test.js` asserts both floors, including inside the `@media (hover: none)` branch | `src/raw/styles/raw.css` `src/raw/styles/colourRoles.test.js` |
| `is-pinned` and `is-minimized` were applied to every window's markup and had ZERO rules in the stylesheet, so a pinned window was indistinguishable from an unpinned one and a minimized one kept a divider under a body that no longer existed | Classes added when the behaviour shipped; the CSS half was never written and nothing failed | Pinned windows label themselves in the header; minimized windows drop the dangling divider. Guard: `colourRoles.test.js` asserts both selectors exist | `src/raw/styles/raw.css` `src/raw/styles/colourRoles.test.js` |
| `.raw-card { background: var(--di-card) }` — a token defined nowhere in the codebase, so the declaration was invalid at computed-value time and the background silently never applied. Nothing errored; it just did nothing | The token was never authored into `base.css`, and the rule had no fallback. The class itself is dead (0 references in any component) | The dead `.raw-card` rules deleted. Guard: `colourRoles.test.js` fails on any `var(--token)` used WITHOUT a fallback that is not defined in base.css, raw.css, or set inline by a component — the fallback-less form is the defect class, `var(--x, 1)` is a knob | `src/raw/styles/raw.css` `src/raw/styles/colourRoles.test.js` |
| On a phone the starter desk broke twice in one day, and the second break was caused by the first fix meeting someone else's: with a window docked to each edge, `getGraphEdgeInsets` saw a pinched corridor and the fit zoomed the graph to 34% — below `CARD_CONTROL_MIN_ZOOM`, where the enter chevron is not rendered at all. "tap Studio ›" pointed at something untappable again, by the opposite route from the first bug (windows covering the cards) | Two correct fixes composing wrongly. Stacking the windows instead is not enough either: `getGraphEdgeInsets` files a full-width window under whichever edge it sits NEARER, so a second window whose distance-to-bottom beats its distance-to-top is filed as bottom-docked, the two insets claim more than the surface, and the fit discards them entirely — cards land back over the windows | Phone window heights are DERIVED from the classifier's own rule (`2y + h ≤ vh`, i.e. nearer the top than the bottom) rather than picked, so both windows read as one top inset and the fit hands the whole lower band to the graph — 66% zoom, four readable cards, every chevron rendered. Guards: two cases in `starterWorkspace.test.js` assert `2y + h ≤ vh` for every visible seeded window at 390×844 and at 375×667 | `src/project/graph/examples/starterWorkspace.js` `src/project/graph/examples/starterWorkspace.test.js` |
| A new floating panel in Raw was invisible exactly where it was most wanted: opened while standing inside a Scene, it rendered BEHIND the room's `<canvas>`. The control that opens it is in the marker strip (z-index 1400) and stayed perfectly clickable, so pressing it looked like a button that does nothing | Entering a `universe.world` sets `isWorldFullscreen`, and `.raw-world-fullscreen` is `position: fixed; inset: 0; z-index: 1200`. A `DesktopWindow` takes its z-index from its frame, and every existing one uses **20** — correct for a panel that fullscreen is meant to cover, wrong for chrome that explains the thing you are standing in. Not visible in any screenshot: the window paints, it is simply underneath | The frame opens at a named `RAW_ANATOMY_Z = 1250`, between the fullscreen room (1200) and the way out of it (marker 1400), with the whole stack written down beside the constant. Found and confirmed by hit-testing the middle of the sheet with `document.elementFromPoint` — it returned `CANVAS` before and `raw-anatomy-port` after; a DOM-presence assertion was green throughout | `src/raw/utils/windowLayout.js` `src/raw/components/RawEditor.jsx` |
| A new button read `what is it made of` on screen and answered to a completely different name — anyone driving by voice, and every accessible-name query in the test suite, had to say "What A container is made of" instead | `aria-label` REPLACES the accessible name rather than adding to it, so the visible words were not in the name at all (WCAG 2.5.3, Label in Name). Found by accident: a Playwright locator written against the visible text matched nothing, which is precisely the failure a voice user hits | The label leads with the visible string verbatim and appends the node: `what is it made of — A container`. The rule generalises — if a control's visible text is not a prefix of its `aria-label`, the label is wrong | `src/raw/components/RawEditor.jsx` |
| Two docked panels landed on top of each other on both surfaces at once: on a desktop the new sheet opened under the selection inspector (docked right), and on a phone it finished 3px inside the selection sheet (docked bottom at `38dvh`). Entering a node SELECTS it, so the inspector is always up when this opens — the collision was the default case, not an edge one | Three separate misreadings of geometry that already existed. The inspector's dock was never consulted; the phone band was computed against `floor(vh * 0.62)` while `dvh` rounds the other way; and a window renders ~3px taller than the height it is handed (two 1px edges plus the 2px family stripe), which no arithmetic accounted for | Dock LEFT on desktop; on a phone take a top band ending a named `RAW_WINDOW_CHROME_ALLOWANCE` (12px) above the sheet's own top, computed with `ceil` so rounding runs the safe way. Both measured in a real browser at 1440×900 and 390×664 rather than reasoned about; the unit test asserts the clearance, not the numbers, so it survives the sheet changing size | `src/raw/utils/windowLayout.js` `src/raw/utils/windowLayout.test.js` |
| The public repo's front page sent every reader to a lane that no longer exists: a `/<space>/beta` row in the routes table, `src/beta/` listed under "where to work", and `http://localhost:5173/main/beta` in the local-dev route list. Beta was deleted 2026-08-06 and the directory went with it, so a first-time contributor's first three clicks were all at nothing | A README describes structure but is wired to none of it. Deleting `src/beta/` could not fail a build, a test, or a link check, because nothing anywhere reads the README. It had been wrong for thirteen days and would have stayed wrong indefinitely | Rewritten to name only the two editors a person can actually open (`/<space>/studio`, `/<space>/raw`), and the "Surfaces" table — a word the product had stopped using — became "Where Things Live". Guard: `copyVocabulary.test.js` reads `public-README.md` as copy, so `Beta`, `lane`, `workspace`, `entity`, `Raw` and `di.i` cannot come back into it silently | `public-README.md` `src/copyVocabulary.test.js` |
| The landing page's four-step "how it works" told a first-time visitor to "Use the Library panel" and to "Tweak with the Inspector on the right". Neither panel is on screen in the editor those steps lead to. Step 01 also left the visitor in a space with nothing open, so step 02's instruction had no object to act on | Onboarding copy written against an editor layout that has since been rebuilt. Copy that names a component is coupled to it, but nothing expresses the coupling — renaming a panel touches JSX, and prose in a different file quietly goes stale | Steps rewritten against what is actually on screen: a space holds projects, open one first; the Create window adds objects; the World window sets sky, background and lighting. Guard: `copyVocabulary.test.js` holds the landing copy to the dictionary — but it cannot prove a panel a sentence names still exists, so renaming a panel still means grepping the copy for its old name | `src/landing/LandingPage.jsx` `src/copyVocabulary.test.js` |
| Both starter page templates a creator publishes shipped with the primary call-to-action wired to `mailto:studio@example.com` — "Launch Inquiry" and "Apply to the open call". A visitor pressing the main button on a published open call composed mail to a placeholder address that reaches nobody, and nothing on the page said it was a placeholder | The templates are exported HTML string constants, published verbatim. No build step, no route and no test ever looks inside them, so a placeholder `href` is indistinguishable from a real one until a real applicant uses it. `studio@` also reads as a plausible address, which is what made it survive review | Placeholders now read `you@example.com`, which cannot be mistaken for a working address, and each template carries a visible line telling the creator to replace it before publishing. Button text says the action ("Get in touch") instead of naming a genre of email | `src/utils/presentationTemplates.js` |
| Two buttons on the node editor's hub both said `studio`. One created-or-opened the Studio container node inside the current project; the other navigated away to the Studio hub at a different route. Same word, two destinations, and nothing on either button to tell them apart | Labels written as the name of a thing rather than as the action they perform — the failure mode of every noun-only button. The two controls were added months apart, in different sections of the same file, and neither read the other | The labels say what they do: "open the Studio node" places and enters the container, "studio projects" leaves for the hub. The hub's status strings also dropped `raw`, which was telling a person the branch name of the lane they were standing in. Guard: `copyVocabulary.test.js` covers the strings; two nouns colliding is still a thing only a reader catches | `src/raw/components/RawHub.jsx` `src/copyVocabulary.test.js` |
| Every product surface that promised the node project list — Studio's "Nodes" button, admin's "Nodes" action and "Node Editor Path" — landed on `/{space}/raw`, the per-browser localStorage scratch canvas, while the actual list at `/{space}/raw/projects` had exactly one inbound link: the back button of the editor you can only reach from it | The route builder was named `buildRawHubPath` and the route constant `RAW_PAGE_HUB`, so every caller reasonably believed the address was the hub; RawApp has always rendered that page as the blank local canvas. A name that lies recruits new wrong callers forever | Renamed `buildRawHubPath`→`buildRawCanvasPath` and `RAW_PAGE_HUB`→`RAW_PAGE_CANVAS` so the name says what renders, and repointed every list-promising link at `buildRawProjectsPath`. Guards: StudioHub test pins Nodes→`/{space}/raw/projects`; PreferencesShared test pins the admin bundle | `src/raw/utils/rawRouting.js` `src/studio/components/StudioHub.jsx` `src/components/preferences/PreferencesShared.jsx` |
| `/admin`'s non-admin gate button "Go to my spaces" navigated to `/main/studio` — one restricted space's project list, where a non-admin session has no scope and gets walled again — instead of the Spaces page its copy promised | `buildStudioHubPath()` with no argument silently falls back to `DEFAULT_STUDIO_SPACE_ID` ('main'); the builder even carries a comment noting the dead branch. The same label-promises-top-level-delivers-one-room bug was fixed on the landing CTA and documented there, but the gate was never swept | The gate calls `buildStudioSpacesPath()`. Guard: `PreferencesPageGate.test.jsx` renders the gate and pins the destination to `/studio` | `src/components/PreferencesPage.jsx` `src/components/PreferencesPageGate.test.jsx` |
| Every OAuth sign-in returned the person to the landing page: signing in "to open the editor for this space" dumped them on `/`, and an `?invite=` token riding the original URL was lost with the round-trip — an invitee who had to sign in first lost the invite | The signed anti-CSRF `state` carried only a nonce and timestamp; no return path existed anywhere in the flow, so the callback could only redirect to the configured frontend origin | `getOAuthUrl` (the one builder every sign-in button uses) sends `?returnTo=` (current path + query); the start routes seal it into the signed state (`r`), and the callback redirects there — `sanitizeReturnTo` admits only same-site paths, so the callback cannot become an open redirect. Guards: `loginState.test.js` round-trips `r` and rejects off-site values | `serverXR/src/loginState.js` `serverXR/src/routes/authRoutes.js` `src/services/apiClient.js` |
