# Known Fixes

**Check here before investigating any bug.** If a symptom matches a row, use the recorded fix instead of re-investigating.
When you solve something that took >5 min to find, add a row here and update `CURRENT.md`'s Last commit line.

## Recurring bug class: silent hardcoded fallback

A per-entity value (spaceId, auth scope, asset id) gets read from a source that can be
absent/malformed, and the code substitutes a hardcoded default or trusts the caller instead of
failing loudly or resolving the real value. Four confirmed instances so far — when touching
routing, auth, or scope-resolution code, check whether the change you're making could produce a
fifth:

1. Studio direct-project-link `spaceId` silently hardcoded to `'main'` instead of falling through
   to the project's real space (`getStudioLocationState`, row further down this table).
2. OAuth sessions hardcoded `spaces: null` (meaning *unrestricted* access) instead of a real scope.
3. `spaceId === 'wcc'` string-literal special case bypassing the real `isPublic` auth check.
4. Asset IDs trusted from the client instead of being verified against the actual content hash.

Guardrails: `scripts/check-fallback-patterns.mjs` (CI-gated) greps for the literal pattern;
`serverXR/src/fallbackContracts.test.js` (planned/see `docs/ai/audit-*.md`) encodes it as HTTP-level
contract assertions.

| Symptom | Root cause | Fix | File |
|---------|-----------|-----|------|
| **CI red on a run where every test passes**: `Test Files 170 passed / Tests 908 passed` followed by `Errors 1 error` → `ReferenceError: window is not defined` at `getCurrentEventPriority` ← `refreshStatus` (`hooks/useDriveImport.js:49`), attributed to `AssetPanel.test.jsx`. Blocked the prod deploy 2026-07-28. | `useDriveImport` kicks off `getDriveStatus()` from a mount effect and calls `setStatus` when it resolves, with no unmount guard. When the surface closes (or a test file tears down) before the fetch settles, the setState runs against a dead tree — in jsdom the environment is already gone, so React's internals touch an undefined `window`. Vitest reports it as a suite-level *error* rather than a test failure, so the run exits 1 while every assertion is green. Timing-dependent: it does not reproduce reliably. | An `alive` ref set in a mount effect and cleared on unmount; both `setStatus` calls in `refreshStatus` are gated on it. General rule for this class: any `await` in a hook that setStates on resolve needs an unmount guard — look for the "all tests passed but exit 1" shape and read the `Errors` line, not the failure list. | `src/hooks/useDriveImport.js` |
| **br_id_ge co-presence dead: `wss://di-studio.xyz/serverXR/mesh` returns HTTP 404 on prod AND staging while `/serverXR/api/health` returns 200 (found 2026-07-28, mid-festival).** Every `meshHub.test.js` case was green the whole time. | The hub is only reachable through the client container's nginx, and `nginx.conf` had a WebSocket-upgrade `location` for `/serverXR/socket.io/` but none for `/serverXR/mesh`. The upgrade request fell into the generic `location /serverXR/` block, which uses the default `proxy_http_version 1.0` and so drops `Upgrade`/`Connection` (hop-by-hop headers nginx strips). The backend saw a plain `GET /serverXR/mesh`, no `upgrade` event fired, Express had no such route → 404. The VPS migration (2026-07-15) rebuilt this config and carried over only the socket.io rule; on cPanel the upgrade passed generically through Apache config that lived outside the repo. | Added a `location /serverXR/mesh` block mirroring the socket.io one (`proxy_http_version 1.1`, `Upgrade`, `Connection "upgrade"`, `proxy_read_timeout 86400s`). `proxy_pass` must keep the full `/serverXR/mesh` prefix — `meshHub.js` compares the upgrade URL with a strict `!==`, so any rewrite to `/mesh` silently drops the connection. Deploy rebuilds the **client** container, not `server`. Guard: `meshHub.test.js` now parses `nginx.conf` and asserts the mesh block exists, forwards the upgrade, and preserves the prefix — verified to fail against the pre-fix config. | `nginx.conf` `serverXR/src/meshHub.test.js` |
| Guest work silently lost at sign-in: OAuth/token login replaces the guest cookie, orphaning the guest sandbox until the 7-day sweep eats it (the Share window promised "sign in to keep this space" but nothing carried it) | Session upgrade minted the new identity without looking at the old one — the guest cookie is still on the upgrade request, but nothing read it | `promoteGuestSandbox` runs at both upgrade moments (OAuth callback via `onSessionUpgrade`, token login in POST /api/auth/session): if the prior guest built anything, `spaceStore.moveSpace` re-homes the sandbox (row, ops, projects, directory) onto the account's sandbox id; never clobbers non-empty account sandboxes; redirect gains `&kept=1` → AuthReturnNotice says "your sandbox came with you". Guards: spaceStore moveSpace tests + httpContracts keep-the-room case | `serverXR/src/spaceStore.js` `serverXR/src/index.js` `serverXR/src/routes/authRoutes.js` `src/components/AuthReturnNotice.jsx` |
| Guests land in an empty private void, every new browser mints another `sandbox-*`, and no space is shared — the space model reads as noise (sandbox flood, unclear "where am I / what's mine") | Guest scope was sandbox-only (or one global space *instead*, never both); sandboxes derived per session cookie with nothing reusable per identity; nothing communal existed by default | Three-place model: communal `open` space ensured at boot (kind `global`, public, `canAccessSpace` grants it to every authenticated session via `setCommunalSpaceId` — no cookie re-mint needed), one deterministic sandbox per identity (`getOwnSandboxSpaceId(subject)`, accounts get it `permanent`, implicit access without cookie scope), admin list gets `sandboxSummary` + `POST /api/admin/sandboxes/purge`, daily open-space scene snapshot + `POST /api/spaces/:id/restore-snapshot`. Guards: `authAccess.test.js` + three httpContracts cases | `serverXR/src/authAccess.js` `serverXR/src/index.js` `serverXR/src/routes/spaceRoutes.js` `serverXR/src/spaceStore.js` |
| Admin directory (`/admin` → Manage) flooded with dozens of identical "Guest Sandbox" spaces (26 spaces, most of them sandboxes) | Every anonymous page view called `/api/auth/session`, which eagerly provisioned a sandbox space (FS dir + DB row) at issuance — pure viewers of LIVE spaces each minted one, and they lived the full 30-day `SPACE_TTL_MS`; `GET /api/spaces` also listed every sandbox to anyone with access (admins see all) | Sandboxes now provision lazily on first real space access (`ensureGuestSandbox` in the `/api/spaces/:spaceId` middleware; the guest's own list entry is synthesized until then), are hidden from everyone else's `GET /api/spaces` (admin included, direct GET by id still works), and idle ones reap after 7 days (`SANDBOX_TTL_MS`, second prune query). Guards: httpContracts guest-sandbox case + spaceStore sandbox-TTL prune test | `serverXR/src/index.js` `serverXR/src/routes/spaceRoutes.js` `serverXR/src/spaceStore.js` `serverXR/src/config.js` |
| `open-pr` check shows a red FAIL on every task-branch push in `dob-0/di.iiii` (exit 4, `GH_TOKEN:` empty in the log) | `auto-pr.yml` is the fork-side template shipped in-repo so forks inherit it; upstream intentionally has no `UPSTREAM_PR_TOKEN` secret, so `gh pr create` dies | Job-level `if: github.repository != 'dob-0/di.iiii'` skips it upstream (guard also in the template); fork behavior unchanged | `.github/workflows/auto-pr.yml` `docs/templates/fork-auto-pr.yml` |
| Clicking an in-page `href="#id"` link inside a code-space (public viewer/preview) blanks the iframe with CORS errors from origin `null` for the shell's own bundles | srcdoc iframes inherit the parent shell's base URL, so a fragment click navigates the sandboxed iframe to the shell URL — which can't boot with an opaque origin (no `allow-same-origin`) | Preview bootstrap intercepts `a[href^="#"]` clicks (capture phase), prevents default, and `scrollIntoView`s the target element | `src/utils/presentationPreviewDocument.js` `presentationPreviewDocument.test.js` |
| Clicking Export project appears to do nothing on asset-heavy projects such as WCC | Nineteen large assets (~200 MB) downloaded sequentially with no UI feedback, then JSZip tried to recompress already-compressed media before triggering the download | Show `Exporting n/total` and `Packing %`, fetch three assets concurrently, disable duplicate clicks, keep errors visible, and use ZIP STORE mode | `studioProjectBundle.js` `StudioEditor.jsx` `StudioShell.jsx` `StudioShellPanels.jsx` |
| Studio project export contains asset metadata/URLs but not the actual files | Export serialized only the normalized document JSON | Export one `.studio.zip` with `project.json` plus every project asset under `assets/`; ZIP import re-uploads binaries using stable asset IDs, while legacy JSON import remains supported | `src/project/transfer/studioProjectBundle.js` `StudioEditor.jsx` `StudioShellPanels.jsx` |
| Studio selection lacks V1 parity: no primary/secondary bounding highlights, stale IDs survive document changes, F frames only primary, and Structure replaces multi-selection | Studio implemented selection independently with only a primary label and primary gizmo | Add orange primary/green secondary BoxHelpers, deduped/pruned selection state, visible/unlocked select-all, multi-frame, modifier-aware Structure rows, and multi-selection Inspector context | `projectStore.js` `StudioEditor.jsx` `StudioViewport.jsx` `StudioShell.jsx` `StudioShellPanels.jsx` |
| VR: left-stick strafe and right-stick turn both moved backwards on a real headset (push left → strafe right; push left → view turns right) | A prior session's "hardware verified" fix (2026-07-05) for the strafe sign was never actually re-tested and was wrong; the right-stick turn direction came from `useXRControllerLocomotion`'s own built-in default, which also ran backwards on this device's axis convention | `moveFromStick` strafe now uses standard Gamepad-API convention (`strafe: xAxis`, no negation, confirmed fixed 2026-07-06) | `src/components/xrFlyControl.js` `src/components/xrFlyControl.test.js` `src/components/LiveProjectScene.jsx` |
| VR: right-stick turn/fly reports became internally impossible (2026-07-06) — user reported "still backwards" for both possible turn signs, and separately reported fly backwards (right after a debug-only, non-functional commit) despite fly being independently confirmed correct 3 times before that | A single test round produced contradictory results that cannot both be true for consistent hardware (both signs of a 1-bit flip can't both be "wrong"; a no-op commit can't change behavior) — almost certainly rushed/confused testing across many rapid redeploys, not real hardware behavior changing | Added a live raw-axis debug readout to get an objective number instead of a direction description, then removed it per user request before it was used. Reverted BOTH signs to their best-evidenced state: `flyVertFromStick` back to `-yAxis` (3x independently confirmed), `xrTurnSpeed` back to negating the base speed (geometrically verified, matches the original VR bug report). **User confirmed correct on real headset 2026-07-07** — do not re-flip either sign without a fresh deliberate re-test | `src/components/xrFlyControl.js` `src/components/xrFlyControl.test.js` `src/components/LiveProjectScene.jsx` |
| AR/passthrough (headset in mixed-reality mode with controllers connected) had no stick-based fly — right-stick fly only applied when `isVr` was true | Vertical fly in `XrLocomotion` only added `stickVert` in the VR branch; the AR/`flyMode` branch only summed `touchVert` (handheld phone AR's on-screen buttons), so a controller connected during an AR session was ignored | Added `stickVert` to the `flyMode` branch too, alongside `touchVert` — same right-stick-Y fly as VR now works in AR/passthrough sessions that have a controller, with handheld phone AR unaffected (no controller ⇒ `stickVert` is 0) | `src/components/LiveProjectScene.jsx` |
| `A` selects all Studio entities but dragging the gizmo moves only the primary entity | Studio attached TransformControls directly to the primary entity and had no shared selection pivot | Add a centroid pivot for 2+ selected entities, preview matrix deltas across the selection, and batch-commit all transforms on release | `src/studio/components/StudioViewport.jsx` `src/studio/utils/multiTransform.js` |
| Studio gizmo hotkeys have no `X/Y/Z` axis constraint, and bare `X` deletes the selection | Studio had no axis state wired to Three.js `TransformControls`; Editor also reserved X for delete | Add shared gizmo-axis state, drive `showX/showY/showZ`, toggle repeated axis back to all, reset on G/R/S, and reserve Delete/Backspace for deletion | `StudioShell.jsx` `StudioViewportLayout.jsx` `StudioPresentationSurface.jsx` `StudioViewport.jsx` `StudioEditor.jsx` |
| Studio toolbar `G/R/S` selects a gizmo, but the matching keyboard key launches a different modal operator | The key handler conditionally called `onStartTransform` while the toolbar directly set `gizmoMode` | Make keyboard `G/R/S` use the same gizmo-mode state transition as the toolbar buttons | `src/studio/components/StudioShell.jsx` `StudioShell.test.jsx` |
| Studio floating-panel close and collapse buttons do nothing in Firefox | The button `pointerdown` bubbled into the draggable header, which prevented the default and captured the pointer before the click completed | Ignore interactive descendants in `usePanelDrag`'s drag-start handler; regression test verifies controls are not captured | `src/hooks/usePanelDrag.js` `src/hooks/usePanelDrag.test.js` |
| Socket.IO polling works locally but Firefox cannot upgrade `/serverXR/socket.io` to WebSocket through Vite | The `/serverXR` dev proxy forwarded HTTP only because WebSocket proxying was not enabled | Add `ws: true` to the Vite `/serverXR` proxy | `vite.config.js` |
| Studio lazy module fails on `localhost:5174` while HMR WebSocket is refused on `5173` | Two `npm run dev` stacks were running; `strictPort: false` let the second Vite server drift to 5174 while its direct HMR fallback still targeted configured port 5173 | Set Vite `strictPort: true`; a duplicate dev client now fails fast instead of opening a split HTTP/HMR session | `vite.config.js` |
| White screen / TDZ crash in prod | `manualChunks` missing drei peer deps → circular chunk init order | All drei peer deps in `three-vendor`: `detect-gpu`, `maath`, `camera-controls`, `@monogrid/gainmap-js`, `@react-spring/three` | `vite.config.js` |
| Infinite loading / auth never resolves | No timeout on session fetch | `AbortController` 8 000 ms timeout | `src/hooks/useAuthSession.js` |
| 100+ cascade errors when backend is 503 | `requireAuth` stays false → `AuthGate` skips error screen | Error check moved before `!requireAuth` | `src/components/AuthGate.jsx` |
| Browser can escalate role when both cookie + bearer token are sent | Auth resolution prioritized token over session | Auth now prefers valid session cookie, then falls back to token | `serverXR/src/index.js` |
| Browser back/forward can be inconsistent after route changes | Mixed manual history mutations and router navigation caused divergent history ownership | Route through `appNavigate`; use router `navigate` when mounted and full page navigation fallback when not mounted | `src/utils/appNavigate.js` `src/hooks/useAppRoute.js` |
| Graph nodes stop at left edge while dragging | Drag clamped `x >= 0` | Allow overflow left, clamp top/right only | `BetaGraphSurface.jsx` `DesktopWindow.jsx` `windowLayout.js` |
| Staging serves old build after push | Actions workflow still running | Wait 2–3 min: `gh run list --workflow publish-cpanel-prebuilt-v2.yml` | `.github/workflows/` |
| `assetId is required` on upload | Dead `|| crypto.randomUUID()` fallback removed | SHA-256 must be computed before calling `buildProjectAssetMeta` | `serverXR/src/projectStore.js` |
| Asset served under a sha256 URL doesn't match its bytes / stale cached asset after re-upload | Upload routes trusted a client-supplied `assetId` without hashing the file, so different bytes could replace an immutable-cached content address | Server verifies sha256-shaped ids against the actual content hash (400 on mismatch); contract test guards it. Legacy uuid-style ids still overwrite as before | `serverXR/src/assetHash.js` `routes/projectRoutes.js` `routes/spaceRoutes.js` `projectContracts.test.js` |
| 503 after deploy (server crashes on start) | `shared/projectSchema.cjs` out of sync with `src/shared/projectSchema.js` | Always update both files together; CJS is what serverXR actually loads | `shared/projectSchema.cjs` |
| V1 `/main` objects not draggable | `interaction-mode` button excluded from desktop controls; UI hidden by default gives no affordance to switch to Edit mode | Removed `'interaction-mode'` from exclusion filter in `EditorLayoutContainer`; added it to `hiddenUiButtons` + `EditorOverlays` filter so "Mode: Edit/Navigate" button shows even when UI is hidden | `src/components/EditorLayoutContainer.jsx`, `src/hooks/useControlButtons.js`, `src/components/EditorOverlays.jsx` |
| 503 after deploy — SQLite OOM or build failure | CloudLinux LVE blocks WASM (`node-sqlite3-wasm` OOM), no C++ toolchain for `better-sqlite3` prebuilt | Use Node.js built-in `node:sqlite` (`DatabaseSync`) — zero deps, no WASM, works on Node 22.5+ and 24+ | `serverXR/src/db.js` |
| Beta canvas requires two double-clicks | `preventDefault` on `pointerdown` suppresses `dblclick` (Pointer Events spec); pan start fires on first click of a double-click | Added `event.detail >= 2` guard in `handleSurfacePointerDown`; `user-select: none` on surface CSS replaces the prevented default | `BetaGraphSurface.jsx` `beta.css` |
| Cursor shows wrong type across Beta editor | Multiple issues: I-beam on topbar spans, `grabbing` flash on single click, `crosshair` persists during node/window drag, input ports wrong cursor, window header buttons blocked by `preventDefault` | Added `isPanMoving` state (cursor only `grabbing` on actual drag movement); `cursor: default; user-select: none` on topbar; `draggingNodeId` in surface cursor; port `--in`/`--out` classes; `dragMode` state in DesktopWindow with button-target guard in `startDrag` | `BetaGraphSurface.jsx` `DesktopWindow.jsx` `beta.css` |
| Node 0 UI messy — duplicate "Node 0" shown in topbar AND scope bar below it | Scope bar (`← Exit \| Node 0`) was rendered inside the shell AND breadcrumb in topbar both showed Node 0, causing visual redundancy | Removed scope bar entirely; topbar breadcrumb (`◈ › Node 0`) is the single navigation source; `graphTopInset` simplified to `workspaceTop`; canvas hint updates to "Double-click to place your first node." when inside Node 0 | `BetaEditor.jsx` `BetaGraphSurface.jsx` `beta.css` |
| Vite 8 build fails: `Cannot find package 'esbuild'` in custom JSX-transform plugin | Vite 8 dropped its bundled esbuild (switched to rolldown/oxc by default); `transformWithEsbuild` now needs esbuild as an explicit dependency | Added `esbuild@0.28.1` (patched, outside vulnerable `0.17.0-0.28.0` range) as a direct devDependency | `package.json` `vite.config.js` |
| staging.di-studio.xyz/serverXR/api/health returns 503, no `lsnode` worker for `serverXR-staging` ever spawns | Staging's `.htaccess` had a stray manually-added `RewriteEngine On` / `RewriteRule ... 127.0.0.1:4001` block stacked below the CloudLinux Passenger markers (production's `.htaccess` has only the Passenger block); mod_rewrite intercepts the request before Passenger spawns the app, and nothing listens on raw TCP 4001 (CloudLinux Passenger-for-Node uses its own internal socket) | Removed the extra rewrite block so staging's `.htaccess` matches production's structure, then `touch serverXR-staging/tmp/restart.txt` | `~/staging.di-studio.xyz/serverXR/.htaccess` on the cPanel host (not in this repo) — deploy script only rewrites between the Passenger BEGIN/END markers, so any manual addition below them survives every redeploy |
| CI `npm ci` ERESOLVE after bumping vite to 8 (local `npm install` looked fine) | `vite-plugin-restart`'s peerDependency caps at `vite@^7`; `npm ci` enforces strict peer resolution with no `--legacy-peer-deps` fallback like local installs can use | Inlined its ~20-line restart-on-`public/`-change logic directly as a plugin in `vite.config.js` and dropped the dependency — removes the peer conflict entirely instead of masking it | `vite.config.js` `package.json` |
| 3D viewport collapses to a tiny sliver (canvas defaults to 300×150) on any route that renders `<StudioViewport>` outside the main Studio app shell — reproduced on the V1 public viewer (`/main`) | `studio.css` (which defines `.studio-viewport-shell { height: 100% }`) was only ever imported by `StudioApp.jsx`; consumers like `PublicProjectViewer.jsx` rendered the component without that stylesheet ever loading, so the wrapper had no height and the canvas fell back to its intrinsic default size | Moved the `studio.css` import into `StudioViewport.jsx` itself so the component is self-contained for any consumer | `StudioViewport.jsx` |
| Dragging (graph canvas, nodes, window headers/resizers, 3D viewport) selects page text; mobile pinch-zoom/native gestures fight with app-level drag/pan | No global `user-select` baseline; several drag surfaces (`.beta-window-header`, `.beta-window-resizer`, `.beta-graph-surface`, `.beta-graph-node-card`, `.studio-viewport-shell`) were missing `touch-action: none` | Added global `user-select: none` on `html/body/#root` with explicit `user-select: text` opt-in for `input`/`textarea`/`contenteditable`; added `touch-action: none` to the drag surfaces listed above | `base.css` `beta.css` `studio.css` |
| CI `npm test` randomly fails `BetaHub.test.jsx` with `ReferenceError: window is not defined` from inside react-dom — passes 100% of the time locally | `SpaceSyncPanel`'s `checkStatus()` fires a `fetch` on mount with no `AbortController`; if the component unmounts (test teardown) before it resolves, the `.catch` callback calls `setStatus` after jsdom's `window` is already torn down. Pure timing race — surfaces under CI's different scheduling, not local idle-machine timing | Gave `checkStatus` its own `AbortController` (same pattern already used by `run()`), aborted on unmount, ignore `AbortError` in the catch | `src/components/SpaceSyncPanel.jsx` |
| CI `npm test` randomly fails with `ReferenceError: window is not defined` from react-dom, originating in `StudioShell.test.jsx` — passes locally (same class as the `SpaceSyncPanel` row above) | `useAuthSession`'s mount-time `refresh()` fetch had an 8s abort timeout but nothing tied to unmount: if the component unmounts (test teardown) before `getApiSession` settles, the `finally`'s `setLoading(false)` runs after jsdom's `window` is torn down | Abort the in-flight fetch on unmount (`abortRef`) and guard every state update with `mountedRef` (reset to `true` on effect re-run for StrictMode). Regression guard: `useAuthSession.test.jsx` "aborts the in-flight session fetch on unmount" | `src/hooks/useAuthSession.js` `src/hooks/useAuthSession.test.jsx` |
| Adding a new SQLite column for an existing deployed DB | `CREATE TABLE IF NOT EXISTS` only covers fresh databases — SQLite has no `ADD COLUMN IF NOT EXISTS` | Added `ensureColumn(db, table, column, definition)` helper that checks `PRAGMA table_info` before `ALTER TABLE`, called after `db.exec(SCHEMA)` in `initDb` | `serverXR/src/db.js` |
| Public/unauthenticated space access was a single hardcoded `spaceId === 'wcc'` string check in `RootApp.jsx` | One-off special case instead of a real per-space flag — doesn't generalize (at the time, reads weren't auth-checked server-side at all, see next row) | Added real `isPublic` column on space metadata (`serverXR/src/spaceStore.js`, `db.js`), `useSpacePublicFlag` hook fails closed while loading, `RootApp.jsx` wraps with `AuthGate` unless the space is confirmed public | `serverXR/src/spaceStore.js` `src/hooks/useSpacePublicFlag.js` `src/RootApp.jsx` |
| The login wall never actually appeared for ANY space, public or not, once `REQUIRE_AUTH=true` was set | `GET /api/auth/session` auto-issues a "guest" session (role: editor, spaces: `GUEST_SPACES=['main']`) on every page load before a login form can render; `AuthGate` only checked `authenticated`, never which spaces the session was scoped to; and `GET` routes (`/api/spaces/:id`, `/scene`, `/api/projects/:id`, `/document`) had zero scope checks server-side at all — only writes were scoped (`requireWriteRole`). Net effect: anyone could browse (read-only) any space's full Studio editor regardless of `isPublic` | Added `requireReadRole` middleware mirroring `requireWriteRole` (same `isAuthScopeAllowedForSpace` check, GET/HEAD only, with an `isPublic` bypass via `loadSpaceMeta`); `AuthGate` gained a `requiredSpaceId` prop and shows an "Access restricted" panel instead of silently rendering when an authenticated session is out of scope | `serverXR/src/index.js` (`requireReadRole`) `src/components/AuthGate.jsx` `src/RootApp.jsx` |
| Every GitHub/Google sign-in got unrestricted editor access to every space, with no scoping step at all | `issueSessionAndRedirect` (`serverXR/src/routes/authRoutes.js`) hardcoded `spaces: null` on every OAuth session — `null` means "no restriction" per `isAuthScopeAllowedForSpace`. The `users` table had no `spaces` column to even restrict it | Added `users.spaces` column (default `'[]'` for new accounts via `ensureColumn`, real pre-existing SQL-NULL rows also parse to `[]` deny-all); `upsertUser` defaults new users to no access and never overwrites existing scope on repeat login; new admin-only `GET /api/users` / `PATCH /api/users/:id {spaces: [...] \| null}` lets an admin grant a specific account access to specific spaces, or `null` for unrestricted (own admin accounts); `issueSessionAndRedirect` now reads `user.spaces` instead of hardcoding `null`. **Action taken live:** the two real pre-existing accounts in the dev DB (dob-0/GitHub, Gevorg Grigoryan/Google) were granted `spaces: null` (unrestricted) via the new endpoint so normal OAuth sign-in keeps working as before — do the same on staging/prod DBs after deploying this | `serverXR/src/db.js` `serverXR/src/userStore.js` `serverXR/src/routes/authRoutes.js` `serverXR/src/routes/userRoutes.js` |
| Landing page sections below the hero (`What is di.iiii?`, `How to use di.iiii`, `Made for everyone`, etc.) render with only their eyebrow label visible — title/body/cards invisible, persistent 3D wireframe background floats over empty space | `GridFloorBackground` renders a `position: fixed; z-index: 0` Three.js canvas spanning the *entire* scrollable page, not just the hero. CSS paints non-positioned static elements before positioned z-index:0 elements, so every section's static content was painted first, then visually covered by the fixed canvas painted on top of it. The hero text avoided this by already using `position: relative; z-index: 2` on `.lp-hero-inner` | Added `position: relative; z-index: 1; background: var(--di-black)` to `.lp-section`, lifting every section above the fixed background and giving it its own solid panel, matching the existing hero pattern | `src/landing/landing.css` |
| Test for a sandboxed-iframe `postMessage` listener silently never fired the handler, even though the real code path worked | `waitFor` only waits for the DOM mutation (iframe appears) to be observable — it resolves *before* React's passive `useEffect`s (which attach the `message` listener) actually run, since those are scheduled after paint. Dispatching the event immediately after `waitFor` raced ahead of the listener being attached | Added an empty `await act(async () => {})` after the `waitFor` to flush pending effects before dispatching the test event; also had to stub `iframe.contentWindow` and `event.source` via `Object.defineProperty` (jsdom's real cross-document `contentWindow` identity isn't reliably comparable) — same pattern as `PresentationCanvas.test.jsx` | `src/project/components/PublicProjectViewer.test.jsx` |
| Sign-in popover on staging/production showed "No sign-in providers configured" — nobody could log in via OAuth | `GET /api/auth/providers` (`serverXR/src/routes/authRoutes.js`) just echoes `Boolean(process.env.GITHUB_CLIENT_ID)` / `GOOGLE_CLIENT_ID` (`serverXR/src/config.js`); the cPanel deploy workflow only pushes code, never sets host env vars — OAuth secrets had simply never been configured on either host | Not a code fix — registered/rotated OAuth app credentials per environment (GitHub: `staging di`, `di-studio`; Google: separate client per env) and set `GITHUB_CLIENT_ID/SECRET`, `GOOGLE_CLIENT_ID/SECRET`, `OAUTH_CALLBACK_BASE_URL`, `OAUTH_FRONTEND_URL` directly in each cPanel Node app's environment, then restarted | cPanel host env vars only — not in this repo. Code referenced: `serverXR/src/config.js`, `serverXR/src/routes/authRoutes.js` |
| `GET /api/spaces` (the full space list) revealed every space's metadata — id, label, `isPublic`, `publishedProjectId`, `sceneVersion`, timestamps — to anyone, even unauthenticated, because `requireReadRole` only checks scope when `req.requiredSpaceId` is set, and the bare list route never sets it | The route had no per-item filtering at all; an unauthenticated caller's `state.spaces` resolves to `null` ("unrestricted"), so a naive `isAuthScopeAllowedForSpace` check without first checking `state.authenticated` would have let unauthenticated callers see *everything* | Filter the list to `space.isPublic \|\| (state.authenticated && isAuthScopeAllowedForSpace(state.spaces, space.id))`; skipped entirely when `config.requireAuth` is false | `serverXR/src/routes/spaceRoutes.js` `serverXR/src/index.js` |
| Production: Studio showed "Server error" thumbnails for several spaces (`beyond-form`, `azd`, `br-id-ge`); `GET /serverXR/api/projects/:id/document` returned 500 with `EACCES: permission denied` writing `document.json.*.tmp` | `/var/lib/docker/volumes/dii_data/_data/spaces` (top-level dir) was correctly owned by the container's `app` user (uid 100/gid 101), but every space/project underneath it had been reset to `root:root` at some point — `dii-server-1` runs as non-root `app` and can't write into root-owned directories. Only projects that happened to trigger a schema-coercion rewrite in `readProjectDocument` surfaced the error; others silently worked until their next write. Origin unconfirmed — `deploy/vps-restore.sh` extracts as root inside a bare `alpine` container with no ownership fixup, so any manual/future restore would reproduce this exact bug even though the script had never been run against prod before this incident | Live fix: `chown -R 100:101 /var/lib/docker/volumes/dii_data/_data/spaces` on the VPS. Regression guard: added `chown -R 100:101 /data` as the last step of `vps-restore.sh`'s extraction, so ownership is forced correct regardless of what the archive or extracting container's defaults produce | `deploy/vps-restore.sh` (VPS filesystem itself is not in this repo) |
| Staging deploys silently stopped applying (cPanel Git Version Control showed `SQLITE_IOERR (disk I/O error)` on its own internal `vc_deploy.sqlite`); a push to `dev` never reached the live server even though GitHub Actions succeeded | `~/deploy-backups` on the cPanel host grew to 23GB/25GB (the account's full quota) because the existing prune step (`cpanel-apply-prebuilt-release.sh`, keep-newest-N) only runs as a side effect of a *successful* deploy — once the disk filled, deploys couldn't run, so nothing could ever prune it: a self-sustaining deadlock. 114 backup dirs had accumulated since 2026-06-17 | Manually pruned to newest 5 per environment via `find ... -name "*-staging\|production" \| sort \| head -n -N \| xargs rm -rf` (freed ~20GB), then added a standalone `scripts/cpanel-prune-deploy-backups.sh` on a daily 3am cron job (`~/scripts/` on the host, independent of any deploy succeeding) so this can't deadlock again. Lowered the default keep-count from 5 to 3 in both the cron script and the per-deploy prune step to slow future growth | `scripts/cpanel-prune-deploy-backups.sh` `scripts/cpanel-apply-prebuilt-release.sh` — cron entry lives only on the host (`~/scripts/cpanel-prune-deploy-backups.sh`, not git-tracked) |
| Editing the project linked to the `main` space in Studio Hub had zero visible effect on the landing page / Studio Hub background | `GridFloorBackground.jsx` rendered a hardcoded `LANDING_PROJECT_ID = 'test-file-project'` regardless of whatever project was actually linked/published to the `main` space via Studio Hub's "Change project" — two disconnected notions of "the main project" | `GridFloorBackground` now fetches `main`'s real `publishedProjectId` via `getServerSpace('main')` at mount and uses that, falling back to the old constant only if the lookup fails or nothing is linked | `src/components/GridFloorBackground.jsx` |
| After the above fix, the landing page still showed stale/wrong project content even though the network tab showed the correct project's document being fetched | `useLiveProjectDocument` (`LiveProjectScene.jsx`) had no guard against a stale `projectId`'s document fetch resolving *after* a newer `projectId` had taken over — this was latent and harmless while `projectId` was always a constant, but became a real race the moment a consumer (`GridFloorBackground`) started swapping it at runtime. The older project's slower response would land last and silently overwrite the correct doc | Added an `activeProjectIdRef` set in an effect; `reloadDocument` captures its own `requestedProjectId` and drops the response if `activeProjectIdRef.current` no longer matches by the time it resolves | `src/components/LiveProjectScene.jsx` (`useLiveProjectDocument`) |
| Image/video/model entities in legacy-imported projects (`source: "legacy-import-studio"`) never render anywhere outside Studio — zero asset network requests fire at all | Asset metadata's `url` field is empty for these (`registerAssetSources`, the registry that normally fills it in, only ever runs inside Studio's own `useAssetRestore` editor hook — never in the public/live viewer path). `ImageObject`/`VideoObject`/`ModelObject` fall back to a `data` prop (`entity`'s resolved `asset.url`) when the registry-backed lookup is empty, but that fallback was also empty | `LiveProjectScene`'s `assetMap` now fills in `buildProjectAssetUrl(projectId, assetId)` (already-existing helper, same endpoint Studio itself uses) whenever `asset.url` is empty, so the `data` fallback prop actually has something to use | `src/components/LiveProjectScene.jsx` |
| Walk/fly navigation only ever worked on the landing page background and WCC exhibition — visiting any other space's real public URL gave orbit-only camera controls, with no way to walk or fly around it, for spaces created before or after the feature shipped | Three independent renderers exist for what looks like "the" 3D viewer: `LiveProjectScene.jsx` (landing/WCC, has Walker/fly), `PublicProjectViewer.jsx` -> `StudioViewport` (the actual generic `/:spaceId` public viewer, orbit-only, no Walker at all), and Studio's own editor viewport. A feature added to one was never propagated to the others | Added a "Walk / Fly" toggle to `PublicProjectViewer.jsx` (scene entry-view only — fixed-camera/code presentation modes are a deliberate per-project choice and stay untouched) that swaps in `LiveProjectScene` and back to orbit via its `onExit` callback. Available on every space, past and future, without changing default behavior for existing visitors | `src/project/components/PublicProjectViewer.jsx` |
| WCC's exhibition ring/hub (`/wcc`, `WccExhibition.jsx`) didn't get any of the strafe/pitch-range/drone-fly fixes made to `LiveProjectScene.jsx` in the same session | `WccExhibition.jsx` has its own complete hand-duplicated copy of the Walker/joystick/fly-button code, predating `LiveProjectScene`'s generalization — it never actually imports or reuses the shared component, despite a misleading nearby comment suggesting it does. Per-artist "Enter space" views (via `WccExperience.jsx`) *do* use the real shared component and were unaffected | Ported the same specific fixes (A/D strafe, `WALK_PITCH_LIMIT`/`FLY_PITCH_LIMIT` split, drone-decoupled fly movement) directly into `WccExhibition.jsx`'s own copy, mirroring `LiveProjectScene.jsx` line for line. Full de-duplication (rewriting the hub to reuse the shared component) was scoped out as a bigger, riskier refactor — WccExhibition has extra hub-only features (portals, zones, artist staging) layered on top | `src/wcc/scene/WccExhibition.jsx` |
| Mobile fly mode had no way to ascend or descend at all | Altitude in fly mode is controlled by Space/Q (up) and C/E (down) -- keyboard-only, no touch equivalent existed anywhere (joystick only ever drove horizontal movement, confirmed double so after the drone-decoupling fix made forward/strafe horizontal-only in fly mode too) | Added a `vertTouchRef` fed by two new press-and-hold on-screen buttons (▲/▼), shown only on mobile while `flyMode` is active, stacked above the existing Fly toggle. Ported identically into both `LiveProjectScene.jsx` and `WccExhibition.jsx`'s duplicated copy | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css`, `src/wcc/scene/WccExhibition.jsx` |
| VR/AR sessions had no movement at all — entering a headset session always started at world (0,0,0), completely disconnected from wherever you were walking/flying right before entering | No `<XROrigin>` was ever rendered inside `<XR>` in either `LiveProjectScene.jsx` or `WccExhibition.jsx`, and no locomotion was wired up beyond `@react-three/xr`'s default teleport-pointer (point-and-click teleport only, no continuous movement) | Added `useXRControllerLocomotion` (the library's standard smooth-locomotion hook: left stick moves, right stick turns) driving an `<XROrigin>` that's synced with the same `playerRef` the desktop/touch Walker uses — position carries over correctly entering and leaving a session. **Not verified on real hardware** — this environment has no headset and WebXR emulation is explicitly disabled (`emulate: false`); confirmed only that it builds/lints/mounts without crashing. Needs a real-device check | `src/components/LiveProjectScene.jsx`, `src/wcc/scene/WccExhibition.jsx` |
| GitHub-sync calls failed on cPanel with `fetch failed` / `WebAssembly.Instance(): Out of memory` while `curl` to the same URL returned 200 | Node's global `fetch` (undici) instantiates a WASM HTTP/1.1 parser; under CloudLinux LVE virtual-memory limits that WASM instantiation OOMs. Network itself is fine — only undici's WASM is blocked (same root cause as the SQLite-WASM row above) | Replaced `fetch` with a tiny `node:https`/`node:http` client (`httpRequest`) for the whole GitHub-sync path — the built-in HTTP parser is native C, no WASM | `serverXR/src/httpClient.js` `serverXR/src/githubApp.js` `serverXR/src/index.js` |
| GitHub-sync "first sync failed: connect ECONNREFUSED 127.0.0.1:<port>" — link saved + App auth OK, but the document write/publish step couldn't reach the server's own API | `syncLinkedSpace` writes through the server's OWN tested HTTP document/publish routes via a loopback to `http://127.0.0.1:${config.port}`. Under Passenger (cPanel) the app is fronted by a Unix socket and nothing actually binds that TCP port, so the loopback is refused | Made the base URL env-overridable: `SELF_API_URL` (set to the public origin, e.g. `https://di-studio.xyz/serverXR`) is used when present, falling back to the loopback for normal TCP-listen deployments. Set `SELF_API_URL` in each cPanel app's `.env` | `serverXR/src/index.js` (`internalApiBase`) — plus per-host `.env` `SELF_API_URL` |
| The three rows above reference `src/wcc/scene/WccExhibition.jsx` — searching for it turns up nothing | That file no longer exists; it was consolidated away after those fixes were recorded | `WccExperience.jsx` now renders the WCC hub through the shared `LiveProjectScene.jsx` directly — there's only one Walker/XrLocomotion implementation to maintain now. Treat those three rows' file paths as historical only | `src/wcc/WccExperience.jsx`, `src/components/LiveProjectScene.jsx` |
| VR fly (right-thumbstick-Y, see row above) has no way for a first-time headset user to discover it — no keybind text is visible once inside a VR session (unlike AR, VR gets no dom-overlay) | Only a desktop hint bar (`live-scene-hint`) existed; nothing told a VR user the right stick both turns (X) and flies (Y) | Added a pre-entry DOM hint next to the "Enter VR" button (`.live-scene-hint--vr`), plus a floating in-world `Text` hint inside `XrLocomotion` that tracks the headset's own look direction every frame (not the rig's yaw) and auto-dismisses after 14s or on the first real stick input | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css` |
| Exhibition walker: scrolling tilts the camera into the floor on real mice; mouse look silently dead ("can move but can't look") on Wayland or after Esc | Wheel handler guessed "trackpad vs mouse" from delta size (≤60px pixel-mode = trackpad) — hi-res mouse wheels send small pixel deltas and pass the guard, pitching the view; a denied `requestPointerLock` (Wayland browsers, Chrome's post-Esc cooldown) was caught-and-ignored with no fallback | Wheel never touches pitch: deltaY dollies forward/back through the bounds clamp (scroll = zoom, all deltaModes normalised), deltaX still turns; drag-on-canvas is the look fallback when lock is denied. Regression guard: `npm run check:input` (13 contracts, runs in CI + gates deploys). Never guess input device class — see golden rule | `src/components/LiveProjectScene.jsx` (Walker), `scripts/input-check.mjs` |
| VR fly inverted on real hardware: pushing the right stick up descends, down ascends | A session "fixed" the yAxis sign with a comment claiming Quest verification that never happened — the WebXR xr-standard-thumbstick yAxis is NEGATIVE when pushed up (standard Gamepad convention), so the original negation was correct | Sign convention extracted to `flyVertFromStick()` (`src/components/xrFlyControl.js`), user-verified on a headset 2026-07-05; unit test encodes the convention — do not flip the sign without re-testing on physical hardware | `src/components/xrFlyControl.js` `src/components/LiveProjectScene.jsx` |
| VR left-stick strafe mirrored on real hardware: push left slides right (forward/back fine) | `useXRControllerLocomotion`'s translation rotates the raw stick vector by the camera's full world quaternion — its strafe axis comes out mirrored on-device | Library translation disabled (`translationOptions: false`, right-stick turn kept); left-stick movement is ours: `moveFromStick()` axes drive motion along the camera's horizontal forward and its cross-product right (`right = forward × up`), pure geometry from the verified-correct forward. Unit-tested | `src/components/xrFlyControl.js` `src/components/LiveProjectScene.jsx` |
| Desktop "I can move but can't look" reported in a live space — reproduced under Playwright with the real backend down (502s), NOT reproducible against the live deployed site or a properly-running local stack; existing mouse-look code (pointer lock + drag-look fallback) passes all 13 `check:input` contracts when the backend is up | `reloadDocument()`'s catch was silent: a failed project-document fetch left `doc` null forever, no error, no retry. The loading overlay (`.live-scene-loading`, opaque, `pointer-events: all` while `!doc`) then blocks all mouse/wheel input indefinitely and invisibly, while WASD (independent of `doc`) keeps responding — exactly the reported symptom, likely from a real but transient backend hiccup on the user's original attempt | `useLiveProjectDocument` now tracks `loadError`, auto-retries once after 3s, and the overlay shows a visible "Couldn't load this space — Retry" instead of spinning forever. Regression guard: 2 new `check:input` contracts (forced 502 → visible error → auto-retry recovers) | `src/components/LiveProjectScene.jsx`, `src/components/liveProjectScene.css`, `scripts/input-check.mjs` |
| A full-codebase audit found the `wcc` space bypassing the login gate every other space goes through | `RootApp.jsx` special-cased `spaceId === 'wcc'` and routed straight to `WccExperience`, skipping `useSpacePublicFlag`/`AuthGate` entirely — a regression of the `isPublic`-flag fix recorded further up this table (the "hardcoded `spaceId === 'wcc'` check" row); if WCC were ever marked private, unauthenticated visitors would still get the full client UI | Added `WccSurfaceRoute`, which runs the same `useSpacePublicFlag('wcc')` check as every other space and only skips `AuthGate` while `isPublic` is true. Regression guard: 2 new `RootApp.test.jsx` cases (gated when not public, open when public) | `src/RootApp.jsx`, `src/RootApp.test.jsx` |
| Same audit: `GET /serverXR/api/events` returned recent request URLs and error text to anyone, with no auth check, in any configuration | The route has no `requiredSpaceId`, so the global `requireReadRole`/`requireWriteRole` middleware (which only activates once `requiredSpaceId` is set) never applied to it | `/api/events` now checks `req.authState` and returns `{ events: [] }` to non-admins when `REQUIRE_AUTH` is on (unchanged, full detail, when auth is disabled — local dev). `/api/health` deliberately stays fully public; it's polled unauthenticated by the deploy pipeline, smoke checks, and CI. Regression guard: 3 new `httpContracts.test.js` cases | `serverXR/src/routes/statusRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Same audit: a failed project-document write (network blip, 5xx, expired auth) was silently dropped — the UI kept showing an edit as saved, and it vanished on the next reload with no error, no retry | `flushQueue`'s catch branch only handled 409 (version conflict); any other error just logged a low-visibility activity message and `break`, after the batch had already been spliced out of the pending queue | Non-409 failures now put the batch back at the front of the queue, set a `pendingSyncError` on the store (surfaced as a red `scc-sync-dot` in Studio's control cluster), and retry automatically after 4s. Regression guard: new `useProjectDocumentSync.test.jsx` case forcing a 500 then a successful retry | `src/project/hooks/useProjectDocumentSync.js`, `src/project/state/projectStore.js`, `src/studio/components/StudioControlCluster.jsx`, `src/studio/components/StudioEditor.jsx`, `src/project/hooks/useProjectDocumentSync.test.jsx` |
| "Open Studio" / "Open in Studio" direct links (admin Manage panel, and any bookmarked/typed `/studio/projects/:id` URL with no space segment) loaded the wrong project's assets, metadata, and publish state — the project document itself loaded fine, but everything else silently ran against space `main` | `buildStudioProjectPath` omits the space segment from the URL whenever its `spaceId` arg is falsy; `getStudioLocationState`'s matching route branch then hardcoded `spaceId: defaultSpaceId` ('main') instead of leaving it unset, which pre-empted `StudioEditor`'s own three-tier fallback (`spaceId \|\| document.projectMeta?.spaceId \|\| DEFAULT_PROJECT_SPACE_ID`) from ever reaching the project's real space. Triggered in practice by `AdminManageSection.jsx`'s "Open in Studio"/"Open" buttons building the link from `space?.id`/`space.id` — a `useMemo` lookup against the async-loaded `spaces` list that can resolve to `null`/stale independently of the already-loaded `project` object | `getStudioLocationState`'s space-less project branch now returns `spaceId: null` so the existing `StudioEditor` fallback chain works as designed. Both admin buttons now build the link from `project.spaceId` (the DB-sourced field that travels with the project row itself, `serverXR/src/projectStore.js`'s `rowToMeta`) instead of the separately-loaded `space` object. Regression guards: `studioRouting.test.js` asserts `spaceId` is `null` for a space-less URL; `AdminManageSection.test.jsx` asserts "Open in Studio" navigates using the project's own spaceId | `src/studio/utils/studioRouting.js`, `src/components/preferences/AdminManageSection.jsx`, `src/studio/utils/studioRouting.test.js`, `src/components/preferences/AdminManageSection.test.jsx` |
| Same audit: undo/redo in both Studio and Beta silently diverged from the server — an undo was never persisted or broadcast, and the next real edit after it could reference an entity the server never actually restored | Both editors' Ctrl+Z/Ctrl+Y dispatched `replace-document` straight to the local reducer, bypassing `useProjectDocumentSync` entirely (the only path that calls `updateProjectDocument`/reconciles `versionRef`) | Undo/redo was first routed through `replaceDocument` (network-backed PUT); since 2026-07-08 it replays **inverse ops** through `applyLocalOps` → `POST /ops` (`useOpHistory` + `invertProjectOps`) — same rule, better mechanism: still network-backed, now granular and collaborator-safe instead of a whole-document swap. The invariant to preserve is *undo must go through a network write path*; the component tests assert inverse ops reach the ops submit path and `replaceDocument` is never called for undo | `src/project/hooks/useOpHistory.js`, `src/shared/projectSchema.js`, `src/studio/components/StudioEditor.jsx`, `src/beta/components/BetaEditor.jsx`, `src/studio/components/StudioEditor.test.jsx`, `src/beta/components/BetaEditor.test.jsx` |
| User on Linux/Wayland reported mouse-look "doesn't work" on `/wcc/scene` on staging — not reproducible via headless OR headed real-Wayland Playwright (pointer lock engaged fine, camera rotated correctly in both) | `DRAG_LOOK_SENSITIVITY` (the fallback used when pointer lock is denied — exactly the population most likely to hit it) was deliberately tuned 3x gentler than `POINTER_LOCK_SENSITIVITY` on the untested theory that drag distances "run larger." For a normal-sized human drag gesture this just reads as unresponsive, not broken — user self-diagnosed as "maybe it's the speed" | First unified `DRAG_LOOK_SENSITIVITY` to equal `POINTER_LOCK_SENSITIVITY` (too sensitive per live user testing), then walked down through 0.75x and 0.5x (both still too sensitive) to **0.35x** of `POINTER_LOCK_SENSITIVITY`, user-confirmed. Verified via `npm run check:input` (15/15) at each step | `src/components/LiveProjectScene.jsx` |
| Studio viewport: mouse wheel doesn't zoom, it rotates the camera instead | `StudioOrbit` set `cc.mouseButtons.wheel = e.ctrlKey ? DOLLY : ROTATE` to give trackpad two-finger swipe a "look around" feel — but a plain mouse wheel scroll *also* has `ctrlKey: false`, identical to a trackpad swipe on this signal. Every non-pinch wheel event (i.e. every ordinary mouse wheel) got routed to ROTATE instead of zoom. Same "guess the device from the event" anti-pattern the golden rules already called out once for the WCC Walker (`docs/ai/golden_rules.md` "Input handling never guesses the device") | Removed the override entirely — wheel now always dollies (the `CameraControls` `mouseButtons.wheel: DOLLY` default already declared in JSX), matching the golden rule's "wheel deltaY = dolly, never rotate" semantic. Trackpad pinch (ctrlKey) still dollies too, redundantly but harmlessly | `src/studio/components/StudioViewport.jsx` |
| Walk-mode's mouse/movement tuning constants (look sensitivity per input method, walk/fly speed, pitch limits, joystick/bounds) were inline `const`s at the top of `LiveProjectScene.jsx`, mixed in with unrelated rendering constants — each tuning pass (like the drag-look sensitivity walk above) had to be found and edited in place, with no single obvious location | User asked to "sync mouse and global things... in walk mode" — one canonical place to tune walk mode instead of scattered inline consts | Extracted all walk-mode tuning into `src/components/walkModeConfig.js`, grouped by category (movement, look sensitivity, wheel/dolly, pitch limits, joystick/bounds) with `DRAG_LOOK_SENSITIVITY` defined as a ratio of `POINTER_LOCK_SENSITIVITY` rather than an independent magic number. `LiveProjectScene.jsx` now imports from it. No behavior change — verified via `check:input` (15/15) and the full test suite | `src/components/walkModeConfig.js`, `src/components/LiveProjectScene.jsx` |
| Studio's Scene panel: Transform inspector keeps sliding further down the more objects are in the scene, forcing a scroll past the whole entity list every time | The entity list (`.spa-list`, shared by the Structure list, Drive picker, Commons picker, and Files library) had no height cap — it grows with content, and shares one scroll region (`.sfp-content`) with the `StudioInspector`/Transform section rendered directly below it in the same panel | Capped `.spa-list` to `220px` with its own `overflow-y: auto`, so the entity list scrolls independently and Transform's position is no longer a function of scene size. Applies to all four `.spa-list` usages, all of which had the same latent unbounded-growth issue | `src/studio/styles/studio.css` |
| Inspector's Move/Rotate/Scale gizmo buttons showed garbled characters (`âœ¢` `âŸ³` `â‡²`) instead of icons, for every user | The three icon strings in `GizmoModeButtons.jsx` were double-encoded UTF-8 bytes, not the intended single-codepoint glyphs — flagged by the 2026-07-07 full audit, byte-for-byte confirmed | Replaced with the real glyphs `✢` `⟳` `⇲`. Regression guard: new `GizmoModeButtons.test.jsx` asserts the exact glyphs render and no `â` mojibake marker appears | `src/components/inspector/GizmoModeButtons.jsx`, `src/components/inspector/GizmoModeButtons.test.jsx` |
| Shift+D duplicated the selection twice per keypress (two overlapping clones); Delete/Backspace fired its handler twice (masked because delete is idempotent, but burned an extra undo snapshot) | Two independent window keydown listeners — one in `StudioEditor`, one in `StudioShell` — both matched Shift+D and Delete/Backspace and both called the same non-idempotent handler (`StudioShell`'s `onDuplicateSelected`/`onDeleteSelected` props ARE `StudioEditor`'s handlers) | Removed the bindings from `StudioShell`; `StudioEditor`'s handler is canonical (it also guards input focus, modal transforms, selection, and covers Ctrl/Cmd+D). Regression guard: 3 new `StudioShell.test.jsx` cases asserting the shell never fires duplicate/delete on those keys | `src/studio/components/StudioShell.jsx`, `src/studio/components/StudioShell.test.jsx` |
| Five Studio features silently did nothing: Save current view persisted stale data, Frame Selected (F/".") was dead from the editor, double-click insert always used the ring layout, exiting XR never restored the camera, and a project's saved view was ignored on load | `StudioEditor`'s `controlsRef` was a plain `useRef` that no component ever attached — the real `CameraControls` instance lives two levels down in `StudioViewportLayout`'s `ViewPane`, which only registered a `fitToBox` closure | Panes now register their live controls ref into `shared.paneControlsRef`; the editor's `controlsRef` is a `useMemo` proxy whose `.current` getter resolves the active pane's instance at call time. The perspective pane opens on `worldState.savedView` when present. StudioShell's duplicate F binding removed (editor owns F/"."). Regression guard: `StudioViewportLayout.test.jsx` (ref registration + saved-view-on-load) | `src/studio/components/StudioEditor.jsx`, `StudioShell.jsx`, `StudioViewportLayout.jsx`, `StudioViewportLayout.test.jsx` |
| Collaborator presence/cursors die permanently after a backend restart or network blip — only a full remount (switching space) brings them back | The socket is created with `reconnection: false` and a retry was only scheduled from `connect_error`; a plain mid-session `disconnect` just flipped `isConnected` to false | `disconnect` now takes the same `scheduleRetry` path as `connect_error` (excluding the hook's own cleanup, reason `io client disconnect`). Regression guard: `useSpaceSocket.test.js` reconnection cases | `src/hooks/useSpaceSocket.js`, `src/hooks/useSpaceSocket.test.js` |
| `fetch failed` / `WebAssembly.Instance(): Out of memory` on cPanel from the local↔live sync endpoints (`/api/sync/spaces/:id/{status,pull,push}`) — the same OOM the GitHub-sync path was already cured of | `syncRoutes.js` still used global `fetch` (undici's WASM HTTP parser, blocked under CloudLinux LVE); the bug class was reintroduced in a sibling path after the first fix | Migrated `liveFetch` to the shared `httpClient.js` (`node:https`, native parser). New contract test scans all serverXR source and fails on any global `fetch` call site, so the class can't ship a third time | `serverXR/src/routes/syncRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Deleting a space asset that only a legacy V1 `scene.json` references succeeded silently and broke that scene's rendering — the opposite of what the delete guard promises | The used-by check scanned project documents' `media.assetId` only; V1 scenes reference assets directly on objects (`assetRef`, `asset`, `mediaVariants`…) and were never scanned, though `spaceStore.collectSceneAssetRefs` already enumerates exactly those | Delete guard now also runs `collectSceneAssetRefs` over the space scene and reports `Space scene (V1)` in the 409 `usedBy`; `?force=1` still overrides. Regression guard: new httpContracts case | `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/index.js`, `serverXR/src/httpContracts.test.js` |
| Unauthenticated clients could hammer guest-session issuance (real FS/DB writes per call), login, OAuth, sync-key minting, and uploads at unlimited rate | No request-time throttle existed anywhere in serverXR — abuse was bounded only by the guest-session TTL sweep | Zero-dep fixed-window limiter (`rateLimit.js`, keyed on first-hop X-Forwarded-For since the app runs behind cPanel's proxy without `trust proxy`) applied to those five surfaces; 429 + Retry-After. Guards: `rateLimit.test.js` + login-throttle contract test | `serverXR/src/rateLimit.js`, `serverXR/src/index.js`, `serverXR/src/rateLimit.test.js`, `serverXR/src/httpContracts.test.js` |
| Scenes with lights or groups silently corrupt when the server normalizes a document (op application / replace): every `pointLight`/`spotLight`/`directionalLight`/`ambientLight`/`group` entity becomes a plain `box`, and `parentId` (grouping) is stripped from entities and nodes | `shared/projectSchema.cjs` (the hand-maintained CJS mirror serverXR loads) had drifted from `src/shared/projectSchema.js`: 5 entity types missing from `ENTITY_TYPES` (unknown types coerce to `'box'`), no light/group default components, no `parentId` in either normalizer, and a different assets-window default. The schema-sync test never caught it because it only checked the CJS against hardcoded invariants — its "cannot import the ESM" comment was stale | Synced the CJS mirror, and made `schemaSync.test.js` import BOTH modules and diff-check constants + `normalizeProjectDocument` + `applyProjectOps` over representative fixtures (clock fields zeroed). Hand-mirror misses now fail `test:schema-sync` (pre-push gate + CI) with a real diff instead of passing silently | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| Deleting a group in Studio removed its children on every client but the server kept them — the orphans resurrected on the next reload (found while building op-log undo, 2026-07-08) | One more CJS-mirror drift the fixture-based equivalence test happened not to cover: ESM `applyProjectOps.deleteEntity` cascades through `parentId` descendants, the CJS mirror deleted only the one id | Cascade ported to `shared/projectSchema.cjs`; regression guard: a delete-with-children equivalence case in `schemaSync.test.js`. Lesson repeated from the row above: when touching any `applyProjectOps` branch, add an equivalence fixture that actually exercises it | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| SpaceHub "Sign in to create" button did nothing (console error) — signed-out visitors could never start sign-in from the Spaces page | `onClick={login}` passed the click event object as the token to `useAuthSession.login(token)` → `POST /api/auth/session` with a garbage token; the page has no token field at all, OAuth is the real flow | Button now reveals the OAuth provider links (`getApiAuthProviders` + `getOAuthUrl`, same flow as AccountButton). Regression guard: `SpaceHub.test.jsx` "sign-in button reveals working OAuth provider links" | `src/studio/components/SpaceHub.jsx`, `src/studio/components/SpaceHub.test.jsx` |
| Returning guests counted as real accounts: they could create named spaces (`POST /api/spaces` 201) and the hub showed them Create/quota UI, despite the "guests cannot create spaces" contract | Guest identity lives in a normal session cookie; on later requests `authState.type` is `'session'`, so every `type === 'session'` check treated `guest:<uuid>` subjects as owning accounts. Only issuance-time responses said `'guest'` | `isGuestSubject()` in `authAccess.js` (the `guest:` subject prefix is the shared convention); space creation, session `type`, and quota/`canCreateSpace` now exclude guest subjects. Regression guard: httpContracts "gives each guest a private sandbox" case | `serverXR/src/authAccess.js`, `serverXR/src/index.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/httpContracts.test.js` |
| Dragging in the editor sometimes shows a translucent "screenshot" ghost of the element following the cursor while nothing actually moves (reported on prod Studio) | Links and images are natively draggable in every browser; the app's own drag interactions all use pointer events but nothing suppressed native `dragstart`, so a drag starting on an `<a>`/`<img>` (panel links, thumbnails) launched an HTML5 drag with its snapshot ghost instead | Global capture-phase `dragstart` preventDefault installed at startup (`suppressNativeDrag()`, opt-out via `draggable="true"`) plus `img, a { -webkit-user-drag: none }` in the base stylesheet. Regression guard: `suppressNativeDrag.test.js` | `src/utils/suppressNativeDrag.js`, `src/index.jsx`, `src/styles/base.css` |
| Duplicate (Shift+D/Ctrl+D) and copy/paste on a group produced an empty group shell — every child silently dropped; copy also ignored multi-selections | `handleDuplicateSelected`/`handleCopySelected` cloned only the selected entities themselves — no `parentId` traversal, so hierarchies (the whole point of Group) never made it into the clone; clipboard additionally read only the primary `selectedEntity` | Subtree-aware helpers in `src/studio/utils/entityClipboard.js`: `collectSubtree` (parent-first), `topLevelTargets` (a group + its child selected together clones once), `cloneSubtree` (fresh ids, remapped parentIds, root-only +0.4 offset). Duplicate, copy, paste, cut all use them. Regression guard: `entityClipboard.test.js` deep-copy cases | `src/studio/utils/entityClipboard.js`, `src/studio/components/StudioEditor.jsx`, `src/studio/utils/entityClipboard.test.js` |
| Published scenes silently diverged from the editor: audio entities never played (no renderer case), authored lights didn't illuminate, grouped children rendered at local coordinates (flat entity map), hidden entities stayed visible, new appearance/media props (materials, video sound, text align) reached Studio but not live pages — and portal-embedded groups drew a phantom box | `LiveProjectScene` keeps a hand-copied entity renderer (for billboard text/gate glow/walker) that had drifted from `EntityContent` — same drift class as the CJS schema mirror; `EntityContent` itself also lacked a `group` case so groups fell to the box default | Viewer gained audio/light/group cases, a parent-first hierarchy walk (roots only at top level), and the `runtime.visible` gate; `EntityContent` got `case 'group': null`. Tripwire: `rendererParity.test.js` source-compares case labels, schema ENTITY_TYPES coverage, and consumed appearance/media keys between the two renderers — any one-sided feature fails CI | `src/components/LiveProjectScene.jsx`, `src/project/viewport/EntityContent.jsx`, `src/project/viewport/rendererParity.test.js` |
| Model pipeline was internally inconsistent: OBJ/STL loaded fine in the renderer but Studio refused to place them ("+ Add" disabled, viewport drop rejected); FBX uploaded per the server whitelist but rendered nothing; Draco/Meshopt-compressed GLBs (the default export of Blender/asset stores) failed silently to an invisible model; and embedded animation clips never played — `gltf.animations` was discarded and the deep clone broke SkinnedMesh bindings anyway | Each layer kept its own format list (server `ALLOWED_EXTENSIONS`, `modelFormats.js` loaders, Studio `MODEL_EXTENSIONS` whitelist) and they drifted; GLTFLoader had no decoders registered; `ModelObject` extracted only `gltf.scene` and cloned with `.clone(true)` | Layers reconciled (glb/gltf/obj/stl/fbx placeable end-to-end), Draco (wasm in `public/draco/`) + Meshopt decoders registered, FBXLoader added, `SkeletonUtils.clone` + `AnimationMixer` play embedded clips with `media.playAnimations`/`animationSpeed` controls in both schema mirrors and both renderers. Guards: `assetFormats.test.js` (every loadable format is placeable), rendererParity media keys, animated-GLB E2E (pixel-verified spin + freeze) | `src/objectComponents/ModelObject.jsx`, `src/utils/modelFormats.js`, `src/studio/utils/assetFormats.js`, `public/draco/`, `src/studio/utils/assetFormats.test.js` |
| WCC exhibition on Wayland: walking and fly work but the mouse doesn't move the view at all — third report in this family, and again not reproducible via Playwright (headless OR headed, Chromium/Brave, X11 or native Wayland ozone, localhost or prod: all fine) | Playwright injects input through the browser's own pipeline and never exercises the OS path — but a real mouse's locked-look deltas come from the compositor's relative-motion protocol, and some Wayland setups GRANT `requestPointerLock` and then deliver `movementX/movementY = 0` on every event. The walker only had a fallback for *denied* lock; a granted-but-dead lock froze the view while keyboard (walk/fly) kept working | Broken-lock watchdog in the Walker: 30 consecutive all-zero locked mousemoves (real motion always produces nonzero deltas, and mousemove only fires on motion) ⇒ abandon the lock, latch `lockBroken`, never re-request it — clicks then flow to the existing drag-look fallback. Threshold in `walkModeConfig.js` (`BROKEN_LOCK_ZERO_MOVES`). Regression guard: 2 new `check:input` contracts (now 17) dispatching synthetic zero-delta locked moves — lock abandoned, drag-look takes over | `src/components/LiveProjectScene.jsx` (Walker), `src/components/walkModeConfig.js`, `scripts/input-check.mjs` |
| Wanted a public `wss://` for the br_id_ge live co-presence mesh; a raw `ws` server on its own port isn't reachable (LiteSpeed/Passenger only proxy the one app), and it was unclear whether the front even upgrades native WebSockets | LiteSpeed **does** proxy native WS upgrades (101), but **only under the `/serverXR/*` backend context** — a probe of `wss://di-studio.xyz/socket.io` (root) returns HTTP 200 (SPA), while `wss://di-studio.xyz/serverXR/socket.io` returns 101. Socket.IO masks this because it silently falls back to HTTP long-polling, so "Socket.IO works" does NOT prove native WS works. An arbitrary `/serverXR/<x>` upgrade returns a TCP *socket hang up* (forwarded to Node, then destroyed by engine.io) — proof the whole `/serverXR/` context is WS-proxied, not just the socket.io path | Attach a raw `ws` `WebSocketServer({noServer:true})` to the SAME `httpServer` as Socket.IO, path-routed to `${basePath}/mesh` via one `httpServer.on('upgrade')` handler (ignore non-matching paths). Coexists with engine.io's own upgrade listener because engine.io only destroys a foreign-path upgrade after `destroyUpgradeTimeout` (1s) *and only if `socket.bytesWritten <= 0`* — completing the ws handshake synchronously writes the 101 first, so it's never destroyed. No new port, no env change (public by default; optional `MESH_ROOM_SECRET`). Regression guard: `serverXR/src/meshHub.test.js` (socket.io keeps `websocket` transport + mesh fan-out on same server) | `serverXR/src/meshHub.js`, `serverXR/src/index.js` (calls `initializeMesh(httpServer, config)`), `serverXR/src/meshHub.test.js` |
| "Set as live project" reported *"Published this project"* while the space stayed private — visitors hit a login wall and the creator had no idea; the only cure was knowing the Public toggle lives on a different page (UX audit 2026-07-10, worst creator-facing finding) | Publish intent is split across two flags on two surfaces: `publishedProjectId` (editor Share window) and `isPublic` (SpaceHub card), with a success message that claimed more than the action did. Visibility staying an explicit choice is by design (see SpaceHub `handleLinkProject` comment) — but the state was invisible at the moment of intent | Share window's Live-space-route card now always discloses the space's visibility ("Space is public…" / "⚠ Space is private — visitors will see a login wall") with a one-click "Make space public" button; the set-live activity message tells the truth for both cases; SpaceHub's ⚠ warning gained the same one-click "Make public" for owners/admins. No silent flips — visibility remains explicit. Regression guard: `PublishPanel.test.jsx` (warning + button when private, confirmation when public, nothing before meta loads) | `src/studio/components/StudioEditor.jsx`, `StudioShell.jsx`, `StudioShellPanels.jsx`, `SpaceHub.jsx`, `src/studio/styles/studio-space-hub.css`, `src/studio/components/PublishPanel.test.jsx` |
| OAuth sign-in returned with zero feedback: success redirected to the landing page with no marker (users couldn't tell it worked — the avatar just quietly changed), and the server's `?auth=error` failure marker was silently ignored by the client | The OAuth flow is a full-page redirect, so the moment of return is the only chance to confirm the outcome — and `issueSessionAndRedirect` redirected to plain `frontendUrl` while no client component ever read `?auth=*` | Server now appends `?auth=ok` on success; new `AuthReturnNotice` (mounted once in RootApp, so it works on every surface incl. the landing redirect target) reads `?auth=ok\|error` once, shows a di-styled toast ("Signed in as X." / "Sign-in failed — please try again."), and strips the marker via replaceState preserving other params. Regression guard: `AuthReturnNotice.test.jsx` (ok, error, param-preservation cases) | `serverXR/src/routes/authRoutes.js`, `src/components/AuthReturnNotice.jsx`, `src/components/authReturnNotice.css`, `src/RootApp.jsx`, `src/components/AuthReturnNotice.test.jsx` |
| Creators clicking StudioHub's "Settings" (or BetaHub's "admin" footer link) hit the admin-only console gate and got bounced back — a mislabeled dead end, since no user-settings surface exists | The link was rendered for every role while `/admin` (`PreferencesPage`) gates non-admins out; the audit called it the "Settings dead-end" | The link now renders only for `role === 'admin'` and is labeled "Admin" (StudioHub) — non-admins simply don't see a door that was always locked. Account-level actions (name, role, sign out) remain in the AccountButton popover | `src/studio/components/StudioHub.jsx`, `src/beta/components/BetaHub.jsx` |
| Studio (the main shipped editor) was unusable on phones: it computed `isMobile` (MUI `down('sm')`) and threaded it into StudioShell, which destructured it and never used it — phones got the desktop draggable-floating-panel UI. The one purpose-built mobile shell (`MobileEditorShell`) serves only the deprecated legacy `App.jsx` lane | The prop was wired but the shell had no mobile branch; the good mobile pattern lived on the wrong lane and is too coupled to the legacy panel-registry to port wholesale | StudioShell now branches on `isMobile`: panel content extracted into a shared `panelBodies` map (desktop floating panels render it unchanged), and phones get a top context bar (back · project title · Edit toggle) + five-window bottom nav (Create/Scene/World/Share/Code), one window at a time as a bottom sheet (`studio-mobile.css`, safe-area aware). Regression guard: 3 StudioShell.test.jsx mobile-chrome cases | `src/studio/components/StudioShell.jsx`, `src/studio/styles/studio-mobile.css`, `src/studio/components/StudioShell.test.jsx` |
| Studio `/studio` Spaces list (SpaceHub): page cannot scroll — spaces below the first card are unreachable, most visible on phones (guest hub with open call live) | `html/body/#root` are `position: fixed; height: 100%` (`src/styles/base.css`), so the document NEVER scrolls anywhere in the app — every full-page surface must own its own scroll. `StudioHub` does (`.studio-hub-root` sets `overflow-y: auto`) but SpaceHub's `.ssh-root` never got the rule; its `min-height: 100vh` also overshot the real mobile viewport (address-bar chrome) | `.ssh-root` now mirrors the `.studio-hub-root` pattern: `overflow-y: auto; overflow-x: hidden; -webkit-overflow-scrolling: touch`, and the `min-height: 100vh` was dropped (`.studio-shell-root` already supplies `height: 100%`). Regression guard: SpaceHub.test.jsx asserts the `.ssh-root` block declares `overflow-y: auto` and has no `min-height: 100vh` | `src/studio/styles/studio-space-hub.css`, `src/studio/components/SpaceHub.test.jsx` |
| Links opened from inside any rendered code project (published viewer, Studio preview, Beta, V1 presentation) white-screen in Firefox/WebKit: the new tab loads the app HTML but every module is blocked with "Module source URI is not allowed" / CORS errors against the app's own origin | Code projects render in `sandbox`ed iframes without `allow-popups-to-escape-sandbox`, so a tab opened via `window.open`/`target=_blank` INHERITS the sandbox and gets an opaque origin — its module loads become cross-origin and die. Chromium is more forgiving, which is why direct-URL testing never reproduced it (found via br_id_ge portal-map node clicks) | Added `allow-popups-to-escape-sandbox` to the shared sandbox attribute of all four content-preview iframes (PublicProjectViewer, StudioPresentationSurface, PresentationCanvas, BetaEditor). `allow-same-origin` stays off by design (would defeat the sandbox). Regression guards: sandbox-attr assertions in `StudioPresentationSurface.test.jsx` + `PresentationCanvas.test.jsx` | `src/project/components/PublicProjectViewer.jsx`, `src/studio/components/StudioPresentationSurface.jsx`, `src/components/PresentationCanvas.jsx`, `src/beta/components/BetaEditor.jsx` |
| Beta graph wires were invisible — edges existed in the document, `svg path` elements were in the DOM (the "renders visible wires" test passed), but no wire ever painted on screen | The wire `<svg>` was sized `width/height: 100%` of `.beta-graph-stage`, which collapses to ZERO height (every child is absolutely positioned) — and Chromium paints nothing inside a zero-area svg, `overflow: visible` or not. JSDOM cannot observe paint, so the test named "renders visible wires" never caught it | The svg now has its own 1×1 box with `overflow: visible` (paths render outside a non-zero viewport fine). Regression guard: the wires test additionally asserts the svg's height is not a percentage and overflow is visible — pinning the cause, since paint still can't be asserted in JSDOM | `src/beta/components/BetaGraphSurface.jsx`, `src/beta/components/BetaGraphSurface.test.jsx` |
| WCC exhibition on Wayland, FOURTH report: walk/fly work, mouse look dead — even with the zero-delta watchdog (row above) deployed | Live-diagnosed with `?inputdebug=1` (built for exactly this): lock GRANTED, mousemove FIRING, but every event carries `movementX=-1, movementY=0` — a degenerate constant crawl. Vertical look fully dead, horizontal imperceptible (~1px/event), and the all-zero watchdog never trips because the deltas are nonzero (`zero-streak: 0/30` on screen). Same HUD also showed `buttons: 0` mid-drag, so the drag-look fallback's `e.buttons === 1` gate was one broken-compositor report away from failing too | Watchdog generalised from all-zero to dead deltas: 30 consecutive locked moves with `|movementX| ≤ 4 && |movementY| ≤ 4` (`BROKEN_LOCK_DEAD_DELTA_MAX`; a live headed-Firefox event capture on the user's KDE Wayland box showed locked sweeps arriving as ±1..±4 noise in BOTH axes while the same sweep unlocked yields 50-120px deltas — an earlier `movementY === 0` condition kept resetting on the ±1 Y-jitter) ⇒ abandon lock, latch `lockBroken`, drag-look takes over. Engage-time garbage — one wild spike ~18ms after engage (-19,-116 captured, railed the pitch) plus, in some Chromium builds, a synthetic position-sized event — is handled with a settle window (`BROKEN_LOCK_SETTLE_MS`, 200ms): locked deltas inside it count toward the dead-streak but are never applied to the view. Drag-look now trusts our own pointerdown/up pair (left button only, `setPointerCapture` + pointercancel/blur cleanup) instead of mousemove's `e.buttons`, and derives its deltas from `clientX/clientY` (the visible cursor cannot lie) instead of `movement*` — the same compositor poisons `movementX/movementY` on UNLOCKED moves too, which had made the fallback itself blind (user re-test on staging, Firefox: lock denied for focus, drag-look dead). Const renamed `BROKEN_LOCK_DEAD_MOVES`. Regression guard: 3 new `check:input` contracts (now 20) — constant (-1,0) locked moves on a fresh page, plus a poisoned-movement drag; drag scenarios also probed green in real Firefox (denied lock + real drag, poisoned drag, no-button move). Diagnose future reports with `?inputdebug=1` FIRST — it turned three unreproducible reports into a one-screenshot root cause | `src/components/LiveProjectScene.jsx` (Walker), `src/components/walkModeConfig.js`, `scripts/input-check.mjs` |
| Full-codebase deep audit (2026-07-14, 7-agent parallel find + adversarial verify across studio/project/beta/shared/serverXR/scripts) surfaced 16 confirmed real bugs, headlined by two critical ones: any authenticated editor could push/pull ANY space's scene via `/api/sync/spaces/:id/*` regardless of grants, and no window in the entire Beta lane could be dragged or resized | **Sync auth bypass:** `syncRoutes.js` has no `/api/sync/spaces/:spaceId` middleware setting `req.requiredSpaceId`, so the global per-space scope check silently no-ops (`canAccessSpace(state, null)` always passes). **Beta drag/resize dead:** `DesktopWindow`'s pointermove/pointerup-attaching effect gated on a mutable ref (`interactionRef.current`) instead of the `dragMode` *state* and excluded `dragMode` from its dependency array — starting a drag never re-ran the effect, so listeners were never attached, for every window in the entire lane. Other 14, by area: rate limiter trusted attacker-suppliable first-hop X-Forwarded-For (defeats all abuse throttling); `data-cleanup.mjs`'s prod-delete confirmation read the wrong field and always showed "0 spaces"; `jsonStore.readJson` silently overwrote corrupted files with a truncated recovery, no backup; Studio's "Save current view" used a nonexistent camera-controls API (`.object`/`.target`) and silently no-opped; a document-load race could revert the project on slow connections; client/server typeId validation diverged on `createNode`, causing permanent per-client document drift; presentation fixed-camera lacked a far>near guard (degenerate frustum); anonymous inscriptions had a lost-update race (no CAS, unlike `/ops`); Drive OAuth tokens fell back to a hardcoded, publicly-visible encryption key; project delete could leave a space silently unpublished on partial failure (2 components); shared assets went stale in the commons after a space move/delete; Studio's numeric fields couldn't accept a typed leading `-`; a dead no-op cleanup stub masked what FK cascade actually does; and a Beta density feature was half-wired (stub function, orphaned CSS, unused field) | Sync routes: added the missing `req.requiredSpaceId` middleware (mirrors `/api/spaces/:spaceId`). Beta drag: effect now gates on `dragMode` state (in its deps), `draftRef` replaces the closed-over `draft` read in the pointerup handler. Rate limiter: key on the LAST XFF hop (nginx's own append), not the first. Full fix list is 16 files across `serverXR/src/{routes/syncRoutes.js,rateLimit.js,jsonStore.js,driveTokenStore.js,projectStore.js,spaceStore.js,commonsStore.js,routes/inscriptionRoutes.js,index.js}`, `src/{shared/projectSchema.js,studio/components/StudioEditor.jsx,studio/components/StudioInspector.jsx,studio/components/StudioHub.jsx,studio/components/StudioProjectsPanel.jsx,project/hooks/useProjectDocumentSync.js,beta/components/DesktopWindow.jsx,beta/utils/deviceDetection.js}`, `shared/projectSchema.cjs`, `scripts/data-cleanup.mjs`. Regression guards: `httpContracts.test.js` (sync-route scope denial), `DesktopWindow.test.jsx` (new file, drag→onPatch), `jsonStore.test.js` (new file, corruption backup), `rateLimit.test.js` + `projectSchema.test.js` (updated to assert the corrected behavior) | see file list above |
| Viewport goes permanently black mid-session: console shows "WebGL context was lost." / "THREE.WebGLRenderer: Context Lost." and nothing recovers until a manual page reload (seen on staging open-jam, Firefox/Linux) | GPU driver resets, memory pressure, or too many concurrent GL contexts make the browser kill the WebGL context. three calls `preventDefault()` on `webglcontextlost` (so restoration is possible), but no app code listened for lost/restored — and on Linux/Mesa the browser often never fires `webglcontextrestored`, so the dead canvas persisted forever | Shared `useWebglContextGuard` + `WebglContextLostOverlay` wired into all four R3F surfaces (StudioViewport, SceneCanvas, LiveProjectScene, BetaViewport): on loss show an overlay and auto-remount the Canvas once (key bump after 2s — a fresh element gets a fresh context); browser-restored contexts just clear the overlay; repeated losses (GPU genuinely under pressure) wait for the overlay's "Restore 3D view" button instead of remount-looping. Regression guard: `WebglContextGuard.test.jsx` (3 cases) | `src/components/WebglContextGuard.jsx`, `webglContextGuard.css`, `src/studio/components/StudioViewport.jsx`, `src/components/SceneCanvas.jsx`, `src/components/LiveProjectScene.jsx`, `src/beta/components/BetaViewport.jsx` |
| 6-dimension full audit (2026-07-16, 6-agent parallel) found login CSRF: GitHub/Google `passport.authenticate` never requested a `state` param, so the OAuth authorize→callback round-trip had zero anti-CSRF binding — an attacker could start their own login flow and lure a victim into completing it, silently signing the victim into the attacker's account | Both strategies are constructed with no `state`/`store` option, so passport-oauth2 falls back to `NullStore` (`node_modules/passport-oauth2/lib/state/null.js` — `verify` always calls back `true` regardless of the value), and this app's `session: false` login flow can't use passport's built-in session-backed state store anyway | Hand-rolled signed/timing-safe/TTL'd `state` param (same HMAC pattern as the existing Drive-connect flow in `integrationRoutes.js`, just not bound to a user id since no one's signed in yet): `signLoginState()` is passed as the literal `state:` string to `passport.authenticate(...)` on the `/api/auth/{github,google}` routes (passport sends it straight through, bypassing NullStore on the way out); `requireValidLoginState` middleware verifies it by hand on the `/callback` routes before `passport.authenticate` runs, since NullStore would otherwise accept anything. A parallel finding — project routes ostensibly missing the space-membership auth checks `spaceRoutes.js` has — turned out to be a false positive on investigation: `index.js` already sets `req.requiredSpaceId` upstream for every `/api/projects/:projectId*` route via `resolveProjectContext`, and the shared `requireReadRole`/`requireWriteRole` gates enforce `canAccessSpace` on it before the route file ever runs. The one real gap found there was narrower: `/api/projects/:projectId/assets` POST had no rate limiter, unlike the equivalent space-asset upload route — fixed with the same `uploadLimiter` | `serverXR/src/routes/authRoutes.js`, `serverXR/src/index.js` |
| Same audit: staging's 0.4/0.2-vCPU ceiling on `docker-compose.staging.yml` and production's `docker-compose.yml` CPU limits were never actually enforced — staging could in theory starve production despite the whole point of the small-footprint design being that it couldn't | `deploy.resources.limits` is a Swarm-mode-only Compose key; plain `docker compose up` (no `--compatibility`, no Swarm, which is how both `deploy-vps.yml`/`deploy-vps-staging.yml` invoke it) parses and then silently ignores it entirely — the numbers were documentation, not a real ceiling | Switched every service in `docker-compose.yml` and `docker-compose.staging.yml` from `deploy.resources.limits.{cpus,memory}` to the top-level `cpus`/`mem_limit` keys, which the Docker Engine applies directly regardless of Swarm/`--compatibility`. Kept the exact same numbers (they already summed to a correct 1.8 vCPU / 2 vCPU host budget from an earlier rebalance) — only the enforcement mechanism changed | `docker-compose.yml`, `docker-compose.staging.yml` |
| Same audit: Space Hub cards loaded the full-resolution original for every space's preview image (no thumbnails anywhere), and `GET /api/spaces` had no pagination — both fine at today's scale, both would bite as the space/sandbox population grows | No image-resize path existed at all; `listSpaces()` (`SELECT * FROM spaces ORDER BY updated_at DESC`) had no LIMIT, and its `GET /api/spaces` handler filters (isPublic/canAccessSpace/sandbox synthesis) the *entire* result in JS before responding, so pagination has to happen after filtering, not in SQL | Added `sharp` (ships prebuilt Alpine/musl binaries — safe for the `node:22-alpine` Docker build target, unlike the cPanel-era native/WASM issues logged above, which were about a restricted shared-hosting environment this app no longer deploys to). `serveAsset`'s `?w=<width>` query param resizes+caches a `.thumb-<width>.webp` variant per (asset, width) next to the original (clamped to 640px, images only, falls back to the original on any failure); asset delete now also sweeps cached variants (`removeAssetThumbnails`). `SpaceHub.jsx`'s preview `<img>` requests `?w=480`. `GET /api/spaces` pagination is opt-in via `?limit=`/`?offset=` — omitting them returns the full list exactly as before (no existing caller passes them yet), but `total`/`offset`/`limit`/`hasMore` are now available for a future "load more" UI | `serverXR/src/spaceStore.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/index.js`, `src/services/serverSpaces.js`, `src/studio/components/SpaceHub.jsx` |
| Same audit: `client`/`caddy` had no `healthcheck` (only `server` did — `caddy`'s `depends_on: client` was `service_started`, so it could start proxying to a container that was up but never actually serving), and no service anywhere capped its log driver, so 24/7 containers on a small VPS had no ceiling on `json-file` growth | Never added when the compose stack was first written — `docker inspect`/`docker compose logs` on the real host confirmed no `max-size` anywhere and the default json-file driver is genuinely unbounded | Added `logging: {driver: json-file, options: {max-size: "10m", max-file: "3"}}` to every service in `docker-compose.yml` (the staging override inherits it — `logging` is a mapping field, and staging's file never sets that key, so plain Compose merge picks up the base's). Added a `wget`-based `healthcheck` to `client` (hits `/`) and `caddy` (hits its own loopback-only admin API at `:2019/config/` — independent of TLS/domain state, so it can't false-negative on the inert `STAGING_DOMAIN` block with no DNS yet); `caddy`'s and `tunnel`'s `depends_on: client` tightened from `service_started` to `service_healthy` now that the healthcheck exists | `docker-compose.yml` |
| **PRODUCTION OUTAGE (2026-07-16):** the `client`/`caddy` healthcheck fix directly above took production down for ~5 minutes on the very next real deploy — `dii-client-1` was recreated, never passed its new `wget -qO- http://localhost/` healthcheck, timed out to unhealthy, and `dii-caddy-1` — now gated on `depends_on: client: condition: service_healthy` — never started at all, taking down the only container publishing ports 80/443 | Not fully confirmed, but the leading theory is `nginx:alpine`'s official image doesn't ship `wget`/busybox the way `node:22-alpine` does (whose own healthcheck, using the identical command, has always worked) — the check itself may simply have been unrunnable inside that container. This was never verified against a real container before shipping | Reverted immediately: dropped the `client` and `caddy` healthchecks and both `depends_on: client` conditions back to `service_started` (their pre-existing state), keeping the unrelated/harmless `logging:` caps. Site confirmed back at 200 within ~5 min. **Do not re-add a `client`/`caddy` healthcheck without first confirming the exact check command actually runs inside that specific image** — e.g. `docker run --rm <image> sh -c 'which wget \|\| which curl'` — before it ever reaches a real deploy | `docker-compose.yml` |
| **OAuth sign-in on di-studio.xyz intermittently failing with "Sign-in failed — please try again." (2026-07-16)**, seen the same day as the login-CSRF `state` audit fix above and several production redeploys/container restarts (healthcheck incident + revert) | The fallback-secret theory below (first fix attempt) turned out to be wrong — `AUTH_SESSION_SECRET` was already correctly configured on prod, confirmed by SSHing into the VPS. **The real bug, found the same way (curl the live endpoint, look at the actual response) once the earlier fix "worked, then broke again ~24 minutes later":** `router.get('/api/auth/github', passport.authenticate('github', { state: signLoginState(stateSecret) }))` calls `signLoginState()` — and therefore `passport.authenticate(...)` — exactly **once**, at route-registration time (server startup), not per request. That one state value gets baked into the closure and reused for every `/api/auth/github` hit for the rest of the process's life. Live-verified: two `curl` requests to prod seconds apart returned the byte-identical `state` in the redirect. `verifyLoginState` rejects any state older than `STATE_TTL_MS` (10 min), so sign-in only worked within 10 minutes of container start and failed for everyone — both GitHub and Google — after that, until the next restart. This is why the first fix appeared to work immediately after its own redeploy (fresh state, timestamp ≈ startup) and then broke again once 10 minutes passed | **Actual fix:** wrapped `passport.authenticate(...)` in a per-request handler — `(req, res, next) => passport.authenticate('github', { state: signLoginState(stateSecret) })(req, res, next)` — so the state is signed fresh on every hit. Regression test calls the authorize handler twice and asserts the two `state` values differ. Verified live on prod post-deploy: two direct `curl`s to `/api/auth/github` and `/api/auth/google` each now return different `state` values. The fallback-secret fix from the first attempt (unchanged, still shipped) remains a legitimate hardening for any deployment that legitimately runs with no `AUTH_SESSION_SECRET` — it just wasn't the cause of *this* incident. **`AUTH_SESSION_SECRET` was confirmed already set on both prod and staging** (`/opt/di.iiii/.env`, `/opt/di.iiii-staging/.env` via `STAGING_AUTH_SESSION_SECRET`) — nothing to configure there. Separately, **staging's OAuth was found fully unconfigured** (`GITHUB_CLIENT_ID`/`SECRET`/`GOOGLE_CLIENT_ID`/`SECRET`/`OAUTH_CALLBACK_BASE_URL` all empty in `/opt/di.iiii-staging/.env`, confirmed via `GET /api/auth/providers` returning `{github:false,google:false}`) — since resolved: user created a dedicated "staging di" OAuth App on GitHub and used a pre-existing dedicated "staging di" Google OAuth client, both configured with staging's callback URLs. **Gotcha that cost real time**: `docker-compose.staging.yml` maps the container's `GITHUB_CLIENT_ID`/`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`OAUTH_CALLBACK_BASE_URL` env vars from `STAGING_`-*prefixed* host variables (`${STAGING_GITHUB_CLIENT_ID:-}` etc.), not the bare names — editing the bare `GITHUB_CLIENT_ID=` line in `/opt/di.iiii-staging/.env` (which also exists in the file, left over from the shared base `.env.example` template) has zero effect on the running container; only the `STAGING_`-prefixed lines matter there. `GET /api/auth/providers` now returns `{github:true,google:true}` on staging and both flows were verified live end-to-end (real GitHub + Google sign-in) | `serverXR/src/routes/authRoutes.js`, `serverXR/src/routes/authRoutes.test.js`, `serverXR/src/routes/integrationRoutes.js` |
| Visiting `staging.di-studio.xyz/wcc` (or `/br_id_ge`) showed "Access restricted — your session isn't scoped to 'wcc'. Allowed: open, sandbox-guest\<id\>" — looked like a broken auth/scoping bug | Not a bug — staging's database is genuinely empty of that content. Staging was set up as a brand-new, isolated environment (2026-07-15/16) with its own DB from scratch, specifically so deploys could be tested without touching prod's real data; nothing had ever synced prod's actual spaces (`wcc`, `br-id-ge`, `beyond-form` — real content built over months in Studio) into it. Staging only auto-creates the default `open` space at boot. Note the *id* uses hyphens not underscores (`br-id-ge`, `beyond-form`) — `br_id_ge`/`beyond_form` (with underscores) are the display/repo names, not the space id; `scripts/space-bundle.mjs`'s slug regex (`^[a-z0-9-]{3,48}$`) will reject the underscore form outright, which is a useful early signal if you get the id wrong | Used `scripts/space-bundle.mjs export <id>`/`import <bundle>` — an existing tool built for exactly this ("move a space to another install," strips secrets/sync-keys) — to copy `wcc`, `br-id-ge`, `beyond-form` from prod's DB into staging's. Ran it via `docker cp` into each running container (the script isn't baked into the production image — dev/ops tool only) and `docker exec`'d it there, since that's where Node + the actual `/data` volume both already are. Hit one wrinkle: `import`'s `require('../serverXR/src/db.js')` is a literal relative path from the script's own file location, but the container's image flattens `serverXR/src/` to `/app/src/` (no `serverXR/` prefix) — fixed by placing the script at `/app/scripts/space-bundle.mjs` and symlinking `/app/serverXR → /app` so the relative require resolves. Verified prod's `updatedAt` timestamps were unchanged after export (read-only), and all three spaces return `isPublic: true` from staging's `/api/spaces/:id` afterward. Cleaned up the temp script/bundle files from both containers and the VPS host when done | `scripts/space-bundle.mjs` (pre-existing, unmodified — this incident was ops/data, not a code change) |
| **Full 6-phase repo audit (2026-07-16, per-lane parallel agents: serverXR, schema/op-log, node system, 3D/viewport, Studio/Beta frontend, infra/deploy) found ~28 issues; the top 5 were fixed the same session** — see below for what shipped and what didn't | (1) `syncRoutes.js`'s `/api/sync/spaces/:spaceId/{status,pull,push}` never sanitized `req.params.spaceId` before using it in a filesystem path, and the auth-scope middleware (`index.js`) treats a normalization failure as `requiredSpaceId = null` → "unscoped, allow" (`authAccess.js`'s `isAuthScopeAllowedForSpace`) — a scoped user could reach a traversal payload (e.g. `..%2f..%2fetc` decoded by Express into a literal `..` id) that bypasses scope and hits the filesystem raw. (2)+(3)+(4) `POST /api/spaces/:spaceId/ops`, `POST /api/projects/:projectId/ops`, `PUT /api/projects/:projectId/document`, and `replaceSceneAndBroadcast` all check a version number across multiple `await`s (read version → read document → compute → write document → append ops → bump meta) with no atomicity around the whole sequence — two concurrent requests at the same `baseVersion` both pass the check and both write, one silently clobbering the other while both callers got `200`; `project_ops`/`space_ops` also had a non-unique index on `(id, version)`, so duplicate-version rows landed with no rejection and no read-order tiebreaker. (5) audit claimed no automated backup existed — **false positive**, the audit only reviewed the git repo; a working nightly backup cron (`/root/vps-backup.sh`, `17 3 * * *`) was already live on the VPS, just never committed to the repo, so nothing here documented it | (1) `syncRoutes.js` now normalizes+rejects the space id with 400 before touching the filesystem, same pattern every other route already used — regression test hits all three routes with traversal payloads. (2)(3)(4) new `serverXR/src/asyncLock.js` (`createKeyedLock`) serializes the whole check-then-write per space/project id, re-fetching the current version *inside* the lock rather than trusting a pre-lock snapshot; applied to all four call sites. `db.js` gained a `dedupeAndUniqueOps` migration — deletes any pre-existing duplicate-version rows (keeps highest `seq`) then makes `idx_space_ops`/`idx_project_ops` genuinely `UNIQUE`, so a future reintroduction of this bug class fails loudly instead of silently corrupting data; `opsSelect` queries also gained a `seq ASC` tiebreaker. Regression tests fire two **truly concurrent** (`Promise.all`, not sequential) HTTP requests at a real spawned server and assert exactly one 200 + one 409, plus DB-level tests proving the UNIQUE index rejects a duplicate insert and the migration dedupes pre-existing ones. Golden rule added for the general pattern. (5) committed the backup script into the repo for the first time (`deploy/vps-backup.sh`, mirrors the live `/root/vps-backup.sh` exactly) plus a new companion `deploy/vps-restore.sh` that didn't exist before — validated by dry-running the extraction against a disposable scratch Docker volume (never touching production) and confirming the restored DB opens and contains real data. Documented both in `docs/deploy/VPS_DOCKER_DEPLOY.md`, including the still-open gap (backups are VPS-local only, no off-box copy yet) | `serverXR/src/routes/syncRoutes.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/routes/projectRoutes.js`, `serverXR/src/asyncLock.js`, `serverXR/src/db.js`, `serverXR/src/spaceStore.js`, `serverXR/src/projectStore.js`, `deploy/vps-backup.sh`, `deploy/vps-restore.sh`, `docs/deploy/VPS_DOCKER_DEPLOY.md`, `docs/ai/golden_rules.md` |
| Audit finding #10: GHCR image pull auth on the VPS was undocumented — could be either a silent public-exposure or a working setup nobody wrote down | Checked directly: no `docker login` existed on the VPS (`~/.docker/config.json` empty), yet every deploy's `docker compose pull` had been succeeding — proved via an anonymous, credential-free `curl` against the GHCR API that both `dii-server` and `dii-client` were **publicly pullable** (200 on an anonymous pull token + tags/list). Never a deliberate decision — just GHCR's default visibility, never explicitly set, undermining the point of the repo being private (compiled backend/frontend source was pullable by anyone with the URL, though not live secrets — those come from runtime env vars, not the image) | User flipped both packages to private via GitHub's package settings UI (re-verified after: anonymous pull now 403). Fine-grained PATs turned out not to support GHCR package permissions at all (no "Packages" option in the account-permissions picker) — had to fall back to a classic PAT (`read:packages` scope only) instead. `docker login ghcr.io` as root on the VPS, once — persists in `/root/.docker/config.json` and covers both prod and staging (same Docker daemon, same box). Verified both images still pull successfully post-login. Documented in `docs/deploy/VPS_DOCKER_DEPLOY.md` including rotation steps; the token itself lives only on the VPS, never in this repo or CI | `docs/deploy/VPS_DOCKER_DEPLOY.md` (no code — this was an infra/access finding) |
| Audit finding #17 claimed `updateNode`'s raw `patch.values` merge was unnormalized (unlike `updateEntity`/`updateComponent`, which route through `normalizeEntity`) — read as an exploitable "unbounded garbage in node.values" gap | **Correction on closer inspection: not actually exploitable.** `applyProjectOps` (`src/shared/projectSchema.js`/`shared/projectSchema.cjs`) already calls `normalizeProjectDocument` on the *entire* document right before returning, regardless of which op ran — that call normalizes every node via `normalizeNodesList` → `normalizeProjectNode` anyway, so `updateNode`'s merged result was already being normalized, just via an outer pass rather than inline in its own case. Verified directly: a crafted non-string `assetRef` patch produced `null` in both the pre-fix and post-fix code, proving no behavior difference exists in the current pipeline | Routed the merge through `normalizeProjectNode` inline anyway — not because it changes behavior today, but for architectural consistency with the sibling `update*` cases (each op's own code should be locally correct, not depend on an external final-pass contract) and as defense-in-depth if a future refactor ever applies a single op without that outer wrapper. Applied identically to both the ESM and CJS mirror; `schemaSync.test.js` confirms parity | `src/shared/projectSchema.js`, `shared/projectSchema.cjs` |
| Audit finding #24: `shared/projectSchema.cjs` (the hand-maintained CJS mirror) was missing three exports the ESM original has — `defaultPresentationFixedCamera`, `defaultWorkspaceState`, `normalizeWorkspaceState` — a silent drift of exactly the kind the schema-sync equivalence tests exist to catch, just on the export surface rather than normalization behavior | The CJS mirror is hand-written (not generated), so any new ESM export has to be remembered and copied manually — these three were added to `src/shared/projectSchema.js` at some point without the matching `module.exports` update, and nothing checked export-surface parity, only behavioral equivalence on a fixed fixture set | Added the three missing exports to `shared/projectSchema.cjs`. `serverXR/src/schemaSync.test.js` gained a regression test asserting all three exported members match their ESM counterparts, so a future one-sided export addition fails the schema-sync gate instead of silently drifting | `shared/projectSchema.cjs`, `serverXR/src/schemaSync.test.js` |
| Audit finding #28: `AudioObject.jsx`'s HTML-`<audio>` fallback (used for formats `PositionalAudio` can't decode, e.g. FLAC) never released the underlying `Audio` element on real component unmount — its per-render effect's cleanup only called `.pause()`, unlike the sibling early-return branch a few lines above it (triggered when `canUsePositional` flips true) which correctly cleared `.src` and nulled the ref. A removed audio node left a live `HTMLAudioElement` (and its decoder/network resources) referenced only by a stale ref, never garbage collected | The per-render cleanup can't simply clear `.src` either, because it re-runs on every dependency change (volume/loop/paused/sourceUrl), and clearing `.src` there would interrupt a legitimate in-flight source swap, not just a real unmount | Added a second, `[]`-dependency effect whose only job is real-unmount teardown (`.pause()` + `.src = ''` + null the ref) — same pattern already used for GLTF disposal in `ModelObject.jsx` earlier in this audit pass. Regression test spies on the global `Audio` constructor, mounts with a forced-fallback (`.flac`) source, unmounts, and asserts `.pause()` was called and `.src` was cleared; confirmed failing against the pre-fix code via `git stash` before shipping | `src/objectComponents/AudioObject.jsx`, `src/objectComponents/AudioObject.test.jsx` |
| Audit finding #25: "No general schema-migration mechanism" for the project document shape (`shared/projectSchema.cjs`/`src/shared/projectSchema.js`) — confirmed real, not a false positive | `PROJECT_DOCUMENT_VERSION` is stamped onto every document but never read/compared/branched on anywhere — it's a one-way stamp, not a version gate. The only thing standing in for migration is defaulting-tolerant normalization (each `normalize*` helper defaults a missing field), which only covers *additive* schema changes; a breaking change (rename, enum change, restructure) has no detection or transform path today | **Deliberately documented as accepted debt rather than code-fixed this pass** — a real migration mechanism (versioned transform registry dispatched by `source.version`) needs a design pass across ~8 call sites that construct/read project documents, and no breaking document-shape change has actually happened yet to force the issue; building one speculatively risked exactly the kind of premature abstraction this project's own operating rules warn against. Documented in full in `docs/architecture/PROJECT_SURFACES.md` ("Document Schema Versioning" section), including the concrete rule for next time: any *breaking* `projectSchema` change must ship its own explicit read-time transform + fixture regression test, not rely on normalization's defaulting | `docs/architecture/PROJECT_SURFACES.md` (no code — documentation-only, matches audit finding #5's backup-script precedent of "verify/document rather than assume") |
| Audit finding #23: `nodeGraphRuntime.js`'s single-pass graph evaluation (`evaluateNodeOutput`/`evaluateNodeInput`) had no memoization — a node feeding two or more downstream consumers (a diamond dependency, e.g. one `sin`/color/value node driving both the viewport light and grid, or multiple `BetaViewport` nodes sharing an upstream math chain) got fully recomputed from scratch once per consumer per render, instead of once per pass. Perf-only (no wrong output, just wasted CPU that scales with fan-out and graph depth) | The existing `stack` parameter is a `Set` used purely for cycle detection — it's rebuilt fresh on every top-level `evaluateNodeOutput`/`evaluateNodeInputs` call and never shared across sibling call chains within the same render, so it never doubled as a cache | Added `outputCache: new Map()` to the context object `createNodeGraphContext` already builds once per pass (memoized via `useMemo(..., [document])` at every call site — `BetaViewport.jsx`, `BetaEditor.jsx`, `viewportWorldState.js` — so one cache naturally spans every node evaluated in that pass). `evaluateNodeOutput` checks the cache by `${node.id}:out:${portId}` before computing (factored the switch into a new `computeNodeOutput` helper) and writes the result after. Caching is safe through the cycle-detection short-circuit for every node that ISN'T itself part of a cycle: the `stack.has(key) → return undefined` truncation happens before the cache write, so only nodes actually sitting on the cycle stack ever get a truncated result cached — an acyclic node reached via two different paths always computes and caches its one true value before either caller sees it. (A node genuinely inside a cycle has no well-defined value to begin with — memoization just makes whichever consumer evaluates first fix that frame's arbitrary truncated result for the rest of the pass, which is fine since cyclic output was never meaningful.) Regression test spies on `Math.sin` (the only thing `math.sin` calls) with a shared upstream `sin` node feeding two `math.add` consumers and asserts it's called exactly once across both; confirmed calling it twice pre-fix via `git stash` | `src/project/graph/nodeGraphRuntime.js`, `src/project/graph/nodeGraphRuntime.test.js` |
| Audit finding #26: `Dockerfile`, `serverXR/Dockerfile`, and `docker-compose.yml` (`caddy`/`tunnel`) all pinned base images to floating tags (`node:22-alpine`, `nginx:alpine`, `caddy:2-alpine`, `cloudflare/cloudflared:latest`), not digests — a rebuild/pull tomorrow could silently use a different actual image than today's, with no diff or review | Never deliberately decided — just never pinned. Confirmed the actual image behind each tag via `skopeo inspect` rather than guessing | Re-pinned all four to `tag@sha256:<digest>` (the digest each tag currently resolves to, so this change is a no-op for what's actually running — zero deploy risk from image content changing). Documented the trade-off (no automatic security patches — deliberate, re-pin manually) and the re-pin procedure in `docs/deploy/VPS_DOCKER_DEPLOY.md` ("Base Image Pinning"), including the caution that `docker-compose.yml` is the same file that caused the 2026-07-16 healthcheck outage, so future edits here get the same care | `Dockerfile`, `serverXR/Dockerfile`, `docker-compose.yml`, `docs/deploy/VPS_DOCKER_DEPLOY.md` |
| Audit finding #27: the `client` container (nginx serving the built SPA) ran as root, unlike `server` which already had a dedicated non-root `app` user — an inconsistent hardening posture, and a real risk if nginx or a served asset were ever exploited (root inside the container, though the base image already avoided some worse defaults) | Never addressed when the client Dockerfile was first written; server got the non-root treatment, client didn't | Swapped `nginx:alpine` for `nginxinc/nginx-unprivileged:alpine` (official pre-hardened variant, runs as `nginx` user, listens on 8080) rather than hand-patching root-owned nginx directories with no Docker daemon available locally to test the result first — see the user's explicit choice between options when asked. Rippled the resulting 80→8080 container-internal port change through `nginx.conf`, `docker-compose.yml` (client ports/healthcheck/tunnel target), and `Caddyfile` — all host-facing ports are unchanged, only the internal one moved. Full writeup incl. the one residual unverified risk (whether this image ships `wget` the same way plain `nginx:alpine` was confirmed to) in `docs/deploy/VPS_DOCKER_DEPLOY.md` ("Client Container Non-Root Hardening") — watch the first staging deploy closely, same as the 2026-07-16 healthcheck incident | `Dockerfile`, `nginx.conf`, `docker-compose.yml`, `Caddyfile`, `docker-compose.caddy-hardened.yml`, `docs/deploy/VPS_DOCKER_DEPLOY.md` |
| Beta deep audit (2026-07-16/17): Node 0 is a normal, deletable node whose card is deliberately hidden from the graph canvas ("topbar is node0's presence"), but the *entire* Beta topbar (back-to-hub, breadcrumb, Chat, Outliner, Help, node-size control) is gated on it existing (`hasNodeZero`) — deleting it silently removed that whole UI with no warning and no way back except double-clicking the canvas to place a fresh one | Two separate, both-reachable deletion paths existed with no guard: `handleDeleteSelected` (the "Delete" FAB / Delete key off the graph surface) and the `onDeleteNode` callback passed to `BetaGraphSurface` (the graph canvas's own internal Delete/Backspace listener). The second one is non-obvious — it fires off `workspaceState.selectedNodeId` globally, and Node 0 *can* be selected via the Outliner (which lists all graph-surface nodes, Node 0 included, since `matchesNodeTypeSurface` always returns true for the 'graph' surface) even though it has no visible graph-canvas card | Added `isRootGraphNode(node, rootTypeId)` — pure, testable — to `src/project/graph/nodeGraphAuthoring.js` (shared engine, so Studio's graph view gets the same guard for free once it gains write access; it's read-only today). Both `BetaEditor.jsx` call sites now `window.confirm(...)` before deleting a node this returns true for, matching the existing destructive-action pattern already used for project/space deletion elsewhere. Regression tests cover both call sites, a confirm-and-cancel-survives-intact case, and a normal-node-never-prompts case; verified live — selected Node 0 via the Outliner, clicked Delete, confirmed the real browser `confirm()` fires with the right message and Node 0 + the topbar both survive a Cancel | `src/project/graph/nodeGraphAuthoring.js`, `src/beta/components/BetaEditor.jsx` |
| Mouse-look (pointer-lock/drag camera rotation) silently stopped working on `/wcc/scene` and any other public project with `worldState.spawn` set — WASD movement worked, `?inputdebug=1` showed healthy lock state and changing `yaw`/`pitch` numbers, but the rendered camera never turned. Pre-existing bug (not caused by the 2026-07-17 perf audit), found by `git log -L` blaming the exact effect rather than guessing/bisecting build changes | Commit `a79c689c` (2026-06-29, "data-driven spawn") reassigned `playerRef.current = {...}` (a whole new object) once a project's `worldState.spawn` loaded. `Walker`'s mouse/touch-look listeners are wired up in a mount-only `useEffect` (deps include the stable `playerRef` container, not `.current`) that closed over the *old* object at mount time — mouse-look kept mutating that orphaned object forever, while the camera's `useFrame` and the debug HUD both read `playerRef.current` fresh, so WASD (also read fresh per-frame) and the yaw/pitch numbers looked fine but the camera never moved | Changed the spawn effect to `Object.assign(playerRef.current, {...})` — mutates the existing object in place instead of replacing it, so the mount-time closure and the per-frame reader stay pointed at the same object | `src/components/LiveProjectScene.jsx` |
| User onboarding to Beta felt wrong for the intended TouchDesigner-style workflow: a brand-new project forced a manual "double-click to place Node 0" step before anything else was possible, then auto-entered its interior and hid its topbar/whole-UI behind that first click — user explicitly said this didn't match "enter any node like a folder, Node 0 is just the root dir" | Node 0 (`universe.node0`) was special-cased everywhere: `useNodeGraphScope({ rootTypeId: NODE_ZERO_TYPE_ID })` auto-created-and-entered it on load, the whole topbar was gated on `hasNodeZero`, its graph card was hidden ("topbar is node0's presence"), and an audit pass the same day had just added it to `SINGLETON_TYPE_IDS`/`nodeRegistry.js`'s `singleton:true` as a consistency bug fix — all of this assumed Node 0 must be a forced, unique, auto-entered root, which is exactly the model the user rejected | **Product decision 2026-07-17, confirmed explicitly by the user: Node 0 is now an ordinary node type — placeable, deletable, enterable exactly like any other node, no auto-creation, no auto-entry, no singleton enforcement.** Reverted the same-day singleton fix (`SINGLETON_TYPE_IDS` in both schema files, `nodeRegistry.js`'s `singleton:true`) — this is an intentional reversal, not a missed regression, flagged in comments at every reverted site so a future audit pass doesn't silently re-add it. `BetaEditor.jsx`: dropped `rootTypeId` from `useNodeGraphScope` (the hook's generic auto-enter-root effect now never fires — true document root, `currentScopeId === null`, is just a plain always-available scope), removed `handleStartFromNodeZero`/`hasNodeZero`/`NODE_ZERO_TYPE_ID`, topbar chrome now always renders (no more collapsed pre-seed state), double-click always opens the node palette directly, deleting any node (Node 0 included) no longer shows a special confirm dialog. Regression tests rewritten to match (`BetaEditor.test.jsx`, `nodeRegistry.test.js`, `projectSchema.test.js`) | `src/beta/components/BetaEditor.jsx`, `src/beta/components/BetaEditor.test.jsx`, `src/project/nodeRegistry.js`, `src/project/nodeRegistry.test.js`, `src/shared/projectSchema.js`, `shared/projectSchema.cjs`, `src/shared/projectSchema.test.js`, `docs/architecture/RECURSIVE_NODE_CORE.md` |
| User report: "after walk/fly, to go back to previous view mode you need to exit and enter space again" on a real published space (not the landing page's decorative background — that was a wrong first guess; also added a real, separate view-toggle there, but it wasn't the actual complaint). The mechanism actually already worked (confirmed live: click "Walk / Fly" → walk around → click the header button → back to the orbit viewer, `Walk / Fly` button reappears) — the real defect was the label | `PublicProjectViewer.jsx` renders `LiveProjectScene` in walk mode with `showChrome`, whose header exit button was hardcoded to the text `← Exit` — identical wording to the landing page's own "Exit space" button, which really does leave the page. In this context the button only swaps `navMode` back to `'orbit'` *within the same page*, so the label read as "this will kick me out" when it wouldn't | Added an `exitLabel` prop to `LiveProjectScene` (default unchanged, `← Exit`, still correct for `WccExperience.jsx`'s real full-exit case). `PublicProjectViewer.jsx` now passes `exitLabel="← View mode"` for its walk-mode round trip. Regression test mocks `LiveProjectScene` to assert the walk-mode button reads "← View mode" (not "← Exit") and that clicking it returns to the `Walk / Fly` button | `src/components/LiveProjectScene.jsx`, `src/project/components/PublicProjectViewer.jsx`, `src/project/components/PublicProjectViewer.test.jsx` |
| User wanted to freely place multiple Worlds/Lights/Backgrounds/Grids (and generally build "like lego") instead of hitting the one-per-scope restriction — a session earlier the same day had shipped a warning UI for exactly that block, then the user asked for the opposite. Broader than a single bug: the request evolved (via a deep research pass across TouchDesigner, Houdini, Blender, Nuke, Unreal Blueprints, vvvv, Resolume, Cables.gl, Max/MSP, VCV Rack, QLab, Ableton) into "no node type should be a singleton at all" | The remaining singleton restrictions (`time`, `source.ar`, `universe.world`, `world.light`, `world.background`, `world.grid`) were the same class of forced-uniqueness the Node 0 reversal above already rejected for one type — just not yet generalized. `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` were still enforcing it in both schema files | **Product decision 2026-07-19: no node type is a singleton, anywhere, full stop.** Deleted `SINGLETON_TYPE_IDS`/`SCOPE_SINGLETON_TYPE_IDS`/`getSingletonDedupKey` outright from both `src/shared/projectSchema.js` and `shared/projectSchema.cjs` (not just unused — gone, so a future pass can't accidentally re-enforce them; **do not re-add without checking with the user first**, same rule as the Node 0 row). Stripped the now-dead `singleton:true` from the 6 affected `nodeRegistry.js` definitions. Removed the same-day blocked-create warning from Beta (`getPaletteBlockReason`, `NodePalette.jsx`'s `getBlockReason` prop, the CSS) since there's nothing left to block. For the few types where exactly one "active" result is still wanted (World/Light/Background/Grid), added an explicit **active marker** instead of a restriction — `workspaceState.activeNodeIdByTypeScope` (new, generic, keyed `` `${typeId}::${scopeId}` ``) alongside the pre-existing `liveWorldNodeIdByScope`, following the "hierarchy is the connection" pattern confirmed via Kantan Mapper (a shipped TouchDesigner tool: adding a mapping shape is just adding a child, no wire). Landed as a **new lane, `src/raw/`**, forked wholesale from Beta rather than patched in place (first lane-fork in the project, no prior graduation policy existed — see `docs/architecture/PROJECT_SURFACES.md`'s "On forking a new lane from Beta" section) — Beta itself keeps its simpler `.find()`-first-match lookups (kept as the original sketch). Also added a universal, fully inert "view as code" inspector section (`values.__code`) to every node type, not just `node.null`. Full design writeup: `docs/architecture/RECURSIVE_NODE_CORE.md`'s "Nesting" and "The `raw` lane" sections | `src/shared/projectSchema.js`, `shared/projectSchema.cjs`, `src/shared/projectSchema.test.js`, `serverXR/src/schemaSync.test.js`, `src/project/nodeRegistry.js`, `src/project/nodeRegistry.test.js`, `src/project/graph/nodeInspectorSections.js`, `src/project/graph/nodeInspectorSections.test.js`, `src/beta/components/BetaEditor.jsx`, `src/beta/components/NodePalette.jsx`, `src/beta/styles/beta.css`, `src/raw/**` (new), `src/RootApp.jsx`, `docs/architecture/RECURSIVE_NODE_CORE.md`, `docs/architecture/PROJECT_SURFACES.md` |
| Live click-through of the free-nesting work above (previous row) found the headline feature didn't actually work: nesting anything *inside* a `universe.world` node — the whole point of "put geometry/light/background inside a World and see it render" — silently landed the new node as a **sibling** of World instead of its child, at any nesting depth. The 3D viewport correctly renders nothing, because nothing is really parented to it. First caught by trusting a wrong UI signal (clicking World's topbar toggle *looked* like scope navigation — a breadcrumb-ish highlight appeared) and reported as working; only re-verified as broken by fetching the raw document from `/api/projects/:id/document` and reading real `parentId` values directly, which is the only way this was actually caught | `universe.world` (and every other `render:'panel-2d'` node type — Text/Image/Browser/stream monitors) is deliberately excluded from `RawEditor.jsx`'s `graphCardNodes` list, because it gets its own floating `DesktopWindow` instead of a graph card. But `onEnterNode`/`handleEnterNode` (the only thing that calls `scopeEnterNode` and actually changes `currentScopeId`) was wired *only* to a graph card's double-click — so panel-2d nodes had no reachable way to enter their own scope at all. World's topbar button and its `DesktopWindow`'s Pin/Minimize/Close only ever toggled window-visibility state (`values.frame`), never scope | Added an `Enter ›` button to `DesktopWindow.jsx`'s header (new `onEnter` prop, renders only when passed), wired in `RawEditor.jsx` to the same pre-existing `handleEnterNode(node.id)` graph cards already used — no scope-navigation logic needed changing, it already handled `universe.world` correctly (including flipping to fullscreen 3D), it just had no caller. Applies to every panel-2d type uniformly, consistent with "no node type is exempt from free-nesting". Verified live at both 1 level (root World) and 4 levels deep (`Light → Add → World`): new children now get the correct `parentId`, confirmed via the same raw-document API fetch that caught the bug, and render live in the 3D viewport (a cube/sphere actually appears on the grid) | `src/raw/components/DesktopWindow.jsx`, `src/raw/components/DesktopWindow.test.jsx`, `src/raw/components/RawEditor.jsx`, `src/raw/components/RawEditor.test.jsx` |
| Landing "Open Studio" (and nav "Studio") dumped guests into the stripped-down jam editor at `/open/studio/projects/open-jam` instead of the studio hub | The open-jam work made the open-space hub auto-forward into the communal jam project ("door, not a lobby", `StudioHub.jsx`); the landing CTAs already pointed at `/open/studio`, so every "Open Studio" click hit the door | Point `OPEN_STUDIO_HREF` at `/open/studio?browse=1` (the hub's existing forward opt-out); "Step inside" keeps the plain door URL on purpose. Regression tests lock both hrefs | `src/landing/LandingPage.jsx` |
| Browser Back is dead after "Step inside": a visitor who enters the open space from the landing page can never get back out, Back just re-enters the jam editor | The open-space auto-forward used a *pushing* navigate, so history read landing → `/open/studio` → `…/projects/open-jam`. Back popped to `/open/studio`, whose forward effect fired again and pushed the visitor straight back in — a self-rebuilding trap | Forward with `{ replace: true }` so the door consumes its own history entry and Back reaches whatever preceded it. `appNavigate` already supported the option. Regression test asserts the option, not just the path | `src/studio/components/StudioHub.jsx` |
| Raw lane: nesting worlds a few levels deep freezes the tab — console fills with "WebGL context was lost."/"THREE.WebGLRenderer: Context Lost.", the context-guard overlay flickers, and the page stops responding entirely (worse than the single-canvas loss two rows up, which recovers) | `RawEditor`'s panel-window list filtered the *whole document* (`nodes.filter(isPanelNode)`) instead of the current scope, so every `universe.world` node at every depth kept a live `<Canvas>` mounted in every scope. Browsers cap concurrent WebGL contexts (~16); past that they kill the oldest, `useWebglContextGuard` answers a loss by remounting the Canvas, that remount evicts another context, and the guard cascades into an unbreakable remount loop. Fullscreen world made it worse — the panel viewports stayed mounted underneath a view that covered them completely | Pure selector `selectMountedPanelNodes` (`src/raw/utils/windowLayout.js`): mount only panels whose `parentId` matches `currentScopeId` (panels are now scoped exactly like graph cards) and mount none at all while a world is fullscreen — the fullscreen `RawViewport` renders outside the panel list, so nothing blanks. Contexts stay bounded by scope width, not document size. Regression guard: 5 cases in `src/raw/utils/windowLayout.test.js` incl. a 24-deep nesting chain asserting <16 mounts | `src/raw/utils/windowLayout.js`, `src/raw/utils/windowLayout.test.js`, `src/raw/components/RawEditor.jsx` |
| `syncRoutes.test.js > pull proceeds to fetch the live scene when the space is writable` fails with "Test timed out in 5000ms" on some machines/networks and passes on others — nothing in the product changed between runs | The test never injected `httpRequest`, so `registerSyncRoutes` fell back to its real `node:https` default and the handler made an actual network call to `https://live.example` with an 8000ms client timeout — inside a 5000ms test timeout. Offline or behind a slow/hijacking resolver that's a hang; it only "passed" where DNS happened to reject fast. Every route test must inject `httpRequest` — the default is real network | Inject `vi.fn().mockRejectedValue(...)` as `httpRequest` in the read-only-enforcement `setup()` and assert the intent directly (`httpRequest` was called ⇒ the writable check let it through; `next` called with that exact error) instead of the vague "next got something". Suite went 5.7s → 0.7s. Checked the rest of the repo: `httpContracts.test.js` only fetches its own spawned `server.baseUrl`, no other test reaches the real network | `serverXR/src/routes/syncRoutes.test.js` |
| Uploaded photos leaked their EXIF — GPS coordinates, device model, capture timestamp — to anyone who could open the asset URL. Published spaces are public, so a phone photo dropped into a scene published the photographer's location | Originals were stored and served byte-for-byte (`spaceStore.js:508`); a repo-wide grep for `exif` returned zero hits, so nothing ever stripped metadata. sharp thumbnails do drop it, but the thumbnail is a derivative — the full-size original is what a visitor downloads. Found by the 2026-07-28 privacy audit (`docs/ai/privacy-data-inventory.md`), not by a report | Shared `scrubImageMetadata` (`serverXR/src/assetScrub.js`) re-encodes JPEG/PNG/WebP/TIFF/AVIF/GIF on ingest; sharp doesn't copy input metadata, so EXIF/IPTC/XMP are gone. Three traps handled: (1) `.rotate()` bakes EXIF orientation into pixels before the tag dies — skipping it turns portraits sideways; (2) it must run BEFORE the content hash, since the asset id is the sha256 of the stored bytes, so a client-precomputed id is dropped for scrubbed files and the recomputed id returned (bundle-import callers already remap); (3) `req.file.size` is stale after re-encode — re-stat or every scrubbed asset records a wrong size. SVG skipped (sharp would rasterize it), non-images pass through, and a scrub failure logs and continues rather than failing the upload. Regression guard: 7 cases in `serverXR/src/assetScrub.test.js` incl. a real GPS-EXIF JPEG and an orientation-6 portrait asserting 40x60 -> 60x40. **Not retroactive — assets uploaded before this keep their metadata** | `serverXR/src/assetScrub.js`, `serverXR/src/assetScrub.test.js`, `serverXR/src/routes/spaceRoutes.js`, `serverXR/src/routes/projectRoutes.js`, `src/wiki/wikiContent.js` |
| Landing "Open Studio" opens a project list instead of all spaces — the label promises the top level and delivers one room inside it | `studioHref` resolved to `/open/studio?browse=1` for signed-out and guest sessions (`LandingPage.jsx:127`). That path is `STUDIO_PAGE_HUB` -> `StudioHub.jsx`, which despite the name is a **single space's project list** (the open space's), not the spaces hub. The `?browse=1` only suppressed StudioHub's auto-forward into the jam editor, so an earlier fix ("opens the browsable hub, not the jam door") stopped one level too deep, and the follow-up that added session-awareness only corrected the signed-in branch. Because these are static `href`s rather than click handlers, a signed-in owner also got the guest destination during the tick before `getApiSession` resolved | All five Studio controls now use `buildStudioSpacesPath()` -> `/studio` -> `STUDIO_PAGE_SPACES` -> `SpaceHub`, the actual list of all spaces, for every session. Verified on staging that `/studio` signed-out is fully usable (Spaces, GRID/MAP, Open Space, sandbox, "Sign in to create"), so no session loses out. Dropped the now-dead `OPEN_STUDIO_HREF`, `buildStudioHubPath` and `useAuthSession` imports. Regression guard: the two `LandingPage.test.jsx` cases that asserted `/open/studio?browse=1` now assert `/studio`, plus a case pinning the same destination across all three session states | `src/landing/LandingPage.jsx`, `src/landing/LandingPage.test.jsx` |
