# Parallel Agent Workflow

This is the `self-host` branch — a clean, local-only copy with no GitHub token, no fork, no PR flow. The multi-agent / fork-contribution workflow this file normally documents doesn't apply here.

See this file on the `dev` branch for the real contributor workflow (fork mode, git worktrees, role-scoped collaboration, the auto-PR setup, and the unattended sync/work/push contract for fork agents).
