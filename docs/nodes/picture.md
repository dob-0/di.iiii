# pictures — node reference

Generated by `scripts/generate-node-reference.mjs` from the registry and
`src/project/graph/examples/nodes/`. Do not hand-edit; `npm run docs:nodes` regenerates
this file, and `npm run docs:nodes:check` fails CI if it has drifted.

[← index](README.md)

## Camera In (`top.camera`)

A live camera feed as a picture operator — wired into Difference here. Its picture stays on the GPU; `out` is null in the graph (see the file header).

**Inputs:** none

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_camera_cam",
      "typeId": "top.camera",
      "label": "Camera In",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "device": "",
        "deviceLabel": "",
        "mirror": false
      }
    },
    {
      "id": "ex_top_camera_diff",
      "typeId": "top.difference",
      "label": "Difference",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "compensate": true,
        "colour": false
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_camera_e_cam_out_diff_a",
      "fromNodeId": "ex_top_camera_cam",
      "fromPort": "out",
      "toNodeId": "ex_top_camera_diff",
      "toPort": "a"
    }
  ]
}
```

## Difference (`top.difference`)

What changed since the last frame — Camera In feeds it, and it feeds Level, the real motion-detection chain the all-nodes example wires end to end.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_difference_cam",
      "typeId": "top.camera",
      "label": "Camera In",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "device": "",
        "deviceLabel": "",
        "mirror": false
      }
    },
    {
      "id": "ex_top_difference_diff",
      "typeId": "top.difference",
      "label": "Difference",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "compensate": true,
        "colour": false
      }
    },
    {
      "id": "ex_top_difference_level",
      "typeId": "top.level",
      "label": "Level",
      "graphX": 520,
      "graphY": 64,
      "values": {
        "machine": "",
        "threshold": 0.05,
        "gain": 5,
        "brightness": 0,
        "gamma": 1,
        "invert": false,
        "opacity": 1
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_difference_e_cam_out_diff_a",
      "fromNodeId": "ex_top_difference_cam",
      "fromPort": "out",
      "toNodeId": "ex_top_difference_diff",
      "toPort": "a"
    },
    {
      "id": "ex_top_difference_e_diff_out_level_a",
      "fromNodeId": "ex_top_difference_diff",
      "fromPort": "out",
      "toNodeId": "ex_top_difference_level",
      "toPort": "a"
    }
  ]
}
```

## Level (`top.level`)

Threshold, gain, brightness, gamma, invert, opacity — one card of picture adjustment, fed by Difference and feeding Blur.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_level_diff",
      "typeId": "top.difference",
      "label": "Difference",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "compensate": true,
        "colour": false
      }
    },
    {
      "id": "ex_top_level_level",
      "typeId": "top.level",
      "label": "Level",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "threshold": 0.05,
        "gain": 5,
        "brightness": 0,
        "gamma": 1,
        "invert": false,
        "opacity": 1
      }
    },
    {
      "id": "ex_top_level_blur",
      "typeId": "top.blur",
      "label": "Blur",
      "graphX": 520,
      "graphY": 64,
      "values": {
        "machine": "",
        "size": 2
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_level_e_diff_out_level_a",
      "fromNodeId": "ex_top_level_diff",
      "fromPort": "out",
      "toNodeId": "ex_top_level_level",
      "toPort": "a"
    },
    {
      "id": "ex_top_level_e_level_out_blur_a",
      "fromNodeId": "ex_top_level_level",
      "fromPort": "out",
      "toNodeId": "ex_top_level_blur",
      "toPort": "a"
    }
  ]
}
```

## Blur (`top.blur`)

Softens a picture by Size — fed by Level, feeding Feedback, so a motion trail glows instead of hard-edging.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_blur_level",
      "typeId": "top.level",
      "label": "Level",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "threshold": 0,
        "gain": 1,
        "brightness": 0,
        "gamma": 1,
        "invert": false,
        "opacity": 1
      }
    },
    {
      "id": "ex_top_blur_blur",
      "typeId": "top.blur",
      "label": "Blur",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "size": 3
      }
    },
    {
      "id": "ex_top_blur_trail",
      "typeId": "top.feedback",
      "label": "Feedback",
      "graphX": 520,
      "graphY": 64,
      "values": {
        "machine": "",
        "trail": 0.9,
        "mode": "0"
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_blur_e_level_out_blur_a",
      "fromNodeId": "ex_top_blur_level",
      "fromPort": "out",
      "toNodeId": "ex_top_blur_blur",
      "toPort": "a"
    },
    {
      "id": "ex_top_blur_e_blur_out_trail_a",
      "fromNodeId": "ex_top_blur_blur",
      "fromPort": "out",
      "toNodeId": "ex_top_blur_trail",
      "toPort": "a"
    }
  ]
}
```

## Edge (`top.edge`)

Outlines a picture by Strength — wired straight off Camera In so it can join Feedback in a Blend.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_edge_cam",
      "typeId": "top.camera",
      "label": "Camera In",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "device": "",
        "deviceLabel": "",
        "mirror": false
      }
    },
    {
      "id": "ex_top_edge_edge",
      "typeId": "top.edge",
      "label": "Edge",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "strength": 2
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_edge_e_cam_out_edge_a",
      "fromNodeId": "ex_top_edge_cam",
      "fromPort": "out",
      "toNodeId": "ex_top_edge_edge",
      "toPort": "a"
    }
  ]
}
```

## Feedback (`top.feedback`)

Fades its own last frame by Trail, then joins the new one — the motion-glow trick, fed by Blur, joined with Edge in a Blend.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_feedback_blur",
      "typeId": "top.blur",
      "label": "Blur",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "size": 2
      }
    },
    {
      "id": "ex_top_feedback_trail",
      "typeId": "top.feedback",
      "label": "Feedback",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "trail": 0.9,
        "mode": "0"
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_feedback_e_blur_out_trail_a",
      "fromNodeId": "ex_top_feedback_blur",
      "fromPort": "out",
      "toNodeId": "ex_top_feedback_trail",
      "toPort": "a"
    }
  ]
}
```

## Blend (`top.blend`)

Combines two pictures by Mode and Amount — Feedback and Edge joined here, the way the all-nodes example lights the projector.

**Inputs:**
- `a` (texture) — In
- `b` (texture) — In B

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_blend_trail",
      "typeId": "top.feedback",
      "label": "Feedback",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "trail": 0.9,
        "mode": "0"
      }
    },
    {
      "id": "ex_top_blend_edge",
      "typeId": "top.edge",
      "label": "Edge",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "machine": "",
        "strength": 2
      }
    },
    {
      "id": "ex_top_blend_blend",
      "typeId": "top.blend",
      "label": "Blend",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "mode": "2",
        "mix": 1
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_blend_e_trail_out_blend_a",
      "fromNodeId": "ex_top_blend_trail",
      "fromPort": "out",
      "toNodeId": "ex_top_blend_blend",
      "toPort": "a"
    },
    {
      "id": "ex_top_blend_e_edge_out_blend_b",
      "fromNodeId": "ex_top_blend_edge",
      "fromPort": "out",
      "toNodeId": "ex_top_blend_blend",
      "toPort": "b"
    }
  ]
}
```

## Picture Out (`top.out`)

What the projector shows — a pass-through sink so the output page never has to know which operator happens to be last. Fed by Blend here.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_out_blend",
      "typeId": "top.blend",
      "label": "Blend",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "mode": "0",
        "mix": 1
      }
    },
    {
      "id": "ex_top_out_projector",
      "typeId": "top.out",
      "label": "Picture Out",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": ""
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_out_e_blend_out_projector_a",
      "fromNodeId": "ex_top_out_blend",
      "fromPort": "out",
      "toNodeId": "ex_top_out_projector",
      "toPort": "a"
    }
  ]
}
```

## Analyze (`top.analyze`)

Picture to numbers — Brightness, Amount, and the lit centre (X, Y) are measured a few times a second and published live while the editor runs. Fed by Level here.

**Inputs:**
- `a` (texture) — In

**Outputs:**
- `out` (texture) — Picture
- `brightness` (number) — Brightness
- `amount` (number) — Amount
- `x` (number) — Centre X
- `y` (number) — Centre Y

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_top_analyze_level",
      "typeId": "top.level",
      "label": "Level",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "machine": "",
        "threshold": 0,
        "gain": 1,
        "brightness": 0,
        "gamma": 1,
        "invert": false,
        "opacity": 1
      }
    },
    {
      "id": "ex_top_analyze_meter",
      "typeId": "top.analyze",
      "label": "Analyze",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "machine": "",
        "threshold": 0.2
      }
    }
  ],
  "edges": [
    {
      "id": "ex_top_analyze_e_level_out_meter_a",
      "fromNodeId": "ex_top_analyze_level",
      "fromPort": "out",
      "toNodeId": "ex_top_analyze_meter",
      "toPort": "a"
    }
  ]
}
```
