# bring in — node reference

Generated by `scripts/generate-node-reference.mjs` from the registry and
`src/project/graph/examples/nodes/`. Do not hand-edit; `npm run docs:nodes` regenerates
this file, and `npm run docs:nodes:check` fails CI if it has drifted.

[← index](README.md)

## Model (`geom.model`)

A file the person brought in, standing in space — Scale is wired here. No `src` asset ships with this example (honest: a freshly placed Model is exactly this empty until a file is chosen), so nothing renders yet.

**Inputs:**
- `src` (string) — Model — default ""
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]
- `scale` (vec3) — Scale — default [1,1,1]
- `playAnimations` (boolean) — Play — default true
- `animationSpeed` (number) — Speed — default 1, step 0.1
- `animationClip` (string) — Clip — default ""

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_model_scale",
      "typeId": "value.vec3",
      "label": "Vector · scale",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": [
          1.2,
          1.2,
          1.2
        ]
      }
    },
    {
      "id": "ex_geom_model_mesh",
      "typeId": "geom.model",
      "label": "Model",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "src": "",
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ],
        "playAnimations": true,
        "animationSpeed": 1,
        "animationClip": ""
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_model_e_scale_out_mesh_scale",
      "fromNodeId": "ex_geom_model_scale",
      "fromPort": "out",
      "toNodeId": "ex_geom_model_mesh",
      "toPort": "scale"
    }
  ]
}
```

## Video (`media.video`)

A video file standing in space, wired to Loop — its playing picture publishes to the Frame port only while the window that renders it is open (the same idiom as Webcam).

**Inputs:**
- `src` (string) — Video — default ""
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]
- `scale` (vec3) — Scale — default [1,1,1]
- `muted` (boolean) — Muted — default true
- `volume` (number) — Volume — default 1, min 0, max 1, step 0.05
- `loop` (boolean) — Loop — default true

**Outputs:**
- `frame` (texture) — Frame

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_media_video_loop",
      "typeId": "value.boolean",
      "label": "Boolean · loop",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": true
      }
    },
    {
      "id": "ex_media_video_clip",
      "typeId": "media.video",
      "label": "Video",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "src": "",
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ],
        "muted": true,
        "volume": 1,
        "loop": true
      }
    },
    {
      "id": "ex_media_video_preview",
      "typeId": "view.image",
      "label": "Image panel",
      "graphX": 520,
      "graphY": 64,
      "values": {
        "frame": {
          "x": 60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Image",
          "visible": false
        }
      }
    }
  ],
  "edges": [
    {
      "id": "ex_media_video_e_loop_out_clip_loop",
      "fromNodeId": "ex_media_video_loop",
      "fromPort": "out",
      "toNodeId": "ex_media_video_clip",
      "toPort": "loop"
    },
    {
      "id": "ex_media_video_e_clip_frame_preview_src",
      "fromNodeId": "ex_media_video_clip",
      "fromPort": "frame",
      "toNodeId": "ex_media_video_preview",
      "toPort": "src"
    }
  ]
}
```

## Sound (`media.audio`)

A sound file standing in space — Volume and Loop are wired; Volume/Low/Mid/High publish live analysis numbers while the editor's own playback runs.

**Inputs:**
- `src` (string) — Sound — default ""
- `position` (vec3) — Position — default [0,0,0]
- `volume` (number) — Volume — default 1, min 0, max 1, step 0.05
- `distance` (number) — Distance — default 10, min 0, step 1
- `loop` (boolean) — Loop — default true
- `autoplay` (boolean) — Autoplay — default true

**Outputs:**
- `volume` (number) — Volume
- `low` (number) — Low
- `mid` (number) — Mid
- `high` (number) — High

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_media_audio_gain",
      "typeId": "value.number",
      "label": "Number · volume",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 0.8
      }
    },
    {
      "id": "ex_media_audio_loop",
      "typeId": "value.boolean",
      "label": "Boolean · loop",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": true
      }
    },
    {
      "id": "ex_media_audio_speaker",
      "typeId": "media.audio",
      "label": "Sound",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "src": "",
        "position": [
          0,
          1.2,
          0
        ],
        "volume": 1,
        "distance": 10,
        "loop": true,
        "autoplay": true
      }
    }
  ],
  "edges": [
    {
      "id": "ex_media_audio_e_gain_out_speaker_volume",
      "fromNodeId": "ex_media_audio_gain",
      "fromPort": "out",
      "toNodeId": "ex_media_audio_speaker",
      "toPort": "volume"
    },
    {
      "id": "ex_media_audio_e_loop_out_speaker_loop",
      "fromNodeId": "ex_media_audio_loop",
      "fromPort": "out",
      "toNodeId": "ex_media_audio_speaker",
      "toPort": "loop"
    }
  ]
}
```

## Webcam (`source.webcam`)

The live camera frame — real once its window is open (permission-denied and no-camera are normal states it shows). Wired here into a Plane's Texture; a live frame always wins over a Texture URL.

**Inputs:** none

**Outputs:**
- `frame` (texture) — Frame

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_source_webcam_cam",
      "typeId": "source.webcam",
      "label": "Webcam",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "frame": {
          "x": -60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Webcam",
          "visible": false
        }
      }
    },
    {
      "id": "ex_source_webcam_screen",
      "typeId": "geom.plane",
      "label": "Plane",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffffff",
        "width": 2,
        "height": 2,
        "textureUrl": "",
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_source_webcam_e_cam_frame_screen_texture",
      "fromNodeId": "ex_source_webcam_cam",
      "fromPort": "frame",
      "toNodeId": "ex_source_webcam_screen",
      "toPort": "texture"
    }
  ]
}
```

## Microphone (`source.mic`)

Volume 0..1, live while the window is open — wired here into a lamp's Intensity so the room pulses with sound. Frequency is a raw spectrum array; nothing in the registry can consume it yet.

**Inputs:** none

**Outputs:**
- `volume` (number) — Volume
- `frequency` (any) — Frequency

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_source_mic_input",
      "typeId": "source.mic",
      "label": "Microphone",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "frame": {
          "x": -60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Microphone",
          "visible": false
        }
      }
    },
    {
      "id": "ex_source_mic_lamp",
      "typeId": "light.point",
      "label": "Light",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffe9c4",
        "intensity": 6,
        "position": [
          0,
          1.2,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_source_mic_e_input_volume_lamp_intensity",
      "fromNodeId": "ex_source_mic_input",
      "fromPort": "volume",
      "toNodeId": "ex_source_mic_lamp",
      "toPort": "intensity"
    }
  ]
}
```

## MIDI In (`device.midi.in`)

A hardware controller, live while its window is open — listening on every channel (0) by default so a controller set to any channel is heard. Note is wired into a Sphere's radius here.

**Inputs:** none

**Outputs:**
- `note` (number) — Note
- `velocity` (number) — Velocity
- `cc` (number) — CC
- `value` (number) — Value
- `trigger` (signal) — Trigger

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_device_midi_in_controller",
      "typeId": "device.midi.in",
      "label": "MIDI In",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "deviceId": "",
        "channel": 0,
        "frame": {
          "x": -60,
          "y": 88,
          "width": 380,
          "height": 340,
          "zIndex": 7,
          "title": "MIDI In",
          "visible": false
        }
      }
    },
    {
      "id": "ex_device_midi_in_ball",
      "typeId": "geom.sphere",
      "label": "Sphere",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_device_midi_in_e_controller_note_ball_radius",
      "fromNodeId": "ex_device_midi_in_controller",
      "fromPort": "note",
      "toNodeId": "ex_device_midi_in_ball",
      "toPort": "radius"
    }
  ]
}
```

## Button (`view.button`)

The desk's Go — Presses is the authored, undoable count (one document op per press); Pressed is this window's live finger, honest only while actually held.

**Inputs:**
- `title` (string) — Title — default "Go"

**Outputs:**
- `presses` (number) — Presses
- `pressed` (boolean) — Pressed

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_view_button_go",
      "typeId": "view.button",
      "label": "Go",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "title": "Go",
        "presses": 2,
        "frame": {
          "x": -60,
          "y": 88,
          "width": 220,
          "height": 160,
          "zIndex": 7,
          "title": "Button",
          "visible": false
        }
      }
    },
    {
      "id": "ex_view_button_lamp",
      "typeId": "light.point",
      "label": "Light",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffe9c4",
        "intensity": 6,
        "position": [
          0,
          1.2,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_view_button_e_go_presses_lamp_intensity",
      "fromNodeId": "ex_view_button_go",
      "fromPort": "presses",
      "toNodeId": "ex_view_button_lamp",
      "toPort": "intensity"
    }
  ]
}
```

## Keyboard (`device.keyboard`)

The operator's other hand — a chosen key, live while the editor is open. Count rises once per press, wired here into a lamp's Intensity; a wire into Key itself is accepted but ignored (the listener reads node.values.key directly).

**Inputs:**
- `key` (string) — Key — default "Space"

**Outputs:**
- `pressed` (boolean) — Pressed
- `count` (number) — Count

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_device_keyboard_hand",
      "typeId": "device.keyboard",
      "label": "Keyboard",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "key": "Space"
      }
    },
    {
      "id": "ex_device_keyboard_lamp",
      "typeId": "light.point",
      "label": "Light",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffe9c4",
        "intensity": 6,
        "position": [
          0,
          1.2,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_device_keyboard_e_hand_count_lamp_intensity",
      "fromNodeId": "ex_device_keyboard_hand",
      "fromPort": "count",
      "toNodeId": "ex_device_keyboard_lamp",
      "toPort": "intensity"
    }
  ]
}
```
