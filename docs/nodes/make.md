# make — node reference

Generated by `scripts/generate-node-reference.mjs` from the registry and
`src/project/graph/examples/nodes/`. Do not hand-edit; `npm run docs:nodes` regenerates
this file, and `npm run docs:nodes:check` fails CI if it has drifted.

[← index](README.md)

## Cube (`geom.cube`)

The plain box — Colour and Size come from wires here, not the [0,1,0] "invisible cube" bug the all-nodes example used to ship (docs/ai/known-fixes.md).

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `size` (vec3) — Size — default [1,1,1]
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.5,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `bounds` (vec3) — Bounds
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_cube_colour",
      "typeId": "value.color",
      "label": "Colour · cyan",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "#4df9ff"
      }
    },
    {
      "id": "ex_geom_cube_size",
      "typeId": "value.vec3",
      "label": "Vector · size",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": [
          0.8,
          0.8,
          0.8
        ]
      }
    },
    {
      "id": "ex_geom_cube_box",
      "typeId": "geom.cube",
      "label": "Cube",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "size": [
          1,
          1,
          1
        ],
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          0.5,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_cube_e_colour_out_box_color",
      "fromNodeId": "ex_geom_cube_colour",
      "fromPort": "out",
      "toNodeId": "ex_geom_cube_box",
      "toPort": "color"
    },
    {
      "id": "ex_geom_cube_e_size_out_box_size",
      "fromNodeId": "ex_geom_cube_size",
      "fromPort": "out",
      "toNodeId": "ex_geom_cube_box",
      "toPort": "size"
    }
  ]
}
```

## Sphere (`geom.sphere`)

A ball whose Radius and Colour are both wired — grow it by changing the Number, not the card.

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `radius` (number) — Radius — default 0.5
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.5,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_sphere_radius",
      "typeId": "value.number",
      "label": "Number · radius",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 0.6
      }
    },
    {
      "id": "ex_geom_sphere_colour",
      "typeId": "value.color",
      "label": "Colour",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": "#ff79c6"
      }
    },
    {
      "id": "ex_geom_sphere_ball",
      "typeId": "geom.sphere",
      "label": "Sphere",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_sphere_e_radius_out_ball_radius",
      "fromNodeId": "ex_geom_sphere_radius",
      "fromPort": "out",
      "toNodeId": "ex_geom_sphere_ball",
      "toPort": "radius"
    },
    {
      "id": "ex_geom_sphere_e_colour_out_ball_color",
      "fromNodeId": "ex_geom_sphere_colour",
      "fromPort": "out",
      "toNodeId": "ex_geom_sphere_ball",
      "toPort": "color"
    }
  ]
}
```

## Plane (`geom.plane`)

A flat panel — Width, Height and Colour come from wires. A Webcam wired into Texture would show live video once its window is opened (not running in this static graph).

**Inputs:**
- `color` (color) — Colour — default "#ffffff"
- `width` (number) — Width — default 2
- `height` (number) — Height — default 2
- `textureUrl` (string) — Texture URL — default ""
- `texture` (texture) — Texture
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_plane_colour",
      "typeId": "value.color",
      "label": "Colour",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "#ffffff"
      }
    },
    {
      "id": "ex_geom_plane_width",
      "typeId": "value.number",
      "label": "Number · width",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": 3
      }
    },
    {
      "id": "ex_geom_plane_height",
      "typeId": "value.number",
      "label": "Number · height",
      "graphX": 0,
      "graphY": 904,
      "values": {
        "value": 1.5
      }
    },
    {
      "id": "ex_geom_plane_panel",
      "typeId": "geom.plane",
      "label": "Plane",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffffff",
        "width": 2,
        "height": 2,
        "textureUrl": "",
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_plane_e_colour_out_panel_color",
      "fromNodeId": "ex_geom_plane_colour",
      "fromPort": "out",
      "toNodeId": "ex_geom_plane_panel",
      "toPort": "color"
    },
    {
      "id": "ex_geom_plane_e_width_out_panel_width",
      "fromNodeId": "ex_geom_plane_width",
      "fromPort": "out",
      "toNodeId": "ex_geom_plane_panel",
      "toPort": "width"
    },
    {
      "id": "ex_geom_plane_e_height_out_panel_height",
      "fromNodeId": "ex_geom_plane_height",
      "fromPort": "out",
      "toNodeId": "ex_geom_plane_panel",
      "toPort": "height"
    }
  ]
}
```

## Merge (`shape.merge`)

Joins two shapes into one — bare it carries nothing on purpose; fed a Sphere and a Cube it merges them into one geometry value.

**Inputs:**
- `a` (geometry) — A
- `b` (geometry) — B

**Outputs:**
- `out` (geometry) — Out

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_shape_merge_head",
      "typeId": "geom.sphere",
      "label": "Sphere · head",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.3,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    },
    {
      "id": "ex_shape_merge_body",
      "typeId": "geom.cube",
      "label": "Cube · body",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "color": "#5fa8ff",
        "size": [
          1,
          1,
          1
        ],
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          0.5,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    },
    {
      "id": "ex_shape_merge_merge",
      "typeId": "shape.merge",
      "label": "Merge",
      "graphX": 260,
      "graphY": 64,
      "values": {}
    }
  ],
  "edges": [
    {
      "id": "ex_shape_merge_e_head_geometry_merge_a",
      "fromNodeId": "ex_shape_merge_head",
      "fromPort": "geometry",
      "toNodeId": "ex_shape_merge_merge",
      "toPort": "a"
    },
    {
      "id": "ex_shape_merge_e_body_geometry_merge_b",
      "fromNodeId": "ex_shape_merge_body",
      "fromPort": "geometry",
      "toNodeId": "ex_shape_merge_merge",
      "toPort": "b"
    }
  ]
}
```

## Array (`geom.array`)

Repeats a shape N times along an offset — a Cube fed in becomes a row of cubes; bare it carries nothing.

**Inputs:**
- `geometry` (geometry) — Geometry
- `count` (number) — Count — default 3
- `offset` (vec3) — Offset — default [1.5,0,0]

**Outputs:**
- `out` (geometry) — Out

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_array_unit",
      "typeId": "geom.cube",
      "label": "Cube",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "size": [
          1,
          1,
          1
        ],
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          0.5,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    },
    {
      "id": "ex_geom_array_row",
      "typeId": "geom.array",
      "label": "Array",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "count": 4,
        "offset": [
          1.2,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_array_e_unit_geometry_row_geometry",
      "fromNodeId": "ex_geom_array_unit",
      "fromPort": "geometry",
      "toNodeId": "ex_geom_array_row",
      "toPort": "geometry"
    }
  ]
}
```

## Cylinder (`geom.cylinder`)

A column — Radius and Height come from wires, standing in for a pillar or a drum.

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `radius` (number) — Radius — default 0.5
- `height` (number) — Height — default 1.5
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.75,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_cylinder_height",
      "typeId": "value.number",
      "label": "Number · height",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 2
      }
    },
    {
      "id": "ex_geom_cylinder_colour",
      "typeId": "value.color",
      "label": "Colour",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": "#bd93f9"
      }
    },
    {
      "id": "ex_geom_cylinder_column",
      "typeId": "geom.cylinder",
      "label": "Cylinder",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "height": 1.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_cylinder_e_height_out_column_height",
      "fromNodeId": "ex_geom_cylinder_height",
      "fromPort": "out",
      "toNodeId": "ex_geom_cylinder_column",
      "toPort": "height"
    },
    {
      "id": "ex_geom_cylinder_e_colour_out_column_color",
      "fromNodeId": "ex_geom_cylinder_colour",
      "fromPort": "out",
      "toNodeId": "ex_geom_cylinder_column",
      "toPort": "color"
    }
  ]
}
```

## Cone (`geom.cone`)

A spike — Radius comes from a wire, so widening the Number widens the cone's base.

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `radius` (number) — Radius — default 0.5
- `height` (number) — Height — default 1.5
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.75,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_cone_radius",
      "typeId": "value.number",
      "label": "Number · radius",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 0.7
      }
    },
    {
      "id": "ex_geom_cone_spike",
      "typeId": "geom.cone",
      "label": "Cone",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "height": 1.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_cone_e_radius_out_spike_radius",
      "fromNodeId": "ex_geom_cone_radius",
      "fromPort": "out",
      "toNodeId": "ex_geom_cone_spike",
      "toPort": "radius"
    }
  ]
}
```

## Torus (`geom.torus`)

A ring — its Tube thickness is wired, so a thin Number makes a delicate hoop and a wide one a fat donut.

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `radius` (number) — Radius — default 0.5
- `tube` (number) — Tube — default 0.18
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.5,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_torus_tube",
      "typeId": "value.number",
      "label": "Number · tube",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 0.12
      }
    },
    {
      "id": "ex_geom_torus_ring",
      "typeId": "geom.torus",
      "label": "Torus",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "tube": 0.18,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_torus_e_tube_out_ring_tube",
      "fromNodeId": "ex_geom_torus_tube",
      "fromPort": "out",
      "toNodeId": "ex_geom_torus_ring",
      "toPort": "tube"
    }
  ]
}
```

## Line (`geom.line`)

A stroke between two points — its far end is wired to a Vector, so moving the vector re-draws the line.

**Inputs:**
- `from` (vec3) — From — default [0,0,0]
- `to` (vec3) — To — default [0,1.5,0]
- `thickness` (number) — Thickness — default 0.02
- `color` (color) — Colour — default "#5fa8ff"
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_line_end",
      "typeId": "value.vec3",
      "label": "Vector · to",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": [
          1.5,
          1.5,
          0
        ]
      }
    },
    {
      "id": "ex_geom_line_colour",
      "typeId": "value.color",
      "label": "Colour",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": "#f1fa8c"
      }
    },
    {
      "id": "ex_geom_line_stroke",
      "typeId": "geom.line",
      "label": "Line",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "from": [
          0,
          0,
          0
        ],
        "to": [
          0,
          1.5,
          0
        ],
        "thickness": 0.02,
        "color": "#5fa8ff",
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_line_e_end_out_stroke_to",
      "fromNodeId": "ex_geom_line_end",
      "fromPort": "out",
      "toNodeId": "ex_geom_line_stroke",
      "toPort": "to"
    },
    {
      "id": "ex_geom_line_e_colour_out_stroke_color",
      "fromNodeId": "ex_geom_line_colour",
      "fromPort": "out",
      "toNodeId": "ex_geom_line_stroke",
      "toPort": "color"
    }
  ]
}
```

## Circle (`geom.circle`)

A flat disc — its Radius is wired, growing and shrinking the mark on the floor.

**Inputs:**
- `color` (color) — Colour — default "#5fa8ff"
- `radius` (number) — Radius — default 0.5
- `roughness` (number) — Roughness — default 1
- `metalness` (number) — Metalness — default 0
- `emissive` (color) — Emission — default "#000000"
- `opacity` (number) — Opacity — default 1
- `position` (vec3) — Position — default [0,0.5,0]
- `rotation` (vec3) — Rotation — default [0,0,0]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_circle_radius",
      "typeId": "value.number",
      "label": "Number · radius",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": 0.9
      }
    },
    {
      "id": "ex_geom_circle_disc",
      "typeId": "geom.circle",
      "label": "Circle",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_circle_e_radius_out_disc_radius",
      "fromNodeId": "ex_geom_circle_radius",
      "fromPort": "out",
      "toNodeId": "ex_geom_circle_disc",
      "toPort": "radius"
    }
  ]
}
```

## Transform (`geom.transform`)

Re-frames one copy of a shape — a Torus fed in, moved and scaled by wires; bare it carries nothing.

**Inputs:**
- `geometry` (geometry) — Geometry
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]
- `scale` (vec3) — Scale — default [1,1,1]

**Outputs:**
- `out` (geometry) — Out

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_transform_ring",
      "typeId": "geom.torus",
      "label": "Torus",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "radius": 0.5,
        "tube": 0.18,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      }
    },
    {
      "id": "ex_geom_transform_position",
      "typeId": "value.vec3",
      "label": "Vector · position",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": [
          0,
          1,
          0
        ]
      }
    },
    {
      "id": "ex_geom_transform_reframe",
      "typeId": "geom.transform",
      "label": "Transform",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "position": [
          0,
          0,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_geom_transform_e_ring_geometry_reframe_geometry",
      "fromNodeId": "ex_geom_transform_ring",
      "fromPort": "geometry",
      "toNodeId": "ex_geom_transform_reframe",
      "toPort": "geometry"
    },
    {
      "id": "ex_geom_transform_e_position_out_reframe_position",
      "fromNodeId": "ex_geom_transform_position",
      "fromPort": "out",
      "toNodeId": "ex_geom_transform_reframe",
      "toPort": "position"
    }
  ]
}
```

## Constructor (`geom.constructor`)

A node made of nodes — enter it, build a shape from parts, and the Constructor stands in the room being that shape. Two spheres merged through its Out door make a snowman.

**Inputs:**
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]
- `scale` (vec3) — Scale — default [1,1,1]

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_constructor_snowman",
      "typeId": "geom.constructor",
      "label": "Constructor",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ]
      }
    },
    {
      "id": "ex_geom_constructor_head",
      "typeId": "geom.sphere",
      "label": "Head",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffffff",
        "radius": 0.3,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      },
      "parentId": "ex_geom_constructor_snowman"
    },
    {
      "id": "ex_geom_constructor_body",
      "typeId": "geom.sphere",
      "label": "Body",
      "graphX": 260,
      "graphY": 484,
      "values": {
        "color": "#dfe8ff",
        "radius": 0.5,
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          1.2,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      },
      "parentId": "ex_geom_constructor_snowman"
    },
    {
      "id": "ex_geom_constructor_merge",
      "typeId": "shape.merge",
      "label": "Merge",
      "graphX": 520,
      "graphY": 64,
      "values": {},
      "parentId": "ex_geom_constructor_snowman"
    },
    {
      "id": "ex_geom_constructor_door",
      "typeId": "port.out",
      "label": "Out · the worn shape",
      "graphX": 520,
      "graphY": 484,
      "values": {
        "value": null,
        "label": "Shape",
        "portType": "geometry"
      },
      "parentId": "ex_geom_constructor_snowman"
    }
  ],
  "edges": [
    {
      "id": "ex_geom_constructor_e_head_geometry_merge_a",
      "fromNodeId": "ex_geom_constructor_head",
      "fromPort": "geometry",
      "toNodeId": "ex_geom_constructor_merge",
      "toPort": "a"
    },
    {
      "id": "ex_geom_constructor_e_body_geometry_merge_b",
      "fromNodeId": "ex_geom_constructor_body",
      "fromPort": "geometry",
      "toNodeId": "ex_geom_constructor_merge",
      "toPort": "b"
    },
    {
      "id": "ex_geom_constructor_e_merge_out_door_value",
      "fromNodeId": "ex_geom_constructor_merge",
      "fromPort": "out",
      "toNodeId": "ex_geom_constructor_door",
      "toPort": "value"
    }
  ]
}
```

## Text (`view.text`)

A text panel — its Content is wired to a String, so what the panel reads is authored one card away.

**Inputs:**
- `content` (string) — Content — default "Hello"

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_view_text_copy",
      "typeId": "value.string",
      "label": "String · line",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "Doors open at 7."
      }
    },
    {
      "id": "ex_view_text_panel",
      "typeId": "view.text",
      "label": "Text panel",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "content": "Hello",
        "frame": {
          "x": 0,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Text",
          "visible": false
        }
      }
    }
  ],
  "edges": [
    {
      "id": "ex_view_text_e_copy_out_panel_content",
      "fromNodeId": "ex_view_text_copy",
      "fromPort": "out",
      "toNodeId": "ex_view_text_panel",
      "toPort": "content"
    }
  ]
}
```

## List (`view.list`)

A checklist panel with groups and rows — the only node whose card shows a summary ("N rows · M groups") without opening the window.

**Inputs:** none

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_view_list_checklist",
      "typeId": "view.list",
      "label": "List panel",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "groups": [
          "Core",
          "Would be good"
        ],
        "items": [
          {
            "id": "row-1",
            "text": "Six laptops",
            "group": "Core",
            "order": 0
          },
          {
            "id": "row-2",
            "text": "A second projector",
            "group": "Would be good",
            "order": 1
          }
        ],
        "frame": {
          "x": -60,
          "y": 88,
          "width": 660,
          "height": 560,
          "zIndex": 7,
          "title": "List",
          "visible": false
        }
      }
    }
  ],
  "edges": []
}
```

## Image (`view.image`)

An image panel — its Source is a texture port. A Webcam wired in would show a live frame once its window is open; nothing is running here.

**Inputs:**
- `src` (texture) — Source

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_view_image_cam",
      "typeId": "source.webcam",
      "label": "Webcam",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "frame": {
          "x": -60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Webcam",
          "visible": false
        }
      }
    },
    {
      "id": "ex_view_image_frame",
      "typeId": "view.image",
      "label": "Image panel",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "frame": {
          "x": 0,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Image",
          "visible": false
        }
      }
    }
  ],
  "edges": [
    {
      "id": "ex_view_image_e_cam_frame_frame_src",
      "fromNodeId": "ex_view_image_cam",
      "fromPort": "frame",
      "toNodeId": "ex_view_image_frame",
      "toPort": "src"
    }
  ]
}
```

## Browser (`view.browser`)

An iframe panel pointed at a URL — same-origin here (/wiki) so it still opens with no network, unlike a page on someone else's domain that may refuse to be framed.

**Inputs:**
- `url` (string) — URL — default "https://example.com"

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_view_browser_frame",
      "typeId": "view.browser",
      "label": "Browser panel",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "url": "/wiki",
        "frame": {
          "x": -60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Browser",
          "visible": false
        }
      }
    }
  ],
  "edges": []
}
```
