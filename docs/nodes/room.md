# the scene — node reference

Generated by `scripts/generate-node-reference.mjs` from the registry and
`src/project/graph/examples/nodes/`. Do not hand-edit; `npm run docs:nodes` regenerates
this file, and `npm run docs:nodes:check` fails CI if it has drifted.

[← index](README.md)

## In (`port.in`)

A doorway: placed inside a Geo, it puts a socket on the Geo's outer face. A Colour wired to that socket from outside reaches this door's Value.

**Inputs:**
- `fallback` (any) — If unwired — default null

**Outputs:**
- `value` (any) — Value

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_port_in_room",
      "typeId": "geom.geo",
      "label": "Geo",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "position": [
          0,
          0,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ]
      }
    },
    {
      "id": "ex_port_in_outside",
      "typeId": "value.color",
      "label": "Colour · outside",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": "#ff79c6"
      }
    },
    {
      "id": "ex_port_in_doorIn",
      "typeId": "port.in",
      "label": "In · a way through the wall",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "fallback": "#5fa8ff",
        "label": "Tint",
        "portType": "color"
      },
      "parentId": "ex_port_in_room"
    }
  ],
  "edges": [
    {
      "id": "ex_port_in_e_outside_out_room_ex_port_in_doorIn",
      "fromNodeId": "ex_port_in_outside",
      "fromPort": "out",
      "toNodeId": "ex_port_in_room",
      "toPort": "ex_port_in_doorIn"
    }
  ]
}
```

## Out (`port.out`)

A doorway the other way: placed inside a Geo, its Value input is fed by a Number, and the Geo grows a matching socket on its outer face — read from outside.

**Inputs:**
- `value` (any) — Value — default null

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_port_out_room",
      "typeId": "geom.geo",
      "label": "Geo",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "position": [
          0,
          0,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ]
      }
    },
    {
      "id": "ex_port_out_level",
      "typeId": "value.number",
      "label": "Number · level",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": 0.5
      }
    },
    {
      "id": "ex_port_out_door",
      "typeId": "port.out",
      "label": "Out · a way back through",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "value": null,
        "label": "Level",
        "portType": "number"
      },
      "parentId": "ex_port_out_room"
    }
  ],
  "edges": [
    {
      "id": "ex_port_out_e_level_out_door_value",
      "fromNodeId": "ex_port_out_level",
      "fromPort": "out",
      "toNodeId": "ex_port_out_door",
      "toPort": "value"
    }
  ]
}
```

## Environment (`world.environment`)

The scene's ambient wash and one sun — Ambient Colour and Sun Intensity come from wires. No output: a scene has exactly one, so nothing downstream needs to read it back.

**Inputs:**
- `ambientColor` (color) — Ambient Colour — default "#ffffff"
- `ambientIntensity` (number) — Ambient Intensity — default 0.8, min 0, max 5, step 0.01
- `directionalColor` (color) — Sun Colour — default "#fff7ea"
- `directionalIntensity` (number) — Sun Intensity — default 1.05, min 0, max 5, step 0.01
- `directionalPosition` (vec3) — Sun Position — default [8,12,4]

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_world_environment_ambient",
      "typeId": "value.color",
      "label": "Colour · ambient",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "#dfe8ff"
      }
    },
    {
      "id": "ex_world_environment_sun",
      "typeId": "value.number",
      "label": "Number · sun intensity",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": 1.2
      }
    },
    {
      "id": "ex_world_environment_light",
      "typeId": "world.environment",
      "label": "Environment",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "ambientColor": "#ffffff",
        "ambientIntensity": 0.8,
        "directionalColor": "#fff7ea",
        "directionalIntensity": 1.05,
        "directionalPosition": [
          8,
          12,
          4
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_world_environment_e_ambient_out_light_ambientColor",
      "fromNodeId": "ex_world_environment_ambient",
      "fromPort": "out",
      "toNodeId": "ex_world_environment_light",
      "toPort": "ambientColor"
    },
    {
      "id": "ex_world_environment_e_sun_out_light_directionalIntensity",
      "fromNodeId": "ex_world_environment_sun",
      "fromPort": "out",
      "toNodeId": "ex_world_environment_light",
      "toPort": "directionalIntensity"
    }
  ]
}
```

## Light (`light.point`)

A lamp standing where you put it — Colour, Intensity and Position all come from wires, root or inside any container.

**Inputs:**
- `color` (color) — Colour — default "#ffe9c4"
- `intensity` (number) — Intensity — default 6, min 0, max 50, step 0.1
- `position` (vec3) — Position — default [0,1.6,0]

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_light_point_colour",
      "typeId": "value.color",
      "label": "Colour · warm",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "#ffe9c4"
      }
    },
    {
      "id": "ex_light_point_brightness",
      "typeId": "value.number",
      "label": "Number · intensity",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": 4
      }
    },
    {
      "id": "ex_light_point_lamp",
      "typeId": "light.point",
      "label": "Light",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#ffe9c4",
        "intensity": 6,
        "position": [
          0,
          1.2,
          0
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "ex_light_point_e_colour_out_lamp_color",
      "fromNodeId": "ex_light_point_colour",
      "fromPort": "out",
      "toNodeId": "ex_light_point_lamp",
      "toPort": "color"
    },
    {
      "id": "ex_light_point_e_brightness_out_lamp_intensity",
      "fromNodeId": "ex_light_point_brightness",
      "fromPort": "out",
      "toNodeId": "ex_light_point_lamp",
      "toPort": "intensity"
    }
  ]
}
```

## Camera (`world.camera`)

An authored eye — Position and Look At come from wires. Standing in the room is not the same as being the shot: the ● toggle (not a port) marks which camera is active.

**Inputs:**
- `position` (vec3) — Position — default [0,2.4,6.5]
- `lookAt` (vec3) — Look At — default [0,0.75,0]
- `fov` (number) — FOV — default 50, min 10, max 170, step 1

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_world_camera_position",
      "typeId": "value.vec3",
      "label": "Vector · position",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": [
          2,
          1.4,
          4
        ]
      }
    },
    {
      "id": "ex_world_camera_lookAt",
      "typeId": "value.vec3",
      "label": "Vector · look at",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": [
          0,
          0.75,
          0
        ]
      }
    },
    {
      "id": "ex_world_camera_eye",
      "typeId": "world.camera",
      "label": "Camera",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "position": [
          0,
          1.2,
          0
        ],
        "lookAt": [
          0,
          0.75,
          0
        ],
        "fov": 50
      }
    }
  ],
  "edges": [
    {
      "id": "ex_world_camera_e_position_out_eye_position",
      "fromNodeId": "ex_world_camera_position",
      "fromPort": "out",
      "toNodeId": "ex_world_camera_eye",
      "toPort": "position"
    },
    {
      "id": "ex_world_camera_e_lookAt_out_eye_lookAt",
      "fromNodeId": "ex_world_camera_lookAt",
      "fromPort": "out",
      "toNodeId": "ex_world_camera_eye",
      "toPort": "lookAt"
    }
  ]
}
```

## Background (`world.background`)

The sky colour, wired to a Colour card so it can be authored beside everything else.

**Inputs:**
- `color` (color) — Colour

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_world_background_sky",
      "typeId": "value.color",
      "label": "Colour · night sky",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "#0a0e16"
      }
    },
    {
      "id": "ex_world_background_backdrop",
      "typeId": "world.background",
      "label": "Background",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#0a0e16"
      }
    }
  ],
  "edges": [
    {
      "id": "ex_world_background_e_sky_out_backdrop_color",
      "fromNodeId": "ex_world_background_sky",
      "fromPort": "out",
      "toNodeId": "ex_world_background_backdrop",
      "toPort": "color"
    }
  ]
}
```

## Grid (`world.grid`)

The floor grid — Visible, Size and Colour all come from wires, so a Boolean can hide it entirely.

**Inputs:**
- `visible` (boolean) — Visible — default true
- `size` (number) — Size — default 24, min 1, max 200, step 1
- `color` (color) — Colour — default "#333333"

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_world_grid_shown",
      "typeId": "value.boolean",
      "label": "Boolean · visible",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": true
      }
    },
    {
      "id": "ex_world_grid_size",
      "typeId": "value.number",
      "label": "Number · size",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": 30
      }
    },
    {
      "id": "ex_world_grid_floor",
      "typeId": "world.grid",
      "label": "Grid",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "visible": true,
        "size": 24,
        "color": "#333333"
      }
    }
  ],
  "edges": [
    {
      "id": "ex_world_grid_e_shown_out_floor_visible",
      "fromNodeId": "ex_world_grid_shown",
      "fromPort": "out",
      "toNodeId": "ex_world_grid_floor",
      "toPort": "visible"
    },
    {
      "id": "ex_world_grid_e_size_out_floor_size",
      "fromNodeId": "ex_world_grid_size",
      "fromPort": "out",
      "toNodeId": "ex_world_grid_floor",
      "toPort": "size"
    }
  ]
}
```

## Scene (`universe.world`)

The 3D place, entered like any container — Title and Sky come from wires, and (since 2026-08-19) the Scene's own Title output can feed a sibling Text panel, an edge crossing OUT of a container.

**Inputs:**
- `title` (string) — Title — default "Scene"
- `bgColor` (color) — Sky — default "#0a0e16"

**Outputs:**
- `title` (string) — Title
- `bgColor` (color) — Sky

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_universe_world_title",
      "typeId": "value.string",
      "label": "String · title",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "Act One"
      }
    },
    {
      "id": "ex_universe_world_sky",
      "typeId": "value.color",
      "label": "Colour · sky",
      "graphX": 0,
      "graphY": 484,
      "values": {
        "value": "#0a0e16"
      }
    },
    {
      "id": "ex_universe_world_scene",
      "typeId": "universe.world",
      "label": "Scene",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "title": "Scene",
        "bgColor": "#0a0e16",
        "hostHint": "any",
        "frame": {
          "x": 16,
          "y": 88,
          "width": 680,
          "height": 480,
          "zIndex": 7,
          "title": "Scene",
          "visible": false
        }
      }
    },
    {
      "id": "ex_universe_world_label",
      "typeId": "view.text",
      "label": "Text panel — mirrors the Scene's title",
      "graphX": 520,
      "graphY": 64,
      "values": {
        "content": "Hello",
        "frame": {
          "x": 60,
          "y": 88,
          "width": 360,
          "height": 280,
          "zIndex": 7,
          "title": "Text",
          "visible": false
        }
      }
    }
  ],
  "edges": [
    {
      "id": "ex_universe_world_e_title_out_scene_title",
      "fromNodeId": "ex_universe_world_title",
      "fromPort": "out",
      "toNodeId": "ex_universe_world_scene",
      "toPort": "title"
    },
    {
      "id": "ex_universe_world_e_sky_out_scene_bgColor",
      "fromNodeId": "ex_universe_world_sky",
      "fromPort": "out",
      "toNodeId": "ex_universe_world_scene",
      "toPort": "bgColor"
    },
    {
      "id": "ex_universe_world_e_scene_title_label_content",
      "fromNodeId": "ex_universe_world_scene",
      "fromPort": "title",
      "toNodeId": "ex_universe_world_label",
      "toPort": "content"
    }
  ]
}
```

## Kiosk (`universe.space`)

Hides the toolbar for everything inside — set directly on the card, honestly: a wire into "Show the toolbar" is accepted by the port but currently ignored by the chrome check (docs/ai/known-fixes.md), so it is not wired here.

**Inputs:**
- `showChrome` (boolean) — Show the toolbar — default true

**Outputs:** none

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_universe_space_kiosk",
      "typeId": "universe.space",
      "label": "Kiosk",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "showChrome": false,
        "hostHint": "any"
      }
    },
    {
      "id": "ex_universe_space_exhibit",
      "typeId": "geom.cube",
      "label": "Cube",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "size": [
          1,
          1,
          1
        ],
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          0.5,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      },
      "parentId": "ex_universe_space_kiosk"
    }
  ],
  "edges": []
}
```

## Geo (`geom.geo`)

The plain place — TouchDesigner's Geometry COMP. Everything spatial placed inside it renders inside it and travels with it; a Cube standing in this Geo IS its Geometry output.

**Inputs:**
- `position` (vec3) — Position — default [0,0,0]
- `rotation` (vec3) — Rotation — default [0,0,0]
- `scale` (vec3) — Scale — default [1,1,1]

**Outputs:**
- `geometry` (geometry) — Geometry

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_geom_geo_room",
      "typeId": "geom.geo",
      "label": "Geo",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "position": [
          0,
          0,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ],
        "scale": [
          1,
          1,
          1
        ]
      }
    },
    {
      "id": "ex_geom_geo_resident",
      "typeId": "geom.cube",
      "label": "Cube",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "color": "#5fa8ff",
        "size": [
          1,
          1,
          1
        ],
        "roughness": 1,
        "metalness": 0,
        "emissive": "#000000",
        "opacity": 1,
        "position": [
          0,
          0.5,
          0
        ],
        "rotation": [
          0,
          0,
          0
        ]
      },
      "parentId": "ex_geom_geo_room"
    }
  ],
  "edges": []
}
```

## Studio (`studio`)

The editor itself, as one node you can enter — its Title comes from a wire and is the only thing it will say about itself; what is inside stays inside.

**Inputs:**
- `title` (string) — Title — default "Studio"

**Outputs:**
- `title` (string) — Title

**Example graph** — build this exact document (createNode/createEdge, src/project/nodeRegistry.js) to get a working demo of this node; ids are stable so re-running the source is idempotent:

```json
{
  "nodes": [
    {
      "id": "ex_studio_title",
      "typeId": "value.string",
      "label": "String · title",
      "graphX": 0,
      "graphY": 64,
      "values": {
        "value": "Studio"
      }
    },
    {
      "id": "ex_studio_editor",
      "typeId": "studio",
      "label": "Studio",
      "graphX": 260,
      "graphY": 64,
      "values": {
        "title": "Studio"
      }
    }
  ],
  "edges": [
    {
      "id": "ex_studio_e_title_out_editor_title",
      "fromNodeId": "ex_studio_title",
      "fromPort": "out",
      "toNodeId": "ex_studio_editor",
      "toPort": "title"
    }
  ]
}
```
