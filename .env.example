# Local dev example:
# VITE_API_BASE_URL=/serverXR
VITE_API_BASE_URL=https://your-api.example.com

# Optional legacy browser-token compatibility only.
# Leave unset for normal builds; protected writes use server sessions.
# VITE_API_TOKEN=change-me
